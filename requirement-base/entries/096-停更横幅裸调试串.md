# 096 「二级状态已停更」横幅裸调试串（用户二次反馈）

来源：用户 2026-08-21 截图（第二次反馈，上一轮未解决）。

- 现状：`WorkspaceViewModel.kt:484` 把诊断参数原样拼进用户可见横幅：
  `二级状态已停更 1374029ms（last_at=<epoch> now=<epoch> workspace=/Volumes/.../cwd）`。
- 要求：
  1. 用户面横幅只说人话：如「状态已 23 分钟未更新，正在重连」。毫秒数、epoch、
     **主机绝对路径一律不上屏**（路径还泄露主机目录结构）。
  2. 调试参数进诊断日志（DiagLog），不进 UI——记判据的操作数到日志，横幅只给结论。
  3. 复核同一 ViewModel/横幅族是否还有其他裸调试串上屏（grep ms（、last_at=、now= 一类）。
