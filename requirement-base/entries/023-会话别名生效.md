# 023 会话别名（window name）需生效

- 状态：已裁定
- 类型：缺陷（发布/实现，必修）D-24
- 出处：leader 草稿，2026-08-12

## 缺陷现象

`scan.go` 已改用 `#{window_name}`，但生产 daemon 是**旧二进制未重编**，App 上看不到会话别名变化。

## 裁定行为

重编并重新部署生产 daemon，使会话别名在 App 侧生效。（代码侧已改，缺的是编译发布。）

## 关联

[[002]] 两级分组模型（session 名是二级列表的展示标签；别名即此展示名的来源）。
