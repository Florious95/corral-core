# 017 场景审计八项裁定

- 状态：已裁定
- 出处：scenario-audit needs-ruling + leader 裁定，2026-08-09

## R-1 特殊键：当期必做

会话页输入条上方**快捷键条**，最小集 Esc / Ctrl-C / Tab / ↑ ↓ ← →
（Claude Code 日常硬依赖）。协议 input 帧加**可选 keys 字段**
（前向兼容增量，不 bump 协议版本）。

## R-2 多行粘贴

含换行文本走 server 既有 `paste-buffer -p` **括号粘贴路径**整段注入 + Enter，
**App 不拆分**。CLI 不支持 `?2004` 的退化风险在文档标注。

## R-3 重配对

当期**设置页入口 + 单档覆盖**，切主机即断开重连；**多主机档案后置议题**。

## R-4 token 轮换

当期**文档化**"删 token 文件重启即全量吊销"；版本化多 token **后置**。

## R-5 通知

当期**全局开关**；**每工作区静音后置**（明示）。

## R-6 国际化

当期**锁中文**并在 README 明示；抽取翻译**后置**。

## R-7 无障碍

当期**徽章补 contentDescription 语义标注**；其余显式**后置**。

## R-8 拍照

**当期补实现**——任务书承诺不可赖账。

## 关联

[[003]] 对话体验、[[012]] 工作区聚合、[[016]] 生产级验收（首触零阻断）。
后置项见 INDEX 未决议题表。
