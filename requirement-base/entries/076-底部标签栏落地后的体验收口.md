# 076 · 底部标签栏落地后的体验收口（三条）

- 日期：2026-08-19
- 来源：用户实测三张截图，原文与图见 `.team/issues/清单-20260819.md`
- 基线包：`~/Desktop/agentmirror-spin-fixed-925e0cea7.apk`（commit `925e0cea7`）

🟢 **先记两条「已经好了、不要再动」**（防止本轮重复修）：
- 契约 073 的**身份键含 socket 已生效**：收藏页三条 `claude_code` 目录各不相同、三颗星互相独立，
  「收藏一个全中、列表只剩一条」**没有复发**。⛔ 本轮不得改身份键。
- 契约 075 的一级菜单转圈已修。

---

## §1 右上角「查看」二级菜单串会话

**用户原话**：「这个列表在收藏里点开会话会窜……」
**用户随后的关键修正（以它为准）**：「准确的说是**最后一个加入收藏的**，查看页面展示的内容」

⇒ 不是「第一次点开的被缓存住」，而是「**不管从哪个会话点，拿到的都是收藏簿里最后写入的那条**」。
这两种说法的修法完全不同：
- 「第一次的被钉住」⇒ 浮层缓存没随会话切换失效。
- 「**永远是最后一个收藏的**」⇒ 浮层**没把「当前会话」当输入**，读了某个被 `addFavorite` 覆盖的共享/单例值。

**截图自证**（`.team/issues/shots/01-查看菜单串会话.jpg`）：
背景终端是「多agent协作」工作区（`Ctx: … | 多agent协作 | main`），
浮层列的 7 行副标题却**全是 `/Volumes…Agent安卓`` —— 错配在同一张图里就抓到了。

**判据 `A-vw-src`**：造两个工作区各一个会话，先收藏 A 再收藏 B（B 是最后一个）；
**从 A 的会话里点「查看」，浮层内容必须是 A 的**。🔴 先在当前实现上验红（现在必出 B）。
🔴 判据要能区分 **「浮层没刷新」** vs **「浮层读错了源」** —— 两者在界面上同形。

---

## §2 底部标签栏的三个连带问题

截图 `.team/issues/shots/02-设置页顶部空隙与竖滑失效.jpg`。

### 2a 顶部空隙过大
状态栏到「‹工作区　设置」标题之间空出约屏高 1/8。
**待查**：`statusBarsPadding` 叠了一层，还是标题栏自带 top padding 没随改版调整。

### 2b 🔴 横滑抢掉竖滑，页面滑不到底（本轮最贵的一条）
底部三栏是横向 pager，横滑手势吃掉竖直滚动：
- 设置页滑不到底（截图里「诊断日志」正被切断）
- 会话一多就看不到下面的（用户：「同理」⇒ **同一根因，不是两个 bug**）

**判据 `A-ly-scroll`**：设置页与会话列表都必须能滚到**最后一个元素完整可见**；
会话数造到超出一屏时同样成立。🔴 先验红。
🔴 判据要能区分 **「滚不动」** vs **「能滚但内容被裁掉」** —— 界面上同形。
🔴 **不得**把横滑切页去掉——那是 067 §4.1 推翻滑动式导航后刚落地的形态。
用户说的是它**抢了竖滑**，不是它不该存在。

### 2c 删掉一二级菜单右上角的「设置」入口
底部已有「设置」tab，顶部重复。**只删「设置」**，
⛔ 不要顺手把旁边的「LAN / tailnet」也删了——用户只点了设置。

---

## §3 收藏页：显示名、状态、布局

截图 `.team/issues/shots/03-收藏页claude_code重名无状态.jpg`。

### 3a 🔴 claude code 的会话显示名恒为 `claude_code`，**要单独处理**
用户原话：「对 claude code 的会话名称存在问题，总为 claude code，**逻辑不一样，需要单独处理**」
⇒ 不能沿用其它 CLI 那套取名规则。

**这条 073 §2 就标了「尚未确定，你去查清再改」，至今未定。本轮必须定下来。**
候选字段：`window_name` / `session_name` / `pane_title` / 工作区名限定。
🔴 名字**可能是中文**，⛔ 不得做 ASCII 假设、不得截断到不可辨认。
🔴 显示名**只用于显示**，身份仍走结构字段 + socket（062/067/073 铁律不变）。

**判据 `A-fv-name`**：同一列表内三个 claude code 会话的显示名**互不相同**且各自可辨识
（⛔ 「claude_code (1)/(2)/(3)」这种编号不算可辨识——它不携带任何让人认出是哪个的信息）。

### 3b 收藏页缺状态标
会话列表每行有「进行中 / 空闲 / 未知」，收藏页没有。
🔴 状态来源**必须和会话列表同一套**——⛔ 不许收藏页自己再算一遍。
两套判定漂移是本工程已经踩过的坑（身份键当年就是这么裂的）。

### 3c 收藏页布局对齐会话列表
星在**行首**、标题、目录副标题、右侧状态标。
⚠️ 截图里收藏页的星仍在**行尾**，而 072 定的是**星移到行首** ⇒ 对齐布局时一并挪，⛔ 别只改状态标漏了星。
