# 078 · 终端左侧遮挡；主题必须与 Agent CLI 一致

- 日期：2026-08-19
- 来源：用户实测左右对照截图 `.team/issues/shots/07-左侧遮挡与主题不一致.png`
  （左＝主机上真实的 Agent CLI，右＝APP 在模拟器里的同一会话，**同一时刻同一内容**）
- 基线包：`~/Desktop/agentmirror-ux3-07f5a1ca7.apk`

⚠️ 这张左右对照图本身就是本条最好的判据形态：**同一内容、两个渲染器、逐像素可比**。

## §1 终端内容左侧仍被遮挡

**用户原话**：「左侧还是有遮挡的现象。」（**「还是」= 这不是第一次报**）

截图红框处：行首的工具调用圆点 `●`，
- 左侧真实 CLI：圆点完整。
- 右侧 APP：**圆点被左边缘裁掉一部分**，只剩右半。

⇒ 终端网格的**第 0 列被容器左边缘吃掉**（或起绘 x 偏负）。

**待查（⛔ 不猜）**：是终端视口的左 padding 为负/为 0 导致首列压边，
还是字形绘制的 x 原点算错了半个字宽。
🔴 判据要能区分 **「布局把首列推出去了」** vs 「**首列画在那儿但被 clip 了**」——两者外观同形。
🔴 量：首列字形的绘制 x 起点 vs 容器 contentLeft，**两个数都要记**。

**判据 `A-cl-col0`**：造一行以 `●` 开头、且行首紧邻左边缘的内容，
首列字符必须**完整可见**（不被裁）。先验红。

## §2 🔴 主题必须与 Agent CLI 完全一致

**用户原话**：「我希望 APP 上看到的和 Agent 的 CLI 上看到的**主题是完全一致的**。
包括它的背景，各个对话的颜色等等。」

截图对照：
| | 背景 | 正文 |
|---|---|---|
| 主机 CLI（左） | **浅色**（近白） | 深色字 |
| APP（右） | **深蓝黑** | 浅色字 |

⇒ APP 把终端配色**写死成了深色**，没跟随主机终端的实际主题。
其余颜色（`查看` 的紫、`uiautomator dump` 的蓝、`Improvising…` 的粉、
`远控 leader` 的青底标签）两边**色相一致**，说明 ANSI 前景色通道是通的，
**坏的是背景/默认前景这一层**。

**这条的含义比"换个配色"大**：APP 是主机终端的镜像，
**镜子和实物不一致，用户就得在脑子里做一次换算**——这正是本产品要消灭的东西。

**待定（⛔ 不猜，本轮要查清并写进说明）**：
- 主机终端的背景/默认前景**从哪儿取**？tmux 的 `default-terminal` / OSC 11 查询 / 用户终端 profile？
- 传输面：走现有 `level2_frame` / 会话帧加字段，还是握手时一次性下发？
- 取不到时的兜底：**必须响亮可见地退回某个明确默认**，⛔ 不许静默用深色假装成功。

**判据 `A-th-bg`**：同一会话，APP 渲染的背景色与主机终端背景色一致
（先支持「浅底/深底」这一档就算过，不要求逐 RGB 相等——但**必须由主机侧数据决定，不是 APP 猜**）。
🔴 判据必须断言**数据来源**，不能只断言"现在是浅色"——
写死成浅色同样能让"现在是浅色"这条判据变绿，那是骗判据。
