# 版本对齐审计（version-alignment audit）

> 日期：2026-08-13。发现：用户手机装的 `agentmirror-v6-9653be0.apk` 构建自 `9653be07f`
> （2026-08-12 21:56），而 D-36 客户端修复首次出现在 `0fa842ace`（2026-08-13 00:24，其后）。
> **七轮取证跑的是含修复的 HEAD，用户跑的是不含修复的版本。** 今天所有「复现不了」的结论
> 都必须按本表核对——是否有「取证版本含用户版本没有的相关修复」。
> 只读核对，不改代码。

## 用户包基线

- **用户包 = `9653be07f`（2026-08-12 21:56）**，已核实。
- 相关修复时间线（都在用户包**之后**）：
  | 修复 | 提交 | 时间 |
  |---|---|---|
  | D-36 客户端修复（onScrollBy 空 buffer 锁 / onHistoryPrepend / SCROLLBACK merged） | `0fa842ace` | 08-13 00:24 |
  | 服务端恢复 v5（含 D-27 no-op resize skip / D-36 服务端半） | `07f065db0` | 08-13 00:28 |
  | 横向栅格收敛（右列截断） | `c84b73950` | 08-13 01:12 |
  | 脏行渲染 + 整屏重写抑制 | `ef04c3c11` | 08-13（今晚） |
  | D-38 真根因修复 | `ba0cb1ff1` | 08-13 08:55 |
  | 捏合预览/提交（一次手势 resize N→1） | `920fd7a16` | 08-13 |
  | P0 回退脏行渲染回整帧重绘 | `d53aba161` | 08-13 09:53 |

## 审计表

> **第二列作废理由（leader msg_c4627f60d4b0）**：「是否在注延迟条件下测过」。没在弱网下
> （delay_proxy.py 400ms）测过的「不复现」同样作废——用户说「你们复现不了也是因为网络好」，
> 把网络弄差就能复现，工具早就有（delay_proxy.py 自证 5.3ms→403.6ms），只是从来没在闪烁相关
> 测试里用过。**捏合闪烁、发消息重排重绘、右列截断，这三条大概率都要重新在弱网下测。**

| 缺陷 | 用户报告时版本 | 我们取证时版本 | 之间有无相关修复 | 是否在注延迟下测过 | 结论是否仍成立 |
|---|---|---|---|---|
| **D-36 上滑失效** | `9653be07f`（用户包） | `270d2f4e4`（d36-forensics，含 D-36 客户端修复） | **有**：`0fa842ace` 客户端修复在取证版本里、用户包没有 | **未注延迟**（本机/局域网） | **作废**：取证「不复现」跑的是含修复版本 + 局域网。回炉动作（回退 diff + 四步序列）是决定性实验 |
| **右列截断**（用户报 4 次） | `9653be07f` | w-base-v2 几何等价 AVD 实测用「当时的 HEAD」= 含 `c84b73950` 栅格收敛 | **有**：`c84b73950` 横向栅格收敛在取证版本里、用户包没有 | **未注延迟** | **作废重测**：含修复版本 + 局域网。可能早已修好，需在用户包等价版本 + 弱网重测 |
| **发消息整屏刷** | `9653be07f` | 各轮不同：脏行渲染 + 抑制 `ef04c3c11` 曾进出 HEAD（后 `d53aba161` 回退）；部分轮测的是含脏行渲染/抑制的版本 | **有**：脏行渲染/抑制在部分取证版本里、用户包没有 | **部分轮注延迟**（w-base-v2 两波渲染测过 400ms），但用户动作基线未测 | **部分作废**：需在用户包等价版本（无脏行渲染/抑制）+ 弱网重测 |
| **D-38 底部空黑** | `9653be07f` | 复现到 106px 那次 = ?（d38-viewport-restore 用 `07f065db0`，在 D-38 修复 `ba0cb1ff1` 之前） | `07f065db0` 无 D-38 修复（D-38 修复在其后），但含服务端恢复 v5 | 未注延迟 | **基本成立**：复现版本无 D-38 修复，与用户包一致。但服务端恢复 v5 是差异，需标注 |
| **捏合闪烁** | `9653be07f` | 对照组用 `d53aba161`（P0 回退后，含 D-38 修复 + 不含脏行渲染） | **有**：`d53aba161` 含 D-38 修复（`ba0cb1ff1`）与整帧重绘恢复，用户包两者都没有 | **未注延迟**（用户亲指「复现不了因为只在局域网测」） | **作废重测**：对照组含用户没有的改动 + 只在局域网。**最高优先级——P6 注延迟测是失败态基准唯一来源** |

## 方法说明

- 用户包 = `9653be07f`（已核实）。
- 取证版本 sha 取自各 `e2e/artifacts/*/REPORT.md`（没记录的标 UNKNOWN_BUILD = 结论不可信）。
- 「之间有无相关修复」= `git log 9653be07f..<取证sha> -- <相关路径>`。

## 结论

**至少 3 条「不复现」结论可能作废**（D-36、右列截断、捏合闪烁），因为取证版本含有用户包没有的
相关修复。**可能直接送回好消息**：右列截断、捏合闪烁这些「仍无根因」的缺陷，也许早已被
`c84b73950` / `ba0cb1ff1` 等修复，只是从没在用户那个版本上确认过它原本存在。

**待办**：对作废条目，用**用户包等价版本**（`9653be07f` 或近似）重测，确认缺陷在用户版本上
**原本存在**还是**早已被修**。D-36 的回退动作正是这样的等价版本测试。
