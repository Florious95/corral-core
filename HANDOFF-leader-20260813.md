# HANDOFF · leader · 2026-08-13

> 写给**刚接手、没看过过程**的人。所有代号首次出现即解释，路径 / sha / 进程号写全。
> 前两份交接是 `HANDOFF-leader-20260812.md`（上一届 leader 退役）与
> `HANDOFF-leader-20260812-night.md`（我昨夜写的）。**三份都不要覆盖，但今天这份优先级最高**——
> 昨夜那份里标为「在途」的任务，今天绝大多数已被用户裁定回退或搁置。

---

## §0 compact 后先做什么

### 一句话现状

今天做了五轮修复，**五轮全部回退**。App 产品代码现已与 v6 基线（提交 `9653be07f`）**零差异**。
服务端有一批未部署的改动。**团队 5 个席位全部 DEAD，生产 daemon 也停了。**

### 开口第一句（对用户说）

> 「两件急事：**生产 daemon 停了，你手机现在连不上**；另外**你桌面上那个
> `agentmirror-HEAD-d2e06bde4.apk` 是有三条倒退的坏包，建议删掉**，别误装。
> 团队席位也全停了。要我先把 daemon 拉起来吗？
> 另外昨天问你的三件事还没答：装不装仪表到生产、哪条现象最影响你用、还是先停下来交接。」

### 必读清单（按优先级）

1. **本文**
2. `/Volumes/nvme/Projects/远程Agent安卓/CLAUDE.md` —— 工程铁律（派单五步、眼见为实、回炉流程）
3. `docs/taskbook-audit-20260813.md` —— **今天提炼的全部纪律（①~⑨）**，〇节
4. `docs/nominal-vs-measured-cell-width.md` —— **用户报了 4 次的「最右列文字跑到屏幕外」的根因**，今天由两个席位独立撞上
5. `docs/d38-three-attempts-postmortem.md` —— D-38 三版失败的认知复盘（代码已退，认知留下）
6. `docs/version-alignment-audit.md` —— **哪些「不复现」的结论是作废的**，以及为什么
7. `docs/fullrepaint-hypothesis-space.md` —— 「发消息整屏刷」11 条假说的排除表
8. `HANDOFF-leader-20260812-night.md` —— 昨夜交接（**注意其中大量「在途」已被今天推翻**）

### 恢复动作

```bash
cd /Volumes/nvme/Projects/远程Agent安卓

# 1. 拉起生产 daemon（当前已停，用户连不上）
bash .team/prod-daemon-launch.sh -host 192.168.31.116
#    核：lsof -nP -iTCP:9900 -sTCP:LISTEN
#    ⚠️ 该二进制编译于 08-13 00:25，是 v5 恢复后、加计数器之前的版本

# 2. 团队全部 DEAD，要用就重启（先 claim-leader，pane 会变）
.team/ta claim-leader --confirm --workspace .
.team/ta restart .
#    ⚠️ 团队状态里只有 5 席，restart 不会复活历史席位（昨夜清理过）

# 3. 删掉桌面上的坏包（用户可能误装）
rm ~/Desktop/agentmirror-HEAD-d2e06bde4.apk
```

---

## §1 身份与不变量

### 角色边界

- **leader 只编排不亲做**。今天违规零次（昨夜两次）。
- 跨 team 转交用全名：框架问题直投
  `/Users/alauda/Documents/code/agent前沿探索/多agent协作::refactor-maintainability/leader`

### 今天新增的九条纪律（原文在 `docs/taskbook-audit-20260813.md` 〇节）

1. **回退期间立的账，不能用回退后的代码核。** 判「假账」的唯一硬判据是
   `git log --all -S<特征串>` 全历史零命中；只在当前 HEAD 找不到，只能得出「已被回退」。
2. **取证结论要带时间戳。**「当前全历史零命中」只在观测那一刻成立。
3. **凡写「用户没报过 X」，必须去 `requirement-wiki/raw/` 与现场基查证**。凭印象下的断言是账面失真主因。
4. **用户的肯定性反馈同样要核实。** 我们对坏消息谨慎，对好消息太轻信。
5. **状态升级类指令不得在信息未完整时发出**，且升级前要有一道确认。
6. **凡涉及被测对象行为，必须用真实被测对象，不用模拟物凑数。**
7. **一条修复 = 一个提交。** 混合提交同时毁掉：单条回退、单条审查、单条追溯。
8. **临时取证 / 诊断代码放独立文件**，不塞进高频改动文件（今天被误提交三次）。
9. **新增的观测仪表，第一件事是验证它测的是不是你以为的那个东西。** 作用域必须显式标出。

### 今天最贵的一条经验（不在纪律里，但比纪律重要）

> **单元测试证明的是「如果输入按我们建模的方式来」。**

今天五次「单测全绿、装上去不行」，形状完全一致：
- 手势模型一个结束点，真机两次抬起（捏合 4→1 实为 4→2）
- Compose insets 模型按预期分发，真机 `imeBottom` 恒为 0（D-38 前两版全错）
- 合成 5 个 MOVE，真机几十个
- Robolectric 模拟不了 Compose 分发链
- **测了函数的行为，没测函数会不会被触发**（几何持久化：`persistGeometry` 只在捏合结束时调，正常进出会话永不写）

**收工判据必须包含「装上去正常用一遍」，这一步今天抓住了全部五次。**

### 客观核对，不凭自报

今天的实证：
- **两条错误线索来自一个坏仪表**（进程级累计计数被打在 per-conn 日志行上，读成单连接异常）
- **两次工具本身是坏的**（捏合注入器在新 AVD 上完全不生效；延迟代理原设计只加延迟不产生背压，迭代四版才修好）
- **七轮取证测错了对象**（在含修复的 HEAD 上找一个只在不含修复版本才有的缺陷）

**凡「复现不了」的结论，先证明三件事：测的是用户那个版本、工具本身有效、网络条件与用户一致。**

---

## §2 排期与封存令

### 用户裁定（2026-08-13，原文）

> 「整体需全量回退到 v6 版本，基本这个版本不可用。**现在修改策略，在 v6 版本优化性能，无论是服务端还是 app**」
> 「**上面那些问题全部搁置**」
> 「**性能优化要有基线和指标**」
> 「**核心是优化 tailscale 的链路**」
> 「我在本地局域网，很流畅，很多闪烁问题都没有，但是我现在基于 ts 就有。**你们复现不了也是因为网络好**」

### 因此当前封存令

- **所有缺陷线全部搁置**：捏合闪烁、上滑看历史（D-36）、回前台底部空黑（D-38）、右列截断、发消息整屏刷、状态误检（D-26）。
- **新方向**：在 v6 基线上优化性能，**核心是 Tailscale 链路**，**先有基线和指标**。
- **只修缺陷零新功能**（更早的裁定，仍生效）。

### 用户那句话的技术含义（务必理解，它改变了整类问题的定性）

**闪烁不是渲染缺陷，是网络症状。** 局域网下数据来得快，中间状态一闪而过；
TS 下数据分批慢慢来，用户看见每一个中间状态。

**推论**：修法在网络层不在渲染层——减少往返次数、减少传输字节、减少「用户要看到几个中间状态」。
**「修缺陷」和「优化性能」在这里是同一件事。**

---

## §3 P0 / 插队项

### A. 【最急】生产 daemon 已停，用户连不上

- `agentmirrord` 零进程，`:9900` 无监听。
- 拉起：`bash .team/prod-daemon-launch.sh -host 192.168.31.116`
- ⚠️ **该二进制编译于 08-13 00:25**，内容是「v5 恢复后」的版本，**不含今天新加的三类计数器**。
- ⚠️ **daemon 会把配对 token 明文打进 stdout → `.team/logs/agentmirrord-prod.log`**。
  这本身是一条未修缺陷。**别 `tail` 那个日志**（昨夜 leader 已经因此把 token 带上屏一次）。

### B. 【急】桌面上有一个坏包，用户可能误装

`~/Desktop/agentmirror-HEAD-d2e06bde4.apk` —— **用户实测的三条倒退就出在它身上**：
捏合重绘错乱、点输入框闪黑屏、发送消息后重排重绘。**建议删掉。**

桌面上正确的包是 **`agentmirror-v6-9653be07f.apk`**（与用户原来那个 `agentmirror-v6-9653be0.apk` 产品代码零差异）。

### C. 团队 5 席全部 DEAD

`w-base-v2` / `w-dev-cols` / `w-dev-d38` / `w-dev-repaint` / `w-librarian` 全部 `stopped / DEAD`。
要用就按 §0 的恢复动作重启。**restart 只会拉起这 5 席**（昨夜已清理掉 104 个历史席位）。

### D. P0 对原排期的扰动

**昨夜交接里的全部「在途任务」今天已作废**：D-38 三版全退、捏合预览提交全退、
D-36 客户端全退、横向栅格收敛全退、脏行渲染与抑制机制全退。
**不要照昨夜那份继续做。**

---

## §4 在途未收尾任务

### 基线与备份（已核实）

| 项 | 值 |
|---|---|
| 当前 HEAD | `6fe0b1128` |
| v6 基线 tag | `v6-baseline` → `9653be07f`（用户手机上那个包） |
| 回退前最后一个提交 tag | `reverted-work-tip` → `9c6727f45` |
| 备份分支 | `v6-plus-reverted-work`（v6 之后的 22 个提交全在上面） |
| 分类补丁 | `docs/reverted-to-v6/`（7 个 patch，按修复分开） |
| **App 产品代码 vs v6** | **零差异**（`git diff v6-baseline -- app/app/src/main app/terminal/src/main` 为空，已核） |
| **服务端 vs v6** | **+1317 行 / 23 文件，未部署**（见下） |

### 4.1 服务端有一批未部署改动（**这是最需要决定的一件事**）

`git diff v6-baseline --name-only -- server/` 共 23 个文件，包含三摊东西：

**① v5 恢复带回来的**（已在跑的 daemon 里，用户亲验会话名恢复正常）
`session_name_test.go` / `listing.go` / `state_wiring.go` / `discovery/*` / `bridge.go` 等。

**② 主机 pane 几何记账修复**（`pane_geometry.go`，证据 `fix-host-pane-geometry.json` = `pass_code_verified`）
异常断连与多订阅者叠加两条路径。**关键发现**：恢复用的 `Resize` 必须用 `context.Background()`，
否则 teardown 时 ctx 已取消、tmux 命令被 exec 提前 kill，**恢复代码写了但静默失败**。

**③ 今天新加的三类观测仪表（未部署，已验证正确）**
- `sendq_metrics.go`：`conn.*` per-connection 与 `total.*` process-level 双前缀
  （丢包数 `DeltasDropped` / 快照来源 `SnapshotsFromResize` `SnapshotsFromSubscribe` / 队列峰值 / 帧数）
- `ws_conn.go` 的 `close_reason`（连接为什么断：`read_error` / `write_error` / `client_close`）
- 隔离自检 `TestConnMetricsIsolation` 锚住两条连接计数互不污染

**④ D-26 状态检测的锚点修复**（`anchor_red_test.go` + `rules.go`，证据 `fix-agentstate-anchor-region.json`
= `pass_unit_tests_pending_deploy`）
根因已实测复现：`capture-pane` 抓整屏、规则用 `strings.Contains` 全屏匹配，
**滚上去的历史残留 `esc to interrupt` 被当成「工作中」**，五条真实语料红测全绿。

**下一步**：这四摊要不要重编上线，**需要用户同意**（重启会断他连接几秒）。
**我已向用户提议装仪表到生产，他还没答。**

### 4.2 唯一有数字证明的优化方向（已回退，但方向确认有效）

**「每次进会话白推一屏快照」** —— 客户端用硬编码猜测值 `INITIAL_ROWS=40 / INITIAL_COLS=120`
订阅，服务端先按错误尺寸推一屏（1291B），客户端布局完成后发真实 resize，服务端再推一屏（3414B）。

**实测收益（判据一二均通过）**：
- 第二次进会话 `conn.snapshots_from_resize` 由 **2 → 0**
- 主机 pane 由 `100x24 → 120x40 → 126x107` 三段变为**直接跳到真实尺寸**
- 每次进会话省：1 个往返（TS 400ms 下约 400ms）+ 1.3KB + **1 次客户端整屏重建**

**为什么退了**：装机实测撞到首次进会话内容挤在左侧一条、文字截断重叠；
v6 同设备同会话对照不出现。两个假说依次被数据证伪（空 store 非法尺寸、设备残留 store），
**无新的明确指向，按事先定的上限整条回退。**

**diff 留档**：`docs/reverted-to-v6/geometry-persist.patch`
**将来重做的起点**：`docs/nominal-vs-measured-cell-width.md`

### 4.3 排查中撞上的真根因（**未修，但已定档，值得优先**）

**名义字格宽 vs 实测字形宽，两套栅格永不收敛。**

- `presenter.cellWidth` 恒为名义默认 **10**（只被捏合改，`measureCells()` 从不回写）
- 渲染用 `measureCells()` 的实测 `cellW`（如 11px）
- **上报的 cols 用名义算、绘制用实测算 → 永不一致**

**这是用户报了 4 次的「最右列文字跑到屏幕外」的同一个根因**，
今天由两个席位从两个方向独立撞上（`w-dev-cols` 的横向栅格收敛、`w-dev-repaint` 的 persistGeometry）。

**文档**：`docs/nominal-vs-measured-cell-width.md`（含暴露形态、修法方向、以及为什么它不是小改动）
**注意**：今天 `w-dev-cols` 那版修复改完引入了别的问题，随全量回退退掉了。

### 4.4 性能基线规范（已写，未执行）

- `docs/performance-baseline-spec.md` —— P1~P8 用户动作指标 + R1/R2/R3/R5 工程红线指标
- `docs/ts-link-baseline.md` —— TS 链路本体指标
- `docs/roundtrip-audit.md` —— 往返次数审计（**含一条实测订正**：审计推算「翻页可省数百 ms」，
  实测 6.6ms，**差两个数量级** → 固化纪律「推算的往返次数可信，推算的耗时不可信」）

**已知的往返浪费**（代码推导 + 部分实测）：
| 交互 | 实际 | 理论最少 | 备注 |
|---|---|---|---|
| 冷启动 → 首屏 | 4 往返 | 2 | 已核实 subscribe 不依赖 list、零协议改动可并行 |
| 进一次会话 | 2 往返 + 4.7KB + 2 次整屏重建 | 1 + 3.4KB + 1 次 | 见 4.2 |
| 捏合一次 | 10-20 往返 + 10-20 次全屏快照 | 1 | `raw/041` 早有裁定从未实现 |
| 上滑翻一页 | 服务端每页做两次 capture | 一次 | `#{history_size}` 可零数据替代，省每页 126KB |
| 发消息 → 看到字 | 2 往返 | **已最优** | 确认与回显语义不同，不可合并 |
| 回前台 → 稳定 | 0-1 往返 | **已接近最优** | |

**硬性要求**：每条指标必须有 **0ms 与注延迟（400ms RTT）两组数**。
**只有局域网数字的指标，不算基线**（这是用户主诉：「你们复现不了也是因为网络好」）。

**工具**：`e2e/delay_proxy.py`（用户态 TCP 延迟代理）。
⚠️ **它的原设计是坏的**（只加转发延迟，daemon 的写瞬间成功打在 OS 接收缓冲上，**不产生背压**）。
现已迭代到第四版：**带宽节流 ~20KB/s + `SO_RCVBUF` 收到 2048 字节**，
四次失败原因写在文件头注释里，**以后不用重踩**。

### 4.5 巡检基础设施（在 `test/`，不进 App，不会被回退）

- `test/framework/machine_eye/` —— 空间 / 时间 / 符号三个算子（ffmpeg 取灰度，零 npm 依赖）
- `test/framework/inspection/` —— Layer 1 巡检：场景 → 多指标 + 棘轮
- `test/baselines/` + `docs/inspection-baselines.md` —— 指标 × 状态对照表

**三条已实现的机制**：
1. **改善即收紧**：`KNOWN_BAD 1123 → 改善到 6 → 基线收紧 → 回归到 100 判红`（缺陷不会因「比原来好」被合法化）
2. **产地不可比**：构建 sha / 设备 / 延迟条件 / 前置状态任一不同 → `NOT_COMPARABLE`
3. **存活判据前置**：主机有内容时 App 屏幕必须有非背景像素；**存活不过则后续指标一律 BLOCKED 不得 PASS**

**存活判据的关键教训**：纯像素判据分不出「空是因为没东西可显示」和「空是因为坏了」，
**必须与主机侧 `capture-pane` 事实对账**，拿不到主机事实时只能返回 `INDETERMINATE`。

**硬考卷**：`ime-no-resize` 那 254 帧，时间算子必须算出 `[36,66,93]` 恰 3 帧、纯滚动无重排。

### 4.6 无活进程可查

**今天所有任务都是人工直派，没有后台流水线。** 进度信号只有：
- `.team/ta status` 看席位状态（现在全 DEAD）
- `.team/evidence/*.json` 看证据
- `e2e/artifacts/*/REPORT.md` 看取证报告
- `git log` 看已入库的

---

## §5 运维与外部

### 进程与设备（已核实，2026-08-13）

| 项 | 状态 |
|---|---|
| 生产 daemon | ❌ **已停**，`:9900` 无监听 |
| team | ❌ **5 席全部 DEAD** |
| `server/agentmirrord` 二进制 | ✅ 存在，编译于 08-13 00:25（v5 恢复后、加计数器前） |
| `emulator-5554` | AVD `wedding_user_a_api35`（旧几何 1080×2400） |
| `emulator-5558` | AVD **`agentmirror_geo_1260x2800`** —— **与用户真机几何等价**，480dpi 一次标定命中、行高 20.0px 精确相等。**长期保留，几何相关测试一律用它** |
| 看门狗 | 未复核，接手后请核 |

### 用户环境

- 手机经 **LAN** 连（`ws://192.168.31.116:9900/ws`）；用户报告 **Tailscale 链路质量近期改善**（原因未确认，**不记为我们的功劳**）
- **用户装的包**：`agentmirror-v6-9653be0.apk`（构建自 `9653be07f`）
- **桌面上的正确替代包**：`agentmirror-v6-9653be07f.apk`（产品代码零差异）
- **桌面上的坏包**：`agentmirror-HEAD-d2e06bde4.apk` —— **三条倒退，建议删**

### 测试工具的已知缺陷（**接手必读，今天栽过三次**）

1. **捏合注入器**：`emulator-5558` 上 raw `sendevent` **完全不被 InputReader 识别**
   （`getevent` 证明内核收到，上层不认）。`emulator-5554` 上 **pinch-out 可用、pinch-in 不可用**。
   → **捏合相关的一切结论，先自证注入器有效再采信。**
2. **延迟代理**：见 4.4，第四版才产生真实背压。
3. **`recapprobe` 并行订阅**：今天两次失灵（`chunks=0 insufficient`），根因未查。

### 外部通道

框架问题直投：
`team-agent send '/Users/alauda/Documents/code/agent前沿探索/多agent协作::refactor-maintainability/leader' '...'`

---

## §6 安全约束（原文保留，不可弱化）

- **密钥只存在于 `.team/current/profiles/*.env`，任何席位禁止读其原文。**
  只能用 `team-agent profile show <name> --workspace . --json` 看脱敏诊断。
- **配对 token 与 Tailscale authkey 同级——不落日志、不上屏明文、不入截图，QR 是唯一合法出口。**
  TS authkey 传入只经 `TS_AUTHKEY` 环境变量。
- **凭据分两级**：用户**生产凭据**（`/Users/alauda/Library/Application Support/agentmirror/token`）
  绝不进 argv / 日志 / 截图 / report_result；测试席**自建隔离 daemon 的一次性 token** 属丢弃品，进 argv 可接受。
- **不许手改 App 的 SharedPreferences 来绕过配对流程。**
- **绝不触碰生产 daemon 与用户真实 tmux，只读也不行。** 测试一律自建隔离环境，
  起隔离 daemon **必须**用 `AGENTMIRROR_E2E_DISCOVERY_SOCKET_DIRS` 收窄扫描
  （定义在 `server/internal/api/discoverer.go:21`；不设=生产全扫，设空=一个都不扫，设了=fail-closed 白名单）。
- **席位禁止 git push。**
- **GPL 隔离**：终端内核自研，依赖须 Apache-2.0 兼容。Termux 系 GPLv3 **不可用**；
  herdr-remote AGPL、mosh GPLv3 **只借鉴算法不复制代码**；
  herdr 主仓 / ghostty / xterm.js / alacritty / wezterm 为 Apache-2.0 或 MIT，**可引用**。
- **测试净化前缀** `env -u TEAM_AGENT_*`。
- **⚠️ 已知泄露风险（未修）**：daemon 把配对 token 明文打进 stdout → `.team/logs/agentmirrord-prod.log`。
  **不要 tail 那个日志。** 昨夜 leader 已因此把 token 带上屏一次。
- **⚠️ 今天新增**：`ps aux` 会把 deepseek 席位的 `ANTHROPIC_AUTH_TOKEN` 明文暴露在进程命令行里
  （team-agent 用命令行前缀传环境变量，`.env` 那道防线在进程启动那一刻就被绕过）。
  **本机禁跑无过滤的 `ps aux`，核进程一律 `pgrep -fl <精确路径>`。**
  已向框架 team 报备。

---

## §7 今天的完整账（用户视角）

### 用户亲验修好的（4 条，昨天的成果，今天未动）

D-35 字形缺省、D-22 图片上传 401、D-23 侧滑退出 App、D-32 返回跳级。

### 今天上线又全部回退的（5 条）

| 改动 | 为什么退 |
|---|---|
| 脏行渲染 + 整屏重写抑制 | **让 App 完全不显示**（终端整片空白）；抑制机制三次证明零收益 |
| 横向栅格收敛 | 随全量回退 |
| 捏合预览/提交 + ACTION_UP | 用户实测「重绘错乱，比 v6 更差」 |
| D-38 视口恢复（三个版本） | 三次验收全不通过；第三版引入「点输入框闪黑屏」 |
| 几何持久化 | 判据一二通过但引入首帧渲染异常，按上限回退 |

**用户裁定原文**：「整体需全量回退到 v6 版本，**基本这个版本不可用**。」

### 今天被挡住、没进到用户手里的（7 样）

一个让 App 完全不显示的包 · 三版「屏幕正常但计数器不为 0」的 D-38 ·
一个「4→1」实为「4→2」的捏合 · 两份假账 · 三次误入提交的取证代码 ·
一个会误导人的仪表 · 几何持久化的首帧异常。

**每一样放行，都会以「已修复」的名义进到用户手里。**

### 今天真正留下的

1. **根因定档**：名义 vs 实测字格宽（用户报 4 次的右列截断）、D-26 全屏扫描误判历史残留
2. **三类服务端健康记录**（已验证正确、未部署）：丢了多少数据 / 整屏重建谁触发 / 连接为什么断
3. **一套判据基础设施**（`test/`，不进 App）：机器眼 + 巡检层 + 棘轮基线
4. **九条纪律 + 一条最贵的经验**（单测证明的是「如果输入按我们建模的方式来」）
5. **十几份根因与复盘文档**（见 §0 必读清单）

---

## §8 给后继的四条提醒

1. **用户的判断反复胜过我们的推理。** 今天改变方向的三句话全部来自用户：
   「别被概率性带偏」「局域网流畅、TS 就闪、你们复现不了是因为网络好」「每个改动都应该 commit」。
   **他报的现象和他指的方向，优先于任何内部推理。**

2. **在能验证之前不要改 App 代码。** 今天五次改动五次失败，**每一次当时都有证据**
   （单测全绿、红测转绿、模拟器实测通过）。问题不是不够谨慎，
   **是验证环境里造不出用户的现象**。
   → 唯一的出路是**把观测装到用户真实链路上**（仪表已就绪，等用户同意重启 daemon）。

3. **一条线索给一个明确的上限。** 今天每条线索都追到自然结束而非「值不值得继续」为止：
   D-36 七轮加三轮取证、发消息整屏刷 11 条假说、捏合三轮实现三轮验收。
   **单独看每轮都有理由继续，合起来就是一整天。**

4. **凡「复现不了」，先证明三件事**：测的是用户那个版本、工具本身有效、网络条件与用户一致。
   今天三件全部栽过 —— 七轮测错版本、两次工具是坏的、全程在局域网测一个只在慢网出现的现象。

---

**当前等用户的三个决定**（他一直没答，不要替他决定）：

1. **装不装仪表到生产 daemon**（重启断线几秒，之后他正常用，下次撞见问题直接读真实现场）
2. **哪一条现象最影响他用**（好把全部力量压那一条，而不是同时追五条）
3. **还是先停下来交接**
