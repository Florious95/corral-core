# 远程Agent安卓（暂名）——工程编排约定

手机远程操控主机 tmux 中大量 Agent CLI 的开源产品（Apache 2.0）。
产品需求的唯一权威是 `requirement-wiki/`（LLM Wiki 概念网络，先撞库再问用户）；
原始文档真相源是 `requirement-base/`（只增不删，维基从中分解概念）；
任务状态的唯一权威是 `taskbook.yaml` + `.team/evidence/`。

## 当前待办底册（2026-08-22 全停后）

- **`docs/交接任务书-性能基线与仓库重构.md`** —— 用户 2026-08-22 三连令（打开链路 Debug 日志仪表
  →模拟器性能基线（此后不许低于、优化须高于）→corral-core 重构为可被 corral-app 与桌面端引用）。
  下一任 leader 的开工首读，含打点位表/测定流程/切分骨架/红线。
- **`docs/优化点清单-1820.md`** —— 用户 2026-08-20 晚的 18 个优化点唯一底册（本轮实现已全部作废，
  契约 087–091 仍有效）。重启铁律：从基线 4120c0884 起**一次一条**，用户真机实测不倒退
  （金标准=打开会话秒进秒排，092 §11）才做下一条；图标只用 Provider 原生厂家官方图标（093 §2）。

## 远端仓库与推送（2026-08-22 记）

- **服务端远端仓 = https://github.com/Florious95/corral-serve** ：本地 `server/` 的过滤镜像
  （`--subdirectory-filter server`，剥凭据与二进制）。**本地服务端更新后必须推它**：
  `bash tools/mirror-pr-serve.sh [分支...]`（无参只推 main）。
- app/文档面远端仓 = corral-core：`bash tools/mirror-pr.sh [分支...]`。
- ⚠️ 谱系以 mirror-pr 两脚本为准；⛔ 不要再用 mirror-push.sh 推 main（strip 实现不同源，
  会撞祖先闸重写远端历史，2026-08-22 实撞）。
- 🔴 **PR 开发流程违纪在案（2026-08-21/22，用户点名两次）**：那一轮本地 seal/land 走完了，
  远端却几乎没有 PR；后来补开的 PR 又因 land 后才推而成批 closed 而非 merged，「一事一闭」
  没有成立。**此后铁律**：每格 seal 之后、评审派单之前，先推分支并开远端 PR
  （mirror-pr / mirror-pr-serve 会自动 `gh pr create`）；land 本地并线后立刻推 main 让该 PR
  显示 merged——一事一 PR 一闭环，远端 PR 列表就是流程存在的证明。

## 目录地图

- `requirement-wiki/` — 需求维基（LLM Wiki 概念网络：concepts/techniques/patterns/等，活库可改可删）
- `requirement-base/` — 原始文档真相源（entries/ 条目，分解时不删除）
- `web/` — Web 端客户端（同协议浏览器客户端，测试第一道）
- `test/` — 独立自动化测试工程（不嵌入产品代码）
- `taskbook.yaml` — 任务书（五栏+争议度）
- `agents/` — 席位角色文件（retired/ 为退役归档）
- `.team/evidence/` — 任务证据 JSON（状态唯一来源）

## 席位与模型

- ⛔⛔ **禁止使用 Deepseek 模型**（2026-08-21 用户令）。这条**取代**了下面那条旧的默认通道。
- ⛔⛔ **禁止开 Fable 5；评审席只能用 Opus 5**（2026-08-21 用户令）。
  评审席角色文件必须写 `model: claude-opus-5`。
  ⚠️ `model` 与 `dangerously_skip_permissions` 一样**只在启动时生效** ——
  改文件不够，必须 `remove-agent --force` + `add-agent` 重建，并读 pane 自证底部显示 `Opus 5`。
  ⇒ 新建席位用 `provider: grok`（`grok-4.6`）或 Claude 订阅
  （`provider: claude_code` + `auth_mode: subscription` + `profile: claude-default`）。
  异源评审席必须与实现席不同源：实现走 grok ⇒ 评审走 Claude 订阅。
- **旧条（已作废，留档）**：默认通道 `provider: claude_code` + `auth_mode: compatible_api`
  + `profile: worker-api`（deepseek-v4-flash）。升级仅两种：① `contention: contract`；② 返工达上限。
- 🔴 **新建席位必须在角色文件里写 `dangerously_skip_permissions: true`**（2026-08-21 实撞）。
  写 `false` 的席位会**停在 provider 的第一个确认提示上等人按键**，而编排层给出的唯一症状是
  `send_unverified_exhausted`（读起来像投递问题）。**该字段只在启动时生效**——
  改了文件再 `reset-agent` **不够**，必须 `remove-agent --force` + `add-agent` 重建。
- 🔴 **席位卡住时读它的屏**：`tmux -S /private/tmp/tmux-501/ta-<team> capture-pane -p -t <pane>`
  （team 私有 socket，不是用户真实 tmux）。用户不在屏幕前时，这是唯一能认出「卡在提示上」的手段。
- ⛔ **席位不许写 `/tmp` 或任何项目外路径**，临时文件写 `.team/nodes/<格>/tmp/`。
  各家 provider 对项目外路径的默认策略不同，靠加权限治不了根。
- 多模态缺陷（需看截图判断渲染效果）用 Claude 订阅席位，不用 deepseek。
- ⚠️ **cursor 席位按「restart = 失忆」对待**（框架队 2026-08-21 装机通告3 的运营事实，三路手工定性实锤）：
  cursor vendor 的 `--resume` **不载入历史回合** ⇒ 该席位 restart 后上下文实际丢失，机器面已诚实标注。
  ⇒ 若将来用 cursor 做执行席，**重要上下文必须靠任务书与产物落盘**，⛔ 不许指望席位记得。
  （本工程当前未使用 cursor 席位，这条是预防性记录。）
- 密钥只存在于 `.team/current/profiles/*.env`，任何席位禁止读其原文。
- **`.team/current/profiles/tailnet-test.env` 全员禁读**（含 leader）。里面是用户 tailnet 的
  auth key，只能通过 `TS_AUTHKEY` 环境变量注入测试节点，任何形式的 cat/grep/plist/Read 都禁止。
  取值只用 `set -a; . <file>; set +a` 注入子进程，不打印、不落日志、不入截图。
- **查任何配置前先想凭据**：`grep -i tailscale` 一个"偏好设置"文件就把 authkey 打上了屏
  （2026-08-13 实发，已请用户轮换）。同类禁令：无过滤 `ps aux`（暴露席位 API key）、
  `tail .team/logs/agentmirrord-prod.log`（daemon 明文打配对 token）。
  **Shadowrocket 的偏好 plist 与 `tailscale_keys.bin` 列入禁读。**
- 给席位发消息只走 `team-agent send`，禁 tmux `send-keys`。
- **凭据已泄露 ≠ 停工**（2026-08-13 用户裁定，2026-08-14 重申并批评过一次违反）：
  用户对 `tailnet-test.env` 的长期决定是「既然泄露了，就写进文件，接下来就用它去测」。
  再次泄露时**只做三件事：一行上报（不复述泄露的值）、就地收紧做法、继续干活**。
  **禁止**因此停工、禁止等新 key、禁止把删本地产物当成风险处置——
  片段一旦进入上下文就擦不掉，删截图减少的是执行者的不适而非真实风险。
  轮换与否是用户的事，不是开工前置条件。

- **起隔离 tmux 后必须自检"我在自己的 socket 上"**（2026-08-14 实发，当天即发现两个机理）：
  `tmux` 在建 socket 失败时**不报错，静默回退到默认 socket** —— 也就是用户的真实 tmux。
  于是"我在隔离"当场变成"我在动生产"，**且无任何告警**。已实证的两条回退路径：
  ① `TMUX_TMPDIR` 路径过长（unix socket 上限 ~104 字节，scratchpad 路径天生就长，必踩）；
  ② `TMUX_TMPDIR` 目录**未预先存在**（建目录失败也静默回退，已复现两次）。
  **不要去背这两条**——它们只是同一类问题的两个已知实例，第三个随时会出现。
  **唯一可靠的不变量是自检**：
  ```
  mkdir -p /tmp/e2e-<席位名>          # 短路径，且预建
  unset TMUX
  tmux -S <sock> new-session -d ...
  tmux -S <sock> list-sessions         # ← 自检：会话必须在自己的 socket 上，否则立刻停手
  ```
  参照 `e2e/layer2.sh` 既有做法。
  **踩了怎么办**：照凭据泄露那条办——一行上报、只清理自己建的东西、就地收紧、继续干活，
  不停工、不做补偿性大动作。

- 派单必经 `.team/ta`（净化包装器）。
- 派单必写 intent，否则看门狗全盲。
- 同一 Gradle 模块同一时刻只放一席施工。

## 提交纪律（2026-08-14 用户裁定）

**commit 无需用户确认。每个关键点都必须提交。**

- 关键点 = 一个缺陷修完并验过 / 一个功能落地 / 一次归档回退 / 一次契约裁定落纸。
- **一次修复一个提交**（纪律⑦）：不要把多个缺陷、多个功能揉进一个提交，
  否则回滚时分不清、也追不出"哪一次改动引入了倒退"。
- **验过才提交**：席位自报不算，leader 客观核过（跑门 / 核 dex / 用户实测）才算。
  未验的挂着不提交，比提交一个假的绿更安全。
- **不许攒**：本工程 2026-08-12 发生过整条修复以未提交状态被回退抹掉的事故。
  攒着不提交 = 把已完成的工作暴露在下一次回退里。
- **重启生产 daemon 无需确认**（2026-08-14 用户裁定：「没有什么可配合的，什么时候断服务端都可以」）。
  照旧只做三件：先备份现二进制、换、起完核 `9900` 在听。不要为此停下来问。
  席位仍然禁止碰生产 daemon，这条只对 leader 生效。
- **不写 `Co-Authored-By: Claude`**（2026-08-14 用户裁定「Contributor 应该是我」）。
  这条**覆盖** Claude Code 默认要求加该署名的行为。历史上已有的 210 个带该署名的 commit
  不动本地（sha 是证据链的引用键），由 `tools/mirror-push.sh` 在推远端时摘掉。

## 诊断日志纪律（2026-08-14 实发教训）

**记判据的操作数，不要只记判决。**

一个分支没走进去时，日志里只写「未触发」等于什么都没说——你分不清是
「压根没被调用」「被调用了但守卫拦下」还是「守卫算错了」。这三种在日志里都是沉默。

- 凡是有守卫/阈值/比较的地方，**把参与比较的两边的原始数值都记下来**，
  再记结论。例：`viewportOutgrewEmulator: viewport_rows=42 emulator_rows=17 → true`。
- 记**触发来源**（IME / 可见性 / 旋转 / 用户操作），否则同一条记录出现两次时分不清是谁引起的。
- 判据永远是那句：**光看导出的日志，就能判断出是「该做而没做」还是「做了但做错了」。**
  做不到就是仪表没做够，不是用户没描述清楚。

实发：2026-08-14 用户报「回前台后终端只画了上面三分之一」，导出日志后一无所获——
`TermViewPresenter` 全文只有一处 `DiagLog.record`，且只被字号变更与实测回写调用，
`onViewportSizeChanged` / `onRealViewportChanged` / `viewportOutgrewEmulator` **零记录**。
我们拿着一个为诊断而生的日志，诊断不了这个。

## 工程红线

- 代码必须带外骨骼注释（机器可校验标注），架构维基从代码现算
- halt 是默认：缺字段、判不出 ⇒ 停下问，绝不猜
- 契约级议题定夺前，相关模块不施工

## 对外部框架队的配合边界（2026-08-15 用户裁定）

**禁止为框架队取证。唯一配合的事项是：换用他们发布的新基础设施。**

- 他们要求「做一次复现」「按这个顺序取证」「跑一下这个阶梯」「保留现场等我来」——
  **一律拒绝**。不做复现、不做取证阶梯、不为他们保场。
- 他们已经装了被动采集的基础设施（如 tmux trace shim）。**我们正常干活，
  证据他们自己就采到了。** 取证是他们的工作，不是我们的。
- **唯一配合项**：他们发布新版本/新二进制并通知更换时，照做（重启角色、换二进制、
  自证版本号）。除此之外一概不做。
- 我方发现问题仍然可以报（现象+日志路径，一封信一个文档路径），但**报完就走**，
  不进入「他回一封、我查一轮」的循环。

### 全自动编排出问题时：投完报告就继续（2026-08-18 用户裁定，**取代** 2026-08-15 的「停工等修好」）

**凡是编排中断、卡住、或框架侧出缺陷：① 写 bug 分析报告（现象 / 日志支撑含量具身份 / 复现 / 原因分析及其边界）
② 投给对应的 leader ③ 投完就让任务继续跑，不停工。**

- 用户原话：「如果框架停下来，你不用停下来。我们来日方长，使用这个框架的机会还很多。
  你把报告投递了，就可以让任务继续了。」
- **凡是编排中断、又没有提醒我结束的，一律 P0**（2026-08-18 令），必须写报告投递，
  收件人是 `/Volumes/nvme/Projects/讨论team-agent::team/leader`。
- 仍然不许：绕过缺陷自己打补丁改他们的 skill/引擎、人肉编排顶替全自动编排、
  为框架队做复现取证阶梯（2026-08-15 那条边界不变）。
- 仍然要先分归属：我方账本/任务书写错（我自己改任务书重跑，不算框架缺陷），
  还是对方投递/编排层的缺陷（才写 P0）。**归属判错就会白报或白等。**
  实测：一天内三次「编排卡住」，两次根因在我的任务书歧义上。

**为什么改**：2026-08-15 那条的成因是 leader 替对方做了四轮调查、用户要的改动点一个都没往前走。
真正要防的是**替对方调查**，不是**继续干自己的活**。写完报告投出去，责任就交割了；
停下来等，既不加快他们修复，又让自己的交付停摆。

**旧条（已作废，留档）**：2026-08-15「① 立刻反馈 ② 中断手上全部工作 ③ 停下等修好」。

## 工程常识红线

交付物功能验收之外必须自证：
1. **静默经济**：空闲 CPU 趋近 0、无固定频率子进程派生
2. **进程卫生**：单实例守卫、退出后零孤儿零残留端口
3. **资源有界**：内存/磁盘增长有界或有轮转
4. **可达性常识**：广播地址必须对端可达
5. **失败可见**：任何动作有限时间内必得可见结果

## 派单铁律（2026-08-12）

1. **basegen 必须走**：`python3 tools/basegen.py <task-id>` 编译影响闭包作为知识基底。
2. **开发+测试+审查三席并行**：测试席写场景红测，审查席写根因探针红测，开发席直接开始改——三席并行不阻塞，在红测上汇合。审查席不替代也不干扰测试席。
3. **archwiki --strict-t3 必须 PASS**。
4. **一次只改一个缺陷**，模拟器实测验证不倒退后才打包下一个。
5. **心跳周期接近 1 小时**（保护缓存，缓存过期 1 小时）。

## 眼见为实铁律（2026-08-12，v5 教训）

修缺陷两头都必须在模拟器/真机上**亲眼看到**：
1. **改之前必须复现**：在模拟器上走复现步骤，截图/录屏留证，看不到问题不开工。
2. **改之后必须看到修复**：同样的复现步骤重走一遍，问题消失 + 不倒退，看不到修复不收工。
3. **单元测试绿 ≠ 问题修了**：测试验证代码正确性，模拟器验证用户体验，两者不可替代。
4. **不允许标记"已修"除非有模拟器实测截图**：自报完成 ≠ 真完成，截图是唯一证据。

违反本条的后果已实证：v5 五个修复中三个 QA PASS 但引入输入框闪烁回归，因为跳过了"不倒退"验证。

## 「回炉」流程（2026-08-12）

凡修复后实测未生效，一律执行：
1. 回退改动
2. 开审查席看回退的 diff，反推根因，产出**根因探针**
3. **回退后跑探针**：命中 → 诊断正确；不命中 → 诊断错误
4. 审查席+测试席+开发席三席并行：审查写探针、测试写场景红测、开发改代码
5. **修完后跑探针**：不再命中 → 修复有效；仍命中 → 没改到点上

原则：失败的 diff 是信息不是垃圾。根因探针是验收标准。
回炉的工程原理：agent 上下文有限倾向加代码，腐败代码正反馈堆叠，回炉先删再做打断循环。

## 测试三层流水线（W-02）

1. Web 端 + Chrome DevTool MCP → 拦 80% 问题
2. 安卓模拟器 + adb/uiautomator → 端到端
3. 用户手测 → 最终验收

### ✅ 第 2 层已解除（2026-08-19 用户指令：「这次要模拟器调试通过」）

**2026-08-19 起模拟器可用**，且对反复出问题的缺陷**必须**用模拟器验到通过，不许再只交单测。
解除理由是用户原话：同一个问题已经两轮没修好，「让我来手动看不行」。
⚠️ 资源仍要看：起之前确认 free+inactive 内存充裕、load 不高（当初禁它是因为 free 一度只剩 59MB、load 29）。

**以下为 2026-08-14 的旧禁令，已作废，留档说明当时为什么禁：**

### ~~⛔ 第 2 层暂停（2026-08-14 用户指令）~~

**暂时不能开模拟器。任何席位不得启动 emulator / qemu，不得依赖 adb 设备。**

- 原因：本机资源到临界——模拟器单进程占约 6 核 + 2.38GB，被终止前全机 free 内存
  一度只剩 59MB、load 29；终止后 free 回到 3.5GB。
- 替代路径：**第 1 层 web 端**承接协议层与可计算的几何/状态验证；
  **第 3 层用户真机**承接安卓特有的渲染时序与生命周期（闪烁、首帧尺寸、CJK 字形、
  IME 与后台回前台），证据形式 = 用户截图 + 导出的诊断日志。
- **禁止用模拟器截图充当验收证据**（现在也没有模拟器可用）。
- 解除由用户或 `refactor-maintainability` team leader 通知，未通知前不得自行启动。

**附记（这不是妥协，是纠正）**：模拟器 2026-08-14 曾给出一个错误的绿——
在**裸 shell**（`alt=0 hist=1850`）上验滚动全绿，而用户实际用的是 **alt-screen 的
Claude Code**（`alt=1 hist=0`），两者不是同一种东西。**用户真机才是权威被测对象。**
因此第 2 层暂停期间，「补仪表」的要求只升不降：真机是唯一的渲染验收路径，
而用户能给我们的只有截图和日志——仪表做不够，就只能反复问他"当时是什么状态"。

## 看门狗

由 `.team/watchdog-supervisor.sh` 守护托管，每次派单前按 cwd 核活。
macOS 无 setsid，用 `python3 -c 'os.setsid(); os.execvp(...)'`。

## leader 不亲力亲为：探索性活派给席位，用追问收敛（2026-08-22 用户令）

**用户原话**：「你不要亲力亲为，一些探索性任务你可以让 team mate 去做，有疑问你还可以追问。
像这种提问并且得到回复的，效率是最高的，对于你上下文来说是最节省的。」

- **探索性任务一律外派**：找锚点/摸代码/查现状/试环境/读大文件——这些是席位的活，不是 leader 的活。
  leader 亲自 grep 一圈，等于把整棵文件树塞进自己的上下文，而结论只有一句话。
- **追问是主要手段，不是补丁**：席位回来的答复不清楚、有歧义、或你想换个角度看，
  **直接 `team-agent send` 追问**（同一席位带着上下文，一问一答成本极低），
  ⛔ 不要因为「问不清就自己去看」而把活收回来。
- **leader 只保留三件事**：定判据、做裁定、并线（seal/land/推 PR）。
  其余凡是能写成一句话派出去的，都派出去。
- 与既有铁律的关系：这条**强化**「leader 不亲写产品码」——现在连**探索性阅读**也归席位；
  leader 的上下文留给判断，不留给素材。

## 席位不许主动给 leader 发消息（2026-08-22 用户令）

**用户原话**：「不允许 TeamMate 在非编排调整的情况下给你发消息。」

- 席位的**唯一正常出口是 `report_result` + 落盘产物**。想说的话写进
  `.team/nodes/<本格>/说明.md`，⛔ 不要 `team-agent send` 给 leader。
- **唯一例外 = 编排调整**：本格没法继续、需要 leader 改账本/加格/改判据/裁定归属——
  这类才允许发消息，且一条说清（现象+已试过什么+需要 leader 决定什么）。
  进度汇报、思路说明、「我开始干了」、「这里我有个想法」⛔ 一律不发。
- **理由**：每一封信都在花 leader 的上下文，而上下文就是判断力。
  席位的思考应该落在文件里给判者看，不是塞进 leader 的会话。
- 这条要写进**每个新席位的角色文件**与派单任务书，否则对席位不生效（席位只读派单正文+任务书）。
