// Package config loads the daemon configuration from command-line flags and
// environment variables.
//
// See config.go for the contract summary. This file exists so the package
// retains a one-sentence doc comment even if config.go is refactored; keep
// the package documentation in sync between both files.
package config
