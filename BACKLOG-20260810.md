# agentmirror 剩余任务点总账（2026-08-10 22:0x 落盘）

> 立此文的原因：用户判定「团队管理混乱、开发进度混乱、改动面未知」，并已下令**一次性清空全部 64 个席位**
> （已执行，零存活、零 codex 残留、team session 已空）。本文是清场后的唯一进度权威快照，
> 用于重新梳理"这一轮到底还剩什么要修"。
>
> **判据纪律**：本文每一条的状态都不采信席位自报，标注来源为下列三种之一——
> `代码实证`（我 grep/读源码看到）｜`验收实证`（我自己复跑 acceptance 的 rc）｜`未验`（只有交件自述）。

---

## §0 一句话现状

- 任务书 55 案 DONE；本轮开出的 6 案中 **4 案交件 pass、2 案被清场杀在半路**。
- **全部改动悬在工作区未提交**：23 个源码/测试文件 + 7 个证据文件。这就是"改动面未知"的实体。
- dogfood 13 个有效缺陷中，**9 条本轮已有代码落地但未入库**，2 条未开工，2 条已撤销/降级。
- 对外交付（重打 APK 交用户）**尚未发生**，用户手机上仍是 2026-08-09 21:19 的旧版。

---

## §1 待入库：工作区未提交的改动面（最高优先，先结这笔账）

清场时这些席位已交件或已改完代码，产物全在工作树里没提交。**任何后续动作之前必须先把这笔账结掉**，
否则下一次误操作就全丢了。

### 1.1 已交件 `pass`（证据在，需我复跑 acceptance 后入库）

| 任务 id | 覆盖缺陷 | 证据自述 | 需复跑的 acceptance |
|---|---|---|---|
| `fix-dogfood-pairing-ux` | D-07 token 明文上屏 / D-14 无重配入口且注释谎称有 / D-11 token 吊销文档欠账 | pass，2 条 tests，0 deviation | `cd app && ./gradlew -q :app:testDebugUnitTest`；`cd server && go test -count=1 ./internal/pairing/...` |
| `fix-dogfood-upload-media` | D-03 上传名丢扩展名 / D-02 拍照直传（017 R-8）/ D-01 相机权限二次拒绝哑按钮 | pass，8 条 tests，0 deviation | `cd app && ./gradlew -q :app:testDebugUnitTest` |
| `fix-dogfood-term-ux` | D-04 滚动方向反 / D-08 缺加载态 / D-09 emoji 吃后随空格 | pass，7 条 tests，0 deviation | `cd app && ./gradlew -q :app:testDebugUnitTest`；`cd app && ./gradlew -q :terminal:test` |
| `fix-recovery-baseline` | discovery 全程失败后恢复基线不推 delta（红线5 失败可见） | pass，3 条 tests，0 deviation | `cd server && go test ./internal/api/...` |

### 1.2 被清场杀在半路（代码已落地，缺验收与证据）

| 任务 id | 代码实证 | 缺什么 |
|---|---|---|
| `fix-upload-auth` | `server/internal/api/upload.go` 已有 `uploadBearerToken()`；`api_test.go` 已有 `TestUploadAuthentication`；`options.go`/`server.go`/`docs/protocol.md`/`server/README.md` 均有改动 | 没跑完 acceptance、没落证据。**这是 P0 安全项**（`POST /upload` 原先零鉴权，任何能连 :9900 的人可往用户主机写文件） |
| `perf-thresholds-enforce` | `e2e/api-user-scenarios.sh` 已改 | 门限是否真落进 `baseline.json.performance.hard_numeric_thresholds` 未验；`large_output` / `upload` 两项基线仍可能为 null |

### 1.3 改动面清单（23 个源码/测试文件，git status 实测）

```
app/app/src/main/java/dev/agentmirror/app/AgentMirrorApp.kt
app/app/src/main/java/dev/agentmirror/app/MainNavState.kt
app/app/src/main/java/dev/agentmirror/app/pairing/PairingScreen.kt
app/app/src/main/java/dev/agentmirror/app/session/SessionScreen.kt
app/app/src/main/java/dev/agentmirror/app/termview/TermSurfaceView.kt
app/app/src/main/java/dev/agentmirror/app/workspace/WorkspaceScreen.kt
app/app/src/main/java/dev/agentmirror/app/workspace/WorkspaceViewModel.kt
app/app/src/test/kotlin/dev/agentmirror/app/workspace/WorkspaceViewModelTest.kt
app/terminal/src/main/kotlin/dev/agentmirror/terminal/CharWidth.kt
app/terminal/src/test/kotlin/dev/agentmirror/terminal/CharWidthTest.kt
app/terminal/src/test/kotlin/dev/agentmirror/terminal/WideCharTest.kt
docs/protocol.md
e2e/api-user-scenarios.sh
server/README.md
server/internal/api/api_test.go
server/internal/api/discovery_failure_test.go
server/internal/api/options.go
server/internal/api/server.go
server/internal/api/upload.go
（+ e2e/artifacts/test-api-user-scenarios-perf/ 下 4 个运行产物）
```

---

## §2 dogfood 13 缺陷逐条销账表

来源 `e2e/artifacts/dogfood/REPORT.md`（682 行，48 张截图，两代 dogfood 席位产出）。

| 编号 | 缺陷 | 级别 | 当前状态 | 判据来源 |
|---|---|---|---|---|
| D-01 | 相机权限二次拒绝后按钮静默无响应 | P1 | **代码已落地未入库** — `SessionScreen.kt:153` 有 `transientError = "相机权限未授权，请到系统设置中开启后重试"` | 代码实证 |
| D-02 | 「+」只有相册，017 R-8 拍照直传缺席 | P1 | **代码已落地未入库** — `SessionScreen.kt:587` 有 `camera-${timestamp}.jpg` 的 MediaStore 写入 | 代码实证 |
| D-03 | 上传名传 MediaStore 数字 ID，扩展名全丢 | P1 | **代码已落地未入库** — `SessionScreen.kt:559/569` 改查 `OpenableColumns.DISPLAY_NAME` 并加 `attachmentFileName(displayName, mimeType)` | 代码实证 |
| D-04 | 终端滚动方向与全平台惯例相反 | P1 | **代码已落地未入库** — `TermSurfaceView.kt` 有改动，具体符号需入库前复核 | 代码实证（弱：未逐行确认符号） |
| ~~D-05~~ | 触控目标不足 48dp | — | **已撤销**（dogfood 席位自查误判，撤销记录保留在 REPORT §1） | 报告实证 |
| D-06 | 空闲/未知色相同构 | 观察 | **降级为观察项**（有意设计），不作为缺陷 | 报告实证 |
| D-07 | 配对 token 明文上屏 | P2 | **代码已落地未入库** — `PairingScreen.kt:479` 仍有 `Text("配对 token")` 标签（这是输入框 label，非 token 值本身，入库前需确认值不上屏） | 代码实证（弱：需复核值是否遮蔽） |
| D-08 | 连接后先闪空态，缺加载态 | P2 | **代码已落地未入库** — `WorkspaceScreen.kt`/`WorkspaceViewModel.kt`+对应测试有改动 | 代码实证 |
| D-09 | emoji 单宽排版吃掉后随空格 | P2 | **代码已落地未入库** — `CharWidth.kt` + `CharWidthTest`/`WideCharTest` 有改动 | 代码实证 |
| ~~D-10~~ | 命令拼接 | — | **已撤销**（复验不复现） | 报告实证 |
| D-11 | 017 R-4 token 吊销文档欠账（`-token` flag 亦缺文档） | P2 | **代码已落地未入库** — `server/README.md` + `docs/protocol.md` 有改动，需确认确实写了"删 token 文件重启即全量吊销" | 代码实证（弱） |
| D-12 | 017 R-6 README「锁中文」明示缺失 | P2 | **未开工** | — |
| D-13 | 上传目录无上限/无清理/无说明 | P2 | **部分**：并入 `fix-upload-auth` 目标，代码是否含目录上限未验 | 未验 |
| D-14 | 配对后再无重配入口，注释谎称有设置钮 | P1 | **代码已落地未入库** — `app/app/src/main/java/dev/agentmirror/app/SettingsScreen.kt` 存在且注释写明"单档设置页：重新配对成功时覆盖现有主机配置" | 代码实证 |
| D-15 | 017 R-5 通知全局开关未实现 | P2 | **未开工** | — |
| D-16 | 客户端在线时 daemon 持续 14.8% CPU | P1 | **已入库**（`fix-connected-idle-economy`，证据 status=pass）；API 套件三态实测已连接单订阅 CPU 1.20% | 验收实证 |

**小结**：13 条有效缺陷（D-05/D-10 已撤销、D-06 降级）中——
**9 条代码已落地待入库**、**2 条未开工（D-12、D-15）**、**1 条部分（D-13）**、**1 条已入库（D-16）**。

---

## §3 017 场景审计八项承诺兑现表

| 承诺 | 当期口径 | 状态 |
|---|---|---|
| R-1 特殊键 | 会话页快捷键条 Esc/Ctrl-C/Tab/方向键；协议 input 帧加可选 keys 字段 | **已入库**（`feat-input-keys-server` + `feat-input-keys-app`）；但 judge 曾指出"仅验回执、未验终端效果"，**终端效果验证仍欠** |
| R-2 多行粘贴 | 走 server 既有 `paste-buffer -p` 括号粘贴整段注入，App 不拆分；`?2004` 退化风险文档标注 | **未验**（API 套件未验证项含 "Real Claude Code/Codex multiline bracketed-paste behavior"） |
| R-3 重配对 | 设置页入口 + 单档覆盖，多主机档案后置 | **代码已落地未入库**（D-14 同项） |
| R-4 token 轮换 | 文档化"删 token 文件重启即全量吊销"，版本化多 token 后置 | **代码已落地未入库**（D-11 同项） |
| R-5 通知 | 全局开关；每工作区静音后置 | **未开工**（D-15） |
| R-6 国际化 | 锁中文并在 README 明示；抽取翻译后置 | **未开工**（D-12） |
| R-7 无障碍 | 徽章补 contentDescription 语义标注；其余后置 | **未验**（API 套件未验证项含 "Chinese-only UI and accessibility content descriptions"） |
| R-8 拍照 | 当期补实现，不可赖账 | **代码已落地未入库**（D-02 同项） |

---

## §4 只能在用户真机上验的项（自动链覆盖不到，必须走真机验收）

来源：`e2e/artifacts/test-api-user-scenarios-perf/baseline.json` 的 `unverified` 字段（套件自述，我复跑过 rc=0）。

1. **真扫码**（相机识别 QR）与多网卡可达性
2. Android workspace/session UI 与终端**视觉一致性**（018 逐图目检关）
3. **通知投递**与全局通知开关（R-5 未实现，实现后仍需真机验）
4. **App 进程被杀后恢复**、**锁屏重连**
5. 设置页单档**重配流程**（代码已落地，真机流程未走）
6. **中文界面与无障碍** contentDescription
7. **真实相机拍摄**（当前只覆盖主机侧上传/路径管线）
8. 真实 Claude Code / Codex 的**多行括号粘贴**行为

> 交付前置：这些项要求先**重打 APK 交用户**——用户手机现装的是 2026-08-09 21:19 版，
> 本轮所有修复（含 TS 链）都不在里面。

---

## §5 产品级缺口（本轮新发现，未立案或立案未完）

| 缺口 | 来源 | 级别 | 状态 |
|---|---|---|---|
| `POST /upload` 无鉴权 | API 场景套件实测 | **P0 安全** | 代码已落地未验收（§1.2） |
| 无独立 HTTP 配对端点，配对只能经 WS auth/auth_ack 测 | API 场景套件 deviations | 设计观察 | 未定夺是否要补 |
| 上传目录无大小上限/无轮转 | D-13 | P2（红线3 资源有界） | 并入 upload-auth，未验 |
| 性能门限尚未硬化 | 基线已出，门限已由 leader 依基线定（见 taskbook `perf-thresholds-enforce`） | P2 | 代码已改未验收 |
| `large_output` / `upload` 两项性能基线为 null | baseline.json 实测 | P2 | 未补齐 |
| 生产 daemon 死因 unknown | `audit-prod-daemon-lifecycle`（已入库 8a8ac60） | 观察 | 已加日志落盘启动脚本 + 存活探针，等下次复现取证 |

---

## §6 编排/基础设施侧欠账（不影响产品功能，但今天烧掉了大半时间）

详见同日 `docs/orchestration-deviation-20260810.md`。摘要：

1. 解释器需回归**无状态**（状态只从 taskbook + evidence 推，删私有 case 台账）
2. 需补**进度停滞判据**（全队 0 BUSY 且 backlog 非空 = 最高级异常）——今天三次长时间空转全部漏报
3. 返工需改用 **`fork-agent` 黄金基底分身**，而非弃 id 重建（今天 TS 案重建 8 代，每代从零读现场）
4. 失败预算 2 用尽后应开**顾问席出一页纸方案**，而不是同路重派
5. 核验需配**阳性对照**（当前多处"空输出即通过"）
6. 契约级变更（如验收口径从摄像头改 API）应**先过顾问站/需求基**，不得直接改任务书就派

---

## §7 建议的推进顺序（我自裁，出错我认）

1. **先结未提交账**：逐案复跑 acceptance → 绿则窄提交入库 → 红则记差口（§1）
2. **P0 安全收口**：`fix-upload-auth` 跑完验收入库（§1.2）
3. **补两条未开工**：D-12（README 锁中文明示）+ D-15（通知全局开关）
4. **性能门限硬化 + 补两项 null 基线**（§5）
5. **R-1 终端效果验证**、**R-2 括号粘贴真实行为验证**（§3 的两处"未验"）
6. **重打 APK 交用户 → 真机验收 8 项**（§4）——这一步需要用户参与，是唯一必须升级人工的节点
7. 编排侧校正（§6）与产品推进并行，不阻塞产品线
