# t.verify · 回炉④ 模拟器复验（ledger.hl1.v1 r21）

结果=stillblank
截图=.team/nodes/hl1-verify/shot-static-cold16-end.png, .team/nodes/hl1-verify/shot-static-re16-end.png, .team/nodes/hl1-verify/shot-static-resume16.png, .team/nodes/hl1-verify/shot-hist-cold16-firstframe.png, .team/nodes/hl1-verify/shot-hist-re16-firstframe.png, .team/nodes/hl1-verify/shot-hist-resume16.png, .team/nodes/hl1-verify/shot-l2-static.png, .team/nodes/hl1-verify/shot-l2-hist.png, .team/nodes/hl1-verify/shot-01-pairing.png, .team/nodes/hl1-verify/shot-l1-static.png

status=stillblank

## 开工自检

```
PWD=/Volumes/nvme/Projects/远程Agent安卓/.worktrees/hl1.verify
BRANCH=（空：detached）
HEAD=cb044e6d9acb5567e732bed2cafbf4b36dce99b7
```

`pr/srv-first-frame` 已被 `.worktrees/hl1.sfix` 占用，本格 `git checkout --detach pr/srv-first-frame`（与该分支同一 SHA）。本格不建分支、未 commit、未 push、未碰 main。

## 被测物（显式偏离：分支 commit 无 unique diff）

`git log main..pr/srv-first-frame` 为空；分支 SHA `cb044e6d9` 落后 main `612c27525`。修复只存在于 hl1.sfix **未提交** 工作树：`server/internal/api/ws_handler.go` +20/−14（handleSubscribe 先 snapshot 再 Resize 再 Subscribe）。

- daemon：`go build` 自 `.worktrees/hl1.sfix/server` → `.team/nodes/hl1-verify/tmp/agentmirrord`（未 `stash apply`）
- app：仓根 main 的 `./gradlew -q :app:assembleDebug`（UP-TO-DATE），apk sha256=`22a1aa66fba38eaf927f3e6adf31f56371f81b9acddf505dddb326510624483d`
- 这是任务书「用 pr/srv-first-frame go build daemon、配当前 main debug 包」在「分支尚未落 commit」下能做的最接近做法，不是静默改验收定义。

## 隔离环境（契约 092 §4）

- 模拟器 `emulator-5554` 已在跑。未自起、未杀。adb=`~/Library/Android/sdk/platform-tools/adb`
- tmp 短路径 `.team/nodes/hl1-verify/tmp/`（socket 81 字节）
- `ln -s /bin/bash tmp/bin/claude`；`ps -o comm=` 两 pane 均为 `claude`（未取 argv）
- `unset TMUX`；`TMUX_TMPDIR=tmp/tmux tmux -f tmp/tmux.conf` 会话 `hl1v-static` / `hl1v-hist`
- 自检：`TMUX_TMPDIR=tmp/tmux tmux -f /dev/null list-sessions` 见上述两会话；socket=`tmp/tmux/tmux-501/default`
- 静态备用屏：`source frozen-alt.sh` → `alternate_on=1` + capture 含 `STATIC_ALT_MARKER_092`（订阅前）
- 大滚回：`history-limit 50000` + `seq 1 40000` → `history_size=39983`，alt=0，末行 `HIST_DONE_092` + `claude-3.2$`
- daemon listen `0.0.0.0:22351`，`AGENTMIRROR_STATE_DIR=tmp/state`（不碰 9900 pidfile）；`adb reverse tcp:22351 tcp:22351`
- 冷启：`pm clear` 后装 debug 包；手填 `ws://10.0.2.2:22351/ws` + 本席 token；截图前 `keyevent 111`
- 只点 L1 的 `cwd-static` / `cwd-hist`（cwd 在本 tmp）。未点「远程Agent安卓」「多agent协作」等真实舰队。daemon 仍会扫到默认 socket（10 workspaces / 48 sessions），名单里有舰队是预期，手指没点。

## 步骤与计秒（画布量具：y=320..2000 子采样；绿= dark≥45% 且 color≥30；顶栏/底栏不计入）

### 0. 配对

1. pairing 页可见「手填连接」。shot-01-pairing.png
2. 填 url 后点「连接」。首轮 daemon 未设 `TMUX_TMPDIR`，L1 只有舰队 8 区（shot-03-l1-timeout.png）。重启本席 daemon 并带 `TMUX_TMPDIR=tmp/tmux` 后 L1=10 区，出现 `cwd-hist` / `cwd-static`。shot-l1-static.png

### 1. 静态 alt-screen（cwd-static）· 冷点开

1. force-stop 冷启 → 只点 `cwd-static` → L2 一行 `bash` / 空闲。shot-l2-static.png
2. 点该行。顶栏（bash / LAN / 查看）+ 底栏（Esc/Tab/输入指令）在；**终端画布全黑无字**。
3. 连拍至 16.4s：全程 n=26250 dark=26250 color=0。无 FIRST_FRAME。
   - shot-static-cold16-end.png（t=16.4s 仍无字）
   - 先前 8s 轮次 shot-static-cold-end.png 同形
4. **2s 门：失败。16s 仍失败。**

### 2. 静态 · 返回列表再进

1. BACK 回 L2 再点 `bash`。
2. t=1.61s..16.27s 全程 color=0。shot-static-re16-end.png
3. **仍无字形。**

### 3. 静态 · 后台→前台

1. HOME 后 `am start` 回到会话页。shot-static-resume16.png
2. 画布仍全黑 color=0。**仍无字形。**

### 4. 大滚回（cwd-hist，hist=39983，alt=0）· 冷点开

1. force-stop 冷启 → 只点 `cwd-hist` → L2 `bash` / 空闲。shot-l2-hist.png
2. 点该行：
   - t=1.791s color=0（画布仍空）
   - t=2.709s color=214 → FIRST_FRAME。shot-hist-cold16-firstframe.png 可见 `HISTLINE 39979..40000` / `HIST_DONE_092` / `claude-3.2$`
3. **2s 门：失败（首个有字样本 2.709s）。不是整页白，是晚于 2s 的首帧。** 量具第一张 raw screencap 约 1.6s 才回来，1.79s 那帧仍空，不能倒推成「2s 内已有字」。

### 5. 大滚回 · 再进 / 回前台

1. 再进：t=1.569s 已有字。shot-hist-re16-firstframe.png
2. HOME→前台：画布仍有 HISTLINE。shot-hist-resume16.png
3. 这两步有字形；冷点开仍未进 2s。

## 判定

| pane | 冷点开→字形 | 再进 | 回前台 | 对 2s 门 |
|---|---|---|---|---|
| 静态 alt-screen | **>16s 无字** | **>16s 无字** | **无字** | 失败=白屏 |
| 大滚回 hist=39983 | 2.709s 有字 | 1.569s 有字 | 有字 | 冷点开未进 2s |

验收绿条件是两种 pane 都「冷点开 2s 内画布有字形」。静态备用屏仍是 repro2 同形（chrome 在、画布空），故 **status=stillblank**。

daemon 在静态订阅路径上有 `snapshots_from_subscribe`（sendq 非 0），不等于画布有字。与 sprobe 世界 B / repro2「snapshots_from_subscribe=1 仍空画布」同形。本席不据此改判据。

## 未做 / 边界

- 未点真实舰队会话。
- 未自己起/杀模拟器。
- 未 git commit / push。
- 架构闭包无卡命中（BASE 原文）；本格只做模拟器复验，不猜包。
- 量具第一帧 ~1.6–1.8s 才采样，不能分辨「1.0s 已有字」。静态 16s 全黑不受此限。
