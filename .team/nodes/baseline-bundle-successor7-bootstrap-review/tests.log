successor7 apparatus bootstrap zero-context review (read-only)

Inputs reviewed: successor7 taskbooks, BOOTSTRAP-RESULT.md, all seven
bootstrap logs, owned-emulator/apparatus/continuity/impl-bypass/bypass-
regression/emulator-regression/verify scripts, fixture and apparatus helper.

Read-only checks:
- shell syntax for the seven successor7 shell scripts: exit 0;
- Python AST parse for baseline-bundle-successor7-apparatus.py: exit 0;
- historical provenance object 548572dfd7d8ee2e3f602a274268e8bd881ef8b2 and
  referenced tests.log: present;
- fixed fixture path and digest inputs: present; candidate fixture is currently
  untracked in this pre-commit bootstrap WT, so durability still requires the
  promised bootstrap commit;
- no adb/qemu/emulator/device was started (per task boundary).

Refuting findings:
1. owned-emulator.sh lines 17-30 enters fixture mode whenever
   SUCCESSOR7_FIXTURE_MODE=1 and production only concatenates four named
   SUCCESSOR7_TEST_* variables. It neither rejects a non-empty fixture-mode
   variable in production nor rejects arbitrary SUCCESSOR7_TEST_* variables.
   The task requires SUCCESSOR7_FIXTURE_MODE and all SUCCESSOR7_TEST_* to be
   empty in production. This permits an override surface to reach production.
2. apparatus.py verify_evidence checks the JSON fields but never checks the
   official evidence file mode. atomic_json writes 0600, but a post-write chmod
   to 0644 is accepted; the required fixed-0600 gate is therefore not enforced.
3. owned-emulator.sh run_bounded lines 49-52 sends TERM and then performs an
   unbounded wait. stop_runner likewise waits without a deadline. This violates
   the task's all-waits/commands-bounded cleanup contract and can prevent the
   required cleanup/recovery decision from being reached.

Bootstrap logs are producer claims only and do not erase these source-level
counterexamples. verdict: refutes
