successor7 apparatus bootstrap second review (fresh, read-only)

Inputs: current successor7 taskbooks, BOOTSTRAP-RESULT.md, bootstrap logs,
owned-emulator/apparatus/continuity/impl-bypass/bypass-regression/
emulator-regression/verify scripts, fixture, and apparatus helper.

Fresh source checks:
- all seven shell scripts parse with `sh -n`: exit 0;
- apparatus helper AST parse: exit 0;
- fixture digest matches its contract; historical provenance object
  548572dfd7d8ee2e3f602a274268e8bd881ef8b2 and referenced path exist;
- no successor7 final ledger is present; fixture remains pending the promised
  bootstrap commit, as required at this bootstrap stage;
- no adb/qemu/emulator/device was started.

Previous refutes closed:
- selector source accepts only empty/unset production or exact `1` isolated
  mode and rejects other non-empty values before runner launch;
- production counts `SUCCESSOR7_TEST_*` names before apparatus setup and
  isolated mode requires the fixed harness and node-local fixture root;
- `require_private_regular` enforces regular, non-symlink, exact `0600`
  official evidence; regression logs include the derived-path and 0644 teeth;
- timeout and runner cleanup use deadline, TERM grace, known-PID KILL grace,
  bounded reaping, bound-serial cleanup, and reject forced cleanup with exit 2.

Remaining positive evidence: permanent fixture green/forged/missing are
0/1/2 with historical provenance; continuity binds the four successor6 tasks,
retained `wt-maple-core`, manifest/archive identities, and the expected
successor6 commit; safety log records no final ledger and no real tools/device.

verdict: pass
