# t.q8 · 说明（查了什么、哪条卡住）

席位：`in-dev2`。worktree：`.worktrees/wt-q8`。本格 **只读产品码、不改**。

## 查了哪些文件（任务书点名的坐标，没有全仓 grep）

| 路径 | 用来答 |
|---|---|
| `server/internal/bridge/bridge.go` | Q1 Snapshot=`capture-pane -e -p`；Q2 Resize；Q8 tmux 次数 |
| `server/internal/bridge/stream.go` | **Q5** 单 pipe、先 detach 再 attach、detach 无参 `pipe-pane` |
| `server/internal/bridge/doc.go` | pipe-pane / capture 职责 |
| `server/internal/api/ws_handler.go` | Q2 subscribe 顺序；Q1 snapshotWithCursor；Q6 resize no-op；Q7 ensureInitialScan vs handleList |
| `server/internal/api/ws_conn.go` | Q5 relay EOF 无通知；Q8 perf_subscribe |
| `server/internal/api/pane_geometry.go` | Q4 acquire/release |
| `server/internal/api/server.go` | Q7 listingLoop |
| `server/internal/api/options.go` | defaultListInterval=2s |
| `server/internal/api/listing.go` | listing 的 cols/rows 来自 discovery Width/Height |
| `server/internal/discovery/scan.go` | `#{pane_width}x#{pane_height}` |
| `server/internal/protocol/frametype.go` / `frames.go` | Q6 动词闭集 |
| `docs/基线-20260822-release.md` | Q8 三时间戳有记录、无分段预算 |
| `server/internal/api/host_pane_geometry_red_test.go` | Q4 多客户端基线意图（注释里的旧红已由 pane_geometry 修记账） |

`server/internal/bridge/restart_pipe_test.go` 只证实「残留 pipe 会 toggle」，用来理解 detach-first，不单独当 Q5 答案。

## 卡住 / 标了「需实测」的

- **Q1 / Q3**：daemon 确定不折行。一行是否等于网格、行尾宽字符 capture vs 重放是否一致，是 tmux 两条路径。本格在 `/tmp/e2e-q8` 起过隔离 tmux（自检在自己的 socket 上），但 pane 里是带 prompt 的 shell，capture 被 prompt 污染，**数字作废、不写入答复**。收工已 `kill-server` 并删目录。要测：空 `cat` pane、固定 cols、只 `printf` 一行再 `capture-pane -e -p`。
- **Q8**：能数同步 `tmux` 次数，切不出捕获/编码/WS 各占多少。没有跑 loopback 550 次（任务没要求复现他们的 180ms）。
- **Q5 与 2026-08-23 白屏**：源码确定多订阅者会抢 pipe。当天是否「网页后订抢走手机」**没有日志**，答复里写成可能叠在死 socket CPU 上，不写成已证实的同一根因。

## 没改任务定义

8 条都答了。置信度分布见判据输出。没有为了凑「源码确定」去猜 tmux。

`sh tools/perfbase/judge-q8.sh` → `rc=0`（日志 `.team/nodes/t.q8/judge.log`）。首次跑红是因为「源码确定」块里写了无目录前缀的 `ws_handler.go` / `stream.go`，判据按相对 worktree 的路径找文件；已改成 `server/internal/...` 全路径后再绿。

## 生产 / 残留

未碰 `:9900`。隔离实验目录已删。
