# t.core 说明（仓内切分 :core-protocol / :core-terminal / :core-conn）

席位：施工席 `pb-impl` / 格 `pb-core`。worktree：`.worktrees/wt-pb-core`。未 commit / 未 push。

本 worktree HEAD 是立格账本 `ed79f179c`（仪表/白屏修复在其它 worktree）。conn 层当时无 PerfTrace 调用，打点注入只覆盖 DiagLog。

## 改了什么

一次一个模块，每步全量单测：

### 1. `:core-terminal`（改名复用 :terminal，未另抄）

- `app/terminal/src` 整树搬到 `app/core-terminal/`。
- `:terminal` 留作 `api(project(":core-terminal"))` 门面，保留 `:terminal:test` 任务名。
- 单测：`.team/nodes/pb-core/tmp/step1-terminal.log` — `BUILD SUCCESSFUL in 37s`

### 2. `:core-protocol`

- 搬：`BinaryFrame` / `FrameCodec` / `Frames` / `FrameEnums` / `Keys` / `CodecErrors` / `Frame`。
- 编解码红测 `ConnCodecTest` 随模块走。
- `json` 由 `internal` 改为模块公开（跨模块访问，逻辑未改）。
- 单测：`.team/nodes/pb-core/tmp/step2-protocol.log` — `BUILD SUCCESSFUL in 28s`

### 3. `:core-conn`

- 搬：`Connection` / `ConnectionManager` / `ReconnectPolicy` / `Clock` / `Transport`。
- `DiagLog.record` 改为 `ConnDiag.record` 函数引用；app `DiagLog` init 注入。核模块零 `import android.`。
- `ConnReconnectPolicyTest` 随模块走；依赖 FakeWebSocketTransport 的 conn 单测仍在 `:app`（`internal` 测试夹具给 app 其它测试用）。

三核 `build.gradle.kts` 均 `org.jetbrains.kotlin.jvm` + `java-library`，无 `com.android.*`。

## 转绿（判据）

```
$ bash tools/perfbase/judge-core-split.sh
```

stdout：`PASS 三核模块切分成立且全量绿`  退出码：**0**

日志：`.team/nodes/pb-core/tmp/split-run.log` — `BUILD SUCCESSFUL in 28s`

## 没做到 / 边界

- 未改包名（仍 `dev.agentmirror.app.conn` / `dev.agentmirror.terminal`），避免全仓 import 重写。
- 本格不把 app 壳迁到 corral-app（那是 t.repo）。
- 本树无八事件 PerfTrace 接线；若后续把仪表树合进来，核里继续走 `ConnDiag` 同类回调，事件名/格式由 app 壳保持。
- 没开模拟器、没碰 9900、没 commit/push、没改 `tools/perfbase/*.sh`。
