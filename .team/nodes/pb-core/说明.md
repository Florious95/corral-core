# t.core 说明（rev24 · 在含仪表+白屏修复的 main 上重做切分）

席位：施工席 `pb-impl` / 格 `pb-core`。worktree：`.worktrees/wt-pb-core2`。未 commit / 未 push。

基点：`829ae28a3`（main 已含仪表 t.instr* 与白屏修复 t.fixblank `e8343236e`）。上一轮切在 `ed79f179c` 上所以量具为 0，本轮在当前 main 重做。

## 改了什么（一次一模块）

### 1. `:core-terminal`（改名复用 :terminal，未另抄）

- `app/terminal/src` 搬到 `app/core-terminal/`。
- `:terminal` 留 `api(project(":core-terminal"))` 门面。
- 单测：`.team/nodes/pb-core/tmp/step1-terminal.log` — `BUILD SUCCESSFUL in 45s`

### 2. `:core-protocol`

- 搬帧编解码（`BinaryFrame`/`FrameCodec`/`Frames`/…）。`json` 仅放宽 `internal`→公开。
- 单测：`.team/nodes/pb-core/tmp/step2-protocol.log` — `BUILD SUCCESSFUL in 37s`

### 3. `:core-conn`（量具与白屏修复跟着走）

- 搬 `Connection`/`ConnectionManager`/`ReconnectPolicy`/`Clock`/`Transport`。
- **`addBinaryListener` 按 ref 分发原样留在 ConnectionManager**（进核）。
- `SessionViewModel.init` 仍 `addBinaryListener(ref, this)`（留在 app 壳）。
- PerfTrace 仍在 app.perf（Android Log/Handler）；核通过 **`ConnPerf` 回调** 转发，八事件名与格式未改。
- `DiagLog` 经 **`ConnDiag`** 注入。核模块零 `import android.`/`androidx.`。

## 转绿

```
$ bash tools/perfbase/judge-core-split.sh
```

stdout：`PASS 三核模块切分成立且全量绿`  退出码：**0**

日志：`.team/nodes/pb-core/tmp/split-run.log` — `BUILD SUCCESSFUL in 33s`

自证量具未拆：`PerfTrace.kt` 仍有 `EV_TAP`/`ws_binary_recv`/`reason=ref_mismatch`/`listener_ref=`；
`ConnectionManager.addBinaryListener` 在 `:core-conn`。

## 没改的

- 未改包名、未改分发逻辑、未改八事件格式。
- 没开模拟器、没碰 9900、没 commit/push、没改 `tools/perfbase/*.sh`。
