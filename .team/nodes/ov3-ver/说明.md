# t.ver · 070 跨语言线上帧回炉验收

账本：`ledger.overlay-reforge.v1` revision 3，`t.ver`  
契约：`requirement-base/entries/070-悬浮窗回炉与跨语言线上帧判据.md`  
探针只读：`.team/nodes/ov3-probe/probe-wire.sh`  
仓根干活（`worktree_id=wt9.ver` 只是互斥标签）。未改探针、未改产品（定点破坏已还原）。未开模拟器、未碰默认 tmux、未扫 argv。

时刻：`2026-08-19T03:32:55Z`

## 修前红 / 修后绿

来源：t.probe 说明（exit 1）对照本席重跑探针（exit 0）。

| | 修前（t.probe 红） | 修后（本席绿） |
|---|---|---|
| 线上 `overlay_subscribe` | `{"socket":"/tmp/tmux-1000/default\u001f%3"}` | `{"socket":"/tmp/tmux-1000/default"}` |
| listing 金样 `ref=s1` | `socket raw="s1"` → **红：不是路径** | `listing_extracted=""`，**不发**该帧（空则就地拦） |
| 结构 ref 字面 `\u001f` | `structural_extracted` 整段含 `\u001f` → **红：字面分隔符** | `structural_extracted="/tmp/tmux-1000/default"` |
| Go ③ | `FAIL overlay_subscribe.json socket 含字面\u001f` + `FAIL …_listing_token.json socket 不是路径` | `TestWireFixtureImport exit=0` |
| 探针 | exit 1 | **exit 0** `probe-wire ALL PASS` |

修前红在 **③「别的」**（socket 有字段但不是可订阅路径），不是 JSON 缺字段。同语言闭环当时是绿的。

### 修后探针原文

```
PASS ① fixtures=13 -> app/wire-fixtures
{"v":1,"type":"overlay_subscribe","payload":{"socket":"/tmp/tmux-1000/default"}}
listing_ref="s1"
listing_extracted=""
structural_extracted="/tmp/tmux-1000/default"
TestWireFixtureImport exit=0
probe-wire ALL PASS
PROBE_WIRE_EXIT=0
```

## 全套不倒退

| 命令 | 结果 |
|---|---|
| `cd server && go test ./... -count=1` | **exit 0**（10 包：cmd/agentmirrord, api, bridge, config, discovery, overlay, pairing, protocol, provider, tsnetd） |
| `cd app && ./gradlew -q testDebugUnitTest` | **exit 0** |

## debug APK + dex

```
./gradlew -q assembleDebug
-rw-r--r--  39539389  app/app/build/outputs/apk/debug/app-debug.apk
```

`dexdump`（build-tools 35.0.0）Class descriptor：

- `Ldev/agentmirror/app/overlay/OverlayEmulator;`
- `Ldev/agentmirror/app/workspace/FavoriteBook;`
- `Ldev/agentmirror/app/workspace/FavoriteRecord;`
- `Ldev/agentmirror/app/workspace/FavoriteKey;`
- `Ldev/agentmirror/app/ThreePane;`

三个名字都在包里。未用模拟器截图。

## 定点破坏（对照席）

破坏：`sessionSocketFromRef` 退回只切 U+001F、无 `/` 守卫（t.probe 红样本）。

```
MUTATED_PROBE_EXIT=1
overlay_subscribe.json socket raw="/tmp/tmux-1000/default\u001f%3"
FAIL ③ overlay_subscribe.json socket 含字面\u001f
overlay_subscribe_from_listing_token.json socket raw="s1"
FAIL ③ … socket 不是路径 raw="s1"
```

恢复后 `RESTORED_PROBE_EXIT=0`。`SessionViewModel.kt` 仍含字面 `\u001f` 切分与 `/` 守卫。sha256 `dd095bf5bd417c54ff6232701645e083f0f94a999ecf40af56b0be5d6f070aa6`。

自检：坏实现下 `probe-wire.sh` 不会仍返回 0。

## 机械五条

| 判据 | 期望 | 实测 |
|---|---|---|
| A-v-probe | 0 | **0** |
| A-v-go | 0 | **0** |
| A-v-app | 0 | **0** |
| A-v-apk | 0 | **0**（apk 非空 + dex 三符号在） |
| A-v-doc | 本文件 | 本文件 |
