# t.compose · E2/E3/E4/E5 · 膨胀多行 + 键条并存 + 附件发送进框 + 观感（契约 087）

## r10（2026-08-21）返工：只补功能先验红 + 删 `@Suppress`

评审席 refutes：说明里唯一原始红是 `A-compose-doc` 的 `hit=[]`，机械必然红，
与 E2–E5 无关。功能四条当时只是代码叙述。分支污染由 leader 处理，本席不管。

本回合：⛔ 未改设计。只做两件事：
1. 删 `SessionShellScreen` 上的 `@Suppress("ModifierParameter")`（棘轮已改为不含行号）。
2. 加只用旧 API 的红测，把改前原始输出贴进来。

**为什么原先机械红不算数：** 没说明文件 / 空分支必然红，区分不了「发送闸吞 Tab」或「发送在框外」。
**为什么下面能区分两个世界：** E3 期望 keyFrames +1、改前实际 0；E4 改前没有 `session-composer` 节点。
「高度 == 40.dp」改前会绿，不能当先验红（评审已指出），故不写那条。

### E3 先验红（产品码 = main 的 sendKey 闸）

`E3NoGatePriorRedTest`：`sendDraft()` 进 Sending 后立刻 `sendKey(TAB)`，断言 keyFrames 数 +1。

```
$ git checkout main -- app/app/src/main/java/dev/agentmirror/app/session/ \
    app/app/src/main/java/dev/agentmirror/app/ui/screens/SessionShellScreen.kt \
    app/app/src/main/java/dev/agentmirror/app/ui/theme/DesignTokens.kt
$ env -u TEAM_AGENT_API_KEY ./gradlew :app:testDebugUnitTest --tests 'dev.agentmirror.app.session.E3NoGatePriorRedTest'

E3NoGatePriorRedTest > sendKeyWhileDraftSendingStillEmits FAILED
    java.lang.AssertionError: expected:<1> but was:<0>
	at org.junit.Assert.assertEquals(Assert.java:146)
	at dev.agentmirror.app.session.E3NoGatePriorRedTest.sendKeyWhileDraftSendingStillEmits(E3NoGatePriorRedTest.kt:29)
```

改前 Sending 闸吞键，0 个 Tab 帧。改后必须是 1。

### E4 先验红（旧签名 SessionShellScreen）

`ComposerOldSignaturePriorRedTest.sendCentersInsideComposerSurface`：旧签名、无新参数。

```
ComposerOldSignaturePriorRedTest > sendCentersInsideComposerSurface FAILED
    java.lang.AssertionError: Failed to retrieve bounds of the node.
Reason: Expected exactly '1' node but could not find any node that satisfies: (TestTag = 'session-composer')
	at dev.agentmirror.app.session.ComposerOldSignaturePriorRedTest.sendCentersInsideComposerSurface(ComposerOldSignaturePriorRedTest.kt:83)
```

改前发送在 DraftField 外、没有 composer 表面 tag。改后 tag 存在且 send 中心在表面内。

### 验绿（产品码 = HEAD，已删 Suppress）

```
$ git checkout HEAD -- app/app/src/main/java/dev/agentmirror/app/session/ \
    app/app/src/main/java/dev/agentmirror/app/ui/screens/SessionShellScreen.kt \
    app/app/src/main/java/dev/agentmirror/app/ui/theme/DesignTokens.kt
$ env -u TEAM_AGENT_API_KEY ./gradlew :app:testDebugUnitTest \
    --tests 'dev.agentmirror.app.session.E3NoGatePriorRedTest' \
    --tests 'dev.agentmirror.app.session.ComposerOldSignaturePriorRedTest' \
    --tests 'dev.agentmirror.app.session.ComposerChromeTest' \
    --tests 'dev.agentmirror.app.session.SessionViewModelTest'
BUILD SUCCESSFUL
```

## 开工自检

```
$ pwd
/Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr2.input

$ git branch --show-current
pr/e2-composer
```

- 分支名：`pr/e2-composer`
- **未提交**：席位不 commit / push。封版由 `A-compose-seal` 做。
- 本格只做标题这一条，没有再切去 `pr/e1-resync` 或其它 PR 分支。
- 方案仍是思路 §0 方案 1（本地缓冲 + 光标锚定）。未改成方案 2，未改成发送才同步。

## 改了哪些文件

- `app/.../session/SessionViewModel.kt` — E3：`sendKey` 不占 `inputStatus` 发送闸；独立 `controlKeyStatus`；发送在途仍能发 Tab/Esc/Ctrl-C。
- `app/.../session/SessionScreen.kt` — `composerExpanded`；发送后收起；`sendEnabled` 只跟草稿 Sending；附件菜单改由 shell 加号锚点；`TermSurfaceView` `isFocusable=false`（E5 点终端不抢焦）。
- `app/.../ui/screens/SessionShellScreen.kt` — **write_paths 未列此文件，思路 §3–5 点名它**。E2 膨胀、E3 KeyCap `canFocus=false`、E4 加号/发送收进圆角表面、加号 DropdownMenu。显式报出。
- `app/.../ui/theme/DesignTokens.kt` — 只加 `inputHeightExpanded = 120.dp`。同样不在 write_paths，思路 §3 点名。显式报出。
- `app/.../test/.../SessionViewModelTest.kt` — Sending 断言改为控制键不进闸；`sendKeyWhileDraftSendingStillEmits`。
- `app/.../test/.../ComposerChromeTest.kt`（新）— 膨胀高度、发送在框内、点 Esc 不抢 draft 焦点。
- `app/.../test/.../E3NoGatePriorRedTest.kt`（r10）— 旧 API：Sending 中 Tab 必须发出 keys 帧。
- `app/.../test/.../ComposerOldSignaturePriorRedTest.kt`（r10）— 旧签名：send 在 session-composer 内。
- r10 删了 `SessionShellScreen` 的 `@Suppress("ModifierParameter")`。

未改 KeyBar（旧 composable，不顺手删）。未改 TermSurfaceView 源文件（只在 SessionScreen factory 关获焦）。

## 修法

- E2：收起 `inputHeight` / 展开 `inputHeightExpanded`；点输入框聚焦即展开，点 `❯` 切换，发送成功收起。换行仍走 084，不发 Enter。
- E3：控制键 ack 走 `controlKeyStatus`；键帽 `focusProperties { canFocus = false }`。
- E4：同一 `clip(RoundedCornerShape)` 内放加号 / 文本 / 发送；芯片只展示已附加数量，不写入 TextFieldValue。
- E5：终端 View 不可获焦。深浅主题模拟器截图 **本格未跑**（见查不清）。

## 先验红（改产品码之前）

### A-compose-doc

```
hit=[]
# exit 1
```

### A-compose-seal

改前工作区干净，但 `pr/e2-composer` **已经有** 两份误封的 E1 commit（`3a18ac9e7`、`b0671b9d4`），`rev-list main..HEAD` ≥ 1。
这条机械判据在改前就会绿（断言分支非空，不断言「有 E2 文件」）。**不能**当 E2–E5 的世界变化。世界变化在 ComposerChromeTest / sendKeyWhileDraftSendingStillEmits。

### A-compose-wiki / A-compose-smell（改前回归棘轮，不是本缺陷的红）

开工时 session 包 wiki 新增=0；app lint 基线 16。

### 功能先验红

r10 已把改前原始输出贴在文首 §r10。机械 `A-compose-doc` 不算功能红。

## 验绿

### 功能测试

```
$ env -u TEAM_AGENT_API_KEY ./gradlew :app:testDebugUnitTest \
    --tests 'dev.agentmirror.app.session.SessionViewModelTest' \
    --tests 'dev.agentmirror.app.session.ComposerChromeTest' \
    --tests 'dev.agentmirror.app.session.SessionResyncTest' \
    --tests 'dev.agentmirror.app.session.DiffSyncTest'
BUILD SUCCESSFUL
```

`sendKeyWhileDraftSendingStillEmits`：Sending 中 Tab 仍发出 keys 帧。ComposerChromeTest 三条绿。DiffSync 四条仍绿（F-087-3）。

### A-compose-suite

```
$ cd .../.worktrees/pr2.input/app
$ env -u TEAM_AGENT_API_KEY ./gradlew -q :app:testDebugUnitTest
suite_exit=0
```

### A-compose-wiki

```
pkg=dev.agentmirror.app.session 基线=1 本次=0 新增=0 消掉=1
  ✅ 消掉: T3-4 dev.agentmirror.app.session :0
wiki_exit=0
```

未 `--freeze`。T1 未红。

### A-compose-smell

r10 已删 `@Suppress`。棘轮键不含行号后：

```
face=app 基线=16 本次=16 新增=0 消掉=0（键不含行号）
smell_exit=0
```

### A-compose-doc

本文件。含「先验红」与「git branch --show-current」。

### A-compose-seal

席位不跑。

## 查不清

- E5 user gate 模拟器深浅主题截图：本格未起模拟器（skill 默认把 emu 日志写 `/tmp`，本格禁 `/tmp`；E5 是用户拍板）。结构已按公开 README 做圆角一体框。
- `TermSurfaceView` 改前是否抢焦：未 `adb dumpsys`。factory 里关了 `isFocusable`，若本来就不能获焦则无行为差。
- Left/Right `eolUncertain`（思路 §2.4）本格未做。
- 附件预贴是否支持 UI 撤单张：无 API，芯片只读。
- write_paths 只有 `session/` + `pr2-compose/`，E2/E4 的壳在 `ui.screens` / `ui.theme`。已改并报出，没有把 Composer 重写进 session 包。
