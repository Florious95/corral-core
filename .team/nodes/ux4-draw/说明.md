# t.draw 返工 r9 · 8000µs 只卡改后；真负载下两密度都变快

结构改动留着（跳过默认底 / 同色底合并 / advance 缓存 / 进页行列缓存 / TermDrawMeter）。
**本轮不再做任何新的绘制优化。** 未 git checkout/restore。仓根施工。

`dt_us_p95 < 8000` **只对改后（opt=1）**。改前只记录。8000 数值未改。

## 计时边界（真机可复现）

`TermSurfaceView` 继承 `android.view.View`，**不是** SurfaceView。

| 字段 | 含义 |
|---|---|
| `dt_lock_us` | 恒 `-1`。没有 `lockCanvas`。 |
| `dt_post_us` | 恒 `-1`。没有 `unlockCanvasAndPost`。 |
| `surface=view` | 哨兵：这条路径不是 Surface 提交。 |
| `dt_us` | `onDraw` 方法入口→出口（含 `super.onDraw`）。**不含** 返回之后的 HWUI 合成 / GPU。 |
| `dt_super_us` | `super.onDraw` |
| `dt_chrome_us` | `publishThemeChrome` |
| `dt_clear_us` | 整画布一次 `drawRect` 铺默认底 |
| `dt_lines_us` | 逐行 `drawLine`（铺格 + drawText + 几何） |
| `dt_body_us` | chrome + clear + lines（我们自己的 CPU） |

p95 环只收 `textDraw>0` 的稳态帧，丢掉前 10 帧 warmup；空屏走 `source=onDrawEmpty`。10s 窗口 `[term-draw]` 上限 **12**。n≥120 才是稳态。

## 本轮权威读数（驱动器独立复跑，真负载）

`textDraw` 445/365，不再是空格 run。前后同负载（cellsNonBlank/textDraw 逐项相同）。

| 密度 | 组 | n | bgRect | textDraw | cellsNonBlank | dt_us_p95 | gfx total |
|---|---|---|---|---|---|---|---|
| d480 | 改前 | 120 | 1637 | 445 | 1636 | 7905（只记） | 774 |
| d480 | 改后 | 120 | 1 | 445 | 1636 | **6496**（<8000） | 794 |
| d440 | 改前 | 253 | 2230 | 365 | 2229 | 8270（只记，≥8000 不判红） | 823 |
| d440 | 改后 | 256 | 14 | 365 | 2229 | **6417**（<8000） | 819 |

- d480：**−18%**（7905→6496）
- d440：**−22%**（8270→6417），**比 d480 还多**

`OUTSIDE d480 before 23095µs / after 22504µs`，远大于 `dt_us_p95` 6.5ms ⇒ 瓶颈在 onDraw 之外。

## 作废：第 3 轮「d440 收益约等于零」

第 3 轮驱动复跑 d440 3916→4005（+2.3%）时，负载几乎是空屏（`textDraw` 约 42 条空格 run，甚至改后 `textDraw=0 cellsNonBlank=0` 的空帧）。空屏上跳过默认底几乎没活可干，所以看不出收益。

**那条结论作废。** 真负载下两个密度都变快，d440 并不更差。

先前本席自跑的 −8.4% 同样是空屏/噪声，**不当结论**。

## 结论

1. 计时边界正确：`View.onDraw` 方法体，没有 lock/post。62ms 是冷首帧。
2. **真负载**：跳过默认底 d480 −18%、d440 −22%。收益不是「依赖密度才有」，第 3 轮的零收益是空屏负载造成的。
3. 改后 `dt_us_p95` 6.4–6.5ms < 8000。改前 d440 8270 只记录。
4. `outside_us` 22–23ms **大于** onDraw ⇒ 瓶颈仍在 `onDraw` 返回之后。
5. **不再加绘制优化。**

## 风险

- 真机尚未导出；用户看 `[term-draw]` 的 `n` / `dt_us_p95` / `dt_lines_us_p95`。
- 083 §0：gfxinfo 绝对值无渲染分辨力；本格只用它证明「比 onDraw 墙钟大」。
- 085 / 083 未动。无遮掩手段。
