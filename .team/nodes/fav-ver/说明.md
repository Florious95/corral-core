# t.ver 验收：067 收藏 + 三栏

- 账本：`ledger.favorites.v1` revision 4，`t.ver`
- 契约：`requirement-base/entries/067-收藏与三栏布局.md`
- 探针：`.team/nodes/fav-probe/probe-fav.sh`（只读，未改）
- 仓根干活（`worktree_id=wt7.ver` 只是互斥标签，未 `git worktree add`）
- 未开模拟器、未碰默认 tmux、未扫 argv
- 时刻：`2026-08-18T23:40:30Z`

## 机械三条

| 判据 | 命令 | 结果 |
|---|---|---|
| A-v-probe | `bash .team/nodes/fav-probe/probe-fav.sh` | **exit 0**（t.probe 时为红 / exit 1） |
| A-v-suite | `cd app && ./gradlew -q testDebugUnitTest` | **exit 0**，XML 汇总 106 套件 / **552** 测 / 0 fail / 0 err / 0 skip |
| A-v-doc | 本文件 | 非空 |

自检：坏实现下 `probe-fav.sh` 仍会返回 0 吗？**不会**——见下定点破坏。

## 四条裁定的实测输出

### 裁定 1：身份只用结构字段（不得用标题当键）

探针产品世界 ①（夹具标题 `PROBE_TITLE_067_TASKSUMMARY_WILL_CHANGE_xyz` 故意不进结构字段）：

```
product ① operands blob='[{"sessionName": "favprobe_sess", "windowIndex": "13", "windowName": "favprobe_win", "addedAt": 1700000000123}]' title_in_blob=False has_session_name=True has_window_index=True has_window_name=True
PASS product ① 记录无标题且含结构字段
```

扫描：`persist_fields=['sessionName', 'windowIndex', 'windowName', 'addedAt']`，无 `title`。

配套：`TestFavorite.persistRebuildKeepsStructuralKeyNotTitle` — XML `failures=0`。

### 裁定 2：窄屏三页滑动，冷启动中间页（会话）

探针产品世界 ④：

```
=== product scan ===
pager pages=['favorites', 'sessions', 'settings'] initial='sessions'
product ④ operands pages=['favorites', 'sessions', 'settings'] page_count=3 initial='sessions' want_pages=['favorites','sessions','settings'] want_initial='sessions'
PASS product ④ 冷启动落在中间页（会话）
```

配套：`TestThreePane.panesAreFavoritesSessionsSettingsAndStartOnSessions`、`coldStartRendersSessionsPageNotFavorites` — XML `failures=0`。

### 裁定 3：按加入时间倒序（最近收藏在最上）

探针不覆盖排序。具名测试 `TestFavorite.newestAddedSortsFirst`：先藏 `old@addedAt=1`，再藏 `new@addedAt=9`，断言 `rows.sessionName == [new, old]`。

```
TEST-dev.agentmirror.app.workspace.TestFavorite.xml
  tests=5 failures=0 errors=0 skipped=0
  testcase newestAddedSortsFirst time=0.02
```

`./gradlew testDebugUnitTest --tests '*Favorite*' --tests '*ThreePane*'` → BUILD SUCCESSFUL。

### 裁定 4：失联保留并置灰，绝不自动移除

产品世界 ③（live 从有到空）：

```
product ③ operands live_before=[('favprobe_sess', '13', 'favprobe_win')] live_after=[] rows_before=[{'key': ('favprobe_sess', '13', 'favprobe_win'), 'online': True, 'gray': False}] rows_after=[{'key': ('favprobe_sess', '13', 'favprobe_win'), 'online': False, 'gray': True}] present=True gray=True online=False
PASS product ③ 会话消失后该行仍在且置灰
```

扫描：`reconcile_policy='keep_gray'`。

③ 先在自动移除写法上验红（探针 phase 0，exit 2 守卫未触发）：

```
auto-remove ③ operands live_before=[('favprobe_sess', '13', 'favprobe_win')] live_after=[] rows_before=[{'key': ('favprobe_sess', '13', 'favprobe_win'), 'online': True, 'gray': False}] rows_after=[] present=False gray=False online=None
FAIL auto-remove ③ 会话消失后未保留置灰行 present=False gray=False online=None
RED-EVIDENCE ③ auto-remove: favorite written, session dropped from live list, row vanished (auto-remove filtered it out) → ③ FAIL as required
```

配套：`TestFavorite.ghostKeptGrayAndReviveBySameStructuralFields`、`offlineRowNotOpenableButStarRemoves`、`TestThreePane.offlineFavoriteDoesNotOpen` — XML `failures=0`。

## 定点破坏（对照席）

破坏对象：`app/app/src/main/java/dev/agentmirror/app/ThreePane.kt` 一处 `rememberPagerState(initialPage = 1)` → `initialPage = 0`。

破坏后 `probe-fav.sh`：

```
pager pages=['favorites', 'sessions', 'settings'] initial='favorites'
product ④ operands pages=['favorites', 'sessions', 'settings'] page_count=3 initial='favorites' want_pages=['favorites','sessions','settings'] want_initial='sessions'
FAIL product ④ 冷启动不是中间会话页 pages=['favorites', 'sessions', 'settings'] initial='favorites'
RED product ④
FAIL 收藏探针未全绿
MUTATED_PROBE_EXIT=1
```

恢复 `initialPage = 1` 后再跑：`RESTORED_PROBE_EXIT=0`，`probe-fav ALL PASS`。`git diff -- ThreePane.kt` 空。sha256 `6da03521183e983221a6a13d3856e05d7a133602fa545f267baf6c700ec44832`。

两头成立：坏实现下具名命令不返回 0；恢复后返回 0。探针未改。

## 探针全文（恢复后 / 验收用）

```
=== phase 0 自动移除写法（③ 必须红）===
auto-remove ① operands blob='[{"session_name": "favprobe_sess", "window_index": "13", "window_name": "favprobe_win", "added_at": 1700000000123}]' title_in_blob=False has_session_name=True has_window_index=True has_window_name=True
PASS auto-remove ① 记录无标题且含结构字段
auto-remove ② operands before_n=1 after_keys=[('favprobe_sess', '13', 'favprobe_win')] want=('favprobe_sess', '13', 'favprobe_win')
PASS auto-remove ② 重建后收藏仍在
auto-remove ③ operands live_before=[('favprobe_sess', '13', 'favprobe_win')] live_after=[] rows_before=[{'key': ('favprobe_sess', '13', 'favprobe_win'), 'online': True, 'gray': False}] rows_after=[] present=False gray=False online=None
FAIL auto-remove ③ 会话消失后未保留置灰行 present=False gray=False online=None
RED-EVIDENCE ③ auto-remove: favorite written, session dropped from live list, row vanished (auto-remove filtered it out) → ③ FAIL as required
=== product scan ===
favorite_impl_files=["app/app/src/main/java/dev/agentmirror/app/workspace/FavoriteRecord.kt", "app/app/src/main/java/dev/agentmirror/app/workspace/FavoriteBook.kt", "app/app/src/main/java/dev/agentmirror/app/workspace/FavoriteStore.kt", "app/app/src/main/java/dev/agentmirror/app/workspace/FavoriteList.kt"]
persist_fields=['sessionName', 'windowIndex', 'windowName', 'addedAt'] reconcile_policy='keep_gray'
pager pages=['favorites', 'sessions', 'settings'] initial='sessions'
=== phase 1 产品世界 ===
product ① operands blob='[{"sessionName": "favprobe_sess", "windowIndex": "13", "windowName": "favprobe_win", "addedAt": 1700000000123}]' title_in_blob=False has_session_name=True has_window_index=True has_window_name=True
PASS product ① 记录无标题且含结构字段
product ② operands before_n=1 after_keys=[('favprobe_sess', '13', 'favprobe_win')] want=('favprobe_sess', '13', 'favprobe_win')
PASS product ② 重建后收藏仍在
product ③ operands live_before=[('favprobe_sess', '13', 'favprobe_win')] live_after=[] rows_before=[{'key': ('favprobe_sess', '13', 'favprobe_win'), 'online': True, 'gray': False}] rows_after=[{'key': ('favprobe_sess', '13', 'favprobe_win'), 'online': False, 'gray': True}] present=True gray=True online=False
PASS product ③ 会话消失后该行仍在且置灰
product ④ operands pages=['favorites', 'sessions', 'settings'] page_count=3 initial='sessions' want_pages=['favorites','sessions','settings'] want_initial='sessions'
PASS product ④ 冷启动落在中间页（会话）
probe-fav ALL PASS
PASS 四条断言全过
```

## 未做

- 未改探针、未改产品（破坏已恢复）。
- 未用模拟器截图。验收路径 = 探针 + JVM 单元测试。
