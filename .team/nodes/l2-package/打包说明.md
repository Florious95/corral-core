# 061 打包（t.package / r8）

只打包，未改任何实现代码。未起模拟器，未碰用户 tmux / 生产 daemon。

## APK

- 路径：`/Volumes/nvme/Projects/远程Agent安卓/app/app/build/outputs/apk/debug/app-debug.apk`
- 命令：仓根 `app/` 下 `./gradlew :app:assembleDebug --rerun-tasks`（exit 0）
- size：39377065
- sha256：`304b4fdf5af2f72c76f749e2ba3b7de3bf15ee683001fddc17b705ff04a4f839`
- mtime：2026-08-18 21:01:29

## 不是旧产物

对 `app/app/src/**/*.kt` 与 APK mtime 比较：`newer_kt_count=0`。没有任何 `.kt` 比这份 APK 新。

## 收口：跨端探针（不是 dex 符号）

`bash .team/nodes/l2-design/probe.sh` → exit 0

- 隔离 tmux socket `/tmp/e2e-l2-design/tmux/l2p`，自检会话 `l2probe`
- 隔离 daemon 听 `127.0.0.1:52779`（非 9900）
- 订阅后收到 `type=level2_frame`，title 含 marker，`status=working`
- 5s 内改成 ✳ 后再收到 `status=idle`

`PASS 跨端：订阅后收到真实 ◐ 行 working，5s 内 ✳ 变为 idle`
