# Go / Rust 语料对齐（t.parity / r4）

- 单一真相：`tools/nodeprobe/fixtures/titles.tsv`（`title<TAB>state<TAB>provider`）。
- Go 测试 `TestL2FixtureCorpusParity`（`server/internal/api/l2detect_corpus_parity_test.go`）沿 cwd / 源文件向上找**这一份**文件，逐行对 `classifyPaneTitle` 断言 status。
- **没有**把语料复制到 `server/`。`find server tools -name titles.tsv` 只能命中 1 个。
- 加新 CLI：只往这份 tsv 加样本，Go 与 Rust 同时被约束。
