# t.ver 独立验收报告 · ledger.vz.v1 r19

席位：vz-v1-ver。仓根 `/Volumes/nvme/Projects/远程Agent安卓`（未进 `.worktrees/`）。
**结论：通过。** 未改产品码。相对 r18 三条红（光学对齐 / 灯+空查看菜单 / term-left-edge 连打）在本场复测已绿。
模拟器 `emulator-5554`，已装仓根 debug APK（lastUpdateTime=2026-08-20 10:56:00）。
adb reverse `tcp:9900`。截图前 `keyevent 111`。trap 后 `wm density` = Physical 480。

机械：
- `A-vr-test` `*VzVerify*`：**绿**
- `A-vr-suite` `./gradlew :app:testDebugUnitTest`：**绿**（exit 0，33s）
- `A-vr-ui` `bash .team/nodes/vz-ver/ui-check.sh`：**绿**（`PASS t.ver vz-ver`）
- `A-vr-shot` 本目录 png
- `A-vr-density` 下表两组读数

---

## B. 多密度读数（整数 480 / 3.0 · 非整数 420 / 2.625）

标题 UI 树：`远程控制 app 开发`。token：`term-theme-light source=app-theme`。
截图：`shot-d480-session.png` / `shot-d420-session.png`。树：`ui-tree-d480.xml` / `ui-tree-d420.xml`。

| 操作数 | d480（3.0） | d420（2.625） |
|---|---|---|
| `wm density` | 480 | 420 |
| SurfaceView bounds | `[12,305][1248,2404]` | `[11,284][1249,2452]` |
| 外间距 left_px / scale | 12 / 3.000 = **4.00dp** | 11 / 2.625 = **4.19dp** |
| 内 padding（源码） | **6.0dp** | **6.0dp** |
| 边→首字符 total | **10.00dp** | **10.19dp** |
| 纸色采样 RGB | (247, 248, 251) | (247, 248, 251) |
| 纸色 luma | 248 | 248 |
| dist(paper, #F7F8FB) | **0.0** | **0.0** |
| dist(paper, ANSI0 #E7EAF0) | 23.9 | 23.9 |
| 返回墨迹中心 y | 214.5px | 203.5px |
| 标题墨迹中心 y | 214.0px | 203.5px |
| 墨迹中心差 | **0.17dp** | **0.00dp** |
| 顶栏灯 content-desc | **Busy** | **Busy** |
| 灯像素（d480 约 155,205） | (148, 211, 205) 青绿 | — |
| UI 树「已发送」 | absent | absent |
| UI 树 `claude_code` | absent | absent |
| LAN 胶囊 | `LAN`（reverse=LAN 通道） | 同左 |
| + 菜单 | `拍照` / `从相册选择` | 同左 |
| 菜单垫色 pad_xy / RGB | (60,2132) **(236,238,242)** | (54,2201) **(236,238,242)** |
| dist(菜单垫色, M3紫 #6750A4) | 220.8 | 220.8 |

r18 对照：对齐曾 1.83 / 1.90dp；灯 Idle→Unknown；查看 `· 0`。

---

## A. 本轮逐条

### 框线无断点
**单测通过；本场无 U+2500 夹具。** `GlyphSeam` 整数格与 2.75 下 █/─ `right==nextLeft`（缝=0）。本会话是圆角卡片，无法量相邻框线格。083 §0：模拟器截图不能冒充真机非整数密度。

### logo 无黑缝
**单测通过；本场无 U+2588 logo。** 同上几何。截图未见块缝露黑。

### 日志 10s ≤3 且改 viewW 立刻出新记录
**通过。** `diag-export.log`（`files/diag/diag-1787194753277.log`）：
- `[term-left-edge]` **2 条**（≤3）
- 480 窗口：`contentLeft=18 col0Origin=18 cellW=22 viewW=1236 verdict=OK ×28`（同操作数合并，不是连打新行）
- 改密度后立刻新行且操作数不同：`contentLeft=16 col0Origin=16 cellW=19 viewW=1238 verdict=OK ×5`

### grok 浅色下整屏近白
**通过（纸色映射）。** 两密度垫色 (247,248,251) luma=248，dist_paper=0，dist_ansi0=23.9。本场不是 grok pane，未抓 `40m` 字节。单测 index0 → paper ≠ ansi[0]。

### 用户消息块比整体底色深
**单测通过。** 254 → userBlock luma=240 < 纸 248。diag：`userBlock=0xffe6f5f2 paperLuma=248 blockLuma=240 verdict=paper_ne_ansi0`。截图有薄荷绿用户条（相对纸色更深），未把 `#E6F5F2` 精确命中当成唯一判据。

### 屏幕边到首字符 == 10dp
**通过。** 4.00+6.0=10.00（480）；4.19+6.0=10.19（420）。

### 发送成功后无「已发送」；失败仍有错误提示
**通过。** 两密度 `uiassert absent 已发送`。`VzVerifySentHidesSuccessKeepsFailure`：Failed 文案仍在。

### 输入框光标移中间后外部更新不重置
**通过（单测）。** `VzVerifyCursorKeepsSelectionAfterExternalUpdate` selection=4。未向用户 CLI 灌中间编辑。

### LanPill 走 tailnet 时不显示 LAN
**本场 LAN 通道显示 LAN（对）。** tailnet 文案：`VzVerifyNetPillUsesRealConnectionPath` 绿。模拟器 reverse 不是 tailnet。

### 重连提示在标题下方且不顶动列表
**本场未出现重连条。** 套件 `ConsoleChromeBannerSitsBelowTitleWithoutMovingFirstRow` 绿。

### 顶栏 ‹ 与标题墨迹中心差 ≤1dp
**通过。** 同一套墨迹盒算法（不是布局盒）：
- d480：214.5 vs 214.0 → **0.17dp ≤ 1**
- d420：203.5 vs 203.5 → **0.00dp ≤ 1**

### + 菜单跟随设计（非默认紫）
**通过。** 垫色 (236,238,242)，dist_purple=220.8。旧探针点落在「拍照」字墨会采到深色——本场用容器左上内缩。`shot-d480-menu.png` / `shot-d420-menu.png`。

### 顶栏指示灯不切页也能实时变色
**通过（不切页灯=Busy，与查看「进行中」一致）。**
- d480/d420 content-desc **Busy**；灯像素 (148,211,205)
- 查看菜单当前行「进行中」（`shot-sheet.png`）
- 未切页。本场进页时会话已 working，**未再注入 Idle→Busy**（禁灌用户 CLI）。相对 r18：当时正文 working 灯 Idle/Unknown。

### 差分同步纯追加零退格、改中间两边文本一致
**通过（单测）。** `VzVerifyDiffSyncAppendZeroBackspaceMidEditEqual`。未向真 CLI 灌中间字。

---

## C. 回归

| 条 | 结论 | 证据 |
|---|---|---|
| 073 身份键含 socket | **通过** | `VzVerifyIdentityKeyContainsSocket` |
| 075 一级转圈不回弹 | **套件绿** | `SpinnerIdleTest`；本场未点一级 |
| 076 §1 查看按当前会话 | **通过** | `切换会话` + `远程Agent安卓 · 19` + 显示名「远程控制 app 开发」+「当前」+「进行中」。`ui-tree-sheet.xml` / `shot-sheet.png` |
| 076 §2 顶栏无「设置」 | **通过** | 顶栏是「查看」 |
| 076 §3 显示名与状态标 | **通过** | 标题显示名；灯 Busy / 菜单「进行中」 |
| 077 §1 标题是显示名 | **通过** | `远程控制 app 开发`，`absent claude_code` |
| 078 §1 首列不被裁 | **通过** | 截图首列完整 |
| 081 cols 两个数 | **通过** | `[reflow] frame cols=54 render cols=54` → 改密度后 `63` / `63` |
| 082 收藏按工作区 | **套件绿** | `FavoriteIdentityTest`；本场未滑到收藏页 |

---

未碰用户 tmux。未 `git checkout` / `git restore`。产物 `.team/nodes/vz-ver/`。
