# t.ver 独立验收报告 · ledger.vz.v1 r18

席位：vz-v1-ver。仓根 `/Volumes/nvme/Projects/远程Agent安卓`（未进 `.worktrees/`）。
**结论：不通过。** 未改产品码。模拟器 `emulator-5554`，装入仓根 `app-debug.apk`（2026-08-20 09:59:13）。
adb reverse `tcp:9900`。截图前 `keyevent 111`。跑完 `wm density reset`（Physical 480）。

机械：
- `A-vr-test` `*VzVerify*`：**绿**
- `A-vr-suite` `./gradlew :app:testDebugUnitTest`：**绿**（09:58 BUILD SUCCESSFUL）
- `A-vr-ui` `ui-check.sh`：**红**（光学对齐超 1dp；见下）
- `A-vr-shot` png 已落本目录
- `A-vr-density` 两密度读数都在本报告

---

## B. 多密度读数（整数 480 / 3.0 · 非整数 420 / 2.625）

会话页标题 UI 树：`远程控制 app 开发`。token：`term-theme-light source=app-theme`。
截图：`shot-d480-session.png` / `shot-d420-session.png`。树：`ui-tree-d480.xml` / `ui-tree-d420.xml`。

| 操作数 | d480（3.0） | d420（2.625） |
|---|---|---|
| `wm density` | 480 | 420 |
| SurfaceView bounds | `[12,305][1248,2404]` | `[11,284][1249,2452]` |
| 外间距 left_px / scale | 12 / 3.000 = **4.00dp** | 11 / 2.625 = **4.19dp** |
| 内 padding（源码常量） | **6.0dp** | **6.0dp** |
| 边→首字符 total | **10.00dp** | **10.19dp** |
| 纸色采样 RGB | (247, 248, 251) | (247, 248, 251) |
| 纸色 luma | 248 | 248 |
| dist(paper, #F7F8FB) | **0.0** | **0.0** |
| dist(paper, ANSI0 #E7EAF0) | 23.9 | 23.9 |
| 返回墨迹中心 y | 208.5px | 198.5px |
| 标题墨迹中心 y | 214.0px | 203.5px |
| 墨迹中心差 | **1.83dp** | **1.90dp** |
| 顶栏灯 content-desc | Idle | Unknown |
| UI 树「已发送」 | absent | absent |
| UI 树 `claude_code` | absent | absent |
| LAN 胶囊 | `LAN`（本机 reverse=LAN 通道） | 同左 |

cols 上限源码 `TerminalMetrics.maxCols=112`（单测 `VzVerifyGapTotalPaddingIsTenDpAndColsCapped`）。

---

## A. 本轮逐条

### 框线无断点
**证据不足。** 本场会话是 Claude 圆角卡片，屏上没有 U+2500–257F 夹具，无法量相邻格 `right/nextLeft`。
单测 `GlyphSeam`：整数格与 2.75 下 █ / ─ 矩形 `right==nextLeft`（缝=0）；旧居中 `cellW=17 advance=14.2` 缝=2.8。
截图未见线段断点，**不能当成真机非整数密度已修**（083 §0）。

### logo 无黑缝
**证据不足。** 本场无 U+2580–259F 块 logo。`userBlock` 像素在 `shot-d480-session.png` 上 **hits=0**（搜 `#E6F5F2`）。
几何单测同上。不能用「看起来没有黑底」收工。

### 日志 10s ≤3 且改 viewW 立刻出新记录
**不通过（去重）。** 导出 `diag-export.log`（`files/diag/diag-1787160946788.log`）：
- `[term-left-edge]` **4 条**，180ms 内，操作数全同：
  `contentLeft=42 col0Origin=42 cellW=22 viewW=1212 verdict=OK`（01:35:20.145 / .191 / .241 / .324）
- 判据是 ≤3。4>3，且值没变仍连打。
- 本席未完整复跑「改 density 后必须 +1」；glyph 席曾报 480→440：1→2。

### grok 浅色下整屏近白
**通过（纸色映射，非 grok 会话）。** 浅色 token 下垫色 (247,248,251) luma=248，dist_paper=0，dist_ansi0=23.9，**更近纸色不是 ANSI0 灰**。
两密度相同。单测 `VzVerifyPaperNotAnsi0AndUserBlockDarker`：index0 → `defaultBg` ≠ ansi[0]。
本场不是 grok pane，没有抓到 `40m` 字节。

### 用户消息块比整体底色深
**证据不足（像素）/ 单测通过。** 本场截图 `userBlock #E6F5F2` hits=0。
单测：index 254 luma=240 < 纸 luma=248，且 ≠ 纯白。diag：`userBlock=0xffe6f5f2 paperLuma=248 blockLuma=240 verdict=paper_ne_ansi0`。

### 屏幕边到首字符 == 10dp
**通过。** d480：4.00+6.0=10.00；d420：4.19+6.0=10.19（非整数取整，|差|<1.5dp）。

### 发送成功后无「已发送」；失败仍有错误提示
**通过（成功态 UI + 失败态单测）。** 两密度 `uiassert absent 已发送`。
`SessionScreen` `is InputStatus.Sent -> null`，仍有 `InputStatus.Failed`。
`VzVerifySentHidesSuccessKeepsFailure`：Sent 无「已发送」，Failed「发送失败：超时」显示。
本场未在模拟器上注入一次真实发送失败。

### 输入框光标移中间后外部更新不重置
**单测通过，模拟器未做外部重组。** `VzVerifyCursorKeepsSelectionAfterExternalUpdate`：selection 停在 4。
源码 `draft: TextFieldValue`。未在真会话输入框里移光标再触发重组（会污染用户 CLI）。

### LanPill 走 tailnet 时不显示 LAN
**模拟器本场无法证 tailnet。** reverse 走 LAN，UI 树读到 `LAN`（与通道一致，不是写死错显示）。
单测 `VzVerifyNetPillUsesRealConnectionPath`：`ConnectionPath.TAILNET` 屏上是 `tailnet`、无 `LAN`。

### 重连提示在标题下方且不顶动列表
**本场未出现重连条。** 单测 `ConsoleChromeBannerSitsBelowTitleWithoutMovingFirstRow`（套件绿）：banner.top ≥ title.bottom，首行 y 不变。

### 顶栏 ‹ 与标题墨迹中心差 ≤1dp
**不通过。** 断言的是墨迹盒不是布局盒。
- d480：back_ink_cy=208.5 title_ink_cy=214.0 → **Δ=1.83dp > 1**
- d420：back_ink_cy=198.5 title_ink_cy=203.5 → **Δ=1.90dp > 1**
截图 `shot-d480-session.png` / `shot-d420-session.png`。单测几何公式绿 ≠ 屏幕墨迹绿。

### + 菜单跟随设计（非默认紫）
**通过（浅色）。** 点 `+` 后 UI 树：`拍照` / `从相册选择`（`ui-tree-d480-menu.xml`）。
截图 `shot-d480-menu.png`。菜单容器采样 (236,238,242)，dist(M3 紫 #6750A4)=220.8。
单测 `VzVerifySchemeSurfaceContainerNotFrameworkPurple`。

### 顶栏指示灯不切页也能实时变色
**不通过。** 停在会话页、无切页：
- 终端正文写着会话 **working**（「验收席 vz-v1-ver 正在干活」）
- d480 灯 **Idle**；随后变为 **Unknown**
- 再停 **10s**：仍 **Unknown**（未变绿 Busy）
操作数：content-desc 前=`Idle`/`Unknown`，后 10s=`Unknown`；期望 Busy。
单测 `VzVerifyLampFollowsLiveOverlayWithoutNavigation` 绿，**模拟器世界没跟上**。

### 差分同步纯追加零退格、改中间两边文本一致
**单测通过，未向真 CLI 灌中间编辑。** `VzVerifyDiffSyncAppendZeroBackspaceMidEditEqual`：`l`→`ls` backspaces=0；`hello`→`hallo` CLI==本地。

---

## C. 回归

| 条 | 结论 | 证据 |
|---|---|---|
| 073 身份键含 socket | **通过（单测）** | `VzVerifyIdentityKeyContainsSocket`：两 ref 不同 socket → 两把 FavoriteKey |
| 075 一级转圈不回弹 | **套件绿，本场未点一级** | `SpinnerIdleTest` 在 A-vr-suite |
| 076 §1 查看按当前会话取数 | **不通过** | 点「查看」树只有 `切换会话` + ` · 0`，没有当前显示名、没有会话行。`ui-tree-sheet.xml` / `shot-d480-sheet.png` |
| 076 §2 横滑/设置滑到底/右上无「设置」 | **部分** | 会话顶栏是「查看」不是「设置」。设置页未滑到底 |
| 076 §3 显示名与状态标 | **部分** | 标题是显示名；灯 Unknown 与正文 working 不一致 |
| 077 §1 会话页标题是显示名 | **通过** | `远程控制 app 开发`，`absent claude_code` |
| 078 §1 终端首列不被裁 | **通过（截图）** | d480/d420 首列字母完整（如 `vz-v1` / `Monitor`） |
| 081 cols 仪表两个数 | **通过** | diag `[reflow] frame cols=51 render cols=51` |
| 082 收藏页按各工作区取数 | **未在模拟器走到收藏页** | 右滑仍停在会话。单测 `FavoriteIdentityTest` 套件绿 |

---

## 实现席定位（红项操作数）

1. **光学对齐**  
   d480: 208.5 vs 214.0 px（scale 3 → 1.83dp）  
   d420: 198.5 vs 203.5 px（scale 2.625 → 1.90dp）  
   标题墨迹中心系统性偏下 ~5px。

2. **顶栏灯**  
   不切页，正文 working，灯 Idle→Unknown，10s 仍 Unknown。overlay 空（查看菜单 `· 0`）很可能是同一条订阅断了。

3. **查看菜单空**  
   期望含当前显示名 + 会话行；实际只有 `切换会话` 和 ` · 0`。

4. **term-left-edge 去重**  
   同操作数 4 条 / 180ms：`contentLeft=42 col0Origin=42 cellW=22 viewW=1212`。

---

未碰用户 tmux。未 `git checkout` / `git restore`。产物均在 `.team/nodes/vz-ver/`。
