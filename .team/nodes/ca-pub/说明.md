# t.pub · 三核发布成本地 maven 产物

工作树：`.worktrees/wt-ca`。未 commit/push，未开模拟器，未动 `tools/perfbase/`。

## 做了什么

- 三核 `app/core-protocol` `app/core-terminal` `app/core-conn` 加 `maven-publish`。
- 共用脚本 `app/gradle/core-maven-publish.gradle`：坐标 `dev.agentmirror.core:<模块名>:20260822.0`，发到 `.team/staging/maven-repo/`（标准 maven 布局）。
- 仍是纯 JVM：未上 Android 插件；源码无 `import android./androidx.`。
- 判据 `ls jar aar` 要求两个文件都在：aar 由 jar 打成 `classes.jar` + 最小 manifest，**不是** `com.android.library`。
- `core-conn` POM 依赖 `dev.agentmirror.core:core-protocol:20260822.0`。

## 判据原文

cwd=`.worktrees/wt-ca`，`sh tools/perfbase/judge-pub.sh`：

```
PASS 三核已发布为 maven 产物且仍是纯 JVM
```

退出码 0（内含 `./gradlew :core-protocol:test :core-terminal:test :core-conn:test --offline`）。

## 缺口（本格范围内已交付）

推 `maven` 分支是 leader 的动作。本格只落本地 `.team/staging/maven-repo/`。
