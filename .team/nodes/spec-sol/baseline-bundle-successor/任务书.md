# ledger.baseline-bundle.successor.v1 — plan P0 后的审计连续性 successor

## 背景与必读坐标

本账本是 `ledger.baseline-bundle.v1` revision 1 的执行 successor：这是框架 plan P0 后的审计连续性 successor，不是历史重置。它只解决 ledgerdsl v0.1.1 `plan` 无法读取同版 DSL 生成的 `Task.parallel`、导致旧账本不能安全注入返修创作面的框架停机；它不改变“金标准 APK 丢失死锁根治”的产品需求、任务图、机械门或用户 gate。用户在 `338fd8976fc9d92b83c07a3b1ce4f47aaa049bb7` 再次裁定：框架 P0 投递后产品任务不得停工。

每个席位除本任务书外，仍必须读并遵守 `.team/nodes/spec-sol/baseline-bundle/任务书.md` 与自己原有路径级任务书；若本任务书没有逐字重述某项要求，原任务书仍全量有效，不得解释为删除或放宽。

审计 provenance（全部只读，不得修改、复制为新账本运行事实或改写结论）：

- 旧首红 commit：`c4846de5497903bf1a0b2db98fe9ede4c2d8ac5d`；旧 live `.team/ledgers/baseline-bundle-v1.json` revision 1，SHA-256 `89ba716e85f151b06f05bf61a5631eacbcad910c0f1190a8f264a9b69a6b5723`；
- 首红独立归因：`.team/nodes/baseline-bundle-repro-diagnosis/VERDICT.md`，SHA-256 `a336e7a1201937d267a27b40e7c5b73e0f49a7aa9e586dd55415a77f3e0441e0`；
- 返修 commit：`488a1f25bc8b0a35afb62df85e3afd5ce666bf8e`；
- 返修独立判词：`.team/nodes/baseline-bundle-repro-fix-review/VERDICT.md`，SHA-256 `d1e5740a0388f4c5dba936af79a6d889c4939f2becb2d8b69dcdd52fdf04fdf0`，末行 `verdict: pass`；其 fresh 原始索引为同目录 `tests.log`；
- framework P0：`.team/artifacts/ledger-p0-ledgerdsl-plan-rejects-parallel-20260824.md`。

## Successor 身份与历史关系

- 新 ledger id：`ledger.baseline-bundle.successor.v1`；
- 新 DSL 源：`.team/ledgers/src/baseline-bundle-successor-v1.py`；
- 新编译账本：`.team/ledgers/baseline-bundle-successor-v1.json`；
- 关系：`supersedes ledger.baseline-bundle.v1 revision 1 for future execution`，但旧账本仍是首红与 failed attempt 的权威历史载体。

新账本是从已经独立判过的返修创作面重新 compile 出来的全新 ledger.v2，所以所有任务初始为 `planned`、没有 `attempts`。这只表示新 ledger 尚未运行，不表示旧 attempt 被成功、删除、复制、重放或清洗。旧 live 中 `t.baseline-bundle.repro=failed_retryable` 与 attempt `att-t.baseline-bundle.repro-seq1-t1787590762873` 必须原样保留；禁止对旧 live 做 plan/apply、手写 JSON、清 attempt 或重派。

## 精确交付与图约束

successor 必须保留旧链九格及原依赖：

```text
repro → (test ∥ probe ∥ impl) → verify → user-gate → migrate → measure → final
```

其中 `test`、`probe`、`impl` 仍属于 `baseline-bundle-wave`，max_concurrency=3、failure_policy=halt；verify 仍依赖三者 success。角色、seat、worktree_id、write_paths、read_paths、required artifacts、机械 Check、budget、四态、fallback 与全部 `requires_success` 边不得弱化。successor 只允许在每格增加本任务书和上述历史 provenance 为只读输入。

逐格交付继续以原路径级任务书和 successor 编译账本的合取为准：

- repro：`REPRO.json`、`REPRO.md`，必须重跑两次真实 probe；不得搬用旧 attempt 冒充新 case；
- test：`RED.md`；probe：`PROBE.md`；
- impl：`ROUTE.md`、`IMPL.md`、`BUNDLE-MANIFEST.json`、`INSTALL.md`、`RETRIEVE.md`；
- verify：`VERDICT.md`；user-gate：`USER-GATE.json`、`USER-GATE.md`；
- migrate：`MIGRATION.md` 及原任务书要求的机械前置/计划/历史保留证据；
- measure：`MEASURE.md`、`perf-ab-bundle.json`、`PRE-MEASURE.json`；
- final：`VERDICT.md`。

repro 的 acceptance 仍为 expected 0：只有两次真实 legacy probe rc=1、固定 shape/provenance 与新 `REPRO.json` 全吻合，translator 才把“旧链按预期红”转成 repro acceptance 0；probe/事实不可判传递 exit 2，伪造或非预期形状 exit 1。final 仍须让同一 real-chain probe 在迁移完成后真实 exit 0，本格的转译绿不能替代终局绿。

## 不得弱化的产品与安全约束

1. exact A 缺失必须诚实进入 `blocked_missing_baseline`/不可判；路线只能是 `recover_exact_artifact` 或 `rebaseline_with_equivalence_proof`，不得回到 `failed_retryable` 无限 park。
2. A2 必须证明运行内容等价；Baseline Bundle 至少绑定源码身份、归一化运行内容摘要、制品摘要/签名、构建报告与等价报告。
3. 测量前必须机械验证可取回、摘要匹配、可安装、独立备份可恢复；完成前归档并重新取回校验。
4. 性能阈值仍为三夹具 A/B/A/B、每夹具每段 n>=10、同批身份、nearest-rank p50/p95、所有 B/A<=1.10；测前 envcheck，缺环境/资产/有效 raw 为 exit 2；测试禁缓存。
5. 最终仍需用户在蜂窝网络+广州中转路径确认真机“秒开无空白”；agent、模拟器或实验室绿不得代判。
6. 不公开分发原 debug 签名 APK，不读取凭据；不得改 App/server 来绕基线资产问题。
7. 旧 perf-regress 的迁移/停止仍只能在 bundle verify 与 user gate 全绿后，由 migrate 格按原机械前置处理；不得把 successor 的建立解释为已经停止旧链。
8. 开发/测试/审查继续隔离；破坏齿、独立 verify 与 user gate 保留。不得因为 successor 是连续性路径而跳格或降低 required Check。

## 启动边界与合法出口

本次只创作、编译和预检 successor，禁止执行 `ledger-run --drive`、禁止创建 lease/PID/attempt、禁止向席位派单。leader 审核后才可另行启动；启动 successor 不需要也不允许先修改旧 live。

运行后每格只在 required artifacts 全部落盘后 `report_result` 一次，不另发进度消息。机械门 0=通过、1=产品/交付不通过、2=环境或事实不可判；不可判必须如实说明缺口与恢复条件，不得折成成功或失败。发现 framework P0 可另案投递，但投递后按本 successor 的既定需求继续，不以框架回复为前置。

verdict: pass
