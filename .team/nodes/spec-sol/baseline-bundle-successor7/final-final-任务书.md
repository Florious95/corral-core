# successor7 final 独立终审任务书

目标：独立合取 successor7 九格，不信任产出方 summary，不将 apparatus/用户/环境 exit2 改成成功。

输入：continuity、APPARATUS.json、fresh VERIFY、USER-GATE、MIGRATION、fresh MEASURE/raw、bundle 主/备归档、permanent fixture、real-chain probe 及实际 diff。

交付：`.team/nodes/baseline-bundle-final/{VERDICT.md,EVIDENCE-MATRIX.md,MUTATION.md}`。矩阵必须列 da46/0df provenance、apparatus bytes SHA/bundle/0600/三项 cleanup/forced_kill=false、双归档取回、用户真机“秒开、没有空白”、迁移、A/B/A/B nearest-rank/n>=10/B/A<=1.10。

验收：重跑 `baseline-bundle-successor7-final.sh` 与 real-chain probe，并在副本对 apparatus SHA/bundle/cleanup、backup 恢复、迁移 PID或1.10比例中选至少一枚破坏齿，改坏必红、还原必绿且 porcelain 无自造污染。严格合取为0；有效反证为1；任一环境/证据/用户事实不足为2。`VERDICT.md` 末行 `verdict: pass|fail|unjudgeable`，只 `report_result` 一次。
