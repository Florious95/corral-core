# baseline-bundle successor7 apparatus bootstrap

## 背景与冻结 provenance

successor6 revision 5 的 repro/impl/probe/test 已在 commit `3528c2ad5c9308a049f4fdb135f372d035633a90` durable succeeded；verify 诚实 rc2，只缺 adb+fresh owned emulator 与 permanent impl-bypass fixture。不得把 verify rc2 改成1/0，不得清旧 attempt、重派四格或把四格重放当新证据。

必读：`.team/nodes/baseline-bundle-successor6-verify-diagnosis/VERDICT.md`、`tools/perfbase/{envcheck.sh,run-input-ab.sh,test-run-input-ab-emulator-ownership.sh}`、`.team/nodes/input-full-auto/perf-design/CONTRACT.md`、successor6 impl/verify/taskbooks/ledger、`.team/ledgers/acceptance/{emu-own.sh,baseline-bundle-bypass-probes.sh,baseline-bundle-successor3-bypass.sh}`。

本轮仅生成 Git 可跟踪的 successor7 bootstrap 脚本、fixture、四份路径级任务书与日志；禁止生成 source/compiled final ledger，禁止启动 adb/qemu/emulator，禁止修改旧 ledger/attempt 或产品实现。leader 独立语义审查 pass 并提交 bootstrap 后，才以该 commit 生成 final successor7 ledger。

## 连续性裁定：复用 wt-maple-core

successor7 选择**复用现存 `wt-maple-core`，按 `continuity → apparatus(command) → fresh verify` 串行**；不导出/复制 debug APK，不把 private bundle 提交 Git，不另建空 WT 冒充原 bundle。理由：successor6 的 manifest、primary/backup archive、未提交 bundle 工具和 verify 报告都只在该 WT；`3528c2ad5` 保存的是四格 ledger acceptance refs，不包含 private APK payload。

bootstrap commit 形成后、生成 final ledger 前，leader 必须先机械证明 retained WT HEAD 是 bootstrap commit 祖先、WT dirty/untracked 路径与待 fast-forward 路径零交集，然后只允许 `git merge --ff-only <bootstrap-commit>` 把已审 bootstrap 文件带入同一 WT；预检不满足则不更新、记 exit2。不得 checkout/restore/reset、不得清理 WT、不得复制 APK。fast-forward 后 `.team/ledgers/acceptance/baseline-bundle-successor7-continuity.sh` 必须 exit0，绑定：

- `3528c2ad5` 内 successor6 ledger SHA-256 `30bc51c09b2deb00c0213d5fce815e03e5181c6ca9cf93d9862fe5d45e5e241c`；
- repro/impl/probe/test 四格 state/required 精确成功，verify 保持 planned；
- 当前 cwd Git worktree 精确为 registered `wt-maple-core`，HEAD 包含 `548572dfd`；
- retained manifest、primary/backup SHA、sealed/regular/独立 inode 与 `implementation: pass` 仍成立。

缺 WT/资产/Git object 为 exit2；固定 ledger/state/摘要或 archive 被有效反证为1。该门是证据绑定，不把旧 result 包装为 successor7 新执行。

## final ledger 冻结设计（本轮不生成）

final successor7 使用新 ledger_id/revision1。新 apparatus test 与 probe 在两枚全新隔离 WT 并行，先锁 bootstrap 结构/破坏齿；它们与 continuity success 合取后，apparatus 在 retained `wt-maple-core` 用 `executor=command` 精确执行：

```text
/bin/sh .team/ledgers/acceptance/baseline-bundle-successor7-owned-emulator.sh
```

不得由 agent 自报替代 command exit。apparatus required 为 successor7 apparatus validator；随后 fresh verify 同 `wt-maple-core`，required 为 successor7 verify + permanent bypass。verify 后仍串行 user-gate → migrate → fresh measure → final，保留 B/A<=1.10、三夹具 A/B/A/B 每段 n>=10 与用户真机“秒开、没有空白”。无自定义 statuses，所有门 expect0/unjudgeable2。

## bootstrap 文件

- owned emulator：`.team/ledgers/acceptance/baseline-bundle-successor7-{owned-emulator,apparatus,emulator-regression,continuity}.sh` 与 `baseline-bundle-successor7-apparatus.py`；
- permanent bypass：`.team/ledgers/acceptance/baseline-bundle-successor7-{impl-bypass,bypass-regression}.sh` 与 `fixtures/baseline-bundle-successor7/impl-bypass/{BUNDLE-MANIFEST.json,contract.json}`；
- fresh verify：`.team/ledgers/acceptance/baseline-bundle-successor7-verify.sh`；
- 本目录 `apparatus/test/probe/verify-任务书.md`、bootstrap 日志与 `BOOTSTRAP-RESULT.md`。

启动审查返修冻结：`SUCCESSOR7_FIXTURE_MODE` 只允许 unset/空（production）或精确 `1`（isolated regression），其它非空值 exit1；production 环境出现任意 `SUCCESSOR7_TEST_*` 名称即 exit1，且不得读取其值或进入真实 runner。test mode 还必须带固定 harness 标记、node-local fixture root，并拒绝未知 `SUCCESSOR7_TEST_*`。所有 apparatus 派生文件以 `umask 077` 创建，持久 evidence 必须 regular/non-symlink 且 mode `0600`；`0644` 是固定破坏齿并 exit1。

所有 command/TERM/wait/cleanup 都有固定上界。超时先 TERM；仍存活时只能向本任务 `$!` 绑定 PID、runner PID 或 runner event 已绑定 qemu PID升级 KILL，之后再次有限等待。必须分别验证 runner PID 消失、bound serial 不再 `device`、owned qemu PID 消失；任一强杀、超时或残留均 exit2，不得写 pass evidence，不得按名称/目录扫描或杀 foreign PID。

四态：0=apparatus/fixture/continuity facts全绿；1=有效伪造、摘要/身份/清理被反证；2=环境、SDK、system image、adb、唯一 ownership、超时、Git/WT/fixture缺失不可判；不适用仅为未派发。不得放宽 envcheck 12/5%/dead socket 阈值，不得碰他人 qemu、生产 daemon/真实 tmux，不得读凭据或公开 debug APK。

verdict: pass
