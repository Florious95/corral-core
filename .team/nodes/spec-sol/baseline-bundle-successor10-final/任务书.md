# baseline-bundle successor10 final recovery ledger 任务书

## 1. 背景、身份与只读历史

本账本是 successor9 r4 apparatus 不可判后的审计连续性 successor，不是重置历史、重放旧 attempt 或把 AVD 前置失败改写为产品失败。冻结坐标：

- `efed31310d94a077940544c65845eec5ec759d3e`：`ledger.baseline-bundle.successor9.v1` revision 4 及同轮 driver。三 consume 格各有且仅有一次 `command-success` 并为 `succeeded`；apparatus r4 的 command exit 2、attempt=`acceptance_pending`、stderr 安全尾部为旧 `fresh AVD creation rc=1`，没有 Work 成败。
- `918b4c06ff93271b0c84bfc187bde1f3a5a93db3`：successor9 apparatus 只读归因；selector/envcheck/continuity/tool/image 均越过，首失败为 fresh AVD creation，且零 emulator/qemu/install/APPARATUS。
- `9ea73dff8b70e054fbc899bb9fd55c3e490fcc43`：独立语义审查通过并冻结的 successor10 AVD bootstrap；包含无输入/有限时 helper、fake regression、owned wrapper、任务书、结果与 review。
- `ad7468f747d421305279632f0db9cbc227b08cd4`：本 final 的 immutable provenance HEAD，包含上述三提交及 retained `wt-maple-core` ff-only 证据；retained maple HEAD=`9ea73dff8`，cedar/orbit HEAD=`25517d808`。
- command-compatible pair 固定 source/schema/DSL HEAD `7485102b26ed34eb828e94900902147d5e00e995`，binary `/Volumes/nvme/cargo-target-w7-builder-b/debug/ledger-run` md5 `627f5e6fa5f47a61d23a09b918b50567`。不得换旧 binary 或把 command 格降成 agent 自报。

旧 successor1–9 ledger、lease、attempt、driver、工作树与产物只读保留；不 collect、不清 attempt、不重派旧 case、不 plan/apply 旧账本。新身份固定：

- ledger id：`ledger.baseline-bundle.successor10.v1`
- revision：1
- DSL 源：`.team/ledgers/src/baseline-bundle-successor10-v1.py`
- compiled：`.team/ledgers/baseline-bundle-successor10-v1.json`
- authoring 结构门：`.team/ledgers/acceptance/baseline-bundle-successor10-structure.sh`
- 无自定义 statuses

## 2. 精确九格图与首 frontier

任务图固定为：

`continuity-consume ∥ apparatus-test-consume ∥ apparatus-probe-consume → apparatus(command) → fresh verify → user-gate → migrate → fresh measure → final`

只允许 9 tasks、8 条 `requires_success` 边。首 frontier 精确只有三个 `executor=command` consume，parallel group=`successor10-consume-wave`、max=3、failure=halt，均 `write_paths=[]`、expect 0、unjudgeable `[2]`：

1. `wt-maple-core`：`/bin/sh .team/ledgers/acceptance/baseline-bundle-successor7-continuity.sh`；
2. `wt-s7-cedar`：`/bin/sh .team/ledgers/acceptance/baseline-bundle-successor7-test.sh`，消费 SHA-256 `04cdbd661548a4b3261c88d491cf80c48f98dcbe3c080e710fb7d12bbe6c105a` 的 `RED.md`；
3. `wt-s7-orbit`：`/bin/sh .team/ledgers/acceptance/baseline-bundle-successor7-probe.sh`，消费 SHA-256 `88868a1a1979d3f1504e5efd6876dc5ca8ed5cc6b45a2eb6f6dd23b8e5176cf7` 的 `PROBE.md`。

三格只把 successor9 r4 已成功的原 command 与原 WT 作为 provenance，仍须 fresh 重跑机械 required；不读取旧 attempt 当新结果，不发 agent、不等待 `report_result`。三边全部 success 后才解锁 apparatus；dry-run 时 apparatus 必须显示 dependency unsatisfied。

## 3. successor10 apparatus 精确合同

apparatus 只在 retained `wt-maple-core` 运行唯一 argv：

`/bin/sh .team/ledgers/acceptance/baseline-bundle-successor10-owned-emulator.sh`

顺序不可改变：strict `envcheck.sh --gate` → successor9 唯一 SDK selector → successor10 fail-closed AVD preflight/create/evidence → retained bundle resolve → successor7 owned PID+serial runner → install → owned-only cleanup → strict host recovery。AVD 非 0、证据 validator 非 0、name/dir 身份不成立时必须零 runner/emulator/qemu/install。

command artifacts 与 handoff 均精确要求 mode 0600：

- `.team/nodes/baseline-bundle-apparatus/AVD-CREATE.json`
- `.team/nodes/baseline-bundle-apparatus/APPARATUS.json`

required 集合精确为五项，禁止删旧门或换成自报：

1. `M.baseline-bundle.successor10-avd` → `baseline-bundle-successor10-avd-regression.sh`；
2. `M.baseline-bundle.successor9-sdk-selector` → `baseline-bundle-successor9-sdk-regression.sh`；
3. `M.baseline-bundle.successor7-apparatus`；
4. `M.baseline-bundle.successor7-fixture`；
5. `M.baseline-bundle.successor7-continuity`。

AVD 固定 package=`system-images;android-35;google_apis;arm64-v8a`、device=`pixel_6`、task-local `ANDROID_AVD_HOME` mode 0700、production 唯一新 name、stdin=`/dev/null`、owned process-group TERM→KILL deadline。只持久化固定 schema reason/rc/digest，原始 stdout/stderr 0600 node-local 后清理，禁止输出 SDK/AVD 路径或原文。wrapper 仍须绑定唯一 owned qemu PID、PID start identity 与 adb serial，只清该 PID，有限时验证 serial/qemu/host 恢复，绝不 kill 非 owned qemu。

## 4. 后链逐字段不弱化

`verify → user-gate → migrate → measure → final` 的 title、owner、seat budget、handoff、required/mechanical、worktree、write paths、环境忠实度与旧 read paths 必须逐字段等于 successor9 r4，只允许在 read_paths 前缀增加 successor10 provenance。路径级执行任务书仍为：

- `.team/nodes/spec-sol/baseline-bundle-successor7/verify-任务书.md`
- `.team/nodes/spec-sol/baseline-bundle-successor7/final-user-gate-任务书.md`
- `.team/nodes/spec-sol/baseline-bundle-successor7/final-migrate-任务书.md`
- `.team/nodes/spec-sol/baseline-bundle-successor7/final-measure-任务书.md`
- `.team/nodes/spec-sol/baseline-bundle-successor7/final-final-任务书.md`

fresh verify 只消费同 case 的 AVD/APPARATUS/bundle/cleanup 与 permanent fixture，不重启设备伪造证据。user-gate 必须绑定同一 verified bundle，并由用户真机蜂窝+广州中转原始确认“秒开、没有空白”；无用户原始确认 exit 2。migrate 只有 fresh verify 与 user-gate 成功后才可按旧任务书对旧 perf-regress 做有机械前置的 ledgerdsl plan/apply，不清历史、不手写 live JSON。

fresh measure 必须先 strict envcheck、SDK 与 baseline bundle 取回/摘要/可安装门，再做唯一同批三夹具 A/B/A/B；每夹具每段 n>=10，独立 nearest-rank p50/p95，A2/B 身份绑定，全部有效 B/A<=1.10。空 raw、段序错误、样本不足、摘要/批次不一致均不得 pass。机械 1.10、模拟器或 agent 结论不能替代真机 gate。

测试禁缓存：Go `-count=1`，Gradle `--rerun-tasks --no-build-cache`。不读取/输出凭据、SDK 值、APK 字节或生产 daemon 明文日志；不公开分发 debug 签名 APK；不碰真实 tmux、生产 daemon或他人 qemu。所有 agent 格 required artifacts 齐后只 `report_result` 一次，普通进度不另发 leader。

## 5. 四态、authoring 验收与启动边界

- exit 0：本格合同及所有 required 在有效身份/量具上 fresh 全绿。
- exit 1：身份/量具有效时存在可重复反证，包括 AVD 安全策略/postcondition、产品性能 B/A>1.10 或用户明确白屏/非秒开。
- exit 2：Git/WT/SDK/设备/资产/证据/用户事实/环境或量具不足，不得折成 0/1。
- not applicable：账本尚未派发的格，不执行动作、不制造证据。

authoring 必须 fresh 完成：两次 byte-identical DSL compile、jsonschema、固定 pair `--preflight --json` 与 `--dry-run --json`、Python byte compile、`sh -n`、shellcheck、structure green 与破坏齿、retained maple 上 AVD regression+SDK selector regression+continuity。structure 必须独立重算 successor9 r4 三 success/旧 apparatus exit2、四提交 ancestry/cat-file、三 WT HEAD/RED/PROBE digest、九格八边、三 command frontier、apparatus argv+五 required+双 artifacts、后链逐字段不弱化、无 statuses、无 live successor10 lease。

本轮只生成并验证创作包，禁止 `--drive`，禁止运行 production successor10 owned wrapper，禁止启动 adb/AVD/emulator/qemu，禁止修改旧 ledger/attempt、App/server，禁止 commit。任一 provenance/path/WT/量具不可取回，regression/continuity exit 2，或结构无法闭合，合法结果是 `verdict: blocked`；不得猜测、降门或启动。全部 fresh 闭合才写本目录 `RESULT.md` 末行 `verdict: pass`，正常收工只 `report_result` 一次。
