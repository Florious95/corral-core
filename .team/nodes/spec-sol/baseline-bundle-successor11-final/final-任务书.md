# successor11 final 路径级任务书

只在 continuity/test/probe/apparatus consume、fresh successor11 verify、用户真机 gate、迁移与 fresh measure 全部 success 后执行。只写 `.team/nodes/baseline-bundle-final/{VERDICT.md,EVIDENCE-MATRIX.md,MUTATION.md}`；不改上游产物或产品。

`EVIDENCE-MATRIX.md` 必须逐项绑定 da46/0df 历史连续性与 ebd0dc5c2/3597b8232/13c301fd0/7c1a856ba successor11 连续性，并列出 successor11 fresh schema、APPARATUS hash/bundle/manifest、archived install/pm/cleanup、current_adb_required=false、permanent fixture、用户“秒开、没有空白”和 A/B/A/B n>=10 nearest-rank B/A<=1.10。`MUTATION.md` 保留独立破坏齿的红→还原绿，不得改上游判据凑绿。

required 为 `baseline-bundle-successor11-final.sh` 与既有 `baseline-bundle-real-chain-probe.sh`。successor11 final gate 必须调用 successor11 structure/verify 以及既有 user/migrate/measure/permanent gates，严禁调用 legacy successor6/7 verify。不查 live adb、不启设备、不读 APK bytes。

有效回归/伪造/弱化为 fail/exit 1；环境、资产、用户或样本事实不足为 unjudgeable/exit 2；全链合取才能 pass/0；未派发为 not applicable。产物全部落盘后只 `report_result` 一次。
