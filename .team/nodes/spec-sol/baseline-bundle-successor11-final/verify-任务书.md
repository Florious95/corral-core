# successor11 fresh verify 路径级任务书

## 目标与输入

只在 successor10 r5 四个 command consume 全部 success 后，于 retained `wt-maple-core` fresh 派 `sampler-review-luna2`。必读本任务书、总任务书、`.team/nodes/spec-sol/baseline-bundle-successor11/任务书.md`、successor10 diagnosis 和固定 successor11 contract。旧 successor10 `verdict: unjudgeable` 与 attempt 只是历史，不得改写或冒充新证据。

## 精确交付

原子 fresh 重写且只写 `.team/nodes/baseline-bundle-verify/` 下：

- `VERDICT.md`
- `VERIFY.json`
- `INSTALL.md`
- `RETRIEVE.md`
- `MUTATION.md`

五件全部必须 nonempty、regular、non-symlink、mode 0600。`VERIFY.json` 必须按 bootstrap taskbook 的 exact successor11 schema 交付，含 fresh batch id/epoch、same-batch APPARATUS bytes SHA、bundle/manifest、archived install/pm/cleanup、permanent fixture 和四 Markdown bytes hash。每个 Markdown 必须含同一 batch/cross-link 机器行；`VERDICT.md` 末行与 JSON verdict 精确一致。

## 硬约束与机械门

只消费冻结 same-batch APPARATUS/producer/manifest 与 successor7 permanent fixture。cleanup 后 current/live adb 不得被查询，不得成为通过操作数，不得启动/停止/kill 已有 adb/emulator/qemu。不得引用 legacy temporary fixture、successor6 verify gate 或 successor7 legacy wrapper；不得重启 emulator、重装 APK、读 APK bytes、读凭据或改 App/server。

required 精确为 successor11 verify gate + successor11 fake regression + successor11 structure gate，全部 expected 0、unjudgeable 2。判据为 0=五件 fresh 0600 交叉闭合且 verdict pass；1=有效伪造/矛盾/权限错/verdict fail；2=资产/量具/证据缺失或 verdict unjudgeable；未派发=not applicable。旧 unjudgeable 只改末行必须 1。

任一事实不足都要如实交付 unjudgeable/exit 2，不得造绿。五件先落盘且权限自检完成后，正常收工只 `report_result` 一次，不另发消息。
