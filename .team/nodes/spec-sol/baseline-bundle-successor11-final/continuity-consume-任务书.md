# successor11 continuity consume 路径级任务书

输入是 successor10 r5 `t.baseline-bundle.continuity-consume` 的唯一 succeeded command attempt 和原 `wt-maple-core`。命令只读核 ledger SHA/revision/task/attempt，并在原 WT 运行 `baseline-bundle-successor7-continuity.sh`。

不写任何路径，不派 agent、不 collect/修改 successor10 attempts、不起设备、不读 APK bytes。运行态或 continuity 反证为 1；冻结账本/WT/量具缺失为 2；合取为 0；未派发为 not applicable。executor=command，不需要也不得 `report_result`。
