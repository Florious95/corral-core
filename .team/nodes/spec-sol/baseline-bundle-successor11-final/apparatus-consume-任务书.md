# successor11 apparatus consume 路径级任务书

输入是 successor10 r5 `t.baseline-bundle.apparatus` 的唯一 succeeded command attempt、原 `wt-maple-core` APPARATUS/AVD-CREATE 与 successor7 permanent fixture。command reservation/cwd 使用现存 `wt-archive-probe` 且 `write_paths=[]`；它不提供证据，validator 固定回读原 `wt-maple-core`。命令只读核 ledger SHA/revision/task/attempt、两份 0600 regular non-symlink JSON 的冻结 SHA/schema/install/envcheck/cleanup，并运行 permanent impl-bypass 三臂。

严禁运行 successor10 owned-emulator command，严禁启动/查询/kill adb、AVD、emulator、qemu，严禁读 APK bytes或改归档产物。摘要/schema/cleanup/permanent 反证为 1；账本/WT/资产/量具缺失为 2；合取为 0；未派发为 not applicable。executor=command，不调用 `report_result`。
