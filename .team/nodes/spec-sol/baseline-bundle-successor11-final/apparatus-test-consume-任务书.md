# successor11 apparatus-test consume 路径级任务书

输入是 successor10 r5 `t.baseline-bundle.apparatus-test-consume` 的唯一 succeeded command attempt 和原 `wt-s7-cedar` RED.md。命令只读核 ledger SHA/revision/task/attempt、RED bytes SHA-256=`04cdbd661548a4b3261c88d491cf80c48f98dcbe3c080e710fb7d12bbe6c105a`，再运行原 `baseline-bundle-successor7-test.sh`。

不写任何路径、不重派 test 席、不篡改 RED、不起设备。有效反证/伪造为 1；账本/WT/RED/量具缺失为 2；合取为 0；未派发为 not applicable。executor=command，不调用 `report_result`。
