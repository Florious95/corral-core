# successor11 apparatus-probe consume 路径级任务书

输入是 successor10 r5 `t.baseline-bundle.apparatus-probe-consume` 的唯一 succeeded command attempt 和原 `wt-s7-orbit` PROBE.md。命令只读核 ledger SHA/revision/task/attempt、PROBE bytes SHA-256=`88868a1a1979d3f1504e5efd6876dc5ca8ed5cc6b45a2eb6f6dd23b8e5176cf7`，再运行原 `baseline-bundle-successor7-probe.sh`。

不写任何路径、不重派 probe 席、不篡改 PROBE、不起设备。有效反证/伪造为 1；账本/WT/PROBE/量具缺失为 2；合取为 0；未派发为 not applicable。executor=command，不调用 `report_result`。
