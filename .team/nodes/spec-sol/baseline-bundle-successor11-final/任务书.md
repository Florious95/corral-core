# baseline-bundle successor11 final recovery 总任务书

## 1. 背景与冻结输入

这是 successor10 r5 的审计连续性 successor，不是清洗或重置历史。旧 `ledger.baseline-bundle.successor10.v1` revision 5 及 attempts 保持只读：continuity/apparatus-test/apparatus-probe/apparatus 四格 succeeded，verify 保留机械门 exit 2 的 `failed_retryable`。新账本是 `ledger.baseline-bundle.successor11.v1` revision 1，只从四个审计 command consume 开始。

冻结 provenance 必须同时可取：

- successor11 bootstrap `ebd0dc5c285ee65244824b99db6667a1bc569c83`；
- retained-WT preflight `3597b823204c7d25d5a77367bf2022347532e5d3`；
- successor10 r5 ledger/driver `13c301fd086092b02e1cb8535d1eff38ffcf0173`；
- successor10 verify diagnosis `7c1a856ba0043c87b1aeb9ed8ffac0fefe9ebfce`；
- command-compatible pair HEAD `7485102b26ed34eb828e94900902147d5e00e995`，binary 固定 `/Volumes/nvme/cargo-target-w7-builder-b/debug/ledger-run`，md5 `627f5e6fa5f47a61d23a09b918b50567`。

资源 provenance 统一锁定 `3597b823204c7d25d5a77367bf2022347532e5d3`。启动前 leader 只能把 retained `wt-maple-core` ff-only 到已提交的 final successor11 package commit；该 commit 必须包含 `3597b8232`。原 test/probe worktree 及产物保持只读。

## 2. 任务图、席位与写隔离

首 frontier 精确为四个 `executor=command`，并行上限为 4：

1. continuity consume：读原 `wt-maple-core`；
2. apparatus-test consume：读原 `wt-s7-cedar`；
3. apparatus-probe consume：读原 `wt-s7-orbit`；
4. apparatus consume：命令 reservation/cwd 使用现存且干净的 `wt-archive-probe`，validator 仍固定只读原 `wt-maple-core` 的 APPARATUS/AVD/permanent fixture，绝不复制产物或重跑 emulator chain。

四格均调用固定绝对路径 `baseline-bundle-successor11-consume.sh <kind>`，不派 agent、不 collect、不改写原产物。四边全部 `requires_success` 后才 fresh 派 verify=`sampler-review-luna2`；后链依次 user=`takeover-codex-luna`→migrate=`sampler-dev-luna2`→measure=`sampler-dev-luna2`→final=`sampler-review-luna2`。advisor 仍为 `input-advisor-luna`。无自定义 statuses/transitions。

四个 command 使用四个不同 worktree reservation，且全部 `write_paths=[]`。apparatus 的执行 cwd 不作为证据来源，validator 内部将原 worktree 固定为 `wt-maple-core` 并重算摘要。verify 及后链在四 command 结束后顺序复用 retained `wt-maple-core`。verify 只写 `.team/nodes/baseline-bundle-verify/`；user/migrate/measure/final 的写路径与 successor10 保持一致。

## 3. 四 command consume 精确契约

每个 consume 先对当前 successor10 JSON 重算固定 SHA-256 `018a638ec1fff6a59313609d884c4fadfaca78a0b8d3eaf4b018ab9e9fc81a32`，并核 ledger_id/revision=5、精确 task state=succeeded、唯一 succeeded attempt id/delivery id/writeback revision、旧 command argv/0/2 四态。然后在原 worktree 重做对应无设备 required：

- continuity：`baseline-bundle-successor7-continuity.sh`；
- apparatus-test：`baseline-bundle-successor7-test.sh` 且 RED.md bytes SHA-256 精确为 `04cdbd661548a4b3261c88d491cf80c48f98dcbe3c080e710fb7d12bbe6c105a`；
- apparatus-probe：`baseline-bundle-successor7-probe.sh` 且 PROBE.md bytes SHA-256 精确为 `88868a1a1979d3f1504e5efd6876dc5ca8ed5cc6b45a2eb6f6dd23b8e5176cf7`；
- apparatus：APPARATUS/AVD-CREATE 必须 regular non-symlink 0600，bytes SHA-256 分别为 `fd27d1638ba097ab6a310bc497d80e20720f7625974f61a414a2e63132f23b9d` / `403c9481703f2abff15bf0534809e12a902d75e7c968fb7ce33f0e138f176912`，schema/mode/install/envcheck/owned cleanup 全绿，再运行 permanent successor7 impl-bypass 三臂。

缺 worktree/账本/产物/量具为 exit 2；运行态、attempt、摘要、权限、schema 或 required 被有效反证为 exit 1；全部闭合为 0；未启动新账本为 not applicable。任一 consume 不得启动/查询/kill live adb、AVD、emulator 或 qemu，不读 APK bytes。

## 4. fresh verify 与后链不弱化

fresh verify 精确按 `verify-任务书.md` 重写 `VERDICT.md/VERIFY.json/INSTALL.md/RETRIEVE.md/MUTATION.md`。required 精确为：

- `baseline-bundle-successor11-verify.sh`；
- `baseline-bundle-successor11-verify-regression.sh`；
- `baseline-bundle-successor11-structure.sh`。

旧 successor6/7 verify gate 不得回流。verify 只消费 same-batch archived APPARATUS/producer/manifest 与 permanent successor7 fixture，cleanup 后 current/live adb 既不是门也不是加分项，不得操作已有 adb。

verify 成功后保留 successor7 已审 user-gate→migrate→measure→final required 不变。user gate 仍只认同一 verified bundle 的用户真机蜂窝+广州中转“秒开、没有空白”原始裁定；无用户原始确认 exit 2。migrate 必须在 verify+user 绿后按旧前置停 perf-regress，用 ledgerdsl plan/apply 留历史。measure 必须先 envcheck/资产取回，再做同批三夹具 A/B/A/B，每段 n>=10，nearest-rank p50/p95，所有 B/A<=1.10；缺资产/环境/真实 raw exit 2，有效样本超 1.10 exit 1。final 必须合取 continuity、apparatus、fresh verify、user、migrate、measure、permanent fixture 和 real-chain，不得用 agent 自报代替机械门。

## 5. 编译、启动前置与合法出口

DSL 只能使用 fixed source/schema/DSL HEAD `7485102b26ed34eb828e94900902147d5e00e995` 编译，preflight/dry-run 只能使用 fixed binary `/Volumes/nvme/cargo-target-w7-builder-b/debug/ledger-run`。必须 fresh 完成 DSL byte compile、byte-identical recompile、fixed-pair schema/preflight/dry-run、structure 破坏齿、retained continuity、successor11 regression。dry-run 首 frontier 必须恰为四个 command consume，不含 verify/设备动作。

本创作格不启动 ledger/drive/resident，不执行 production verify、user/migrate/measure/final，不启动设备，不读 APK bytes，不改旧 ledger/attempt，不 commit。启动前若 final package 尚未提交或 retained WT 无法 ff-only 包含 final package，必须 blocked/exit 2，不得绕过。席位正常收工只 `report_result` 一次。
