# baseline-bundle successor4 — required 门与 SDK 前置闭环

## 目标与审计连续性

本账本是 `ledger.baseline-bundle.successor4.v1` revision 1。它 supersede 旧四本账的 future execution，但不修改、清理、复制或重派其 ledger/attempt；不是重置历史。只读 provenance 为当前 main `ec1145820186b8862949b84fe56f9309c1b0754f`，该提交包含 bootstrap `f0fce0a44182558c23e4bba66d6b0b6ea91bdf1d`。冻结坐标：

- `ledger.baseline-bundle.v1` revision 1，SHA-256 `89ba716e85f151b06f05bf61a5631eacbcad910c0f1190a8f264a9b69a6b5723`；
- `ledger.baseline-bundle.successor.v1` revision 1，SHA-256 `d0e11364813b81dde02e1056682a3216743b43faac0ddd3e5a1927480ebf58be`；
- `ledger.baseline-bundle.successor2.v1` revision 4，SHA-256 `39bb9fcf77f04dc1212a098f1e5f50166b10b45d550bb43a2af7d4e4c0e2a9c0`；
- `ledger.baseline-bundle.successor3.v1` revision 3，SHA-256 `449b92d2c74e16c8da2c26b2bf35d4af16f9619c7cd334fb267718264c61dada`；
- 首红/返修/连续性提交 `c4846de5497903bf1a0b2db98fe9ede4c2d8ac5d`、`488a1f25bc8b0a35afb62df85e3afd5ce666bf8e`、`ef7a02c1d23d85eaf08c1093efafd50376fa4db5`，以及 `.team/nodes/baseline-bundle-successor3-run1-diagnosis/VERDICT.md`。

successor3 的 repro/test 已成功不等于 successor4 新证据；旧结果只作 provenance。successor4 从 fresh repro 开始，首 frontier 只能有 `t.baseline-bundle.repro`。

## 输入、输出与任务图

图保持九格十边：

```text
repro → (test ∥ probe ∥ impl) → verify → user-gate → migrate → measure → final
```

核心顺链 WT=`wt-atlas-42`，test WT=`wt-quartz-77`，probe WT=`wt-nimbus-93`。三者在制账时磁盘、git worktree metadata 与历史引用均不存在；test/probe/impl 并行写隔离，verify 与后续核心链复用 core WT 串行。leader 审核并提交本 successor4 包后，所有 WT 由 ledger-run 从启动时 current main 创建；该 main 必须包含本包并是上述 immutable provenance 的后代。不得在包尚未可由 Git 取回时启动，不得手建或复用旧 WT。

路径级输入/输出沿用 `.team/nodes/spec-sol/baseline-bundle/` 原九格任务书并叠加本目录的 impl/test/probe 返修任务书。每格只写 ledger 声明的 `write_paths`，交既定 required artifacts，齐后只 `report_result` 一次，不直接发消息。

## required 精确集合

以下集合及 mechanical 脚本路径必须逐项相等，不能额外挂旧门：

| task | required / mechanical |
|---|---|
| test | `M.baseline-bundle.successor4-test` → `baseline-bundle-successor4-test.sh` |
| probe | `M.baseline-bundle.successor4-probe` → `baseline-bundle-successor4-probe.sh` |
| impl | `M.baseline-bundle.successor4-impl` → `baseline-bundle-successor4-impl.sh`；`M.baseline-bundle.successor4-bypass` → 已跟踪 bootstrap `baseline-bundle-successor3-bypass.sh` |

全账禁止 acceptance id `M.baseline-bundle.impl-bypass` 与 `M.baseline-bundle.probe`。probe 不要求旧字段 `source_tree_sha256`；只认 successor4 PROBE 契约与真实 canonical/fixture 操作数。test/probe 各自门必须调用 `baseline-bundle-successor4-structure.sh`，并以 required 顺序、id、argv 与 legacy-name-negative 齿防止回流。

## 所有 Gradle 格的开工前置

test、probe、impl、verify、measure、final 如需运行 Gradle，worker 在任何 Gradle/SDK 命令前都必须完成：

1. 只从当前进程 `ANDROID_SDK_ROOT` 取值；为空才取 `ANDROID_HOME`。不得读取 provider profile、`tailnet-test.env`、偏好 plist、`tailscale_keys.bin` 或生产 daemon 日志。
2. 环境值非空且所指目录存在、可读；否则本格写明恢复条件并走 exit 2 / blocked，不得把环境缺失判产品失败。
3. 在本格 WT 内安全生成非版本化 `app/local.properties`，唯一必需行是 `sdk.dir=<所选目录>`；命令不得回显环境值或文件内容。生成后只用无输出比较确认 `sdk.dir` 与所选目录相同且为目录。
4. `git ls-files --error-unmatch app/local.properties` 必须失败，文件不得提交；收工报告不得包含 SDK 路径或文件内容。
5. 运行 `baseline-bundle-successor4-sdk.sh`。成功必须静默；缺环境、目录、文件或工具为 2，误提交 local.properties 为 1。禁止为了放行改阈值或读取凭据。

推荐由 worker 在关闭命令回显的 shell 中执行等价步骤：选取环境值、`umask 077`、以 `printf` 写入 `app/local.properties`、再做无输出校验。任务书只规定不变量，不允许日志打印操作数中的敏感路径。

## 不得弱化的产品与机械终局

- canonical 真实门继续调用 bootstrap 已跟踪的真实 `baseline_bundle.py retrieve`、固定 `control-contract.json` 与 bundle_id/path projection 红绿齿；不得用 synthetic hash 恒等式替代。
- controlled bypass 保持合法 root、非空 raw 与单变量 provenance 伪造；fixture 缺失/摘要漂移为2，伪造被拒为1，旁路红不算。
- 缺冻结 A 必须为 `blocked_missing_baseline`，只能走 `recover_exact_artifact` 或 `rebaseline_with_equivalence_proof`；Baseline Bundle 仍覆盖源码身份、归一化运行内容、APK 摘要/签名、构建与等价报告、独立备份恢复。
- 性能先 `sh tools/perfbase/envcheck.sh --gate`；fresh 三夹具四段 A/B/A/B、每夹具每段 n>=10、同批 A2/B、非空 raw、nearest-rank p50/p95，所有 B/A<=1.10。Gradle `--rerun-tasks --no-build-cache`，Go `-count=1`。
- 用户仍须在蜂窝网络+广州中转对绑定确切 bundle 裁定“秒开、没有空白”。agent、模拟器或旧口述不得代判。
- verify 与 user gate 全绿后，migrate 才可在精确 PID/账本状态机械前置下处置旧 perf-regress；漂移时不发信号。不可逆停止留给 migrate 格。
- 不改 App/server，不公开分发原 debug 签名 APK，不读凭据，不启动本账本。

## 四态与完成定义

0=契约与事实全绿；1=产品/实现/结构被反证；2=SDK、fixture、资产、量具或事实不可判；不适用只用于互斥路线未派发。完成前必须 fresh compile/schema/preflight/dry-run、byte recompile、sh-n/shellcheck、新 WT 不存在、provenance/fixture 可取回、required 精确集合、SDK 四态与 legacy negative 齿全有可核日志。此创作格不启动账本。

verdict: pass
