successor4 SDK preflight four-state checks

The selected SDK value and app/local.properties content were never printed.

fresh results:
  valid environment + matching untracked local.properties: rc=0, output_bytes=0
  ANDROID_SDK_ROOT and ANDROID_HOME both absent: rc=2
  selected environment directory absent: rc=2
  local.properties presented as tracked through an isolated alternate git index: rc=1

The alternate index was node-local; the repository index and app/local.properties were not modified.

mapping:
  0=usable environment
  1=delivery violates the do-not-commit invariant
  2=environment/apparatus unavailable

result: pass
