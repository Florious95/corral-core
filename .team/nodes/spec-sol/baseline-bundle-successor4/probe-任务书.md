# successor4 probe — 真实 probe 门与 legacy negative 齿

## 输入与范围

只写 probe WT 的 `.team/nodes/baseline-bundle-probe/PROBE.md`；旧 PROBE 和 source_tree_sha256 门只作失败 provenance。开工先按总任务书无输出生成非版本化 `app/local.properties` 并核 SDK；缺失2、误提交1，不打印值、不读凭据。

## 输出结构与验收

独立重算真实 bundle_id、independent_builds、apk_relpath、固定 control-contract、canonical 红绿与 controlled provenance 齿，并固定写入：

- `SUCCESSOR4_REQUIRED_EXACT`
- `SUCCESSOR4_LEGACY_NEGATIVE`
- `SUCCESSOR4_SDK_PREFLIGHT_NO_VALUE_DISCLOSURE`
- `SUCCESSOR4_CANONICAL_REAL`
- `SUCCESSOR4_FIXED_FIXTURE`
- `probe_required=successor4_probe`
- `legacy_probe=absent`
- `source_tree_sha256=not_required`

必须以候选 compiled ledger 为输入，证明 probe required/mechanical 只有 successor4 probe 且 worktree_id=`wt-nimbus-93`；插入 `M.baseline-bundle.probe`、旧脚本 argv 或 source_tree_sha256 形式门均被结构齿拒绝1；账本/SDK/fixture不可用2。记录 canonical 与伪造控制的两边操作数，但不输出 SDK 路径。PROBE.md 不冒充实现已绿。

产物齐后只 report_result 一次。
