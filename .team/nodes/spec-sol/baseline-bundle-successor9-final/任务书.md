# baseline-bundle successor9 final recovery ledger 任务书

## 1. 背景、身份与不可改写的历史

本账本是 successor8 r4 apparatus 不可判后的审计连续性 successor，不是重置、重放成功或清洗旧 attempt。冻结坐标如下：

- `61af5e3c49e65711db6c819fc383f177948338ff`：`ledger.baseline-bundle.successor8.v1` revision 4 与同轮 driver 日志；三 consume 格为 `succeeded`，apparatus 使用旧 successor7 command 并因 `fixed system image unavailable` exit 2/`acceptance_pending`，不得改判产品失败或成功；
- `6dbf110a5fdd4c97246926fafee2a67867523883`：apparatus r4 不可判归因；
- `bd48271b954f7d2699a1383b12d821eae5275c8f`：source-local candidate 与 sdkmanager root 分裂、exact package XML 操作数；
- `0fdee1072bbba941d3d7de0cb17268f81952da05`：经独立语义审查通过的 successor9 selector/bootstrap；
- `e6c2e2625bb5ce463282b13b2949b3538a2dbbbe`：main 证据 HEAD，记录 retained `wt-maple-core` 已 ff-only 到 `0fdee1072`，selector regression 与 continuity 精确 rc0；它包含上述四个提交；
- command-compatible pair 固定为 source/schema/DSL HEAD `7485102b26ed34eb828e94900902147d5e00e995`，binary `/Volumes/nvme/cargo-target-w7-builder-b/debug/ledger-run` md5 `627f5e6fa5f47a61d23a09b918b50567`。不得换默认旧 binary，也不得删 command 字段降级为 agent 自报。

旧 successor1–8 ledger、lease、attempt、driver 与工作树只读保留；本轮不 collect、不重派旧 case、不 plan/apply 旧 ledger。新身份固定为 `ledger.baseline-bundle.successor9.v1` revision 1；DSL 源 `.team/ledgers/src/baseline-bundle-successor9-v1.py`，compiled ledger `.team/ledgers/baseline-bundle-successor9-v1.json`，无自定义 statuses。

## 2. 精确九格图与交付

图固定为：

`continuity-consume ∥ apparatus-test-consume ∥ apparatus-probe-consume → apparatus(command) → fresh verify → user-gate → migrate → fresh measure → final`。

只有 9 tasks、8 条 `requires_success` 边。首 frontier 三格均为 `executor=command`、`write_paths=[]`、expect 0/unjudgeable 2，并只在 successor8 r4 的原 WT 重跑原 required：

1. `wt-maple-core`：`/bin/sh .team/ledgers/acceptance/baseline-bundle-successor7-continuity.sh`；
2. `wt-s7-cedar`：`/bin/sh .team/ledgers/acceptance/baseline-bundle-successor7-test.sh`，消费 SHA-256 `04cdbd661548a4b3261c88d491cf80c48f98dcbe3c080e710fb7d12bbe6c105a` 的 `RED.md`；
3. `wt-s7-orbit`：`/bin/sh .team/ledgers/acceptance/baseline-bundle-successor7-probe.sh`，消费 SHA-256 `88868a1a1979d3f1504e5efd6876dc5ca8ed5cc6b45a2eb6f6dd23b8e5176cf7` 的 `PROBE.md`。

三格不发 Agent、不等新 `report_result`，只信 command exit code；三边 success 才解锁 apparatus。apparatus 必须在 retained `wt-maple-core` 精确运行：

`/bin/sh .team/ledgers/acceptance/baseline-bundle-successor9-owned-emulator.sh`

该 wrapper 先 strict `envcheck.sh --gate`，再 fail-closed 选择唯一完整 SDK root，再进入原 successor7 owned-emulator；不得输出 SDK 值。apparatus required 是严格增强的四项精确集合：

- `M.baseline-bundle.successor9-sdk-selector` → `baseline-bundle-successor9-sdk-regression.sh`；
- `M.baseline-bundle.successor7-apparatus`；
- `M.baseline-bundle.successor7-fixture`；
- `M.baseline-bundle.successor7-continuity`。

不得删除或替换后三个旧门。apparatus 交付仍是 mode 0600 的 `.team/nodes/baseline-bundle-apparatus/APPARATUS.json`，且必须绑定唯一 owned qemu PID + `emulator-5554` serial；每个退出路径有限时，只清理本任务 PID，禁止 kill 非 owned qemu。

后续 verify、user-gate、migrate、measure、final 的 title/owner/budget/handoff/required/mechanical/worktree/write paths 与 successor8 r4 完全继承，只允许增加只读 provenance。路径级执行任务书继续使用 successor7 已冻结的：

- `.team/nodes/spec-sol/baseline-bundle-successor7/verify-任务书.md`
- `.team/nodes/spec-sol/baseline-bundle-successor7/final-user-gate-任务书.md`
- `.team/nodes/spec-sol/baseline-bundle-successor7/final-migrate-任务书.md`
- `.team/nodes/spec-sol/baseline-bundle-successor7/final-measure-任务书.md`
- `.team/nodes/spec-sol/baseline-bundle-successor7/final-final-任务书.md`

所有 agent 格产物齐后只 `report_result` 一次；进度、普通结论不另发 leader。只有编排无法继续、约束互斥或不可逆动作需要裁定时，才允许一条编排调整消息。

## 3. 性能、用户 gate 与安全硬约束

- 所有测试禁缓存：Go `-count=1`；Gradle `--rerun-tasks --no-build-cache`。
- apparatus/性能之前先 strict envcheck；环境门不绿、SDK root 为零/多根、设备 ownership/证据不全均 exit 2，不得调大阈值或把不可判说成失败/通过。
- fresh measure 必须使用同批 A2/B、三夹具、A/B/A/B 四段、每夹具每段 n>=10，独立 nearest-rank p50/p95 重算，所有有效 B/A≤1.10。空 raw、段序错、身份不一致或样本不足不得 pass。
- 最终金标准仍是用户真机蜂窝网络+广州中转打洞打开会话“秒开、没有空白”。agent、模拟器、机械 1.10 全绿不能代替用户 gate。
- migrate 只有 fresh verify 与用户 gate 全绿后才可按旧任务书，以精确 PID/状态前置对旧 perf-regress 做 ledgerdsl plan/apply；不清历史，不手写 live JSON，不把停止动作提前到本格。
- 不读取/输出凭据、SDK 值、APK 字节或生产 daemon 明文日志；不公开分发 debug 签名 APK；不碰真实 tmux、生产 daemon 或他人 qemu。
- command 格和 mechanical 判据四态：0=本格合同机械全绿；1=在量具/身份有效时被反证；2=环境、工具、资产、身份或证据不足不可判；未派发=not applicable。exit 2 不得折成 0/1。

## 4. 创作验收、失败出口与启动边界

authoring 判据 `.team/ledgers/acceptance/baseline-bundle-successor9-structure.sh` 必须机械重算：新 id/revision、九格八边、三 command frontier、原 WT/argv/产物、successor8 r4 三 succeeded 与 apparatus exit2 历史、五个 provenance commit 的 ancestry/cat-file、retained WT HEAD、固定 command pair、apparatus successor9 command+四 required、后链逐字段不弱化、无 statuses。

必须 fresh 完成两次 byte-identical DSL compile、jsonschema、固定 pair `--preflight --json` 与 `--dry-run --json`、`sh -n`、shellcheck、structure green/破坏齿、retained selector regression 与 continuity。dry-run 首 frontier 必须精确只有三 command，apparatus 显示 `dependency_unsatisfied`。这些检查不得 `--drive`、不得运行 successor9 owned-emulator、不得启动设备。

任一 commit/path/WT/量具不可取回、selector/continuity exit 2、schema/preflight 不可用或结构无法闭合，合法结果是 `verdict: blocked`，不得猜。全部 fresh 机械事实闭合才写 `RESULT.md` 末行 `verdict: pass`。本轮只创作并验证包，不启动 ledger、不 commit；正常收工只 `report_result` 一次。
