# baseline-bundle successor6 bootstrap — 内容身份与槽位投影解耦

## 背景与只读 provenance

`ledger.baseline-bundle.successor5.v1` revision 4 的 impl 因 apparatus 契约冲突 rc1：合法 manifest 的 canonical projection 重算等于 `bundle_id`，两份独立 build 路径为固定非 id 槽位；但 successor5 wrapper 传递调用旧 `baseline-bundle-impl.sh`，旧门强制 `.team/private/baseline-vault/{bundle_id}/builds/` 前缀。由于 `apk_relpath` 本身参与 bundle_id hash，这构成 id↔path 固定点，不是 SDK/fixture/provenance 或已证明的产品红。

冻结输入为 main `9ac4eeec81f610b43147b0e513c01511b2161cdf`、`.team/nodes/baseline-bundle-successor5-impl-diagnosis/VERDICT.md`、successor5 ledger revision 4 SHA-256 `4a705bd8f01815ed0a20a7278465b33b592884215cbb62de20937f16fe5a7ba9`，以及旧五本 ledger/attempt 全部历史。不得修改、清 attempt、重派、启动或把旧 repro/test/probe 重放当 successor6 新证据。

## 两阶段 bootstrap 交付

本阶段只交稳定、可由 Git 跟踪的：

- 本目录总任务书与 `impl-任务书.md`、`test-任务书.md`、`probe-任务书.md`；
- `.team/ledgers/acceptance/baseline-bundle-successor6-{impl,deep,projection-regression}.sh`；
- `.team/ledgers/acceptance/baseline-bundle-successor6-projection.py`；
- `.team/ledgers/acceptance/fixtures/baseline-bundle-successor6/{projection-contract.json,legal-successor5-manifest.json}`；
- bootstrap 真实/fixture/语法/连续性日志与 `BOOTSTRAP-RESULT.md`。

本阶段禁止生成 `.team/ledgers/src/baseline-bundle-successor6-v1.py` 或 `.team/ledgers/baseline-bundle-successor6-v1.json`，没有可启动账本。leader 独立语义审查 pass 并提交上述 bootstrap 后，第二阶段才以该 commit 为 immutable provenance 生成 final successor6 DSL/compiled ledger与全新 WT；不得让未跟踪候选自引用。

## 新真实门的两个独立命题

1. **内容身份：** 从真实 manifest 精确取 `source/runtime/artifact/build/equivalence/implementation`，用 UTF-8、sort_keys、紧凑分隔符的 canonical JSON 重算 SHA-256，必须与 64 位小写 `bundle_id` 相等。输出固定证据 `SUCCESSOR6_CANONICAL_IDENTITY equal=true`。
2. **存储槽位：** `independent_builds` 恰有两个且按 index 匹配固定 Git-tracked contract：
   - `.team/private/baseline-vault/builds/build-1.apk`
   - `.team/private/baseline-vault/builds-second/build-2.apk`
   两条均为仓内相对路径、无 `..`/绝对路径/反斜杠、互异，不含当前 bundle_id、`{bundle_id}` 或 `PENDING`；build_root 非空互异。archive 仍必须是 `.team/private/baseline-{vault,backup}/{bundle_id}/baseline.apk` 的内容寻址路径。输出固定证据 `SUCCESSOR6_SLOT_PROJECTION ...`。

内容身份和槽位必须分别判定；不得以“路径前缀看起来像 id”代替 canonical hash，也不得把独立 build 强搬入 id-scoped 路径。manifest 在 projection 与 deep/antiforgery 门前后 SHA 必须不变，防并发 TOCTOU。

## 不得弱化的完整深门

`baseline-bundle-successor6-deep.sh` 从旧 deep 门外科复制，仅把 independent build 的循环 id-prefix 改成仓根安全前缀；精确槽位由先行 projection gate 负责，manifest hash 前后锁定。其余来源 closure、APK SHA/MD5/size、normalized runtime、签名/包名/版本、两个 no-cache build、独立 build_root、报告摘要、工具 provenance、archive id 路径、regular file/非 symlink/不同 inode/不可写、backup/retrieve、A2 equivalence 与 focused mutation evidence不得删改。

`baseline-bundle-successor6-impl.sh` 顺序必须是：复用 successor5 SDK fallback+regression → successor6 tracked fixture 红绿 → 真实 manifest 内容身份/槽位 → successor6 deep → successor3 canonical real fixture → successor3 controlled bypass → manifest hash不变。不得调用旧 `baseline-bundle-impl.sh` 或 successor3 impl wrapper。

## 永久破坏齿与四态

固定 fixture 的合法 successor5 manifest先证明旧前缀 predicate rc1，再由新 projection rc0。单变量齿：

- 只篡改 bundle_id，不重算 projection → rc1 且原因为 canonical identity；
- 路径越界后重算 bundle_id/archive → rc1 且原因为 unsafe component；
- 槽位路径改名或交换后重算 bundle_id/archive → rc1 且原因为 slot projection mismatch；
- 改成旧 id-scoped build 路径并重算新 id/archive → rc1，不能回到循环投影；
- manifest/helper/contract 缺失或 contract 漂移 → rc2；
- 合法 fixture → rc0，canonical、slot、archive 三个证据齐全。

0=真实内容身份、槽位与深门全绿；1=实现/manifest/摘要/路径/防伪被反证；2=SDK、fixture、资产、工具或事实不可判；不适用只用于互斥路线未派发。判据不得输出凭据、SDK值或私有 APK 内容。

## final 阶段不得弱化

final successor6 仍须完整九格十边 `repro → (test ∥ probe ∥ impl) → verify → user-gate → migrate → measure → final`，新且不存在的三 WT，test/probe/impl 并行隔离，无 statuses，successor6-only required/legacy-negative，复用 successor5 SDK fallback/四态与新 projection 结构齿。后续仍保留 Baseline Bundle 双归档、fresh A/B/A/B、每夹具每段 n>=10、B/A<=1.10、真机“秒开无空白”与迁移机械前置。不改 App/server、不公开 debug APK、不读凭据。

verdict: pass
