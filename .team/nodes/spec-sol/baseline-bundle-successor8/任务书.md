# baseline-bundle successor8 frontier 审计连续性任务书

## 背景与必读坐标

successor7 r1 因 waiter/frontier 未消费而没有推进，不是三门未完成。独立判词 `.team/nodes/baseline-bundle-successor7-frontier-recovery/VERDICT.md` 已在原三 WT 重跑现有门并得 continuity/test/probe 精确 rc0。禁止 collect、重派、修改 successor7 r1 或复制/清洗旧 attempts；新账本是消费已存机械事实的 audit successor，不是把旧 agent 自报改写为新成功。

冻结 provenance：

- successor7 final ledger commit `79cd08f0f53d0bd2e44dfd4d4e2fb33cbde001f2`，r1 compiled SHA-256 `447d8a6fb608c2ab520c0a4f3a3d7bb5ab69f9818d2e008bb049096d103320dd`；
- command-compatible review commit `25517d808cc19e3f002ceba51000b2a269bec362`，唯一兼容 pair source/schema/DSL HEAD `7485102b26ed34eb828e94900902147d5e00e995`；
- 两轮 r1 dispatch 同源日志 `.team/nodes/_driver/baseline-bundle-successor7-v1.out`，SHA-256 `aff56f8ceb238464429d577e33f51a75f51889578f6f3a3cb6ba746802e90b91`；
- frontier verdict SHA-256 `1191853ef9e1abd5cf27a1bc1cbd28a03f59871576c80f832d32fab6f6ecd1d1`；
- `wt-s7-cedar` RED.md SHA-256 `04cdbd661548a4b3261c88d491cf80c48f98dcbe3c080e710fb7d12bbe6c105a`，`wt-s7-orbit` PROBE.md SHA-256 `88868a1a1979d3f1504e5efd6876dc5ca8ed5cc6b45a2eb6f6dd23b8e5176cf7`。

## 精确交付与图

账本源 `.team/ledgers/src/baseline-bundle-successor8-v1.py`，编译产物 `.team/ledgers/baseline-bundle-successor8-v1.json`。ledger id=`ledger.baseline-bundle.successor8.v1`，revision=1，无 statuses。固定九格：

`continuity-consume ∥ apparatus-test-consume ∥ apparatus-probe-consume → apparatus(command) → fresh verify → user-gate → migrate → fresh measure → final`。

首 frontier 三格必须全是 `executor=command`，并在原 WT 精确执行：

- `wt-maple-core`：`/bin/sh .team/ledgers/acceptance/baseline-bundle-successor7-continuity.sh`；
- `wt-s7-cedar`：`/bin/sh .team/ledgers/acceptance/baseline-bundle-successor7-test.sh`，消费已有 RED.md；
- `wt-s7-orbit`：`/bin/sh .team/ledgers/acceptance/baseline-bundle-successor7-probe.sh`，消费已有 PROBE.md。

三条 `requires_success` 边全成功后才解锁原 apparatus command；apparatus 仍精确运行 `/bin/sh .team/ledgers/acceptance/baseline-bundle-successor7-owned-emulator.sh`，required 仍为 successor7 apparatus/fixture/continuity。后续任务、owner、WT、write/read paths、handoff 与 required 全部继承 successor7 final，不弱化。

## 硬约束与四态

command 格不 `team-agent send`、不等 `report_result`，只信现有门的 exit code。三格都不写产品/证据，不重做 RED/PROBE、不启动设备；只有三格成功后的 apparatus 可依旧契约启动本任务 owned emulator。不得碰他人 qemu、生产 daemon/真实 tmux、凭据或公开分发 debug APK。

所有 command/mechanical 四态：0=机械事实全绿；1=有效身份/行为反证；2=环境、WT、产物或可取回性不可判；未派发=不适用。不得把2折成1/0。测试禁缓存：Go `-count=1`，Gradle `--rerun-tasks --no-build-cache`。性能格先 `envcheck.sh --gate`，再做同批三夹具 A/B/A/B，每段 n>=10，nearest-rank p50/p95，全格 B/A<=1.10；终局仍必须用户真机蜂窝+广州中转“秒开、没有空白”。

## 验收与合法出口

authoring 门 `.team/ledgers/acceptance/baseline-bundle-successor8-structure.sh` 必须独立核对九格/8边、首 frontier 三 command、精确 argv/cwd/WT/artifact SHA、r1 两轮 dispatch、79cd/25517d/7485102、无 statuses、apparatus required 与后续门。fresh compile/schema/preflight/dry-run 与结构破坏齿均须留在 `.team/nodes/spec-sol/baseline-bundle-successor8/`。

本轮只创作不启动；不 drive/apply/collect/重派/修改 r1。构造、schema、preflight、dry-run、结构齿和 provenance 全绿为 `verdict: pass`；任一契约事实不能可证地闭合则 `verdict: blocked`，不猜。正常收工只 `report_result` 一次。
