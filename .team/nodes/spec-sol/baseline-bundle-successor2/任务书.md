# ledger.baseline-bundle.successor2.v1 — 新 WT 审计连续性 successor

## 背景与必读坐标

本账本承接 `ledger.baseline-bundle.v1` revision 1 与 `ledger.baseline-bundle.successor.v1` revision 1 run1 的未完成产品目标。这是 successor run1 因复用旧 worktree 而不可判之后的审计连续性 successor，不是重置或洗掉历史。

run1 独立判词 `.team/nodes/baseline-bundle-successor-run1-diagnosis/VERDICT.md` 已证明：successor1 复用 `wt-bundle-core`，实际 cwd 的旧脚本仍 grep magic token，且 translator/regression 不存在，导致一个形式红和一个 exit 127 不可判；这不是产品失败，也不是 result waiter 超时。每个席位必须同时读本任务书、`.team/nodes/spec-sol/baseline-bundle/任务书.md` 与自己原有路径级任务书；本任务书未重述的原要求仍全量有效。

冻结的只读 provenance：

- 旧 ledger 首红 commit `c4846de5497903bf1a0b2db98fe9ede4c2d8ac5d`；旧 live `.team/ledgers/baseline-bundle-v1.json` revision 1 / repro `failed_retryable` / attempt `att-t.baseline-bundle.repro-seq1-t1787590762873`；
- 新 repro translator/regression 返修 commit `488a1f25bc8b0a35afb62df85e3afd5ce666bf8e`；
- successor1 创作与独立启动审查 commit `ef7a02c1d23d85eaf08c1093efafd50376fa4db5`；
- successor1 run1 live `.team/ledgers/baseline-bundle-successor-v1.json` revision 1，保留 attempt `att-t.baseline-bundle.repro-seq1-t1787594275571`；
- successor1 独立启动判词 `.team/nodes/baseline-bundle-successor-review/VERDICT.md`；
- run1 不可判判词 `.team/nodes/baseline-bundle-successor-run1-diagnosis/VERDICT.md`，SHA-256 `524452626ba464cb74f80d2f0938219c6af72dc7d274559337546be5d7e4c799`；
- framework plan P0 `.team/artifacts/ledger-p0-ledgerdsl-plan-rejects-parallel-20260824.md`。

这些路径只读。禁止修改旧两本 live、清/复制旧 attempts、把旧不可判重写成成功、重派旧 case，或从旧 worktree 搬文件冒充新基线。

## Successor2 身份与精确交付

- ledger id：`ledger.baseline-bundle.successor2.v1`；
- DSL 源：`.team/ledgers/src/baseline-bundle-successor2-v1.py`；
- 编译账本：`.team/ledgers/baseline-bundle-successor2-v1.json`；
- 关系：supersedes 上述两本 revision 1 for future execution only；两本旧账继续分别承载首红与 run1 不可判历史。

successor2 由 ledgerdsl 正常 compile 为全新 revision 1，九格初始 `planned`、无 runtime attempts。这只表示 successor2 尚未执行，不是把历史归零。逐格 required artifacts、写范围、机械 Check、budget、角色和合法出口继续以原任务书与编译账本的合取为准。

## 全新 worktree 身份

所有写任务只能使用下列三个新身份；编写时必须机械证明 git worktree metadata 无同名项，且 `.worktrees/<id>` 在磁盘不存在：

| 用途 | worktree_id | 使用任务 |
|---|---|---|
| 串行核心链 | `wt-b2-mainline` | repro、impl、verify、user-gate、migrate、measure、final |
| 并行红测 | `wt-b2-redcase` | test |
| 并行根因审查 | `wt-b2-oracle` | probe |

核心链共享 `wt-b2-mainline` 是为了让串行后继消费同一工作树中的产品修改和证据；这些格由 requires_success 严格串行，不得并发。repro 成功后的 `test ∥ probe ∥ impl` 分别位于 `wt-b2-redcase`、`wt-b2-oracle`、`wt-b2-mainline`，三者身份与写范围隔离。禁止退回 `wt-bundle-core`、`wt-bb-test`、`wt-archive-probe` 或任何已存在 WT。

leader 以后启动时，三个 WT 必须由 ledger-run 从当时的 current main 新建；current main 必须是 `ef7a02c1d23d85eaf08c1093efafd50376fa4db5` 的后代。`resources.provenance.revision` 固定为 `ef7a02c1d...`，表示最低可执行基线：该 commit 已包含且 `git cat-file` 可达以下四个 acceptance 脚本：

- `.team/ledgers/acceptance/baseline-bundle-real-chain-probe.sh`；
- `.team/ledgers/acceptance/baseline-bundle-repro.sh`；
- `.team/ledgers/acceptance/baseline-bundle-repro-translate.sh`；
- `.team/ledgers/acceptance/baseline-bundle-repro-regression.sh`。

新 WT 内任一脚本缺失、HEAD 不是该 commit 后代或摘要/契约漂移，repro 必须 exit 2，不得复制 root/旧 WT 文件补齐。

## 任务图与不得弱化的门

任务图仍为：

```text
repro → (test ∥ probe ∥ impl) → verify → user-gate → migrate → measure → final
```

角色、九格集合、10 条 requires_success、`baseline-bundle-wave` max_concurrency=3/failure_policy=halt、handoff、required artifacts、required checks、expected 0、unjudgeable 2、fallback 均不得弱化。successor2 相对已审 successor1 只允许改变 ledger_id、审计连续性 title、只读 provenance、全新 worktree_id 与资源 provenance revision。

产品金标准继续是严格合取：

1. exact A 缺失诚实走 `blocked_missing_baseline`/exit 2；仅允许 `recover_exact_artifact` 或 `rebaseline_with_equivalence_proof`；
2. A2 运行内容等价证明与不可变 Baseline Bundle 不得省略；测前核可取回、摘要、安装和独立备份恢复，完成前归档后再取回；
3. 性能仍为三夹具四段 A/B/A/B、每夹具每段 n>=10、同批身份、nearest-rank p50/p95、所有 B/A<=1.10；测前 envcheck，测试禁缓存；
4. verify 独立且可否决；用户仍须在蜂窝网络+广州中转路径对确切 bundle 真机裁定“秒开无空白”；实验室或 agent 不得代判；
5. 只有 bundle verify 与 user gate 全绿后，migrate 才可按原机械前置处置旧 perf-regress；建立 successor2 不等于迁移已完成；
6. final 仍须让 real-chain probe 在根治后真实 exit 0；repro 对 legacy rc=1 的固定转译不能替代终局。

## 启动边界与合法出口

本次只创作、编译、校验 successor2；禁止 `ledger-run --drive`，禁止预建三个 WT，禁止创建 lease/PID/attempt，禁止派单。leader 审核后另行启动。

运行后，required artifacts 全落盘才 `report_result` 一次。机械四态：0=通过，1=产品或交付不通过，2=环境/基线/事实不可判；不可判必须写明缺口与恢复条件，不得折色。框架问题投递后继续当前产品链，但不得借“继续”修改 framework 或洗掉旧账。

verdict: pass
