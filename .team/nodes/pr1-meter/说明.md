# t.meter · 给重着色漏斗补仪表（契约 089 §2）

## 自检

```
$ pwd
/Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr1.meter

$ git branch --show-current
pr/d2-meter
```

- 分支名：`pr/d2-meter`
- 开工 HEAD：`cd8d8d0cbf0f4ec32c50cf901db90ed85b35f607`（落 pr1-v1 账本）
- **本回合硬约束禁止 `git commit` / push**，工作区未提交。leader 代开 PR 前需把本 worktree 改动提交到 `pr/d2-meter`。
- 未 push（本仓无 remote，PR 由 leader 代开）

## 改了哪些文件

**产品（write_paths 内）**

- `app/app/src/main/java/dev/agentmirror/app/ui/theme/TermPalette.kt`
  - `indexed` 六条分支各记 `[term-indexed]`：`screen-black` / `index-254` / `near-white` / `ansi16` / `fallthrough-guardRgbBg` / `fallthrough-project`
  - `guardRgbBg` 四条分支各记 `[term-guard-bg]`：`luma-screen-black` / `luma-achroma-white` / `luma-chroma-white` / `project`
  - 每条含操作数：`rgb=` `luma=` `chroma=` `thresh=`（比较两边的阈值原文）`branch=` `out=`
  - 走 `DiagLog.record(..., coalesceKey=...)`，同 tag 同键不新开行
  - **漏斗出口未改**（D2 不修；`userBlockBg` 仍是 APP 常量，本格不等待 t.d1 并线后的主题派生）

**判据（write_paths 未列 test/，但 A-meter-suite 是全量单测，A-meter-branch / A-meter-perf 必须进套件）**

- `app/app/src/test/kotlin/dev/agentmirror/app/ui/theme/TermGuardMeterTest.kt`（新建）

**未改**

- `assembleSlot()` / `userBlockBg` 取值（那是 t.d1）
- 近白/254/luma≥220 的**映射结果**（只记日志）
- `DesignTokens.kt` 等存量 T3 缺 KDoc（账本 rev6/r9：wiki 改增量棘轮，⛔ 不修存量）

## 前置说明

账本写「依赖 t.d1 已并线」。本 worktree 仍停在 `cd8d8d0cb`，`userBlockBg` 仍是 `app.userBlockBackground`。本格只补仪表，与 D1 出口正交，未并 D1、未碰 main。

## 判据

### A-meter-branch 先验红（改 `TermPalette.kt` 前跑 `TermGuardMeterTest`，2 tests failed）

```
2 tests completed, 2 failed
BUILD FAILED in 5s
```

`indexedAndGuardRgbBgEachBranchLogsOperands`：

```
java.lang.AssertionError: A-meter-branch 每条分支必须留下含 rgb/luma/chroma/thresh/branch/out 的记录，缺失：[indexed-screen-black: 无 branch=screen-black 于 [], indexed-254: 无 branch=index-254 于 [], indexed-near-white: 无 branch=near-white 于 [], indexed-ansi16: 无 branch=ansi16 于 [], indexed-fallthrough-guard: 无 branch=fallthrough-guardRgbBg 于 [], indexed-fallthrough-project: 无 branch=fallthrough-project 于 [], guard-luma-screen-black: 无 branch=luma-screen-black 于 [], guard-luma-achroma-white: 无 branch=luma-achroma-white 于 [], guard-luma-chroma-white: 无 branch=luma-chroma-white 于 [], guard-project: 无 branch=project 于 []]
```

世界未变：`guardRgbBg` / `indexed` 零仪表。条件已造出（绕开预计算表，逐分支 `colorFor`）。

### A-meter-perf 先验红（同一跑次）

满屏 80×24=1920 格同色 `Rgb(0,0,0)` 且 `againstBg≠defaultBg`（否则热路径表/真彩 memo 会让「不应每格一条」恒真）：

```
java.lang.AssertionError: A-meter-perf：满屏同色必须至少留下 1 条仪表（否则是恒真上界）。cells=1920 n=0 lines=[]
```

### A-meter-branch / A-meter-perf 验绿

```
cd .worktrees/pr1.meter/app
env -u TEAM_AGENT_API_KEY ./gradlew -q :app:testDebugUnitTest --tests dev.agentmirror.app.ui.theme.TermGuardMeterTest
# exit 0
```

全量 `A-meter-suite`（与 smell 并行时曾因抢写 `TEST-*.xml` 假红；单独重跑）：

```
tests 636 failures 0 errors 0
TermGuardMeterTest tests=2 fail=0
RemapThroughputTest twoHundredThousandColorForBelowDeclaredCap fail=0
BUILD SUCCESSFUL
```

### A-meter-wiki（增量棘轮，账本 r9）

先验：`--strict-t3 --pkg dev.agentmirror.app.ui.theme` 本 worktree 存量 15 条（T3-1 Dims/Radii/…、T3-2 TermSchemes `--sha`、T3-3 Theme 缺 `@inv`、T3-4 未声明 `@consumes`）。**不是本格引入，未修。**

验绿：

```
python3 tools/gate/wiki-ratchet.py --root .worktrees/pr1.meter --pkg dev.agentmirror.app.ui.theme
pkg=dev.agentmirror.app.ui.theme 基线=15 本次=15 新增=0 消掉=0
# exit 0
```

### A-meter-smell

```
python3 tools/gate/smell-ratchet.py --face app --root .worktrees/pr1.meter
face=app 基线=16 本次=16 新增=0 消掉=0
# exit 0
```

### A-meter-doc

本文件含：先验红、`git branch --show-current`、`A-meter-perf`。副本：

- `.worktrees/pr1.meter/.team/nodes/pr1-meter/说明.md`（write_paths）
- `.team/nodes/pr1-meter/说明.md`（required_artifacts）

## 查不清

- Grok 整屏绿底**到底命中哪条分支**：本格只补仪表，未拿用户真机导出日志回放。真机日志里应能看到 `[term-indexed]` / `[term-guard-bg]` 的 `branch=` 与 `luma=`/`index=` 操作数。D2 修复判据仍写不出，不在本格。
- 热路径 87.4ns 本机未复测到纳秒；`RemapThroughputTest`（20 万次 `colorFor` 上限 80ms）绿，且满屏同色记录 `n<=8`（实际 coalesce 成 1 条）。
