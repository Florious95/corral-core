# ca-emu2 · 格4 t.perf r17 —— 同 r15 加样本交付（未重采）

账本 revision 17 再派同一张单（只测 `big_scrollback`、A/B 各 20、落 `raw-capp2`）。
r15 已经采完并汇总：40/40 OK，JSON 分列 round1 n=10 / round2 n=20 + combined n=30。
**本回合未再点冷点开**——不同时刻不同负载不算同一批；重跑会把证伪批次搅进第三批。
下面正文是 r15 落盘原文，数未改。

---

# ca-emu2 · 格4 t.perf r15 —— big_scrollback 加样本（A/B 各 20）

本席**无权判噪声**。只把数打出来。

| 批次 | big_scrollback first_draw p50 | p95 | 相对同批 A 是否慢超 +10% |
|---|---|---|---|
| round1 n=10（上一轮，未重跑） | A=142.0 B=167.5 **(+18.0%)** | A=160.8 B=212.9 **(+32.4%)** | **是（2 段红）** |
| **round2 n=20（本轮新采）** | A=165.5 B=166.0 **(+0.3%)** | A=199.2 B=213.8 **(+7.3%)** | **否（4 段 p50/p95 全过）** |
| combined n=30（分列样本上的合并统计） | A=154.0 B=166.0 (+7.8%) | A=195.3 B=221.9 **(+13.6%)** | p50 否；**p95 仍红** |

layout_settled 三套都在门内（round2 p50 −1.6% / p95 +1.5%；combined p50 +0.0% / p95 +2.3%）。
另外两个夹具本轮**未测**，沿用 round1。

产物：`.team/perf/recheck-20260822-capp-ab.json`（判据只读这份最新的）。
新原文：`.team/perf/raw-capp2/{A,B}/` 各 20 份。⛔ 未覆盖 `raw-capp/`（mtime 仍 16:19）、⛔ 未碰 `raw/`（mtime 仍 14:52）。

---

## 0. 做了 / 没做

| 做了 | 没做 |
|---|---|
| 起跑前探 load1=6.69≤15、free+inactive≈8.7GB | ⛔ 动 real_claude_idle / redraw_tui |
| 只跑 big_scrollback，A/B 交替各 20 次冷点开 | ⛔ 编数、剔极端值、补跑失败次 |
| 样本落 `raw-capp2/`，复用既有 APK 与 `coldopen.sh` | ⛔ 改 `tools/perfbase/*.sh` |
| JSON 里 round1 10 次 / round2 20 次**分列**，另给 n=30 合并统计 | ⛔ 把 30 次揉成一堆 samples |
| | ⛔ 宣布「是噪声」或「是真回退」 |

口径与 r13 相同：只认 `adb logcat -s PerfTrace`；跳过 `emitted=0`；相对该次 `ev=tap`。
脚本：`tmp/runab2.sh`、`tmp/summarize-ab2.py`。

中途一次：第一次用 `nohup &` 起跑被外壳收掉，当时 **0 份完整 logcat**（只留下一行残 load）。清掉 `raw-capp2` 半成品后用托管进程重跑满 20×2。未改任务定义。

---

## 1. 被测物与环境

| | A | B |
|---|---|---|
| 形态 | includeBuild composite release | maven 引用式 release |
| md5 | `aecdbd461deece5daec8f81c70af8e54` | `3ebc9c55703c780c842a2f410b85034e` |
| 大小 | 35044459 | 35044459 |
| 路径 | `tmp/app-release-a.apk` | `.team/staging/corral-app/.../app-release.apk` |

模拟器接手时已在跑：`agentmirror_test_b` gpu=host；隔离 daemon **9325**（pid 70830）仍听；⛔ 未碰 9900。
夹具 tmux 自检在自己的 `/tmp/e2e-ca-emu/tmux-501/default`，`big_scrollback` pane comm=假 `claude`。
配对未清（`install -r` 交替装包）。导航 40/40 OK（`raw-capp2/diag/navigation-runlog.log`）。
批次：`started_at=2026-08-22 19:17:11`，实跑 19:21:54–19:34:53。

---

## 2. load（round2，`raw-capp2/{A,B}/host-load.tsv`）

起跑前探：load1 **6.69**。逐次 load1 全程 ≤15。

| | n | load1 min | max | mean |
|---|---|---|---|---|
| A | 20 | 8.37 | 14.32 | 10.45 |
| B | 20 | 8.38 | 14.45 | 10.53 |

两组几乎一样。round1 该夹具是 A 8.57 / B 8.51；本轮整体更高一档，但 A 与 B 彼此对齐。

---

## 3. 数（ms，极端值未剔除）

### 3.1 round1 n=10（raw-capp，本轮未重跑）

| 段 | A p50 | A p95 | A max | B p50 | B p95 | B max | B vs A p50/p95 |
|---|---|---|---|---|---|---|---|
| first_draw | 142.0 | 160.8 | 164 | 167.5 | 212.9 | 230 | **+18.0% / +32.4% RED** |
| layout_settled | 621.0 | 635.0 | 640 | 633.0 | 664.9 | 686 | +1.9% / +4.7% |

A fd：131 133 138 140 141 143 151 151 157 164
B fd：130 137 138 156 165 170 172 175 192 **230**

### 3.2 round2 n=20（raw-capp2，本轮）

| 段 | A p50 | A p95 | A max | B p50 | B p95 | B max | B vs A p50/p95 |
|---|---|---|---|---|---|---|---|
| first_draw | 165.5 | 199.2 | 221 | 166.0 | 213.8 | 247 | **+0.3% / +7.3% 过** |
| layout_settled | 639.0 | 663.1 | 684 | 628.5 | 673.2 | 678 | −1.6% / +1.5% 过 |

A fd：132 167 155 192 150 165 144 126 161 166 145 153 153 171 168 174 174 198 **221** 182
B fd：166 168 155 177 169 166 196 150 159 135 148 149 **247** 149 185 148 134 212 166 170

B max=247 原文 `.team/perf/raw-capp2/B/big_scrollback-13.log`（该次 load1=10.22）。
mkbaseline 极端值标注（≥p95 且 >1.5×p50）：round2 未命中（247 < 1.5×166=249），`outliers: []`；**max 仍在 stats，未删。**

### 3.3 combined n=30（只统计，样本仍分列）

| 段 | A p50 | A p95 | A max | B p50 | B p95 | B max | B vs A p50/p95 |
|---|---|---|---|---|---|---|---|
| first_draw | 154.0 | 195.3 | 221 | 166.0 | 221.9 | 247 | +7.8% / **+13.6% RED** |
| layout_settled | 630.5 | 660.6 | 684 | 630.5 | 675.8 | 686 | +0.0% / +2.3% |

JSON 里 `combined_n30_big_scrollback` **没有**把 30 条揉进一个 `samples` 数组。

---

## 4. 抽查

| 文件 | first_draw | layout_settled | JSON |
|---|---|---|---|
| A `raw-capp2/A/big_scrollback-01.log` | 12346874−12346742=**132** | 619 | 一致 |
| B `raw-capp2/B/big_scrollback-01.log` | **166** | 625 | 一致 |
| B `raw-capp2/B/big_scrollback-14.log` | **149** | 625 | 一致 |

JSON 内 `raw_log_path` 只指向 `raw-capp/` 或 `raw-capp2/`，零条 `.team/perf/raw/`。

---

## 5. 给下游判者（操作数，不代判）

问的是「加样本之后，那两段红扛不扛得住」。数是：

1. **本轮 20 次同夹具同流程**：p50 +18% / p95 +32% **没有再现**；变成 +0.3% / +7.3%，门内。
2. **把上一轮 10 次留着合并**：p50 落到 +7.8%（门内），p95 仍 +13.6%（门外）。合并集的红来自 round1 那截右尾仍在分位里，外加 round2 B 的 247。
3. round2 的 A p50=165.5 已靠近 round1 的 B p50=167.5；round1 的 A p50=142 在 round2 的 A 分布里偏左（round2 A 最小 126，但中位到了 165.5）。
4. 两组 load 仍对齐，解释不了「只 B 慢」。
5. 本席不把 1 说成噪声、也不把 2 说成真回退。最小样本数要不要提，由判者定。

另外两个夹具未加样本，仍以 r13 的 10 次为准（redraw_tui first_draw p50 +9.2% 未复测）。
