# ca-emu · 格4 t.perf（ledger.coreapp.v1 r4）—— 结论：**PASS**

一句话：**被测物 = 格3 `assembleRelease` 产出的 `app-release.apk`（md5 `3ebc9c55703c780c842a2f410b85034e`），
三夹具各 10 次冷点开，只从 `adb logcat -s PerfTrace` 取数，八键齐，
`judge-perf-nonregress.sh` 退出码 0（逐段 p50/p95 都低于基线 +10% 红线）。**
⛔ 没换夹具、没换次数、没换取数方式；极端值未剔除。

## 一、被测物

| | 值 |
|---|---|
| APK 来源 | `.team/staging/corral-app/app/build/outputs/apk/release/app-release.apk`（格3 说明原文） |
| APK md5 | **`3ebc9c55703c780c842a2f410b85034e`**（拷到本格 tmp 后复算相同） |
| dex | PerfTrace 17 处、`debug.agentmirror.perftrace` 1 处、addBinaryListener 1 处 |
| 核依赖 | `dev.agentmirror.core:{core-protocol,core-terminal,core-conn}:20260822.0` |
| daemon | 本树 `server/` 自编 `tmp/agentmirrord-ca`，端口 **9324**，隔离 state dir，⛔ 没碰 9900 |
| 模拟器 | `agentmirror_test_b`，gpu=host / Apple M1 Pro |

## 二、数字（ms）

判据原文 `.team/nodes/ca-emu/tmp/judge-perf-nonregress.out`：

| 夹具 | 段 | 基线 p50 / p95 | 复测 p50 / p95 |
|---|---|---|---|
| `real_claude_idle` | first_draw | 1450.5 / 1908.0 | 148.5 / 170.1 |
| | layout_settled | 1591.0 / 2009.7 | 626.0 / 637.8 |
| `redraw_tui` | first_draw | 1478.0 / 2280.0 | 152.0 / 178.0 |
| | layout_settled | 1609.0 / 2403.5 | 623.0 / 647.2 |
| `big_scrollback` | first_draw | 1492.0 / 1891.8 | 172.0 / 231.0 |
| | layout_settled | 1586.5 / 2029.7 | 640.0 / 673.1 |

30/30 导航成功。本格只量测，数字更快不是本格改产品码。

## 三、负载

`raw/diag/host-load.tsv` load1：**n=30 均值 12.08、最小 7.89、峰值 18.63**（10 核）。
基线批均值 9.16、峰值 10.49。前 20 次大多 8–12；`big_scrollback` 约 15–18。
⛔ 未另开一批凑低负载。

## 四、环境自证

- 隔离 tmux 自检 `raw/diag/socket-selfcheck.log`：`SOCK=/tmp/e2e-ca-emu/tmux-501/default`，
  三个会话都在自己的 socket；pane comm = 本格 `fakebin/claude`（`ln -s /bin/bash`）。
- 点击：uiautomator 语义定位（cwd 精确匹配 + 夹具名），⛔ 不识图不取帧。
- 起模拟器前 load1≈6.5，free+inactive 充裕；正式批开始 load1=10.21。

## 五、显式报出的偏离

1. **本树原 `baseline-20260822.json` 是 rev12 判不出版**（`app_sha fb50a388b`，stats 全 null）。
   拿它去比，判据只能 UNJUDGEABLE。我换成 PASS 版（`8d811c199`，与 `wt-pb-base` / `wt-pb-cperf` 同文），
   旧文件归档 `.team/perf/archive/baseline-20260822-rev12-判不出.json`。
2. **同目录还有 `recheck-20260822-split-判不出.json`**，`ls recheck-*.json | tail -1` 会抢走本格文件。
   已移到 `archive/`，不改判据脚本。
3. 二级页 `tmux rename-window` 成夹具 key（与基线席同一偏离，否则三行都叫 bash）。
4. 夹具 cwd 是 `…/ca-emu/tmp/cwd`，不是 `…/pb-emu/tmp/cwd`。
5. 隔离 socket 用 `/tmp/e2e-ca-emu`（短路径硬要求；任务书 02 写的例外名是 e2e-pb-emu）。
6. 模拟器是我起的（`-gpu host`，不用 `-no-window`/swangle）。

## 六、收尾

9324 已关、隔离 tmux 已 kill、自起模拟器已停；9900 全程未碰仍在听。

产物：`.team/perf/recheck-20260822-capp.json`。
