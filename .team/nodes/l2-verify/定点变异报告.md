# 定点变异报告 — ledger.level2-list-status.v1 / t.e2e / r7

对照席不实现功能。验的是：判据会不会在实现坏掉时咬人。

## ① 跨端探针（必须先绿）

命令：`bash .team/nodes/l2-design/probe.sh`

- 结果：exit 0（派单后复跑仍绿：marker `L2P178705787060449`）
- 隔离 pane 标题 `◐ L2P178705735316575` → 订阅后收到 `level2_frame`，`status=working`
- 改标题 `✳ …` 后 5s 内收到 `status=idle`
- 结论：实现已在线上工作，探针不是「文件存在就算绿」

## ② 破坏了什么

文件：`server/internal/api/level2.go` 函数 `classifyPaneTitle`

把表外符号（`default` 分支）从

```
return protocol.SessionStatusUnknown, r, false
```

改成

```
return protocol.SessionStatusIdle, r, false
```

这就是方案点名的坏实现：「未知符号回落成空闲」。身份抽取未动，只动这一处。

脚本：`.team/nodes/l2-verify/mutate-and-check.sh`（trap 恢复；哈希对不上则 fail）。

## 哪条测试变红

具名测试：`TestL2UnknownGlyphStaysUnknown`（`server/internal/api/level2_test.go`）

变异后实测：

```
--- FAIL: TestL2UnknownGlyphStaysUnknown (0.00s)
    level2_test.go:159: status = "idle", want unknown (must not fall back to idle)
```

exit ≠ 0。自检：「如果被测对象是坏的，这条命令会不会仍然返回 0？」——不会。坏实现把 `?foo` 判成 idle，测试红。

## 恢复后是否变绿

是。恢复后 `level2.go` sha256 回到基线

`c95e265303ec9345dc801745653da74cfff97e378a4f2aa3f50a5281af6166c7`

`TestL2UnknownGlyphStaysUnknown` exit 0。

`git status --porcelain server/internal/api/level2.go` 为空：破坏自动恢复。

## 跑了几次

| 轮次 | 基线绿 | 变异红 | 恢复绿 | 脚本 exit | 终态哈希 |
| --- | --- | --- | --- | --- | --- |
| 1 | 是 | 是（status=idle） | 是 | 0 | 与基线相同 |
| 2 | 是 | 是（status=idle） | 是 | 0 | 与基线相同 |
| 3（本派单复跑） | 是 | 是（status=idle） | 是 | 0 | 与基线相同 |

三轮操作数相同，稳定。

## 工作区

本席只写仓根 `.team/nodes/l2-verify/`（r6：判据在仓根跑，不写 `.worktrees/`）。`server/` 在变异前后干净。

`A-e2-clean`（`git status --porcelain server app | grep -E '^ M|^MM'`）现为空。未进 worktree 写第二份产物。
