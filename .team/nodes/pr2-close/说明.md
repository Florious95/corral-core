# t.close · E12 · close_session 原子关闭

分支：`pr/e12-close`

## pwd 与 git branch --show-current

```
/Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr2.proto
pr/e12-close
```

开工时 worktree 曾停在 `pr/e14-provider-cmd`（落后 main）。按本格只准一条分支，已 `checkout pr/e12-close` 并以 main（`f4a2f9a0e land pr/sort-label`）为底，避免把 E12 交到别的 PR 分支。

## 改了哪些文件

协议 / 服务端（write_paths 内）：

- `server/internal/protocol/frametype.go` `frames.go` `validate.go` `json.go` `errors.go` `golden_test.go` `json_test.go` `close_session_test.go`
- `server/internal/protocol/testdata/close_session.json` `close_session_ack_ok.json` `close_session_ack_fail.json`
- `server/internal/api/lifecycle.go` `lifecycle_test.go` `ws_conn.go`

App conn / workspace（write_paths 内）：

- `app/.../conn/Frame.kt` `Frames.kt` `FrameEnums.kt` `ConnectionManager.kt`
- `app/.../workspace/WorkspaceViewModel.kt` `WorkspaceScreen.kt` `CloseConfirmDialog.kt` `PackageDoc.kt`

为把二次确认接到已有 `menu-close`（方案 §5.1/§5.4），**显式扩写了账本 write_paths 之外的接线**（否则长按「关闭」仍是空回调）：

- `app/.../ui/screens/SessionListScreen.kt`（透传 `onClose`）
- `app/.../session/SessionScreen.kt` `SessionRoute.kt`
- `app/.../AgentMirrorApp.kt`（根上弹确认框；ack.ok 且当前会话即被关项则退出会话页）

测试：

- `app/app/src/test/java/dev/agentmirror/app/conn/ConnCloseSessionCodecTest.kt`
- `app/app/src/test/kotlin/dev/agentmirror/app/workspace/CloseSessionConfirmTest.kt`

## 显式缺口（方案有、本格没做）

- **未改** `docs/protocol.md`、`web/js/protocol.js`（账本 write_paths 不含 docs/web；未知 type 仍会让旧 Web 客户端报错）。
- **未跑模拟器手测**。进程+pane 用隔离 tmux 测绿；收藏收尾用 JVM 测绿。真机三态同时看见 = 查不清（本席未起模拟器）。
- Grok Bypass `--always-approve` 属 E13/E14，本格未改启动命令。

## 先验红（改之前，原始输出）

### 协议解码 close_session → ErrUnknownType

`go test ./internal/protocol -count=1 -run TestCloseSessionRoundTrip -v`：

```
=== RUN   TestCloseSessionRoundTrip
    close_session_test.go:15: UnmarshalFrame(close_session): protocol: unknown frame type: "close_session"
--- FAIL: TestCloseSessionRoundTrip (0.00s)
FAIL
FAIL	github.com/agentmirror/agentmirror/internal/protocol	0.533s
FAIL
```

### App 解码 close_session → UNSUPPORTED_TYPE

`./gradlew :app:testDebugUnitTest --tests dev.agentmirror.app.conn.ConnCloseSessionCodecTest`（实现前）：

```
2 tests completed, 2 failed
FAILURE: Build failed with an exception.
Execution failed for task ':app:testDebugUnitTest'.
```

XML：`FrameDecodeException: unknown frame type: close_session` 与 `unknown frame type: close_session_ack`。

### A-close-wiki / A-close-smell

改代码前这两条棘轮相对 main 基线无新增（恒绿）。它们断言的是「本格不引入新违规」，不是 close_session 本身。先验红抓的是编解码世界：旧代码不认识该 type。

### A-close-doc / A-close-seal

说明文件本格开工时不存在（判据会红）。封版由 leader 在报完后跑，本席不 commit。

## 验绿（改之后，原始输出）

协议：

```
ok  	github.com/agentmirror/agentmirror/internal/protocol	0.562s
```

隔离 tmux（pane 不在 + pid `Kill(pid,0)` 失败 + 缺 ref 幂等 ok）：

```
=== RUN   TestCloseSessionKillsPaneAndProcess
--- PASS: TestCloseSessionKillsPaneAndProcess (0.12s)
=== RUN   TestCloseSessionMissingRefIsOK
--- PASS: TestCloseSessionMissingRefIsOK (0.04s)
PASS
ok  	github.com/agentmirror/agentmirror/internal/api	0.630s
```

A-close-suite（worktree `app` 上 `./gradlew -q :app:testDebugUnitTest`）：**EXIT:0**。

其中 `ConnCloseSessionCodecTest` 绿：`tests="2" failures="0"`。`CloseSessionConfirmTest`：未确认不发帧；ack.ok 才取消收藏；ack.fail 留对话框且收藏仍在。

A-close-wiki：

```
pkg=dev.agentmirror.app.workspace 基线=8 本次=8 新增=0 消掉=0
pkg=dev.agentmirror.app 基线=1 本次=1 新增=0 消掉=0
pkg=internal/api 基线=3 本次=3 新增=0 消掉=0
pkg=internal/protocol 基线=0 本次=0 新增=0 消掉=0
EXIT:0
```

A-close-smell：

```
face=app 基线=16 本次=16 新增=0 消掉=0
EXIT:0
```

## 行为摘要（对照方案 §5）

1. 二次确认：`AlertDialog` 标题「关闭会话」，正文含「不可恢复」与 `displayName`，tag `close-confirm` / `close-confirm-ok` / `close-confirm-cancel`。未确认不发帧。
2. 协议：`close_session` C→S（req_id≥1, ref）；`close_session_ack` S→C（ok；失败 reason 闭集 `session_not_found` / `close_failed` / `internal`）。pane 已不在 → ok=true（幂等）。
3. 服务端 `lifecycle.go`：空 socket 直接 `close_failed`，不回退默认 tmux socket。`kill-pane` 后轮询 ≤2s（pane 不在 **且** pid 不在）；pane 没了 pid 还在则对进程组 SIGTERM/SIGKILL。只读 `ps -o comm=`，不读 argv。日志带 `pane_gone` / `pid_alive` / `pane_pid` / `socket` / `pane_id`。
4. 客户端：ack.ok 才 `FavoriteBook` 去掉（已收藏才 toggle，失败重试一次）；当前会话即该项则清 `activeSession`。失败对话框留下并带 reason。

未 commit、未 push、未并线。
