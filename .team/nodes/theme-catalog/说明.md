# t.catalog · 终端主题库落位

仓根施工（账本最高优先级覆盖派单 worktree 段）。未改 `TermPalette.kt` / 设置页。

## 先验红（改之前）

A-cat-notice / A-cat-count / A-cat-noimposter：目标文件不存在。

```
FileNotFoundError: NOTICE-iterm2-color-schemes.txt
FileNotFoundError: TermSchemes.kt
```

A-cat-doc：`test -s .team/nodes/theme-catalog/说明.md` → exit 1（文件不存在）。

A-cat-test（`.team/acclogs/A-cat-test-pre.log`）：

```
> No tests found for given includes: [dev.agentmirror.app.ui.theme.TermSchemeCatalogTest](--tests filter)
BUILD FAILED in 1s
```

## 产物

- `tools/import_iterm_schemes.py`：清单写死 30 族；`gh api` 按文件拉 SHA `4cbae6273354e5e91a7641d72c69daa3de6a867f`；缺 key / 未知 Color Space → exit 1。
- `TermSchemes.kt`：52 份唯一上游文件（方案写「51 / 9 个深浅同文件」，表内同文件族实为 8 个，30×2−8=52。未静默改清单）。
- `NOTICE-iterm2-color-schemes.txt`：含 iTerm2-Color-Schemes / Mark Badolato / MIT / 两行版权 / 单主题版权归作者 / 完整 MIT / Apache-2.0 / 未用 Heeler·Ghostty。
- `TermSchemeCatalogTest.kt`：钉 Vesper `0xFF101010`、Afterglow `0xFF212121`。

## §8 判断（查不清处）

51 份并非全是 sRGB：

| 文件 | Color Space | 处理 |
|---|---|---|
| 其余成功文件 | sRGB 或无 | 分量按 0–1 sRGB 取整 |
| `One Half Light.itermcolors`、`Snazzy.itermcolors` | Calibrated | iTerm 遗留设备 RGB，与 sRGB 同编码，按分量使用（不是 Display P3） |
| `Apple System Colors Light.itermcolors`、`Oceanic Next.itermcolors` | P3 | **没有**把 P3 当 sRGB；按 Display P3 线性矩阵转到 sRGB 再取整 |

未在 52 份 XML 里读到与 MIT 冲突的声明。

## 禁令核对

- 未 copy Heeler / 未 clone 上游全仓。
- TermSchemes.kt 不含变体文件名（One Dark Two / Horizon Bright / Rose Pine Moon / Catppuccin Frappe / Dracula+）。
- 未改 `colorFor`。

## 改后判据（仓根）

- A-cat-test：`:app:testDebugUnitTest --tests TermSchemeCatalogTest` exit 0
- A-cat-suite：`:app:testDebugUnitTest` 全量 exit 0
- A-cat-notice / A-cat-count (165 个 `.itermcolors`) / A-cat-noimposter / A-cat-doc：exit 0
- `python3 tools/import_iterm_schemes.py --check`：52 份 hex 与仓内生成物一致

## 返工第 2 轮（未推倒重做）

`pwd` = 仓根。五件产物仍在：TermSchemes.kt 50781B / NOTICE 1719B / import 脚本 / CatalogTest / 说明.md。
金样、165 个 `.itermcolors`、无变体文件名、52 份目录均仍对。
重跑六条：notice/count/noimposter/doc/test/suite 全绿。
（suite 第一次被 SIGKILL 后 Robolectric `UnsatisfiedLinkError` 误红 94 条 Compose 测；catalog 11 条当时已 PASS。清 native 缓存后 suite 再跑 exit 0。）
