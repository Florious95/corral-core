# LIBRARIAN.md · fix-ts-state-dir-e2e 撞库回执（需求维基管理员）

> 只读 requirement-base 撞库：tsnet 状态目录 / state-dir / headscale 隔离 / HOME 禁止改写 / authkey QR 唯一出口 / 部署自由。
> 只给命中条目编号+原文摘录+关联点，不定夺、不改需求库。

## 逐关键词命中清单

### ① tsnet 状态目录 / state-dir
- **无命中（条目）**：requirement-base 全部 17 条 + REVISIONS 无"tsnet 状态目录/state-dir/数据落盘位置"需求条目。tsnet 状态目录属实现层部署细节，需求库未沉淀。
- **弱关联 007**：原文「服务端同理可内嵌 tsnet，主机侧甚至不必装 tailscale」——"内嵌"语义隐含自管状态，但未指定目录位置。

### ② headscale 隔离 / 部署自由
- **命中 007**（007-联网模型-tsnet与扫码.md）：原文「"App 里填 Tailscale token"意味着 App 内嵌 tsnet 库，App 自己就是一个 TS 节点，用户不需要单独装 Tailscale App」；「电脑上起服务端 → 出二维码 → App 扫码即连」。
  **关联点**：内嵌自管控制面（headscale）正合"主机侧不必装 tailscale"的免依赖部署自由；FIELD 要求"自建 headscale + 隔离 daemon"即 007 内嵌组网的隔离化执行。
- **命中 008**（008-生产级定位与开源许可.md）：原文「Apache 2.0 全开源，不盈利，毫无保留」；「面向舰队的产品形态……是空位」。
  **关联点**：全开源隐含部署自由（用户可自托管全部组件含控制面），与 headscale 自建同向。
- **弱命中 004**（004-后台策略-无状态免疫.md）：原文「官方免费推送中转 + 完全自托管（ntfy 式）双轨」——自托管是既定哲学，虽指推送，可类推部署面。

### ③ HOME 禁止改写
- **无命中（条目）**：需求库无"不触碰用户 HOME/系统目录"条目。这属工程安全红线（CLAUDE.md 红线），非需求条目。

### ④ authkey QR 唯一出口
- **命中 007**：原文「两条实现路线**不互斥，可分层**：- (a) QR 只是配对信息载体，底下仍是 TS（扫码 = 自动填 authkey 完成组网）」。
  **关联点**：FIELD"服务端向 App 的唯一合法出口是 QR"正对应 007 路线 (a)——QR 载配对信息（authkey/token），扫码即组网。QR 是配对密钥的唯一出口语义已由 007 裁定。
- **命中 011**（011-技术路线裁定.md）：原文「扫码走**路线 (a)**：QR 载服务端地址 + 配对 token（可选 TS authkey），路线 (b) 自建 P2P 留后续版本」。
  **关联点**：011 收口 007：QR 载服务端地址+配对 token（可选 authkey），明确 authkey 作为配对载体经 QR 出口，不另设 argv 出口。FIELD 禁 `-ts-authkey` argv、只允许 TS_AUTHKEY env + QR 出口，与 011 语义一致（argv 非需求指定出口）。
- **弱关联 R-003**（REVISIONS）：QR 选中 TUN 地址缺陷——QR 承载内容的正确性曾出真机缺陷，佐证"QR 出口必须可靠"。

### ⑤ 部署自由
- **命中 007/008**（见②）：内嵌免装、全开源自托管，构成部署自由的两根支柱。

## 结论（供裁定，非裁定）

需求库对"tsnet 状态目录/HOME 禁止改写"本身**无条目**（属工程安全红线，不在维基）；命中面是：
**007**（内嵌 tsnet、QR 配对唯一出口语义）、**011**（路线 (a) QR 载地址+token、authkey 经 QR）、**008**（全开源自托管=部署自由）、**004**（自托管哲学弱支撑）、**R-003**（QR 承载正确性实证）。
本文件零改动需求库。
