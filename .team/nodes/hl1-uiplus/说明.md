# t.uiplus · 「+ Agent」弹窗重做

## 开工自检

```
pwd
/Volumes/nvme/Projects/远程Agent安卓/.worktrees/hl1.uiplus

git branch --show-current
pr/ui-agent-dialog
```

HEAD（硬约束禁止本席 `git commit`，改动在工作区未提交）: `a946101a01e10cfe95bc569ad34c2da05aef05a3`

未提交文件：
- `app/app/src/main/java/dev/agentmirror/app/workspace/NewAgentDialog.kt`
- `app/app/src/main/java/dev/agentmirror/app/workspace/PackageDoc.kt`
- `app/app/src/test/kotlin/dev/agentmirror/app/workspace/NewAgentDialogUiTest.kt`

## 先验红（改代码之前）

### A-uiplus-doc（红，exit 1）

```
=== A-uiplus-doc prior 2026-08-21T18:42:45+08:00 ===
candidates=['/Volumes/nvme/Projects/远程Agent安卓/.team/nodes/hl1-uiplus/说明.md', '/Volumes/nvme/Projects/远程Agent安卓/.worktrees/hl1.uiplus/.team/nodes/hl1-uiplus/说明.md'] hit=[]
产物两处都不存在
exit=1
```

### A-uiplus-wiki（改前绿，exit 0；本格不改架构边）

```
=== A-uiplus-wiki prior 2026-08-21T18:42:45+08:00 ===
pkg=dev.agentmirror.app 基线=1 本次=1 新增=0 消掉=0（键不含行号）
exit=0
```

### A-uiplus-smell（改前绿，exit 0）

```
=== A-uiplus-smell prior 2026-08-21T18:42:52+08:00 ===
face=app 基线=16 本次=16 新增=0 消掉=0（键不含行号）
exit=0
```

### A-uiplus-suite（改前绿，exit 0；`-q` 无失败行）

```
EXIT:0
```

先验红命中点是 **A-uiplus-doc**：说明产物当时不存在。suite/wiki/smell 在改 UI 前已绿，本格没有靠改判据变绿。

## 截图前=

`.team/nodes/hl1-uiplus/shot-before.png`

（模拟器 emulator-5554，装改前 debug 包，工作区右上角 + 打开弹窗。裸 `○/●` 文字单选、Bypass 无说明、「创建」是弱 TextButton。）

## 截图后=

`.team/nodes/hl1-uiplus/shot-after.png`

（同一模拟器，`install -r` 本分支 debug 包后再开 +。Provider 为 088 §3 卡通图标卡片；bypass 有一句说明；「创建」主色置底。未点「创建」，未点开任何真实会话。）

## 改点=

- 重做 `NewAgentDialog`：Provider 两列卡片 + `ProviderIcon`（不是 RadioButton）；工作区短行可滚；Bypass 勾选带 `NEW_AGENT_BYPASS_HINT`；底栏描边「取消」+ 主色「创建」。
- `buildNewAgentArgv` / `confirmNewAgent` 未改（088 §7 argv 仍由客户端组装、服务端原样执行）。
- 单测 `NewAgentDialogUiTest`：无 `○ Claude Code`、六家 contentDescription、hint 可见、确认钮在 Provider 卡片之下、点 Grok 仍回调选择。
- `PackageDoc` 补 `@consumes dev.agentmirror.app.ui.components`。

## 隔离环境自检

```
TMUX_TMPDIR=.team/nodes/hl1-uiplus/tmp/iso/t tmux -f /dev/null list-sessions
uiplus-iso: 1 windows
socket: .team/nodes/hl1-uiplus/tmp/iso/t/tmux-501/default
假 CLI: ln -s /bin/bash …/iso/bin/claude
```

收工时已 `kill` 本席 daemon，并对该 TMPD `tmux kill-server`（未碰默认 socket）。

## 改后判据

- A-uiplus-suite：`./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL（含 NewAgentDialogUiTest）
- A-uiplus-wiki：基线=1 本次=1 新增=0 EXIT:0
- A-uiplus-smell：基线=16 本次=16 新增=0 EXIT:0
- A-uiplus-doc：本文件含 `截图前=` / `截图后=` / `改点=`
