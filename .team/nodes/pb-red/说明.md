# t.red 说明（任务一 · PerfTrace 先红）

席位：红测席 `pb-test` / 格 `pb-red`。worktree：`.worktrees/wt-pb-red`。未 commit / 未 push。

## 做了什么

1. **API 骨架** `app/app/src/main/java/dev/agentmirror/app/perf/PerfTrace.kt`
   - 方法签名齐：`beginOpen` + 八事件（`tap`/`route_enter`/`subscribe_sent`/`geom_seed`/`first_frame_recv`/`snapshot_applied`/`first_draw`/`layout_settled`）+ 开关/假时钟/假出口注入钩子。
   - 复用 `DiagLog.Clock` 作注入时钟类型；`Sink` 为假出口。
   - **方法体不发日志**（`beginOpen` 返回空串；八个 ev 空实现）。⛔ 没写实现。
2. **红测** `app/app/src/test/kotlin/dev/agentmirror/app/perf/PerfTraceChainTest.kt`
   - 方法名逐字：`perfTrace_关闭时零行` / `perfTrace_一次打开产出八事件且open_id一致且时间单调` / `perfTrace_两次并发打开open_id不串`。无 `@Ignore`。

## 先验红原始输出（必须有）

工作目录：`/Volumes/nvme/Projects/远程Agent安卓/.worktrees/wt-pb-red`

### 命令 1（首次编译+跑测，含 compileDebugKotlin）

```
$ cd app && ./gradlew :app:testDebugUnitTest --offline --tests 'dev.agentmirror.app.perf.PerfTraceChainTest'
```

完整日志：`.team/nodes/pb-red/tmp/red-run-first-compile.log`

退出码：**1**（测试失败，不是编译失败）

关键行（无 `Compilation error` / `e: file:///` / `Unresolved reference`）：

```
> Task :app:compileDebugKotlin
...（仅既有 w: 警告，无 e:）
> Task :app:compileDebugUnitTestKotlin
...（仅既有 w: 警告，无 e:）
> Task :app:testDebugUnitTest FAILED

PerfTraceChainTest > perfTrace_两次并发打开open_id不串 FAILED
    java.lang.AssertionError at PerfTraceChainTest.kt:206

PerfTraceChainTest > perfTrace_一次打开产出八事件且open_id一致且时间单调 FAILED
    java.lang.AssertionError at PerfTraceChainTest.kt:92

3 tests completed, 2 failed

BUILD FAILED in 10s
36 actionable tasks: 36 executed
```

JUnit XML（`.team/nodes/pb-red/tmp/TEST-PerfTraceChainTest.xml`）原文失败信息：

```
tests="3" skipped="0" failures="2" errors="0"

testcase name="perfTrace_关闭时零行"  → 通过
  （骨架不发日志，与「关时零行」同形，允许绿；实现后仍须绿）

testcase name="perfTrace_一次打开产出八事件且open_id一致且时间单调"
  java.lang.AssertionError: beginOpen 必须给出非空 open_id，实际=''

testcase name="perfTrace_两次并发打开open_id不串"
  java.lang.AssertionError: 两次 beginOpen 都必须给出非空 open_id a='' b=''
```

### 命令 2（判据脚本，权威）

```
$ bash tools/perfbase/judge-red.sh
```

stdout 原文：

```
PASS 先红成立 rc=1
```

判据脚本自身退出码：**0**（四态：0=通过，确实先红了）。

判据内部 gradle 日志：`.team/nodes/pb-red/tmp/red-run.log` 末尾：

```
> Task :app:testDebugUnitTest FAILED

PerfTraceChainTest > perfTrace_两次并发打开open_id不串 FAILED
    java.lang.AssertionError at PerfTraceChainTest.kt:206

PerfTraceChainTest > perfTrace_一次打开产出八事件且open_id一致且时间单调 FAILED
    java.lang.AssertionError at PerfTraceChainTest.kt:92

3 tests completed, 2 failed
BUILD FAILED in 1s
```

## 给施工席的接口契约（骨架已钉死，勿改方法名）

- `PerfTrace.beginOpen(): String` → 短随机 `open_id`
- `tap` / `routeEnter` / `subscribeSent` / `geomSeed(rows,cols)` / `firstFrameRecv(kind,bytes)` / `snapshotApplied(seq,alt)` / `firstDraw(glyphs)` / `layoutSettled(quietMs, lastReflowSrc)`
- 一行：`open_id=… ev=… t=…` 空格分隔，`t` 取注入时钟（生产 elapsedRealtime）
- 关：`setEnabledForTest(false)` 后零行（调用点最外层短路是产品接线的事）
- 假出口：`setSinkForTest`；实现应默认接到 `Log.d("PerfTrace", line)` **和** `DiagLog.record("PerfTrace", line)`

## 没改的

- 没写事件格式化 / 没写 Log.d / 没写 DiagLog.record
- 没改 `tools/perfbase/*.sh`
- 没开模拟器、没碰 9900、没 commit/push
