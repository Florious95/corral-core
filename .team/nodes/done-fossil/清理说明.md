# t.done-fossil 清理说明：删 App 侧 DONE（已完成）

> 任务：`.team/ledgers/done-fossil-v1.json` / `t.done-fossil`
> 执行席：`r.dev-state`（2026-08-15）
> 裁定依据：`docs/rulings/20260815-用户裁定原文-直通输入与刷新模型.md` §三
> 「去除完成这个状态，就是工作和空闲，并且要绝对准确」；
> `requirement-base/entries/058-状态检测先归档回退再重建.md`
> （服务端 `StateDone` 已于 55bcada2a 删除，本任务是清 App 侧尾巴）。

## 做了什么（全部完成）

`AgentState.kt` 的 `DONE("done")` 枚举删除（五值闭集 → 四值闭集），并同步清理**全部**引用：

### 写路径内（`conn/` + `test/`）

| 文件 | 改动 |
|---|---|
| `conn/AgentState.kt` | 删 `DONE("done")` 枚举；文档「五值闭集」→「四值闭集」，注释改写不载 `DONE` 字面 |
| `test/.../conn/ConnCodecTest.kt` | `AgentState.DONE` ×4 → `AgentState.IDLE`（golden 夹具已由 t.archive 把 `"done"` 改 `"idle"`，App 端断言对齐） |

### 写路径外（leader 裁定扩大，文件放行）

| 文件 | 改动 |
|---|---|
| `workspace/StateBadgeStyle.kt` | 删 `DONE("完成")` 枚举 + `of()` 的 `AgentState.DONE -> DONE` 分支；文档「五值」→「四值」 |
| `workspace/StateBadge.kt` | 删 `StateBadgeStyle.DONE -> tones.done` 分支；注释「五态」→「四态」 |
| `service/StateWatcher.kt` | `needsAttention` 去掉 `|| curr == AgentState.DONE`，只留 `curr == AgentState.BLOCKED`；注释同步（含 blocked→done 刷新语义移除） |
| `service/NotificationHelper.kt` | 删 `AgentState.DONE -> "任务已完成"` 通知分支（leader 裁定：随 DONE 消失是正确的，死代码）；注释同步 |

### 测试（test/）

| 文件 | 改动 |
|---|---|
| `service/ServiceStateWatcherTest.kt` | 删 `transitionToDone_fires`（done 沿已不存在）；`blockedToDone_refreshesNotification` 改为 `blockedRepeat_suppressed`（保留抑制语义）；`initialListing` 的 DONE 会话 → IDLE；头注 10→8 条 |
| `service/NotificationHelperTest.kt` | 删 `notifyState_done_textCompleted`；注释 blocked→done 刷新语义移除 |
| `workspace/StateBadgeTest.kt` | `of()` 双射删 DONE；label 去重 5→4；删 `badge_done_labelAndSemantics` |
| `workspace/WorkspaceViewModelTest.kt` | 2 处 `AgentState.DONE` → `AgentState.IDLE` |

### 未动

- `OkHttpWebSocketTransportTest.kt:128` 的 `transport.close("done")` —— websocket 关闭原因字符串，**与 AgentState 枚举无关**。
- `WorkspaceViewModel.kt` / `WorkspaceScreen.kt` —— leader 明令禁止（④ 刷新模型实现未提交躺在那）。
- BLOCKED / UNKNOWN 枚举 —— 不动（UNKNOWN 一等公民兜底）。

## 判据结果（leader 会重跑，退出码权威）

| 判据 | 命令 (cwd) | 期望 | 实际 |
|---|---|---|---|
| A-df-no-done | `! grep -rn 'DONE' app/.../conn/AgentState.kt` (.) | 0 | **0** |
| A-df-app-test | `./gradlew :app:testDebugUnitTest --rerun-tasks --console=plain` (app) | 0 | **0**（BUILD SUCCESSFUL, 36 tasks） |
| A-df-server-clean | `! grep -rn 'StateDone' server/` (.) | 0 | **0** |

## 环境备注（判据波动）

首次全量跑 `BUILD SUCCESSFUL`，随后两次因 **gradle daemon 在中文工作路径（「远程Agent安卓」）下的编码 bug + 残留 test-results 干扰**报 `Could not write XML` / `NoClassDefFoundError` / `:terminal:compileKotlin 源文件找不到`——与本次改动无关（失败测试如 BackGestureNavTest/TermRightMarginTest 本次未碰）。**清理 `test-results` + `--no-daemon` 后全量稳定 SUCCESSFUL**。App 测试判据最终以清理后重跑为准。
