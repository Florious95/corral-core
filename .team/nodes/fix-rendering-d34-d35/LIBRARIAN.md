# 需求基 · fix-rendering-d34-d35（撞库回执）

> 检索者：leader（w-librarian 席位 18:26 后无应答，leader 直接查需求权威 `requirement-wiki/`）
> 需求权威：`requirement-wiki/`（LLM Wiki 概念网络）；不可变真相源：`requirement-wiki/raw/`
> 本回执只摘原文，不含检索者观点。

## 命中一：D-35 原文（raw/046，IMMUTABLE）

`requirement-wiki/raw/046-底部bypass符号缺省.md` 全文要点：

```
# 046 底部 bypass 符号缺省
- 状态：已裁定（本轮缺陷，需修复）
- 类型：缺陷 D-35
- 出处：用户 2026-08-12 真机截图实证

## 缺陷现象
状态栏 bypass permissions 前的符号显示为空。

## 处理要求
需**多模态模型**开发者处理。

## 关联
[[018]] UI 视觉标准（状态栏渲染）。
```

维基命题页 `wiki/claims/bypass符号缺省.md`：confidence 0.75，
命题「状态栏 bypass 前符号不应为空（渲染缺陷）；修复需多模态看截图判断」。

### ⚠️ 口径冲突已裁决（依据 raw/046 原文）

| 载体 | 表述 | 判定 |
|---|---|---|
| `requirement-wiki/raw/046`（真相源） | 符号**显示为空** | ✅ 权威 |
| `taskbook.yaml` fix-rendering-d34-d35 | 「bypass 符号**缺省**」 | ✅ 与真相源一致 |
| `docs/defects-v5-status.md` | 「bypass 符号**透明红框**」 | ❌ **错误** |
| `HANDOFF-leader-20260812.md` §3/§4 | 「bypass 符号**透明红框**」 | ❌ **错误** |

**这是「符号该画却没画出来」的字形/渲染回退问题，不是配色或透明度问题。**
按「透明红框」去改配色 = 改错方向。上届把 D-35「错误标记为已修」，与此口径分裂高度相关。

## 命中二：D-34 关联（同一任务另一半）

`wiki/claims/缩放字体堆叠.md`（源 raw/045-缩放到一定比例时字体堆叠.md）：
与 D-35 同属多模态 UI 缺陷，同归 `[[UI视觉标准]]` 状态栏/终端渲染。

## 命中三：UI 视觉标准「审查关」= 本任务的流程红线（raw/018）

`wiki/concepts/UI视觉标准.md`（importance 5，status mature），七项视觉标准中与本任务直接相关：

- 第 7 项**终端页专项**：字形完整（**无豆腐块**）、等宽对齐、滚动 60fps、顶栏紧凑
- 第 1 项现代设计语言：Material 3 统一 Theme，深浅色双套
- 第 5 项状态可视：状态徽章五态可辨且**色弱可辨**

**审查关（流程红线，原文）**：
- UI 任务交件**必附模拟器截图**（全态：正常 / 空 / 错误 / 深色）落 `e2e/artifacts/ui-review/`
- leader 验收**逐图目检**对照七项标准，逐条结论写进证据 JSON 的 `ui_review` 字段
- **测试绿但目检不过 = 不合格打回**
- 真机清单每步附 UI 体验判定（简陋 / 合格 / 优秀），「简陋」即立案
- 多模态模型要求：`[[缩放字体堆叠]]`（D-34）、`[[bypass符号缺省]]`（D-35）需能看截图判断的开发者处理

该条目自述由来：用户三次真机回炉批评「功能绿 ≠ 体验合格」后的结构性修正——
此前 UI 任务只有功能验收，无视觉标准、无审查关。

## 未命中

- termview 层此前的契约级用户裁定：无独立条目（渲染标准统一收在 `[[UI视觉标准]]` 第 7 项）
- D-35 未见与「透明」「红框」「配色」相关的任何原始条目
