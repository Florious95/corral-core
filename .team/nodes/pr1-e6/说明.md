# t.e6 · 「已附加图片」成功态不组节点

## 开工自检

```
$ pwd
/Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr1.e6

$ git branch --show-current
pr/e6-upload-success
```

仓根在 `main`（开工时误在仓根 `git checkout -b`，已立刻切回 `main`，分支只留在本 worktree）。

- 分支名：`pr/e6-upload-success`
- HEAD sha：`cd8d8d0cbf0f4ec32c50cf901db90ed85b35f607`（与切出时 main 同 sha）
- **未提交**：本格硬约束禁止 `git commit` / `push`。改动在工作区。leader 代开 PR 前需要把下面文件提交到本分支。

## 改了哪些文件

- `app/app/src/main/java/dev/agentmirror/app/session/SessionModels.kt` — 加 `TransientSuccess` 标记与共同出口 `bannerFrom`；`InputStatus.Sent` / `UploadStatus.Success` 实现该标记。`bannerFrom` 里**没有** `UploadStatus.Success` case。
- `app/app/src/main/java/dev/agentmirror/app/session/SessionScreen.kt` — `StatusArea` 只调 `bannerFrom`，删掉原来按 case 组「已附加图片」的 when。
- `app/app/src/main/java/dev/agentmirror/app/session/PackageDoc.kt` — 补 4 条本包已有 import 的 `@consumes`（T3-4 先验已红，不加则 `A-e6-wiki` 过不了）。
- `app/app/src/test/java/dev/agentmirror/app/session/StatusBannerTest.kt` — `A-e6-both` / `A-e6-struct`。测试目录不在 write_paths，但 `A-e6-suite` 跑的是 gradle 单测，不写这里判据落不了地。显式报出。

## 修法

成功态共同出口：`bannerFrom` 第一句 `if (status is TransientSuccess) return null`。新成功态实现 `TransientSuccess` 即默认不组节点，不必再补 when-case。失败/在途仍返回文案。

## 先验红（改产品码之前）

先写 `ae6Both_sentAndUploadSuccessDoNotComposeNodes`：造出 `InputStatus.Sent` 与 `UploadStatus.Success("/tmp/shot.png")`，再断言节点数 == 0。当时 `Sent` 已是 null（083 §12），`Success` 仍返回「已附加图片」。

```
$ cd /Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr1.e6/app
$ env -u TEAM_AGENT_API_KEY ./gradlew :app:testDebugUnitTest --tests 'dev.agentmirror.app.session.StatusBannerTest'

StatusBannerTest > ae6Both_sentAndUploadSuccessDoNotComposeNodes FAILED
    java.lang.AssertionError at StatusBannerTest.kt:70

1 test completed, 1 failed
FAILURE: Build failed with an exception.

报告原文：
java.lang.AssertionError: UploadStatus.Success 不得组「已附加图片」节点 expected:<0> but was:<1>
```

Sent 那半段当时已经是 0；红在 Success 组出了 1 个节点。这正是契约 089 §3 说的兄弟成功态。

`A-e6-struct` 依赖共同出口符号，改之前编不过，故先验红只跑 `A-e6-both`（任务书也只要求这一条先验红）。

`A-e6-wiki` 改 PackageDoc 之前原始输出：

```
T3-1 : PASS
T3-2 : PASS
T3-3 : PASS
T3-4 : FAIL — 跨层声明一致：4 条违规
  `dev.agentmirror.app.ui.components` — import 了却未声明 @consumes
  `dev.agentmirror.app.ui.model` — import 了却未声明 @consumes
  `dev.agentmirror.app.ui.screens` — import 了却未声明 @consumes
  `dev.agentmirror.app.workspace` — import 了却未声明 @consumes
结果：未通过（exit 1）
```

这 4 条 import 不是本格引入的；不补 `@consumes` 则机械门 `A-e6-wiki` 过不了，所以补了声明（未改 import、未改行为）。

## 验绿

### A-e6-both / A-e6-struct

```
$ env -u TEAM_AGENT_API_KEY ./gradlew :app:testDebugUnitTest --tests 'dev.agentmirror.app.session.StatusBannerTest'
BUILD SUCCESSFUL in 6s

报告：tests=2 failures=0
  ae6Both_sentAndUploadSuccessDoNotComposeNodes  passed
  ae6Struct_unknownSuccessGoesThroughCommonExit  passed
```

`A-e6-struct`：测试里 `object : TransientSuccess {}`（不是 Sent、也不是 UploadStatus.Success）走 `bannerFrom`，返回 null。断言的是共同出口，不是又补了一个 case。

失败态仍可见：同一条 `A-e6-both` 里把状态改成 `InputStatus.Failed("发送失败：超时")` 后节点仍在。

### A-e6-suite

```
$ cd /Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr1.e6/app
$ env -u TEAM_AGENT_API_KEY ./gradlew -q :app:testDebugUnitTest
# 无输出，exit 0
```

### A-e6-wiki

```
$ python3 tools/archwiki/build_wiki.py --root /Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr1.e6 --check --strict-t3 --pkg dev.agentmirror.app.session
T3-1 PASS / T3-2 PASS / T3-3 PASS / T3-4 PASS
结果：通过（exit 0）
```

### A-e6-smell（账本 rev3 每格必过）

改完产品码后跑，相对冻结基线不许新增。未修存量 16 条。

```
$ python3 /Volumes/nvme/Projects/远程Agent安卓/tools/gate/smell-ratchet.py --face app --root /Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr1.e6
face=app 基线=16 本次=16 新增=0 消掉=0
exit 0
```

本格 diff 没有引入新 lint。未 `--freeze`、未改棘轮脚本。

## 查不清

- 真机截图验收（`A-089-e6-ui`）不在本格，未跑模拟器。
- 硬约束禁止 commit，所以 `git log main..pr/e6-upload-success` 此刻是空的；评审若只看 commit 会以为没改。实际 diff 在工作区。
- `A-e6-doc` 读的是仓根 `.team/nodes/pr1-e6/说明.md`，本文件同时落了 worktree 与仓根两份。
