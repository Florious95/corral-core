# 062 打包（t.pkg / r7）

只打包，未改任何实现代码。未起模拟器，未碰用户 tmux / 生产 daemon。

## APK

- 路径：`/Volumes/nvme/Projects/远程Agent安卓/app/app/build/outputs/apk/debug/app-debug.apk`
- 命令：仓根 `app/` 下 `./gradlew :app:assembleDebug --rerun-tasks`（exit 0）
- size：39377065
- sha256：`2dbc62a51adc957f311857680076f2f1750ccc219df80ca4cac0bfe6865a5f6e`
- mtime：2026-08-18 21:59:15

## 不是旧产物

对 `app/app/src/**/*.kt` 与 APK mtime 比较：`newer_kt=[]`。

## 收口：三态探针（不是 dex 符号）

`bash .team/nodes/l2b-design/probe-tristate.sh` → exit 0

- 隔离 daemon 听 `127.0.0.1:50565`（非 9900）
- Grok 空闲标题 → `idle`
- Grok 工作标题（盲文 + Waiting for response）→ `working`
- `※` 前导 → `unknown`，日志含 `U+203B` 与完整标题
