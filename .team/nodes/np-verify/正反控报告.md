# 正反控报告 — ledger.nodeprobe.v1 / t.verify / r4

对照席不实现、不修。socket（本 team，未碰用户 default）：`/private/tmp/tmux-501/ta-b7cc1c640ccf`。节点：`advisor`（`team-grok-l2` / `%1`）。

真值来源独立于 nodeprobe：本席给 advisor 发一条「思考 30 秒再回一句话」；工作期间标题出现厂商工作标记；对方回完并静置后再采空闲。

## 正控

- 时间：`2026-08-18T14:38:44Z`（UTC）
- 操作：向 advisor 投递一次性无害消息后立即采
- 当时 pane 标题：`⠙ - Waiting for response… - Level2 回炉：归档回退与全空根因探针 - grok`
- nodeprobe 判定：**working**

```json
{
  "session": "team-grok-l2",
  "window_index": 0,
  "window_name": "advisor",
  "pane_id": "%1",
  "name": "advisor",
  "provider": "grok",
  "state": "working",
  "evidence": {
    "method": "pane_title",
    "detail": "provider=grok state=working first=U+2819 ⠙ title=⠙ - Waiting for response… - Level2 回炉：归档回退与全空根因探针 - grok",
    "comms": ["sh", "node", "grok"]
  }
}
```

## 反控

- 时间：`2026-08-18T14:41:34Z`（UTC）
- 操作：advisor 已回完（inbox 有回执），标题工作标记消失后再静置，同一节点再采
- 当时 pane 标题：`Level2 回炉：归档回退与全空根因探针 - grok`
- nodeprobe 判定：**idle**

```json
{
  "session": "team-grok-l2",
  "window_index": 0,
  "window_name": "advisor",
  "pane_id": "%1",
  "name": "advisor",
  "provider": "grok",
  "state": "idle",
  "evidence": {
    "method": "pane_title",
    "detail": "provider=grok state=idle first=U+004C L title=Level2 回炉：归档回退与全空根因探针 - grok",
    "comms": ["sh", "node", "grok"]
  }
}
```

## 结论

同一节点两头都咬住：工作期 working，回完后 idle。恒返回 working 的尺子会栽在反控上。

附：advisor 回「在干活，可供正控」时标题已掉成无前导符号——那一帧若当正控会红。本报告正控用的是更早、标题仍带 `Waiting for response` 的那次（14:38:44）。

## 定点变异

脚本：`.team/nodes/np-verify/np-mutate.sh`

- 破坏：`tools/nodeprobe/src/classify.rs` fallback 把无人认领前导符号从 `unknown` 改成 `idle`
- 语料测试：`tests::fixtures_corpus` 变红（`※probe-unknown-full-title` 得到 idle ≠ unknown）
- 恢复后回绿；`classify.rs` sha256 `e446b4e38a17846155d7be1a31bc134c91ced4fefd2fc9fcfb6e78ee81d88de4`
- 连跑 2 次，两头都成立

自检：坏实现下 `fixtures_corpus` 不会仍返回 0。
