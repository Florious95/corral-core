# overlay 对照报告 — ledger.overlay.v1 / t.verify / r4

对照席不实现、不修。三项都在仓根跑；未碰用户 default tmux，未起模拟器。

## ① probe-overlay.sh — 仍红（exit 1）

命令：`bash .team/nodes/ov-design/probe-overlay.sh`  
隔离 socket：`/tmp/ov-probe/tmux/sock`（`list-sessions` 自检 `ovp`）。连跑 2 次，都是：

```
订阅后没有 overlay_frame … timeout 6000ms waiting overlay_frame
FAIL 未收到动态非空抓屏帧
```

daemon 在订阅后打了 `overlay: scratch client started`（`session=am-overlay`），但 6s 内 **一条 `overlay_frame` 都没进 WebSocket**。不是「文件不存在」，是流没推到线上。

本席不改服务端、不改探针。账本要求本条必须绿；世界现状是红。未静默改任务定义。

## ② size-invariance.sh — 绿（exit 0）

隔离 server `/tmp/ov-sizeinv/sock`（自检会话 `user`）。开/关抓屏路径前后：

| 窗 | BEFORE | AFTER |
| --- | --- | --- |
| scratch:0:sc0 | 80×24 | 80×24 |
| user:0:u0 | 80×24 | 80×24 |
| user:1:u1 | 80×24 | 80×24 |

`PASS 全部窗口 #{window_width}x#{window_height} 开/关抓屏路径前后一致`。最硬约束在隔离 server 上成立。

## ③ 定点变异

脚本：`.team/nodes/ov-verify/ov-mutate.sh`

破坏：`server/internal/api/overlay.go` 的 `unmarkOverlay` 改成空操作（关闭后不退订、不 `Stop`）。

具名测试：`TestOverlayNoResourceWithoutSubscriber`

```
--- FAIL: TestOverlayNoResourceWithoutSubscriber
    overlay_test.go:76: after unsubscribe clients=1, want 0
```

恢复后回绿。`overlay.go` sha256  
`184d3a49e1fecba8f18b73b1e8850fcd74845fb1954552656efb3c72f5f92a81`

| 轮次 | 基线绿 | 变异红 | 恢复绿 | 脚本 exit |
| --- | --- | --- | --- | --- |
| 1 | 是 | 是（clients=1） | 是 | 0 |
| 2 | 是 | 是（clients=1） | 是 | 0 |

自检：坏实现下该测试不会仍返回 0。

## 工作区

只写仓根 `.team/nodes/ov-verify/`。变异自动还原。未 `git worktree add`。
