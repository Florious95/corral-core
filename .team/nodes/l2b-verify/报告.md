# 定点变异报告 — ledger.l2-tristate.v1 / t.verify / r6

对照席不实现、不修。验的是：判据会不会在实现坏掉时咬人。

## ① 三态探针（必须先绿）

命令：`bash .team/nodes/l2b-design/probe-tristate.sh`

- 结果：exit 0（r6 复跑仍绿：marker `L2T178706135017149`）
- Grok 空闲 `修滚动摘要L2T178706101679370 - grok` → `status=idle`
- Grok 工作 `⠼ - Waiting for response…` → `status=working`
- `※unk-L2T…` → `status=unknown`，daemon 日志含 `U+203B` 与完整标题
- 结论：线上三态已工作；不是「文件存在就算绿」

## ② 破坏了什么

文件：`server/internal/api/detect.go` 函数 `classifyFallback`

把「无前导符号」（首个非空白是 Letter/Number）从

```
return protocol.SessionStatusIdle, r, true
```

改成

```
return protocol.SessionStatusUnknown, r, true
```

这是 061 病根回潮：没看到符号就判未知。盲文表未动，只动这一处。

脚本：`.team/nodes/l2b-verify/mutate2.sh`（trap 恢复；哈希对不上则 fail）。

## 哪条测试变红

具名测试：`TestL2NoGlyphIsIdle`（`server/internal/api/l2detect_test.go`）

变异后实测：

```
--- FAIL: TestL2NoGlyphIsIdle (0.00s)
    l2detect_test.go:18: fallback title="修滚动的 bug" first=U+4FEE: status="unknown" known=true → want idle known=true
```

exit ≠ 0。自检：「如果被测对象是坏的，这条命令会不会仍然返回 0？」——不会。

## 恢复后是否变绿

是。`detect.go` sha256 回到

`1616a3d098c80158ea0dc593f4f911e5af2f530666b4bf2ba2e21fc0c8aa64b2`

`TestL2NoGlyphIsIdle` exit 0。本席未改脏的实现文件。

## 跑了几次

| 轮次 | 基线绿 | 变异红 | 恢复绿 | 脚本 exit | 终态哈希 |
| --- | --- | --- | --- | --- | --- |
| 1 | 是 | 是（status=unknown） | 是 | 0 | 与基线相同 |
| 2 | 是 | 是（status=unknown） | 是 | 0 | 与基线相同 |
| 3（r6 复跑） | 是 | 是（status=unknown） | 是 | 0 | 与基线相同 |

三轮操作数相同，稳定。

## 工作区

本席只写仓根 `.team/nodes/l2b-verify/`（`worktree_id` 只是互斥标签，未写 `.worktrees/`）。

r6 已删 `A-v-clean`（git status 分不清「变异没恢复」和「实现未提交」）。恢复保证在 `mutate2.sh`：破坏前后 sha256 比对，对不上则非零，由 `A-v-mut` 咬住。本轮哈希已还原。
