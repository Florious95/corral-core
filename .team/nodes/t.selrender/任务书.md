# 任务书 · t.selrender · 拖选时看不见选中高亮（先归因，⛔ 不许先动手改）

## 一、用户实撞（2026-08-23）

用户在手机上单指按住拖动（内层 Claude Code 把它当拖选）：

> 「我在电脑上它有被选择文本的展示，但是我现在……我在屏幕上用手指点击并且滑动的时候，
> **它选中的文本是没有被其他颜色覆盖的**。这样的话，我看到右下角不断地有复制的文本，
> **右下角不断有提示说你复制了多少长度的文本，但是我不知道我复制了哪些文本。**」

⇒ **功能是通的**（内层确实在选、确实复制了），**看不见的是选中态的视觉反馈**。

用户还提了一句可能是线索也可能是猜测：「由于它整体有主题，有重绘，重新的绘制」。
⚠️ **那是用户的推测，⛔ 不是已确认的根因。** 你要自己查。

## 二、这一格只做一件事：**归因**

⛔ **不许先动手改渲染。** 先说清楚**是谁该画这个高亮、现在断在哪一环**。

至少要排除/确认这几种可能（**每条都要给出证据，带 `文件:行号` 或实测**）：

1. **内层根本没发出高亮属性**：Claude Code 画选中用的是什么？
   反显（SGR 7）？背景色（SGR 48）？还是它自己重绘整行？
   **判据：抓一段真实的输出字节看它发了什么。**
2. **核层解析丢了属性**：`app/core-terminal` 的 VT 解析有没有正确处理反显/背景色，
   有没有存进网格单元。
3. **渲染层没画**：`app/app/.../termview/` 的绘制有没有读那些属性并落到画笔上。
4. **主题覆盖**：是不是我方主题/配色在绘制时无条件刷了背景，把选中背景盖掉。
   ⚠️ 用户猜的是这条，**但⛔ 不要因为他猜了就往这条靠**。
5. **快照重放丢态**：`replaySnapshot` 之后属性还在不在
   （⚠️ `capture-pane` 带不带属性取决于有没有 `-e`，第 1 步的注入路径与这个无关，
   但快照路径要看清楚）。

## 三、必须回答的三个问题

1. **谁负责画选中高亮**——内层 app 通过属性字节让终端画，还是终端自己维护选区？
2. **断在第几环**（上面 1–5 的哪一条，或别的）？给证据。
3. **修它要动哪一层**（核层 / 渲染 / 主题）？给出**最小改动方向**，
   ⛔ 但**这一格不实施**。

## 四、交付物

**`.team/nodes/t.selrender/说明.md`**：

- 三个问题的答案，每条标**置信度**：`源码确定` / `实测确定` / `需实测` / `查不出`
- 证据：代码原文（带 `文件:行号`）或实测输出原文
- **最小改动方向**（改哪一层、大概动什么），⛔ 不写代码
- **你判不出的部分**——⛔ 这一节不许空着，除非三条全部「确定」

## 五、硬约束

- ⛔ **不许改任何产品代码**。这一格是查，不是改。
- ⛔ 不碰生产 daemon `:9900`；⛔ 不碰用户真实 tmux；
  ⛔ **不开模拟器**（用户正在用真机测，机器要让给他）。
  要抓真实字节就用隔离 tmux：`mkdir -p /tmp/e2e-sel`（短路径且预建）→ `unset TMUX` →
  建会话 → **`list-sessions` 自检**（建 socket 失败会静默回退到用户真实 socket）。
- ⛔ 不 commit / 不 push；⛔ 不改任何 `judge-*.sh`。
- ⛔ 临时文件只写 `.team/nodes/t.selrender/tmp/`；⛔ 不读凭据；⛔ 无过滤 `ps aux`。
- **收工清干净**，说明里写清起了什么、清了什么。

## 六、合法出口

- 抓不到内层的真实选中字节（起不了真 Claude Code）⇒ 标 `需实测`，
  写清要抓什么才能定，⛔ 不许拿推断当结论。
- 查不出断在哪 ⇒ `status=inconclusive`，把你排除掉的可能与依据写清。
  **判不出是合法终态，且比编一个说得通的根因有价值得多。**

## 七、收工

产物落盘后 `report_result`，`status` 二选一：`done` / `inconclusive`。
⛔ 不许 `team-agent send`。我从产物里读。
