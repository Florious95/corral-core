# t.repro2 · 打开会话白屏（契约 092 §1 · 第二梯度）· ledger r12

status=done
复现=成功
触发条件=大滚回隔离会话（hist=39983，假 CLI，alt=0）冷点开：会话页顶栏/底栏出现后终端画布全黑，16s 内无首帧。同形还出现在真 claude / 静态备用屏（alt=1、无持续重绘）。持续重绘假 TUI 不复现。构建类型（R8/minify）不是必要条件。
截图=.team/nodes/hl1-repro2/shot-ab2-base-end.png, .team/nodes/hl1-repro2/shot-ab2-new-end.png, .team/nodes/hl1-repro2/rec-ab2-base.mp4, .team/nodes/hl1-repro2/rec-ab2-new.mp4, .team/nodes/hl1-repro2/shot-l3-03-session-cold.png, .team/nodes/hl1-repro2/shot-l1-04-session-cold.png
引入commit=用户实证指向今天三条 server land（573cfa866 e12-close / 01467a88c e13-new-pane / e943e9fe1 gofix-scrubbedenv）；本席秒数未能钉死（A/B 无差）
点开到首帧秒数=base=>14s（force-stop 冷点开 hist=39983；t≥3s 画布全黑 color=0 至 14.4s） new=>14s（同条件 color=0 至 14.0s）
服务端A/B=base(dc9aab11b)=>14s无首帧 sendq snapshots_pushed=1 snapshots_from_subscribe=1；new(main cb044e6d9)=>14s无首帧 同 sendq=1/1；差=无。git diff dc9aab11b..HEAD -- server 的 subscribe/snapshot 路径为零（三条 land 只加 close/create_session）

## 自检

```
PWD=/Volumes/nvme/Projects/远程Agent安卓/.worktrees/hl1.repro2
BRANCH=
HEAD=ce72d6c52
```

detached HEAD，本格不建分支。临时文件 `.team/nodes/hl1-repro2/tmp/`（仓根短路径，unix socket 89 字节）。产物双写 worktree 与仓根 `.team/nodes/hl1-repro2/`。

## 现象（所见，不是整页白）

点开隔离会话后：**顶栏（标题 / LAN / 查看）和底栏（Esc/Tab/方向键/输入指令）都在**，终端画布是全黑无字。等 8 秒仍无字。主机 pane 上 tmux capture 仍有 Claude TUI（alt=1）。

对照用户原话「空白且不会出现对话界面」：对话 TUI 不出现，不是 Activity 没起来。

已订阅、内容持续变化时画布能跟上（shot-l3-00-live 有 Claude Code 启动屏；shot-l2-* 有彩色假 TUI）。离开后再点开、且 pane 不再刷 delta 时，画布空。

未点任何真实舰队会话。只点 `hl1repro2cwd`。

## 环境

- 模拟器：已有 `emulator-5554`（1080x2400）。未自起/未杀。
- adb：`~/Library/Android/sdk/platform-tools/adb`
- 用户包：`~/Desktop/agentmirror-20260821-1324.apk` md5 `e6cbe8d9e34769c4f3e3c58f2d069af4`（命中任务书）。`pm clear` 后 `install -r`。
  - dumpsys：`versionName=0.1.0`，`pkgFlags` **含 DEBUGGABLE**。这份用户包不是「非 debuggable + minify」的对照物，不能据此写 R8 嫌疑。
- 隔离 tmux：`TMUX_TMPDIR=.team/nodes/hl1-repro2/tmp/tmux`，会话 `hl1repro2`，cwd=`.../tmp/hl1repro2cwd`，socket=`.../tmp/tmux/tmux-501/default`（89 字节，自检通过）。假 CLI：`ln -s /bin/bash .../tmp/bin/claude`，`ps -o comm=` = `claude`。
- daemon：worktree 现编 `tmp/agentmirrord` listen `0.0.0.0:22217`；`adb reverse tcp:22217 tcp:22217`；手填 `ws://10.0.2.2:22217/ws`。截图前 `input keyevent 111`。
- 真 claude：`/Users/alauda/.local/bin/claude` 2.1.181，pane `alt=1 cmd=claude.exe`。

## 每级结果表

| 级 | 内容 | 冷点开 | 退出重进 | 后台→前台 | 结论 |
|---|---|---|---|---|---|
| 1 | 用户 APK + 假 CLI 主屏 alt=0 | 有字（zsh 提示 + `claude-3.2$`）shot-l1-04 | 顶栏在、画布空 shot-l1-06 | 同左 shot-l1-08 | 冷点开不复现；重进/回前台画布空（主屏有历史，与用户路径不完全同构） |
| 2 | 假 CLI 备用屏 + **持续重绘**彩色 TUI（CJK/长行）alt=1 | 有字 shot-l2-03 | 有字 shot-l2-05 | 有字 shot-l2-06 | **不复现** |
| 3 | 真 `claude` alt=1 真实界面 | 画布空（等 8s 仍空）shot-l3-03 / shot-l3-04 | 画布空 shot-l3-06 | 画布空 shot-l3-07 | **复现**。同一次已订阅时 live 能画出 shot-l3-00 |
| 隔离 | 假 CLI + **静态**备用屏（一帧后 `read`，无重绘）alt=1 | 画布空 shot-iso-03 / wait shot-iso-04 | — | — | 真 CLI **不是**必要条件 |

## 变量隔离

| 变量 | 操作 | 结果 |
|---|---|---|
| 构建类型 / R8 | 全程同一份用户 APK | 级 2 持续重绘绿、级 3 红 ⇒ **不是**构建类型 |
| 备用屏 | 级 2 持续重绘绿；静态备用屏红 | **静态 alt-screen** 即可红 |
| 真 CLI | 级 3 红；静态假 CLI 同样红 | **不是**真 CLI 专有 |

必要条件（本席证据）：**冷点开（或退后再进）一个 alt=1、且打开之后几乎不再吐 delta 的 pane**。持续重绘会把画布「救回来」，掩盖同一条订阅/首帧路径。

用户真机是 Claude Code alt-screen，与第 3 级同构。

## 092 §5 补测：大滚回 + 服务端 A/B（同一用户 APK；构建类型后置）

生产已回滚 dc9aab11b，用户实测「问题小了」。本席按同一用户 APK、只换隔离 daemon 做 A/B。

可见屏 `capture-pane -e` 对本隔离 pane 本机 **9ms / 2412B**（全历史 90ms / 2.4MB）。首帧快照契约是可见屏，不是全历史。

| daemon | sha | 点开→画布有字 | sendq（force-stop 后） | 连拍/录屏 |
|---|---|---|---|---|
| base | dc9aab11b | **>14s 无字** shot-ab2-base-end | subscribes=1 snapshots_pushed=1 snapshots_from_subscribe=1 | rec-ab2-base.mp4 series tmp/burst2-base/ |
| new | main cb044e6d9 | **>14s 无字** shot-ab2-new-end | 同上 1/1/1 | rec-ab2-new.mp4 series tmp/burst2-new/ |

方法：每次 `am force-stop` 后冷启，只点 `hl1repro2cwd` → `bash`。画布采样 n=26250：进会话页后 dark=26250 color=0（排除 L2 浅色假阳性）。

`handleSubscribe` / `Pane.Snapshot` 在 dc9aab11b..HEAD **零 diff**。三条 server land 只新增 `lifecycle.go` close/create_session 与协议帧。`snapshots_from_subscribe` 在 `recordSubscribe()` 入口就 +1，不等于「客户端已画上」。`frames_sent=0` 只统计 `sendMirror`（delta），不能当「没发出去」。

结论：隔离大滚回上 **A/B 秒数无差**，不能用本席数字把引入钉在这三条 land。用户回滚「问题小了」仍是真机实证；本席没能在秒数上复现出 base 快、new 慢。

## 未做

- 未 bisect 14 条 app land；构建类型那级后置。
- 服务端 A/B 已做（force-stop 冷点开），秒数无差。
- 未点真实舰队。
- 架构闭包无卡命中。

## 完整步骤（可复做）

1. 隔离 tmp 短路径；`ln -s /bin/bash tmp/bin/claude`；`unset TMUX`；`TMUX_TMPDIR=tmp/tmux tmux -f /dev/null new-session -d -s hl1repro2 -c tmp/hl1repro2cwd`，PATH 前置后 `exec claude -i`。自检 list-sessions 见 `hl1repro2`。
2. 现编 agentmirrord `-listen 0.0.0.0:<port>`；`adb reverse`；`pm clear`；装用户 APK；手填 `ws://10.0.2.2:<port>/ws`。
3. 只点工作区 `hl1repro2cwd`，再只点该区那一行。
4. 级 3：pane 内 `exec /Users/alauda/.local/bin/claude`，确认 `alternate_on=1` 且 capture 有 Claude 启动屏，再从二级冷点开。
