# t.instrperf3 · 第 0 步打开会话 A/B 回测（第三轮：回炉修复后，keyecho 关）

席位：`in-dev2`。worktree：`.worktrees/wt-instrperf3`。本格**只测不改产品码**。

## 必须先说清的两句

1. 这轮量的是「**打点开着、按键回显开关关着**」时打开会话的代价。`coldopen` 从 `adb logcat -s PerfTrace` 取数 ⇒ PerfTrace **必然开着**。⛔ **量不到「打点关着时的代价」**。
2. 就算全绿，能说的也只是「**没退超过判据门（10%）**」，⛔ 不是「一点没退」。本轮判据实际是 **FAIL（rc=1）**，6/12 段超过 +10%。

## 两个包怎么构建（commit + md5）

| | A（打点前） | B（回炉后：打点 + 独立开关） |
|---|---|---|
| commit | `a706915ef4410fdc27f7e51c9c86a971b94a0492`（`input/instr-v1` 父提交） | `630c76ab9d8103d4efd5ef045641c3af10c5dd6c`（`630c76ab9`：`debug.agentmirror.keyecho` 默认关） |
| 取法 | `git archive <A> app` → `.team/nodes/t.instrperf3/tmp/src-A/`（⛔ 没 `git worktree add`） | 同样 archive 到 `tmp/src-B/` |
| 构建 | `./gradlew :app:assembleRelease`（先 `--offline`） | 同左 |
| 产物 | `tmp/app-A-release.apk` | `tmp/app-B-release.apk` |
| apk md5 | `ba84e6edc1b167514503a517a02762fd` | `9fe59d8fb6afb219784e2c3859f0c55f` |
| 大小 | 35044459 bytes | 35044459 bytes |
| 签名 | `CN=Android Debug`（apksigner；与 20260822 基线同法）；⛔ 仅本地对比，不分发 | 同左 |
| 源码自证 | archive 后 `EV_KEY_SEND_hits=0` `PROP_KEY_ECHO_hits=0` | `EV_KEY_SEND_hits=2` `PROP_KEY_ECHO_hits=2` |

两个 md5 **不同**。A 与第二轮同 commit 的 md5（`c3fada12…`）也不同——release 包不可复现（时间戳），**没用** `.team/perf/` 里任何存档 APK 顶替。`build_type=release`。`build-A.log`：`BUILD SUCCESSFUL in 23s`；B：`19s`。

## 按键开关保持关（本轮硬要求）

开工时 `getprop debug.agentmirror.keyecho` 已是 `0`（上一格 keyecho.sh cleanup 写过 0）。测量脚本里 **没有任何** `setprop debug.agentmirror.keyecho 1`（`grep` 本格 `tmp/*.sh` 零命中）。

- 装包 / 配对冷启前显式写 `0`（防残留），**不是**发现测量流程打开了再偷偷关掉。
- 每一次 `coldopen` 在 `am start` 前再读一次 prop：若为 `1` 则 **exit 3 停测**，不关了继续。
- 实跑：A/B 各 60 次，`keyecho-prop.tsv` **120/120 都是 `0`**。没有一次 abort。

## 取数方法（显式偏离）

`ls tools/perfbase/` 仍只有 `judge-*.sh` 与 `keyecho.sh`，**没有** `coldopen.sh`。取数脚本按 `.team/nodes/ca-emu/tmp/coldopen.sh` + 第二轮已验证的导航补丁抄到本格 `.team/nodes/t.instrperf3/tmp/coldopen.sh`，口径不变：`adb logcat -s PerfTrace`，跳过 `emitted=0`，相对该次 `ev=tap`。

输出目录：`.team/perf/instr-ab2-raw/{A,B}/`。⛔ 没写 `.team/perf/raw/`。

其它自建（均在本格 tmp / `.team/perf/`，unix socket 短路径例外）：

- 隔离 tmux：`/tmp/e2e-ip3`（目录 0700）。`AGENTMIRROR_E2E_DISCOVERY_SOCKET_DIRS` 只扫自己的 socket。自检：`SOCK=/tmp/e2e-ip3/tmux/tmux-501/default`，三会话在自己的 socket 上，pane comm=`claude`。⛔ 不点真实舰队。
- 假 CLI：`ln -s /bin/bash …/claude`（白名单 comm）。
- **窗口改名**：`rename-window` 成夹具 key（与 pb-emu / 前两轮同一偏离）。
- 隔离 daemon：`0.0.0.0:19951`，setsid 拉起。⛔ 没碰 9900（生产 `agentmirrord` pid **16330** 仍在 `*:9900`）。
- 模拟器：已在线 `emulator-5554`。未新起（当时 free+inactive ≈ 2233MiB < 3000，本来也不许新起）。

### 本格另外几条偏离（不改产品码、不改判据）

1. **daemon 二进制**：与第二轮同一条已验证路径。本格用 `agentmirrord` md5 `1120f96923d9791a14145e3bdc64dc8e`（31158306 bytes，来自 `.worktrees/wt-instrperf/.team/nodes/t.instrperf/tmp/`，抄到本格 tmp）。**被测物仍是上面两个 release APK**。
2. **点工作区行**：Compose 行 `clickable=false`，点最小 clickable 祖先；等到 xml 里有 `3 SESSIONS` 才点。
3. **配对后 force-stop 再冷启**：配对探针会关掉第一条 WS；冷启走 ServiceWire 持久连接。
4. **host-load.tsv 去污染**：worktree 的 `.team/perf/instr-ab2-raw/` 里残留第二轮 07:xx 的 tsv（本格 `coldopen` 往同一文件 append）。log 文件名被覆盖所以样本仍是本批 n=20；tsv 却 120 行混批。已把未过滤副本留在 `tmp/host-load-{A,B}-unfiltered.tsv`，tsv 改成只留 ≥09:32 的本批 60 行后再 `summarize.py`。判据认的 json 是过滤后的 `instr-ab-20260823-104938.json`（`load1=11.98` `load1_mean=9.58`）。过滤前那份 `104816.json` 仍在盘上，`judge-instr-ab.sh` 用 `ls \| tail -1` 会拿到 104938。
5. worktree `.team/perf/` 里还有第二轮抄过来的 `instr-ab-20260823-063415.json` / `085933.json`。本格没删它们。

## load1（同批逐次，见过滤后 host-load.tsv）

json 顶层 `load1` = 本批 host-load.tsv 的 **max**（判据硬核 ≤12）。样本开始时若 load1>12 则等（`load-wait.log` 29 条 WAIT），⛔ 不硬测。tsv 里 **0 条 >12**。

| 组 | n | load1 min | max | mean |
|---|---|---|---|---|
| A | 60 | 6.62 | **11.98** | （见 json host_load.A） |
| B | 60 | 6.52 | 11.74 | （见 json host_load.B） |
| 合计 | 120 | 6.52 | **11.98** | **9.58** |

起跑 09:32:34 load ≈ 9.12 9.72 10.01。收跑 10:48:06。地板负载区间 6.87–10.49；本批 mean 9.58 在区间内。

## 导航与样本量

A/B 交替：三夹具 × 20 × 2 = **120** 次冷点开。

- **120/120 导航 OK**。`FAIL` 0 次。`ABORT` 0 次。
- 每个夹具每组 log 20 个；PerfTrace 均有 `tap` + `first_draw` + `layout_settled`（outliers 空）。
- ⛔ 没有跨批补跑。极端值未剔除。

## 每一批的原始数（batch1，ms，相对 tap；极端值未剔除）

原文 JSON：`.team/perf/instr-ab-20260823-104938.json`（`build_type=release`，`load1=11.98`）。

| 夹具 | 段 | nA | nB | A p50 | A p95 | B p50 | B p95 | B vs A p50 | B vs A p95 |
|---|---|---|---|---|---|---|---|---|---|
| real_claude_idle | first_draw | 20 | 20 | 529.5 | 753.3 | 482.5 | 716.6 | −8.9% | −4.9% |
| real_claude_idle | layout_settled | 20 | 20 | 1089.0 | 1472.0 | 1093.5 | 1532.8 | +0.4% | +4.1% |
| redraw_tui | first_draw | 20 | 20 | 464.0 | 1023.2 | 557.5 | 2683.7 | **+20.2%** | **+162.3%** |
| redraw_tui | layout_settled | 20 | 20 | 1063.0 | 1380.1 | 1091.5 | 2747.2 | +2.7% | **+99.1%** |
| big_scrollback | first_draw | 20 | 20 | 464.0 | 665.7 | 510.5 | 758.0 | **+10.0%** | **+13.9%** |
| big_scrollback | layout_settled | 20 | 20 | 966.5 | 1378.4 | 1097.0 | 1320.0 | **+13.5%** | −4.2% |

`redraw_tui` B 的 p95 被两个极端值拉起来（未剔除）：`redraw_tui-01.log` first_draw=2475ms、`redraw_tui-02.log` first_draw=6648ms。其余 18 个 B first_draw 样本在 333–993ms。

`big_scrollback first_draw p50` 显示 +10.0%，判据用 `pct > 10.0`（464.0→510.5 实际约 +10.02%），所以进 RED。

## 这轮结论能说到什么程度

`sh tools/perfbase/judge-instr-ab.sh` → **FAIL rc=1**
（6 段 B 相对同批 A 超过 +10%）。全文 `.team/nodes/t.instrperf3/judge.log`。量测三闸都过了：release、load1=11.98≤12、每组每夹具 n=20。所以这不是「不可判」，是门红了。

红的六段见上表加粗。同时：

- `real_claude_idle first_draw` **B 更快**（p50 −8.9% / p95 −4.9%）。
- `redraw_tui` 的 p95 爆炸来自两个极端样本，不是整组平移。
- `big_scrollback layout_settled` p50 +13.5% 而 p95 −4.2%，符号不一致。

所以：

- **能**写成：在 release + load1≤12 + n=20 + **keyecho 关** 的达标条件下，判据按 10% 门给出 FAIL。
- **不能**写成「回炉之后打开会话已经不退」。
- **不能**写成「打点让打开会话稳定地全面变慢」（有的段 B 更快；同一夹具 p50/p95 会反向）。
- **不能**写成「一点没退」。
- **不能**写成已经验证「打点关着时的代价」。

产物：`.team/perf/instr-ab-20260823-104938.json`、`.team/perf/instr-ab2-raw/{A,B}/`、`.team/nodes/t.instrperf3/judge.log`、本文。
