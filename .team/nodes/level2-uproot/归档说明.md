# t.uproot 归档说明：二级菜单旧模型 + 状态判定连根拔起（已完成）

> 任务：`.team/ledgers/level2-uproot-v1.json`（revision 2）/ `t.uproot`
> 执行席：`r.dev-state`（2026-08-15）
> 契约：`requirement-base/entries/060-二级菜单改为实时流并取代状态判定.md`
> 拔除清单：`.team/nodes/level2-locate/拔除清单.md` + `verify.sh`

## 做了什么

按拔除清单把「二级菜单旧列表模型 + 状态判定」全部**归档到 `scratch/archive-level2/` 后从主干删除**（归档不是备份，是根因探针输入；失败的 diff 是信息，不直接 rm）。

### 整包/整文件删除（已归档）

| 归档路径 | 原路径 | 内容 |
|---|---|---|
| `server-internal-agentstate/`（6 个 go 文件） | `server/internal/agentstate/` | 整包删（adapters/ansi/doc/identify/sample/track） |
| `server-internal-protocol/state.go` | `server/internal/protocol/state.go` | 状态类型/常量 |
| `server-internal-api/state.go` | `server/internal/api/state.go` | StateProvider 接口 + unknownState |
| `server-internal-api/state_wiring.go`（+测试） | `server/internal/api/state_wiring.go` | wiredStateProvider 全链路 |
| `server-internal-api/aggregate_test.go` | `server/internal/api/aggregate_test.go` | 聚合规则测试 |
| `app-conn/AgentState.kt` | `app/.../conn/AgentState.kt` | 枚举 + 序列化器 |
| `app-workspace/StateBadgeStyle.kt` | `app/.../workspace/StateBadgeStyle.kt` | 徽章语义枚举 |
| `app-service/StateWatcher.kt` | `app/.../service/StateWatcher.kt` | 状态守望 |
| `app-test/StateBadgeTest.kt`、`StateTonesTest.kt`、`ServiceStateWatcherTest.kt` | 对应测试 | 状态专项测试 |

### 改文件（已归档原版 + 改主干）

**Server**：`protocol/frames.go`（删 Workspace.AggregateState / Session.State 字段）、`validate.go`（删 requireState + Workspace/Session 状态校验）、`errors.go`（删 ErrInvalidState）、`api/ws_conn.go`（删 ErrInvalidState 分支）、`api/options.go`（删 StateProvider 字段）、`api/server.go`（删 stateProvider 装配）、`api/listing.go`（重写：删聚合规则，modelSnapshot/diff 保留、去 provider 参数与状态）、`api/doc.go`（清 agentstate 引用）、`cmd/main.go`（删状态装配）、`discovery/model.go`（改注释）、`README.md`（删 agentstate 行）。

**App**：`workspace/StateBadge.kt`（只留 WorkspaceRow，去 aggregateState）、`workspace/WorkspaceViewModel.kt`（重写：保留 ConnectionUi/刷新模型，删二级列表）、`workspace/WorkspaceScreen.kt`（删 SessionList + onOpenSession + aggregateState，保留一级）、`service/NotificationHelper.kt`（只留常驻通知，删状态通知）、`service/MirrorForegroundService.kt`（删 stateWatcher）、`ui/theme/Theme.kt`（删状态色板）、`conn/Frames.kt`（删 aggregate_state/state 字段）、`AgentMirrorApp.kt`（删 onOpenSession）、`MainActivity.kt`（删 handleDeepLink）、`PackageDoc.kt`×2、`session/SessionViewModel.kt`（改注释）。

### 测试清理

**Server**：`diff_test.go`（重写：纯结构 diff，去状态）、`api_test.go`（删聚合断言）、`session_name_test.go`（toSession 单参）、`connected_idle_economy_test.go`（删采样测试，留 discovery 隔离）、`api_tmux_test.go`（删 TestStateNeverGatesMirror）、`protocol/json_test.go` + `golden_test.go`（删 state golden/断言）。

**App**：`ConnCodecTest.kt`（删 state 断言/坏帧测试）、`WorkspaceViewModelTest.kt`（重写：留一级+连接态）、`WorkspaceRefreshTest.kt`/`WorkspaceWiringTest.kt`/`WorkspaceViewModelRefreshTest.kt`（去 state 构造）、`NotificationHelperTest.kt`（只留常驻）、`MainActivityNavTest.kt`（删深链测试，留导航壳）、`ColdStartReconnectTest.kt`/`ForegroundServiceWiringTest.kt`（sessions→sessionCount）。

### 夹具

`server/internal/protocol/testdata/listing.json` + `list_delta.json`（删 state/aggregate_state 键）。

### 保留（060 边界：与三级/一级共用，别误伤）

- WebSocket 传输 / 连接生命周期（Connection/ConnectionManager/ConnectionState/Transport/ReconnectPolicy/BinaryFrame/CodecErrors/Clock/Keys）
- 帧编解码（FrameCodec/FrameEnums/ListingFrame/ListDeltaFrame 结构）
- 一级菜单刷新模型（refresh/refreshing/PullToRefreshBox/listingLoop）
- 三级终端流（SessionRoute/SessionViewModel/SessionScreen/termview/tsnet）
- `sessionRef`/`sessionCatalog`（listing 用）
- 测试：PairingScreenClockPumpTest 等无状态依赖

## 判据结果（leader/驱动器重跑，退出码权威）

| 判据 | 命令 (cwd) | 期望 | 实际 |
|---|---|---|---|
| A-lu-clean | `bash .team/nodes/level2-locate/verify.sh` (.) | 0 | **0**（ALL PASS，13 项攻击 + 8 项防御全过） |
| A-lu-archived | `test -d scratch/archive-level2 && test "$(ls -A ...)"` (.) | 0 | **0**（46 个文件，含 28 个 go） |
| A-lu-app | `./gradlew :app:testDebugUnitTest --rerun-tasks --console=plain` (app) | 0 | **0**（BUILD SUCCESSFUL） |
| A-lu-server | `go test ./... -count=1` (server) | 0 | **0**（全包 ok） |

## 边界遵守

- **归档非备份**：所有删除都留了字节级副本在 `scratch/archive-level2/`（根因探针输入，不是垃圾桶）。
- **未造占位实现**：二级菜单拔空后无内容，是预期的（060「拔完之后二级菜单暂时没有内容是预期的，不要造占位填坑」）。WorkspaceScreen 删了 SessionList，二级导航壳状态（selectedWorkspaceCwd 等）按清单 §七待定，未动。
- **未动 WorkspaceViewModel/Screen 里其他席位的未提交工作**（060 刷新模型）：已保留 refresh/refreshing/PullToRefreshBox 等一级刷新。
- 未 commit / 未 push / 未发 team-agent send。

## 已知待续（下一轮）

- 二级菜单实时流（060 新形态）——本任务只拔不建。
- 服务端 listing/list_delta 是否仍带 state/aggregate_state 的最终确认（本任务已删字段与夹具键，若二级流需要再议）。
