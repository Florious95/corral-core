# t.instr4 说明（任务一 · 两批留痕合批 + 接帧者身份）

席位：施工席 `pb-impl` / 格 `pb-instr4`。worktree：`.worktrees/wt-pb-instr4`。未 commit / 未 push。

只补仪表，⛔ 没改派发语句本身、没修白屏、没改判据、没改红测断言。

## 读到的 emu 事实（说明-r16-第三轮.md）

29/29：`ws_binary_recv kind=snapshot bytes>0`、`listener_null=0`、`no_listener` 零命中；
`first_frame_recv emitted=1` / `snapshot_applied` / 真 first_draw 仍为零。
帧死在 `SessionViewModel.onBinary` 内部。instr2 的 `ref_mismatch`/`no_open` 与 instr3 的
`ws_binary_recv`/`no_listener` 从未同构建。emu 试过打补丁 2/4 hunk 冲突后停手。

## 合了什么

对照 `git diff HEAD...pr/perfbase-instr2` 与 `HEAD...pr/perfbase-instr3` **全量合入**（未 `git commit`，席位禁 commit；未手抄漏条）：

**instr2**
- `SessionViewModel.onBinary`：`frame.ref != ref` → `emitted=0 reason=ref_mismatch frame_ref= want_ref=`
- `emitFirstFrameIfFirst` / `emitSnapshotIfFirst`：`opens[ref]` 落空 → `reason=no_open`

**instr3**
- `Connection.onBinary` 解码后：`ev=ws_binary_recv emitted=0 kind= bytes= frame_ref=`
- `ConnectionManager` 派发前：`listener_null=0|1`，空槽 `reason=no_listener`

**本格新增**
- 派发点并排 `listener_ref=`：`Listener.perfTraceListenerRef()` 默认实现类名；
  `SessionViewModel` 覆盖为自身 `ref`；ServiceWire 包装监听转发 `uiConnector` 的身份
  （`ui=null` 若槽空）。`listener?.onBinary(frame)` 原句未改。

同构建即可分：`ref_mismatch` vs `want_ref`≠`frame_ref` vs `listener_ref` 不是本会话 VM。

## 先验红原始输出

工作目录：`/Volumes/nvme/Projects/远程Agent安卓/.worktrees/wt-pb-instr4`

```
$ bash tools/perfbase/judge-instr4.sh
```

退出码：**1**  日志：`.team/nodes/pb-instr4/tmp/prered-judge.log`

```
FAIL 缺留痕/操作数：reason=ref_mismatch
```

## 转绿

```
$ bash tools/perfbase/judge-instr4.sh
```

stdout：`PASS 两批留痕同时在场且带接帧者身份`  退出码：**0**

全量单测日志：`.team/nodes/pb-instr4/tmp/green.log`

## 没改的

- 没改 `listener?.onBinary(frame)` 派发、没修白屏
- 没改 `tools/perfbase/*.sh`、没改红测方法名/断言/`@Ignore`
- 没开模拟器、没碰 9900、没 commit/push
