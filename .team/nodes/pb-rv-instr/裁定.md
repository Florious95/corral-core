# t.rv.instr 裁定 · 任务一仪表（r8 复审）

status=pass

核的是 `wt-pb-app` 工作区（相对 `9ecab83f7` 的未提交返修）与 `wt-pb-srv` `2426c5b58`，不是产出方 `说明.md` 自报。上一轮 rework 三条均已在代码原文对上，并独立重跑单测。

## 结论（三主线）

- 主线①关时零成本：成立。调用点最外层短路；关路径不 `beginOpen`、不拼 `kind=`、不 `noteReflow`、不发日志。`first_draw` 不另开 O(rows×cols) 遍。`layout_settled` 一次性 `postDelayed`，无关常驻定时器。
- 主线②链路完整性与可算性：成立。守卫失败与成功走同一 `ev=`，用 `emitted=` 与比较两边区分「没发出」和「发出了」；重连重放 `replay=1` 不被 take 吞；`layout_settled` 成功行带最终 `rows=` `cols=`，取消/跳过带 `reason=`。
- 主线③单测：独立重跑全量绿；原三红测仍手调 API（方法名未改），另增 `PerfTraceWiringTest` 走过产品接线。无 `@Ignore`。

取数约定（给 t.base）：只把 `emitted=0` 之外的行当完成事件。`subscribe_sent`/`layout_settled`/`first_draw` 现在会先出现守卫行。

## 证据 · 上一轮三条对照

### 1. 守卫不再沉默；重连重放可见

`ConnectionManager.kt:341-358` 各出口都进 `traceSubscribe`：

```
if (state == ConnectionState.STOPPED) {
    traceSubscribe(..., sent = false, ..., reason = "stopped")
    return false
}
...
if (!conn.isReady) {
    traceSubscribe(..., sent = false, ..., reason = "not_ready")
    return true
}
val ok = conn.send(...)
traceSubscribe(..., sent = ok, ..., reason = if (ok) "sent" else "send_failed")
```

`ConnectionManager.kt:552-553` 关时最外层 return。`PerfTrace.kt:486-500` 失败打 `emitted=0 reason= ready= conn= ok=0 take_before= replay= rows= cols=`，成功打 `emitted=1` 且 `st.subscribeSent = true`（不靠 take 丢重放）。`ConnectionManager.kt:659-670` 重放 `replay = reconnect`，`sent=true` 必打。

### 2. layout_settled 终态 / 取消原因 / 最终行列

成功：`PerfTrace.kt:450-456` `layoutSettled(..., st.lastRows, st.lastCols)`，行内 `quiet_ms=` `last_reflow_src=` `rows=` `cols=`（`PerfTrace.kt:358-362`）。

跳过：`PerfTrace.kt:410-440` `reason=no_open|already_settled|no_handler`。取消：`PerfTrace.kt:253-268` 尚未 settled 则 `emitted=0 reason=unbind settle_pending=` 再 `removeCallbacks`。Handler 懒取（`PerfTrace.kt:599-610`），无 looper 打 `no_handler`。

空屏：`PerfTrace.kt:551-558` 每打开一次 `first_draw emitted=0 reason=glyphs_zero glyphs=0`。`TermSurfaceView.kt:440-445` 关时只读 `isEnabled()`；开时把既有扫格得到的 `cellsWithGlyph` 传入，不另遍网格。

### 3. 接线测（不改原三红测）

`PerfTraceChainTest.kt:60` / `88` / `167` 方法名仍是契约三字。`PerfTraceWiringTest.kt:75-168`：`MainNavState.openSession` → `beginRoute` → `SessionViewModel` 构造里 `manager.subscribe`（`SessionViewModel.kt:193`）未就绪行 → auth_ack 触发 replay → `onBinary` 快照 → `TermSurfaceView.draw` → `ShadowLooper.idleFor(500ms)`；再 `peerClose` 后重放断言 `replay=1 take_before=1`。`PerfTraceWiringTest.kt:172-202` 关开关走同一批调用点，`captured.isEmpty()`。

收藏/列表/悬浮窗仍进 `openSession`：`ThreePane.kt:148` `ThreePane.kt:161` `AgentMirrorApp.kt:164`。

## 证据 · 关时零成本（去证有成本，未证出实质成本）

`MainNavState.kt:70-73`：`if (PerfTrace.isEnabled()) PerfTrace.onUserOpen(ref)`。`SessionRoute.kt:90-96` remember 块内先判关。`ConnectionManager.kt:532-533` resize；`552` traceSubscribe。`SessionViewModel.kt:254` 先 `isEnabled()` 再算 `kind=`。`TermSurfaceView.kt:441-445` 关则不 `emitFirstDraw`。`unbind`（`PerfTrace.kt:254`）关时不 emit。

`cellsWithGlyph` 是画格循环里的一次比较+自增（`TermSurfaceView.kt:547-550`），不是第二遍 O(rows×cols)。`noteReflow` 仍是一次性 delayed message（`PerfTrace.kt:442-459`）。`DiagLog.record` 只在开路径双出口；`record` 写内存环（`DiagLog.kt:196-217`），打开链无新增同步文件 IO。

## 证据 · 独立重跑

```
$ cd /Volumes/nvme/Projects/远程Agent安卓/.worktrees/wt-pb-app/app
$ ./gradlew :app:testDebugUnitTest :terminal:test --offline --rerun-tasks
BUILD SUCCESSFUL in 37s
39 actionable tasks: 39 executed
GRADLE_RERUN_RC=0
```

日志：`.team/nodes/pb-rv-instr/tmp/gradle-r8-rerun.log`

`TEST-dev.agentmirror.app.perf.PerfTraceChainTest.xml` tests=3 failures=0 errors=0 skipped=0。
`TEST-dev.agentmirror.app.perf.PerfTraceWiringTest.xml` tests=4 failures=0 errors=0 skipped=0
（`产品链路_未就绪重放与重连可见且八事件同open_id` / `产品调用点_关闭时不beginOpen且零行` / `onDraw无字_打first_draw_emitted0_glyphs0` / `unbind取消layout_settled必须打reason`）。

```
$ cd /Volumes/nvme/Projects/远程Agent安卓/.worktrees/wt-pb-srv/server
$ go build ./... && go test ./... -count=1
GO_RC=0
ok  	github.com/agentmirror/agentmirror/cmd/agentmirrord	4.672s
ok  	github.com/agentmirror/agentmirror/internal/api	44.624s
ok  	github.com/agentmirror/agentmirror/internal/bridge	20.810s
ok  	github.com/agentmirror/agentmirror/internal/config	2.358s
ok  	github.com/agentmirror/agentmirror/internal/discovery	4.449s
ok  	github.com/agentmirror/agentmirror/internal/overlay	4.008s
ok  	github.com/agentmirror/agentmirror/internal/pairing	1.901s
ok  	github.com/agentmirror/agentmirror/internal/protocol	2.777s
ok  	github.com/agentmirror/agentmirror/internal/provider	1.473s
ok  	github.com/agentmirror/agentmirror/internal/tsnetd	3.315s
```

日志：`.team/nodes/pb-rv-instr/tmp/go-r8.log`

服务端相对上一轮未改；`ws_conn.go:152-154` recv 只打 int64，`ws_conn.go:422-426` 非 Info 不格式化。
