# t.gofix · P0 · 生产码调用 `_test.go` 里的 `scrubbedEnv`

## 开工自检

```
$ pwd
/Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr3.gofix

$ git branch --show-current
pr/gofix-scrubbedenv
```

HEAD `a58e80f87`（detached）上建分支。仓根仍是 `main`。本格只改本 worktree。

## 先验红（改产品码之前）

```
$ pwd
/Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr3.gofix/server

$ git branch --show-current
pr/gofix-scrubbedenv

$ go build ./...
# github.com/agentmirror/agentmirror/internal/api
internal/api/lifecycle.go:252:12: undefined: scrubbedEnv
```

exit=1。与账本现象一致：`lifecycle.go:252` 生产码调用 `scrubbedEnv()`，定义只在 `tmux_test.go`。

复核：`server/internal/api/tmux_test.go` 有 `func scrubbedEnv()`；`server/internal/bridge/bridge_test.go` 另有同名函数（别的包），未动。

## 改了哪些文件

- `server/internal/api/lifecycle.go` — 把 `scrubbedEnv` 原样挪到 `runTmuxOnSocket` 后面（同包生产文件；`os`/`strings` 本文件已 import）
- `server/internal/api/tmux_test.go` — 删掉重复定义（同包留两份会编译失败）

diff：+13 / -13。未碰 `bridge_test.go`。

## 验绿

### A-gofix-build

`go build ./... && go vet ./...` 均 exit 0。

### A-gofix-gotest

`go test ./... -count=1` exit 0。10 个包 `ok`。

`-json` 计：PASS tests=382 FAIL=0 SKIP=0（≥ 353 棘轮，未删测试）。

```
ok  github.com/agentmirror/agentmirror/cmd/agentmirrord  0.605s
ok  github.com/agentmirror/agentmirror/internal/api      45.422s
ok  github.com/agentmirror/agentmirror/internal/bridge   21.445s
ok  github.com/agentmirror/agentmirror/internal/config   1.621s
ok  github.com/agentmirror/agentmirror/internal/discovery 2.478s
ok  github.com/agentmirror/agentmirror/internal/overlay  3.289s
ok  github.com/agentmirror/agentmirror/internal/pairing  4.326s
ok  github.com/agentmirror/agentmirror/internal/protocol 3.473s
ok  github.com/agentmirror/agentmirror/internal/provider 3.891s
ok  github.com/agentmirror/agentmirror/internal/tsnetd   4.917s
```

### A-gofix-app

`cd app && env -u TEAM_AGENT_API_KEY ./gradlew -q :app:testDebugUnitTest` exit 0。

### A-gofix-gofmt（rev2 增量比对）

账本 revision 2 把「整树 gofmt 为空」改成「相对 main 不许新增」。未动那 5 个存量。

main 存量与本分支 `gofmt -l .` 相同：

```
internal/api/api_tmux_test.go
internal/api/delta_merge_scenario_test.go
internal/api/lifecycle_test.go
internal/bridge/bridge.go
internal/overlay/tmux.go
```

`comm -13` 新增为空。本格两个文件 `gofmt -l` 为空。

### A-gofix-doc / A-gofix-seal

本文件。seal 不在本席跑（不 commit / 不 push）。

## 封版后请勿再改这棵 worktree
