# 065 App：悬浮窗走三级模拟器（t.app / r2）

仓根。复用 `TerminalEmulator.feed` + `snapshot()`，不另写渲染。

## 缺陷 2 / 3

- 打开悬浮窗后，抓屏 `text` 喂进独立的 `overlayEmulator`（与本会话 `emulator` 分开，避免污染三级画面）。
- UI 画模拟器抽出的可见文本：CSI / 备用屏 / 清屏被解释，不再出现 `[?1049h` `[K`。
- 连续整帧因 `?1049h` + `[H[2J` 在模拟器里替换，行数有界。

## 订阅带 socket

- `OverlaySubscribeFrame.socket` = 当前会话 `ref` 里 `\x1f` 前的结构字段。
- `ConnectionManager` 簿记该 socket，READY 重放。

## 测试

- `OverlayRendersThroughTerminalEmulatorTest`
- `OverlayScreenReplacesNotAppendsTest`
- `OverlaySubscribeCarriesCurrentSocketTest`
- 064：`OverlayDismissOnOutsideTapTest` / `OverlayUnsubscribesOnCloseTest`
