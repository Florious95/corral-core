# t.sortlabel · E9/E16 · 排序 + 「运行」等宽

## 开工自检

```
$ pwd
/Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr2.listui

$ git branch --show-current
pr/sort-label
```

仓根在 `main`。本格只在 `pr/sort-label`。开工时该分支停在 e8 sha，已 `merge --ff-only pr/icons-longpress`（不另建分支）以免丢掉 E7/E10。未做 E12/E13/E14。

## 改了哪些文件

方案 §9。账本 write_paths 只有 `workspace/`，文案/芯片在 `ui/`，overlay 在 `session/`。照方案做并报出：

- `workspace/SortSessions.kt` — `sortSessions`（收藏↓, Busy↓, 名称↑）
- `workspace/L2SessionList.kt` `FavoriteList.kt` `WorkspaceScreen.kt` — 列表走 sort
- `session/SessionScreen.kt` — 「查看」sheet 走 sort
- `workspace/L2Models.kt` WORKING.label `进行中`→`运行`
- `ui/components/CommonUi.kt` statusVisuals Busy 文案 + StatusChip 文字槽等宽
- `ui/theme/DesignTokens.kt` `statusChipLabelMinWidth`
- 断言 `"进行中"` 的现有测改为 `"运行"`（方案写明是需求变更）
- `TabScrollTest` 末行由字典序最后的 `sess-9` 断言（sort 后不再是 sess-15）

## 修法

纯函数 `sortSessions`。StatusChip 用 TextMeasurer 对「运行」「空闲」取 max，写入 `Dims.statusChipLabelMinWidth`，文字 `Modifier.width(that)`。

## 先验红（改产品码之前）

```
$ ./gradlew :app:testDebugUnitTest --tests 'dev.agentmirror.app.workspace.SortSessionsAndLabelTest'

> Task :app:compileDebugUnitTestKotlin FAILED
e: SortSessionsAndLabelTest.kt:62:19 Unresolved reference 'sortSessions'.
BUILD FAILED
```

## 验绿

### SortSessionsAndLabelTest

四档夹具顺序正确；`chip-busy`/`chip-idle` 宽度差 < 0.5dp；`L2Status.WORKING.label == "运行"`。BUILD SUCCESSFUL。

### A-sortlabel-wiki

```
pkg=dev.agentmirror.app.workspace 基线=8 本次=8 新增=0 消掉=0
```

`SortSessions.kt` 初缺 `@inv` 曾新增 T3-3，补上后绿。未 `--freeze`。

### A-sortlabel-smell

```
face=app 基线=16 本次=16 新增=0 消掉=0
exit 0
```

未 `--freeze`。

### A-sortlabel-doc / seal

本文件。seal 不在本席跑。

## 查不清

- 全量 suite 与其它格抢写 `test-results` XML，本回合未稳定拿到全量 exit 0。
- 无模拟器截图。
