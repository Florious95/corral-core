# 064 打包（t.pkg / r10）

只打包，未改任何实现代码。未起模拟器，未碰用户 tmux / 生产 daemon。

## APK

- 路径：`/Volumes/nvme/Projects/远程Agent安卓/app/app/build/outputs/apk/debug/app-debug.apk`
- 命令：仓根 `app/` 下 `./gradlew :app:assembleDebug --rerun-tasks`（exit 0）
- size：39377065
- sha256：`6d0472b9106d6ab65225c7dbbba7a01c6c7d72f9e5bbb7172ad9a4d18ca7cbed`
- mtime：2026-08-19 00:43:19

## 不是旧产物

对 `app/app/src/**/*.kt` 与 APK mtime 比较：`newer_kt=[]`。

## 收口（不是 dex 符号）

- `bash .team/nodes/ov-design/probe-overlay.sh` → exit 0  
  隔离 daemon 订阅后两帧非空且内容不同。
- `bash .team/nodes/ov-design/size-invariance.sh` → exit 0  
  隔离 tmux 上 user/scratch 窗口 `80x24` 开/关抓屏路径前后一致。
