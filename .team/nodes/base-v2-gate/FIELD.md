# 现场基 · base-v2-gate（leader 手填取证素材）

## 用户裁定原文（2026-08-12，v5 实测后）

用户实测 `~/Desktop/agentmirror-v5-20260812-1530.apk` 后判定 v5 不合格，原话要点：

1. **输入框闪烁**：点开输入框重绘、发消息增加一行时界面疯狂闪烁。**v2/v4 无此问题。**
2. D-23 侧滑返回未修：侧滑仍直接退出 App
3. D-32 返回跳级未修：会话页返回直接跳到工作区列表
4. D-35 bypass 符号问题**从未修复**（上届错误标记为「已修」）
5. D-36 向上滑看历史**从未修复**（上届错误标记为「已修」）

用户对版本的裁定：**HEAD 就是 v2**；v5 代码不删（留给审查席作参考）。

## 版本事实（git 实测，非转述）

- `main` = `7c56353` = **v2 基线**，tag `v2-baseline`
- v5 全部改动（34 文件 / 1604 行）封存于分支 `v5-failed` = `2874c54`，**一行未删**
- **git 里没有 v3/v4 提交**：三个 APK 全部从未提交的工作区直接打包，无 commit / 无 tag / 无 stash。
  因此 v4 源码在版本控制里不可还原，能退到的唯一版本就是 v2。
- 这正是本任务存在的理由：**v2 是否真的干净，从未被验证过，只是被假定。**

## 本任务的证伪对象

前提：「v2 基线不闪」。后续所有缺陷修复都建立在这个前提上。
**若 v2 也闪 → 整个回退决策的地基是错的 → 立刻上报并停下。**
这条比任何其他产出都优先。

## 环境事实（已由 leader 核活）

- 模拟器 `emulator-5554` 在线
- ADB `/Users/alauda/Library/Android/sdk/platform-tools/adb`
- daemon pid 39489 监听 `:9900` 在跑；模拟器访问宿主机走 `10.0.2.2`
- 配对 token 在 `/Users/alauda/Library/Application Support/agentmirror/token`
  —— **值禁止进日志 / 代码 / 截图 / report_result 正文**

## 回归基准要取哪些（依据架构基的波及面）

架构基现算出 `termview` 的反向依赖是 `dev.agentmirror.app.session`——
即改终端渲染必然波及会话页，而**输入框闪烁正是发生在这条边上**。
回归基准必须覆盖这条边的两端：

- R1 输入框点开后终端区渲染（termview↔session 边，闪烁高发区）
- R2 IME 弹起时最后一行终端内容是否可见（D-20 已修，验证仍好）
- R3 CJK + Powerline 符号渲染（D-34 已修）
- R4 键条连按响应（D-37 已修）
- R5 捏合缩放后画面（v2 上 D-28/D-29 未修，**此项是记录现状不是验收**）

## 顺带复现（只复现，不修）

- **D-35**：口径存在冲突，taskbook 写「bypass 符号缺省」，defects 文档写「bypass 符号透明红框」。
  **不要替用户定夺是哪一种**，把实际渲染出来的样子截图，让证据说话。
- **D-36**：会话页向上滑动能否看到历史消息（预期看不到）。

复现不到就如实写复现不到。**禁止编造，禁止用推理代替眼见。**
