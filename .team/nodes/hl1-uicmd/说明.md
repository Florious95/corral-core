# t.uicmd · 设置页 Provider 命令编辑界面重做

status=done

## 开工自检

```
pwd
/Volumes/nvme/Projects/远程Agent安卓/.worktrees/hl1.uicmd

git branch --show-current
pr/ui-provider-cmd
```

HEAD（硬约束禁止本席 `git commit`，改动在工作区未提交）: `a946101a01e10cfe95bc569ad34c2da05aef05a3`

未提交文件：
- `app/app/src/main/java/dev/agentmirror/app/SettingsScreen.kt`
- `app/app/src/test/kotlin/dev/agentmirror/app/AgentLaunchCardUiTest.kt`

## 先验红（改代码之前）

### A-uicmd-doc（红：产物当时不存在）

原始输出：

```
=== A-uicmd-doc prior 2026-08-21T20:42:05+0800 ===
candidates=['/Volumes/nvme/Projects/远程Agent安卓/.team/nodes/hl1-uicmd/说明.md', '/Volumes/nvme/Projects/远程Agent安卓/.worktrees/hl1.uicmd/.team/nodes/hl1-uicmd/说明.md'] hit=[]
产物两处都不存在
```

随后单独再跑同一段 python，`sys.exit(1)`，`A-uicmd-doc real_exit=1`。先验红命中点是本产物文件当时不存在。

### A-uicmd-wiki（改前绿，exit 0）

```
pkg=dev.agentmirror.app 基线=1 本次=1 新增=0 消掉=0（键不含行号）
A-uicmd-wiki exit=0
```

### A-uicmd-smell（改前绿，exit 0）

```
face=app 基线=16 本次=16 新增=0 消掉=0（键不含行号）
A-uicmd-smell exit=0
```

### A-uicmd-suite（改前绿，exit 0）

```
=== A-uicmd-suite prior 2026-08-21T20:42:05+0800 ===
EXIT:0
```

先验红命中点是 **A-uicmd-doc**。suite/wiki/smell 在改 UI 前已绿，本格没有靠改判据变绿。

## 截图前=

`.team/nodes/hl1-uicmd/shot-before.png`

（模拟器 emulator-5554，`pm clear` 后点「以后再说」进工作区，滑到设置并滚到「Agent 启动命令」。六家只剩标题+两行裸文字，无图标、无输入框、无「恢复默认」。未点开任何会话。）

## 截图后=

`.team/nodes/hl1-uicmd/shot-after.png`

（同一模拟器，`install -r` 本分支 debug 包后再走设置。每家 Provider 内卡：卡通图标、等宽命令框、Bypass 旗、恢复默认。与「外观」「终端主题」同款卡片。未点开任何会话。）

## 改点=

- 重做设置页 `AgentLaunchCard`：六个 Provider 各一张内卡，`ProviderIcon` + 「恢复默认」+ 等宽多行「命令」/「Bypass 旗」输入（输入框走 `inputBackground`/`inputBorder`）。
- 点「恢复默认」写回 `ProviderLaunchDefaults.byId`；改命令仍立刻 `launchStore.save`；`buildArgv` 未改（088 §7 分词原样下发，不经 shell）。
- 单测 `AgentLaunchCardUiTest`：六家图标 contentDescription + 六个「恢复默认」；改 grok 为 `grok-local --model x` 后 `buildArgv(..., bypass=true)` == `grok-local --model x --always-approve`；恢复默认回到出厂；命令框接受换行。

## 隔离环境自检

```
TMUX_TMPDIR=.team/nodes/hl1-uicmd/tmp/iso/t tmux -f /dev/null list-sessions
uicmd-iso: 1 windows
socket: .team/nodes/hl1-uicmd/tmp/iso/t/tmux-501/default
假 CLI: ln -s /bin/bash …/iso/bin/claude
```

本格截图只走设置页（跳过配对），未点真实舰队会话。收工时对该 TMPD `tmux kill-server`（未碰默认 socket，未起/杀模拟器）。

## 改后判据

- A-uicmd-suite：`./gradlew :app:testDebugUnitTest` EXIT:0（含 AgentLaunchCardUiTest / TabScrollTest / ProviderLaunchArgvTest）
- A-uicmd-wiki：基线=1 本次=1 新增=0 EXIT:0
- A-uicmd-smell：基线=16 本次=16 新增=0 EXIT:0
- A-uicmd-doc：本文件含 `status=done` / `截图前=` / `截图后=` / `改点=` EXIT:0
