# t.settings · 终端主题入口

仓根施工（知识基底：与派单 worktree 段冲突时以仓根为准）。未改 `TermSchemes.kt` / `TermPalette.kt`。

## 先验红（改前）

- `test -s .team/nodes/theme-settings/说明.md` → `doc_exit=1`（文件不存在）
- `grep -q 440/480 .../theme-settings/ui-check.sh` → `No such file or directory`，`density_exit=2`
- `bash .../theme-settings/ui-check.sh` → `No such file or directory`，`ui_exit=127`
- 设置页源码只有四张卡，「外观」卡文案仍是「终端正文始终保持深色…」
- 模拟器当时停在会话页：`python3 tools/uiassert.py has 终端主题 浅色时 深色时` → `缺失: ['终端主题', '浅色时', '深色时']`（且未装本格 APK）

## 落地

- `SettingsScreen` 外观卡下面加第五张「终端主题」：浅色时 / 深色时两行，`testTag` `term-theme-light-row` / `term-theme-dark-row`，展示名 `term-theme-family-<id>`。
- 过期文案删掉，改为说明外壳走「外观」、终端走浅/深槽。
- 全屏选择页：`LazyColumn` + 搜索（内存 contains）+ 分组「成对深浅 / 仅深色」；预览按**当前槽的半**画。点选立刻 `SharedPreferencesTermThemeStore.save*` 并返回。
- 接线：`app/.../SettingsScreen.kt` 持有 `app_term_theme`；缺键默认 vesper / vesper（store 已有，本格只接 UI）。
- 选择页说明是本席写的中文短句，不是 Heeler `detail`。

## 判据映射

账本 §6.5 口语「浅槽 alabaster / 深槽 afterglow」不是目录族 id。目录里 Afterglow 深半挂在族 `follow-system`。脚本写：

- A：`terminal-theme-light=follow-system`，`terminal-theme-dark=vesper` → 纸色 `(16,16,16)`，token 含 `Vesper.itermcolors`
- B：深槽改 `follow-system` → 纸色 `(33,33,33)`，token 含 `Afterglow.itermcolors`

未发明新族、未改 catalog。

## 改后绿

`bash .team/nodes/theme-settings/ui-check.sh`（已装 debug APK）：

```
DARK_ROW_BEFORE ['Vesper']
PICKER_HAS Dracula Nord
DARK_ROW_AFTER ['Dracula']
density=480 A=(16, 16, 16) distA_vesper=0 token_before=term-theme-dark source=Vesper.itermcolors
density=480 B=(33, 33, 33) distB_afterglow=0 distAB=51 token_after=term-theme-dark source=Afterglow.itermcolors
density=440 A=(16, 16, 16) distA_vesper=0
density=440 B=(33, 33, 33) distB_afterglow=0 distAB=51
PASS ui-check.sh
```

垫点与 bounds 打在日志里。`A-set-density` / `A-set-nomd5` 脚本自检通过。`:app:testDebugUnitTest` 通过。`land-set/ui-check.sh` 仍绿（主机配对 / 字体大小 / 诊断日志 / 外观仍在，新增卡未把「跟随系统」滚丢）。

`import_iterm_schemes.py --check` 本格 skip（生成物属 catalog 席；无本格写范围）。
