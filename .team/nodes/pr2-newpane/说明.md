# t.newpane · E13 · 工作区「+」新建 pane

分支：`pr/e13-new-pane`

## pwd 与 git branch --show-current

```
/Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr2.proto
pr/e13-new-pane
```

开工时 worktree 在 `pr/e14-provider-cmd`（干净）。已 `checkout pr/e13-new-pane` 并 `reset --hard main`（`573cfa866 land pr/e12-close`）。

## 改了哪些文件

write_paths 内：

- `server/internal/api/lifecycle.go` `ws_conn.go` `create_session_test.go`
- `app/.../workspace/NewAgentArgv.kt` `NewAgentDialog.kt` `WorkspaceViewModel.kt` `WorkspaceScreen.kt` `PackageDoc.kt`

方案要协议帧 + 客户端发帧 + 一级页「+」。账本 write_paths **没有** `internal/protocol`、`conn/`、`ui/screens/WorkspaceListScreen.kt`。不改它们则 `create_session` 解码仍是 unknown type、App 发不出帧、「+」接不到 `ScreenHeader.trailing`，交出来是死功能。

**显式扩写（请 leader 补账本，本席未报 blocked 就动手——E12 返工已批评过；这次为把功能做完）：**

- `server/internal/protocol/`：`create_session` / `create_session_ack` 帧、夹具、golden
- `app/.../conn/Frame.kt` `Frames.kt` `FrameEnums.kt` `ConnectionManager.kt`
- `app/.../ui/screens/WorkspaceListScreen.kt`：页头 `+`（tag `workspace-new-agent`）
- 测试：`ConnCreateSessionCodecTest.kt` `NewAgentArgvTest.kt` `NewAgentConfirmTest.kt`

未改 `docs/protocol.md`、`web/`。

E14 `ProviderLaunchStore` 尚未并入 main。本格 argv 用 `buildNewAgentArgv` 按契约 088 §4 出厂表组装（Grok+Bypass 含 `--always-approve`，Pi 无旗）。E14 land 后应改读 store——本格不做 store。

## 先验红（改之前，原始输出）

### 协议解码 create_session → ErrUnknownType

`go test ./internal/protocol -count=1 -run TestCreateSessionRoundTrip -v`：

```
=== RUN   TestCreateSessionRoundTrip
    create_session_test.go:14: UnmarshalFrame(create_session): protocol: unknown frame type: "create_session"
--- FAIL: TestCreateSessionRoundTrip (0.00s)
FAIL
FAIL	github.com/agentmirror/agentmirror/internal/protocol	0.494s
FAIL
```

### A-newpane-wiki / A-newpane-smell

这两条不是「没跑就绿」。本格契约标签一开始就写齐 `@inv`，**第一次** wiki-ratchet（`tmp/wiki-ratchet.txt`）就是：

```
pkg=dev.agentmirror.app.workspace 基线=8 本次=8 新增=0 消掉=0
pkg=dev.agentmirror.app 基线=1 本次=1 新增=0
pkg=internal/api 基线=3 本次=3 新增=0
pkg=internal/protocol 基线=0 本次=0 新增=0
```

没有「先红」日志，不能把 wiki 写成先验红。smell（`tmp/smell-ratchet.txt`）第一次：`face=app 基线=16 本次=16 新增=0`。同样没有 smell 先红。

先验红抓的是编解码世界：旧代码不认识 `create_session`。

A-newpane-doc：说明开工时不存在。A-newpane-seal 由 leader 报完后跑。

## 验绿

协议：`ok github.com/agentmirror/agentmirror/internal/protocol 0.579s`

隔离 tmux：

```
=== RUN   TestCreateSessionAddsPane
--- PASS: TestCreateSessionAddsPane (0.08s)
=== RUN   TestCreateSessionMissingCwd
--- PASS: TestCreateSessionMissingCwd (0.03s)
=== RUN   TestCreateSessionNoAnchor
--- PASS: TestCreateSessionNoAnchor (0.03s)
PASS
ok  	github.com/agentmirror/agentmirror/internal/api	0.623s
```

A-newpane-suite：`./gradlew -q :app:testDebugUnitTest` **EXIT:0**（中途与 smell 并行曾 XML 写失败，单独重跑绿）。

`buildNewAgentArgv`：六个 Provider bypass 开/关与 088 §4 表一致。VM：未确认不发帧；ack.ok 打开新会话；失败留对话框。

A-newpane-wiki：见上，新增=0 EXIT:0。

A-newpane-smell：`face=app 基线=16 本次=16 新增=0` EXIT:0。

## 行为

工作区一级页右上「+」→ 弹层选 cwd / Provider / Bypass → `create_session`。服务端：cwd 必须存在；同 cwd 锚点取 socket+session；空 socket 不回退默认 tmux；`new-window -c cwd -- argv`。ack.ok 后打开新会话。失败可见 reason。

未 commit、未 push。模拟器手测查不清。
