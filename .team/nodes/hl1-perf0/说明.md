# t.perf0 · 打开速度 P0：量具+扫描帧出 readLoop

status=done
先验红输出=subscribe queue_ms=800 want < 50（TestSubscribeQueueMsBehindSlowDiscover，Level2Subscribe 仍占 readLoop）
转绿输出=subscribe queue_ms=0 want < 50
各判据输出=queue_ms 800→0；DiscoverWithDirs 20 socket(15 stale) 0.24s < 1s；listing tick full-table ps forks≤1；并发 List 回复 seq 不倒退
风险自查=go handleList/Level2Subscribe 回复可能乱序，Listing.seq 按连接钳制不倒退（测试锁住）；AttachPreview/Scrollback 未 go（非全量 Discover，ensureInitialScan 命中后 no-op）；日志只打 type/时间戳/socket/ms/panes/ps_ms，无 pane 内容无凭据

## 开工

PWD=/Volumes/nvme/Projects/远程Agent安卓/.worktrees/hl1.perf0
BRANCH=pr/open-latency-p0（基于 main 3e540f8a2）
merge pr/listing-ps-storm=Already up to date（storm 已是 main 祖先，`git merge-base --is-ancestor` 成立）
未 commit / 未 push / 未碰 main

## 方案落点（P0-a + P0-b，未另想）

- `ws_conn.go`：Read 与 handleFrame 重叠，recv 钉在 Read 返回；handleFrame 打 `frame type/phase/recv_ms/start_ms/done_ms/queue_ms`。重叠是 queue_ms 可测的前提（串行 Read+handle 则 queue_ms 恒≈0）。
- `Level2Subscribe` → `go c.handleLevel2Subscribe`（List 已在 095 出 loop）。
- `scan.go`：errgroup 上限 8，单 socket 超时 5s→500ms，每 socket `socket/ms/panes`。
- `proctree.go`：`readProcTable` 移出 `f.mu`，`ps_ms=` 一条。
- 并发 List：`sendListing` 按连接钳制 seq 不倒退。

测试定义相对方案原文的显式差：方案写「背靠背 list+subscribe 先验红 queue_ms>700」。main 已合入 listing-ps-storm，`handleList` 已 `go`，list+subscribe **今天不会红**。先验红改用仍占 handleFrame 的扫描帧 **Level2Subscribe** + subscribe（与 P0-b「补 Level2Subscribe → go」同一缺陷）。测试仍先发 List，再发 Level2Subscribe 与 Subscribe。

AttachPreview/Scrollback 评估：不在进会话全量 Discover 路径上，未移出 readLoop。

## 先验红输出=

未 `go` Level2Subscribe、量具已就位时：

```
--- FAIL: TestSubscribeQueueMsBehindSlowDiscover (0.83s)
    ws_frame_latency_test.go:74: subscribe queue_ms=800 want < 50 (scan frame still on readLoop)
FAIL
FAIL	github.com/agentmirror/agentmirror/internal/api	1.500s
FAIL
```

原文：`.team/nodes/hl1-perf0/tmp/prior-red.log`

操作数：Discover 注入睡 800ms；subscribe `queue_ms=800`（>700 缺陷在）。

## 转绿输出=

`go c.handleLevel2Subscribe` 之后同测试：

```
=== RUN   TestSubscribeQueueMsBehindSlowDiscover
    ws_frame_latency_test.go:76: subscribe queue_ms=0 want < 50
--- PASS: TestSubscribeQueueMsBehindSlowDiscover (0.02s)
=== RUN   TestConcurrentListRepliesDoNotRegressSeq
--- PASS: TestConcurrentListRepliesDoNotRegressSeq (0.08s)
PASS
ok  	github.com/agentmirror/agentmirror/internal/api	0.587s
```

原文：`.team/nodes/hl1-perf0/tmp/prior-green-v.log`

## 各判据输出=

| 判据 | 操作数 | 结论 |
|---|---|---|
| subscribe queue_ms | 先验 800 → 修后 0，门 <50 | 绿 |
| DiscoverWithDirs 20 socket / 15 stale | elapsed=0.24s 门 <1s；live panes=5 | 绿 |
| listing tick 全表 ps | TestListingTickFullTablePsForkAtMostOnce forks≤1 | 绿 |
| 并发 List seq | TestConcurrentListRepliesDoNotRegressSeq 到线序 seq 不降 | 绿 |
| A-perf0-go | `go build ./... && go vet ./... && go test ./...` exit=0（api 46.554s） | 绿 |
| -race（本格测试） | queue_ms / seq / listing-tick / handleList 不堵 loop | 绿 |

Discover 20 socket 原文：`.team/nodes/hl1-perf0/tmp/discover-20.log`

## 风险自查=

- 同连接并发 `go handleList`：回复乱序于其它帧；协议 Listing 带 seq/req_id。`lastListingSeq` 钳制不发倒退 seq，测试锁住。
- Read 与 handle 重叠：teardown 取消 `c.ctx` 时不得拿它做 Read（会掐掉未写出的 auth_ack）。Read 改 `context.Background()`，由 writeLoop Close 解锁。
- 日志字段：type / phase / recv_ms / start_ms / done_ms / queue_ms / socket / ms / panes / ps_ms。无 token、无 pane 正文。
- AttachPreview / Scrollback 保持同步（评估：非进会话扫描帧）。
- 未起模拟器、未碰用户真实 tmux、未写 `/tmp` 席位文件（扫描单测沿用既有 isolated `testSocketRoot`）。
