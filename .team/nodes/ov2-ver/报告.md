# overlay-fix 对照报告 — ledger.overlay-fix.v1 / t.ver / r4

对照席不实现、不修。仓根跑；未碰用户 default tmux，未起模拟器。

## ① probe-ov2.sh — 未全绿（exit 1）

命令：`bash .team/nodes/ov2-probe/probe-ov2.sh`  
隔离：`/tmp/ov2-advisor/tmux/sock-a` / `sock-b`（`list-sessions` 自检 `alpha-ov2` / `beta-ov2`）。

| 断言 | 结果 |
| --- | --- |
| ① 无裸控制序列 | **红** `ESC[ [?1049 [K (B[m [30m/[43m` |
| ② socket 正确且无自我映照 | **绿** 目标会话在、scratch 不在 |
| ③ 替换而非追加 | **红** 行数=251（>24）treeMarks=34 |

探针仍按「把 `text` 当普通字符串画」验（写探针时的 App 口径）。t.app 已改走 `TerminalEmulator.feed`；线上 `overlay_frame.text` 仍是 PTY 字节，所以 ①③ 在探针里继续红。本席不改探针、不把模拟器搬进服务端。未静默改任务定义：账本要整探针绿，世界现状是 ② 绿、①③ 红。

## ② size-invariance.sh — 仍绿（exit 0）

隔离 `/tmp/ov-sizeinv/sock`。开/关抓屏路径前后：

| 窗 | BEFORE | AFTER |
| --- | --- | --- |
| scratch:0:sc0 | 80×24 | 80×24 |
| user:0:u0 | 80×24 | 80×24 |
| user:1:u1 | 80×24 | 80×24 |

064 尺寸硬约束没被这轮改动破坏。

## ③ 定点变异

脚本：`.team/nodes/ov2-ver/ov2-mutate.sh`

破坏：`server/internal/overlay/tmux.go` 去掉 `choose-tree -f` scratch 过滤，且 `stripObserver` 原样返回。

具名测试：`TestOverlayExcludesScratchSession`

```
--- FAIL: TestOverlayExcludesScratchSession
    overlay_test.go:288: frame contains observer token "am-overlay" (self-reflection)
```

恢复后回绿。`tmux.go` sha256  
`9c28d0d1a9b1b90f840cacec599538b77960177dc080623a2335b24797446655`

本轮 1 次：基线绿 / 变异红 / 恢复绿 / 脚本 exit 0。

自检：坏实现下该测试不会仍返回 0。

## 工作区

只写仓根 `.team/nodes/ov2-ver/`。变异自动还原。未 `git worktree add`。
