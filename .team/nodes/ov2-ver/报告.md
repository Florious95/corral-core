# overlay-fix 对照报告 — ledger.overlay-fix.v1 / t.ver / r6

对照席不实现、不修。仓根跑；未碰用户 default tmux，未起模拟器，未扫 argv。

**未静默改任务定义。** 账本 A-v-probe 要求 `probe-ov2.sh` exit 0。实测 **exit 1**。根因不是产品没修，是探针仍按写探针时的 App 口径（把 `overlay_frame.text` 剥掉 ESC 当普通字符串画）。065 修好后：线上仍是 PTY 裸字节，App 走 `TerminalEmulator.feed`。探针打的是线，不是画面。改探针变绿 / 让服务端预渲染 都禁止。下面按实测写。

## ① probe-ov2.sh — 未全绿（exit 1）

命令：`bash /Volumes/nvme/Projects/远程Agent安卓/.team/nodes/ov2-probe/probe-ov2.sh`  
隔离：`/tmp/ov2-advisor/tmux/sock-a` / `sock-b`（`list-sessions` 自检 `alpha-ov2` / `beta-ov2`）。

```
FAIL ① 渲染后仍有 ESC[ [?1049 [K (B[m [30m/[43m
PASS ② 目标会话在、scratch 不在
FAIL ③ 行数=251 (≤24? false) treeMarks=34
--- last frame meta ---
frames=8 last_bytes=25639 last_lines=251
RED ① 裸控制序列: ESC[,[?1049,[K,(B[m,[30m/[43m | ③ 行数=251 rows=24 bounded=false treeMarks=34
FAIL 加强探针未全绿（当前坏实现预期红）
PROBE_OV2_EXIT=1
```

| 断言 | 结果 | 操作数 |
|---|---|---|
| ① 无裸控制序列 | **红** | 探针 `rendered = raw.replace(ESC,'')`，线上 `text` 仍含 CSI |
| ② socket 正确且无自我映照 | **绿** | 目标 `alpha-ov2` 在，无 `am-overlay` / tree / sleep / ov-spin / `beta-ov2` |
| ③ 替换而非追加 | **红** | `lineCount=251` `rows=24` `bounded=false` `treeMarks=34` `frames=8` |

App 侧具名测试（探针打不到的那条渲染路径）基线绿：

- `OverlayRendersThroughTerminalEmulatorTest.renderedTextHasNoBareControlSequences`
- `OverlayScreenReplacesNotAppendsTest`

自检：若只用 `probe-ov2.sh` 当 ①③ 的门，**正确实现也会返回 1**。所以它现在不是「修完变绿」的判据，是「线上仍是 ANSI」的回声。咬 ①③ 的尺子是 App 具名测试（见 §③ B）。

## ② size-invariance.sh — 仍绿（exit 0）

命令：`bash /Volumes/nvme/Projects/远程Agent安卓/.team/nodes/ov-design/size-invariance.sh`  
隔离 `/tmp/ov-sizeinv/sock`（`list-sessions` 自检 `user`）。

```
=== BEFORE ===
scratch:0:sc0=80x24
user:0:u0=80x24
user:1:u1=80x24
=== AFTER ===
scratch:0:sc0=80x24
user:0:u0=80x24
user:1:u1=80x24
PASS 全部窗口 #{window_width}x#{window_height} 开/关抓屏路径前后一致
size-invariance: ALL PASS (exit 0)
```

064 尺寸硬约束没被这轮改动破坏。

## ③ 定点变异 — ov2-mutate.sh exit 0

脚本：`.team/nodes/ov2-ver/ov2-mutate.sh`  
两处破坏，各自恢复；trap 保证中途失败也还原。

### A 去掉 scratch 排除

破坏：`server/internal/overlay/tmux.go` 去掉 `choose-tree -f`，`stripObserver` 原样返回。

具名测试：`TestOverlayExcludesScratchSession`

```
GREEN baseline: TestOverlayExcludesScratchSession
RED confirmed: frame contains observer token "am-overlay" (self-reflection)
  (3) - am-overlay: 1 windows (attached)
  (4) └─> - 0: tree*: ...
  (5)     └─> 0: sleep*: ...
GREEN confirmed: TestOverlayExcludesScratchSession 回绿
```

`tmux.go` sha256 `9c28d0d1a9b1b90f840cacec599538b77960177dc080623a2335b24797446655`

自检：坏实现下该测试不会仍返回 0。

### B 渲染绕过终端模拟器

破坏：`SessionViewModel.applyOverlayFrame` 改为 `overlayText = frame.text.replace(ESC,"")`（与探针同一口径）。

具名测试：`OverlayRendersThroughTerminalEmulatorTest`

```
GREEN baseline: OverlayRendersThroughTerminalEmulatorTest
RED confirmed: 1 test completed, 1 failed  (BUILD FAILED)
GREEN confirmed: OverlayRendersThroughTerminalEmulatorTest 回绿
```

`SessionViewModel.kt` sha256 `6028a4caa48a004cde55afc92f9fdf449c516f9a34fc02945bfa964d99bb2bfb`

自检：跳过模拟器后该测试不会仍返回 0。

## 工作区

只写仓根 `.team/nodes/ov2-ver/`。变异自动还原。未 `git worktree add`。未改探针、未改产品。

## 机械三条对照

| 判据 | 期望 | 实测 |
|---|---|---|
| A-v-probe | 0 | **1**（①③ 红，② 绿） |
| A-v-size | 0 | **0** |
| A-v-mut | 0 | **0** |
| A-v-rep | 本文件非空 | 本文件 |
