# overlay-fix 对照报告 — ledger.overlay-fix.v1 / t.ver / r9

对照席不实现、不修。仓根跑；未碰用户 default tmux，未起模拟器，未扫 argv。

探针已改成 066 三段式（抓真实流夹具 → 喂 App `OverlayEmulator` → 退出码=单测）。本轮按该口径验收。

## ① probe-ov2.sh — 绿（exit 0）

命令：`bash /Volumes/nvme/Projects/远程Agent安卓/.team/nodes/ov2-probe/probe-ov2.sh`  
隔离：`/tmp/ov2-advisor/tmux/sock-a` / `sock-b`（`list-sessions` 自检 `alpha-ov2` / `beta-ov2`）。

```
building isolated agentmirrord...
capture frames=8 last_seq=9 last_bytes=25639 rows=24 cols=80 -> .team/nodes/ov2-probe/frames.jsonl
PASS ① 夹具 8 帧 -> .../frames.jsonl
running OverlayEmulatorFixture via gradlew...
BUILD SUCCESSFUL
gradle OverlayEmulatorFixture exit=0
PROBE_OV2_EXIT=0
```

单测 `OverlayEmulatorFixtureTest.fixtureFramesRenderWithoutCsiAppendOrScratch`（XML `tests=1 failures=0`）在最终屏幕文本上过了 065 三条：

| 断言 | 结果 |
|---|---|
| ① 无裸控制序列 | **绿**（不得含 `[?1049` / `[K` / `(B[m` / `[30m`） |
| ② socket 正确且无自我映照 | **绿**（目标会话在；`am-overlay` / `ov-spin` / `tree` / `sleep` 不在） |
| ③ 替换而非追加 | **绿**（行数 ≤ rows；同一 `├─` 枝不重复堆叠） |

三条全过。探针未改。

## ② size-invariance.sh — 仍绿（exit 0）

命令：`bash /Volumes/nvme/Projects/远程Agent安卓/.team/nodes/ov-design/size-invariance.sh`  
隔离 `/tmp/ov-sizeinv/sock`（`list-sessions` 自检 `user`）。

```
=== BEFORE ===
scratch:0:sc0=80x24
user:0:u0=80x24
user:1:u1=80x24
=== AFTER ===
scratch:0:sc0=80x24
user:0:u0=80x24
user:1:u1=80x24
PASS 全部窗口 #{window_width}x#{window_height} 开/关抓屏路径前后一致
size-invariance: ALL PASS (exit 0)
```

064 尺寸硬约束没被这轮改动破坏。

## ③ 定点变异 — ov2-mutate.sh exit 0

脚本：`.team/nodes/ov2-ver/ov2-mutate.sh`  
两处破坏，各自恢复；trap 保证中途失败也还原。

### A 去掉 scratch 排除

破坏：`server/internal/overlay/tmux.go` 去掉 `choose-tree -f`，`stripObserver` 原样返回。

具名测试：`TestOverlayExcludesScratchSession`

```
GREEN baseline: TestOverlayExcludesScratchSession
RED confirmed: frame contains observer token "am-overlay" (self-reflection)
  (3) - am-overlay: 1 windows (attached)
  (4) └─> - 0: tree*: ...
  (5)     └─> 0: sleep*: ...
GREEN confirmed: TestOverlayExcludesScratchSession 回绿
```

`tmux.go` sha256 `9c28d0d1a9b1b90f840cacec599538b77960177dc080623a2335b24797446655`

自检：坏实现下该测试不会仍返回 0。

### B OverlayEmulator 只剥 ESC、CSI 当字形

破坏：`OverlayEmulator.feed` 遇到 ESC 只跳过 1 字节（不 `consumeEsc`），`[?1049h` 等被当字形画出来。

具名测试：`OverlayEmulatorFixtureTest`（探针 ② 同一把尺子）

```
GREEN baseline: OverlayEmulatorFixtureTest
RED confirmed: 1 test completed, 1 failed (BUILD FAILED)
GREEN confirmed: OverlayEmulatorFixtureTest 回绿
```

`OverlayEmulator.kt` sha256 `23a3e39268b5d1fd2c8d602b92d02ebbc88249e64396e88d04322a89bcd213cd`

自检：CSI 当字形后该测试（因而 `probe-ov2.sh`）不会仍返回 0。

## 工作区

只写仓根 `.team/nodes/ov2-ver/`。变异自动还原，无 `MUTATION[verify]` 残留。未 `git worktree add`。未改探针、未改产品。

## 机械四条

| 判据 | 期望 | 实测 |
|---|---|---|
| A-v-probe | 0 | **0** |
| A-v-size | 0 | **0** |
| A-v-mut | 0 | **0** |
| A-v-rep | 本文件非空 | 本文件 |
