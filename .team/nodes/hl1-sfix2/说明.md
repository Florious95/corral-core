# t.sfix2 · 返工 pr/srv-first-frame 首帧顺序 · ledger.hl1.v1 r39

status=done
裁决=封版 sha f116dd8d1 上 handleSubscribe 真实顺序是 capture → Resize → Subscribe。server 维度与 branches 维度审计（pipe 挂在 Resize 之后，SIGWINCH 重绘字节会丢）读对了。相反读码把 main 的 Resize → Subscribe → capture 当成了封版顺序，读的是另一份代码。
顺序=Subscribe → Resize → capture（pipe 先于任何会触发 SIGWINCH 的动作；首帧取 Resize 后 capture）。冻结 TUI 在 post-resize capture 无字形时回退到 Resize 前 capture，并排空已入队的 WINCH 清屏 delta。
先验红输出=见下
转绿输出=见下

## 开工自检

```
PWD=/Volumes/nvme/Projects/远程Agent安卓/.worktrees/hl1.sfix2
BRANCH=detached HEAD at f116dd8d1（pr/srv-first-frame 已被 hl1.sfix 占用，本格按同一 sha 施工；新 commit 由 leader 封版）
HEAD=f116dd8d16c7bdd9f3836bf2e22f66a53c133603
```

未 commit、未 push、未碰 main。临时文件只写 `.team/nodes/hl1-sfix2/tmp/`。

## 裁决=

在 `git show f116dd8d1:server/internal/api/ws_handler.go` 上逐行核对（hl1.sfix 工作树同 sha）：

1. L157 `snap, err := snapshotWithCursor(c.ctx, br)` — capture
2. L167 `br.Resize(c.ctx, int(s.Cols), int(s.Rows))` — Resize
3. L171 `ch, detach, err := br.Subscribe(c.ctx)` — pipe attach

`git show main:server/internal/api/ws_handler.go` 才是 Resize → Subscribe → capture。

因此：

- audit `ac588cde3bc2b7b8d` / `a45bb4b459b329a80`（pr/srv-first-frame 上 capture→Resize→Subscribe，pipe 无回溯缓冲）**读对了**。
- verdicts 里 real=false 的那份「实际顺序是 Resize → Subscribe → capture」读的是 **main**，不是封版 sha。

## 目标不变量与修法

① 静止 pane 2s 内含字形首帧：冻结探针 `TestHandleSubscribeStaticAltScreenFirstFrameHasGlyphs` 未改断言。
② 会重绘 TUI 的 SIGWINCH 重绘字节不丢：pipe 必须在 Resize 之前挂上。
③ 首帧几何是 Resize 后的：主路径 capture 在 Resize 之后（与 handleResize 注释同一前提：tmux 同步 reflow）。

产品顺序改为：

1. pre-capture（仅作冻结回退）
2. `br.Subscribe`（pipe）
3. `br.Resize`
4. post-capture；有字形则发它，否则发 pre-capture 并 `drainPipe`

## 先验红输出=

新探针在改产品代码之前、当前封版顺序上必须红。命令：

```
cd .worktrees/hl1.sfix2/server && env -u TMUX -u TMUX_TMPDIR go test ./internal/api/ -count=1 -timeout 60s -run TestHandleSubscribeRedrawingTUIWinchBytesReachClient -v
```

原始输出：

```
=== RUN   TestHandleSubscribeRedrawingTUIWinchBytesReachClient
    hl092_static_alt_first_frame_probe_test.go:162: WINCH 重绘字节未到达客户端: sawSnapshot=true got="PRE_WINCH_MARKER_092" marker="WINCH_REDRAW_MARKER_092"
--- FAIL: TestHandleSubscribeRedrawingTUIWinchBytesReachClient (2.65s)
FAIL
FAILgithub.com/agentmirror/agentmirror/internal/api3.950s
FAIL
```

exit=1。快照到了，内容是 Resize 前的 `PRE_WINCH_MARKER_092`，`WINCH_REDRAW_MARKER_092` 从未出现。落盘：`.team/nodes/hl1-sfix2/tmp/probe-redraw-red.log`

## 转绿输出=

同一对探针（冻结断言未改）：

```
=== RUN   TestHandleSubscribeStaticAltScreenFirstFrameHasGlyphs
--- PASS: TestHandleSubscribeStaticAltScreenFirstFrameHasGlyphs (0.76s)
=== RUN   TestHandleSubscribeRedrawingTUIWinchBytesReachClient
--- PASS: TestHandleSubscribeRedrawingTUIWinchBytesReachClient (0.56s)
PASS
ok  github.com/agentmirror/agentmirror/internal/api1.924s
```

exit=0。落盘：`.team/nodes/hl1-sfix2/tmp/probe-green.log`

## server 全量

```
cd .worktrees/hl1.sfix2/server && go build ./... && go vet ./... && env -u TMUX -u TMUX_TMPDIR go test ./... -count=1
```

```
ok  	github.com/agentmirror/agentmirror/cmd/agentmirrord	0.722s
ok  	github.com/agentmirror/agentmirror/internal/api	49.335s
ok  	github.com/agentmirror/agentmirror/internal/bridge	21.956s
ok  	github.com/agentmirror/agentmirror/internal/config	2.085s
ok  	github.com/agentmirror/agentmirror/internal/discovery	3.006s
ok  	github.com/agentmirror/agentmirror/internal/overlay	1.437s
ok  	github.com/agentmirror/agentmirror/internal/pairing	4.173s
ok  	github.com/agentmirror/agentmirror/internal/protocol	3.704s
ok  	github.com/agentmirror/agentmirror/internal/provider	4.576s
ok  	github.com/agentmirror/agentmirror/internal/tsnetd	1.679s
```

exit=0。落盘：`.team/nodes/hl1-sfix2/tmp/go-full.log`

## 改动文件

- `.worktrees/hl1.sfix2/server/internal/api/ws_handler.go`
- `.worktrees/hl1.sfix2/server/internal/api/hl092_static_alt_first_frame_probe_test.go`（只加第二世界用例，未改冻结探针断言）
