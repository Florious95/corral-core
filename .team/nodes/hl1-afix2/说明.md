# t.afix2 · 返工 pr/app-altsnap-render 守卫 + 修 altActive 存量缺陷

status=done
守卫新语义=订阅代际窗口：SNAPSHOT 代际 0（本轮 subscribe/重连 capture）一律 replay，含合法空屏；代际 1（视口 seed 的 resize 补发）仅当入帧无字且屏上有字才保留不 replay；代际 >=2 普通空快照照常 replay。不 apply 仍执行 snapshotGen++ 与 maybeCompleteResync。RECONNECTING 时 snapshotsSinceSubscribe 归零。
altActive修法=replaySnapshot 仍把可见缓冲画进主屏并 altActive=false（capture-pane 无 1049h），同时置 snapshotPaintedVisibleOnMain；不成对 ESC[?1049l] 走 exitAlt 时清主屏。成对 1049h/l 仍恢复主屏。enterAlt / RIS 清该旗标。
先验红输出=见下
转绿输出=见下

## 开工自检

```
PWD=/Volumes/nvme/Projects/远程Agent安卓/.worktrees/hl1.afix2
HEAD=fa87858b6（detached，与 pr/app-altsnap-render 同 SHA；hl1.afix 已占用该分支名）
```

未 git commit / push。未碰仓根 main 工作树。未开模拟器。临时文件只写 `.team/nodes/hl1-afix2/tmp/`。

## 先验红输出=

产品代码未改、只加测试时（`.worktrees/hl1.afix2`，HEAD=fa87858b6 旧守卫）：

命令：

```
cd /Volumes/nvme/Projects/远程Agent安卓/.worktrees/hl1.afix2/app && env -u TEAM_AGENT_API_KEY ./gradlew :app:testDebugUnitTest --tests dev.agentmirror.app.session.AltScreenSnapshotGlyphsTest --tests dev.agentmirror.app.session.ReplaySnapshotUnpairedAltExitTest --rerun-tasks
```

原始失败摘要：

```
AltScreenSnapshotGlyphsTest > ordinaryEmptySnapshotAfterFirstFrameWindowApplies FAILED
    java.lang.AssertionError at AltScreenSnapshotGlyphsTest.kt:103

AltScreenSnapshotGlyphsTest > suppressedResizeSnapshotStillBumpsSnapshotGen FAILED
    java.lang.AssertionError at AltScreenSnapshotGlyphsTest.kt:137

AltScreenSnapshotGlyphsTest > reconnectEmptySnapshotAppliesEvenIfScreenHasGlyphs FAILED
    java.lang.AssertionError at AltScreenSnapshotGlyphsTest.kt:118

ReplaySnapshotUnpairedAltExitTest > replaySnapshotThenUnpaired1049lClearsStaleAltContent FAILED
    org.junit.ComparisonFailure at ReplaySnapshotUnpairedAltExitTest.kt:39

6 tests completed, 4 failed
BUILD FAILED in 11s
```

对照仍绿（旧守卫本就要保住的世界）：`altScreenKindSnapshotGlyphsSurviveEmptyResizeSnapshot`、`pairedEnterExitStillRestoresMain`。

落盘：`.team/nodes/hl1-afix2/tmp/prior-red.txt`、`tmp/prior-red-tail.txt`

## 修法

1. `SessionViewModel.onBinary` SNAPSHOT：用 `snapshotsSinceSubscribe` 判别首帧窗口，去掉「凡空入帧且屏上有字就拒收」。early-return 取消；保留只跳过 `replaySnapshot`。
2. `TerminalEmulator.replaySnapshot`：`snapshotPaintedVisibleOnMain=true`；`exitAlt` 在 `!altActive` 时清主屏。

## 转绿输出=

命令：

```
cd /Volumes/nvme/Projects/远程Agent安卓/.worktrees/hl1.afix2/app && env -u TEAM_AGENT_API_KEY ./gradlew :app:testDebugUnitTest --tests dev.agentmirror.app.session.AltScreenSnapshotGlyphsTest --tests dev.agentmirror.app.session.ReplaySnapshotUnpairedAltExitTest
```

```
> Task :app:testDebugUnitTest

BUILD SUCCESSFUL in 2s
36 actionable tasks: 1 executed, 35 up-to-date
```

全量（账本 A-afix2-suite 同形）：

```
cd /Volumes/nvme/Projects/远程Agent安卓/.worktrees/hl1.afix2/app && env -u TEAM_AGENT_API_KEY ./gradlew :app:testDebugUnitTest :terminal:test
```

```
> Task :app:testDebugUnitTest
> Task :terminal:test

BUILD SUCCESSFUL in 30s
39 actionable tasks: 4 executed, 35 up-to-date
```

wiki-ratchet `--pkg dev.agentmirror.app --root .worktrees/hl1.afix2`：基线=1 本次=1 新增=0。

落盘：`.team/nodes/hl1-afix2/tmp/prior-green.txt`、`tmp/full-green.txt`

## 改动文件

- `.worktrees/hl1.afix2/app/app/src/main/java/dev/agentmirror/app/session/SessionViewModel.kt`
- `.worktrees/hl1.afix2/app/terminal/src/main/kotlin/dev/agentmirror/terminal/TerminalEmulator.kt`
- `.worktrees/hl1.afix2/app/app/src/test/kotlin/dev/agentmirror/app/session/AltScreenSnapshotGlyphsTest.kt`
- `.worktrees/hl1.afix2/app/app/src/test/kotlin/dev/agentmirror/app/session/ReplaySnapshotUnpairedAltExitTest.kt`
- `.team/nodes/hl1-afix2/说明.md`

## 边界

- 单测绿 ≠ 真机画布已通；冷点开/重连真空屏仍要看会话页。
- 窗口长度固定为 2（订阅 capture + 一帧 resize 补发）。更晚的空 resize 按合法空屏应用。
- 不成对 1049l 清的是主屏，宿主主屏在 alt 期间的真实内容客户端从未收到，无法凭空恢复。
- 未 commit（硬约束）
