# t.sent · 「已发送」蓝色悬浮堆叠

## 复现（改前，必须先红）

仓根 `SessionScreen.StatusArea` 临时把 `InputStatus.Sent` 映回 `"已发送"`（用户真机/wt.app 仍是这条），装到 emulator-5554。

隔离 daemon `:9900` + 隔离 tmux `claude` mock。真发 `UX4SENTPROBE1`：

- tmux pane 出现 `echo:UX4SENTPROBE1`（消息真发出去了）
- 发送后 0.2s 内截图 `shot-repro-d480.png`：键盘上方、终端底边蓝色 **「已发送」**
- `uiautomator dump` 约 2.2s，长过 `TRANSIENT_MS=1200`，所以 dump 会漏掉——上一轮 `absent` 绿就是这么漏的

堆叠机理：`AnimatedVisibility(visible=message!=null)` 退出动画把旧 Text 留在树上；每次发送再进一次。用户看到的「几个蓝色悬浮堆叠」是退出副本，不是藏起来就能消。

## 修

`app/app/src/main/java/dev/agentmirror/app/session/SessionScreen.kt`

- `InputStatus.Sent -> null`（083 §4：只删成功态）
- `if (message == null) return` 直接不组节点，删掉 `AnimatedVisibility`
- `InputStatus.Failed` / `Sending` / 上传 / `transientError` 仍走 `Text`

## 改后读数（`ui-check.sh` 实跑）

| 密度 | 改前「已发送」节点 | 改后节点 | marker 进 pane | 失败态 |
|---|---|---|---|---|
| d480 | ≥1（shot-repro-d480.png 键盘上方蓝字） | **0** | UX4SENTd480 | `连接未就绪，无法发送` |
| d440 | （同复现包） | **0** | UX4SENTd440 | `连接未就绪，无法发送` |

截图：`shot-repro-d480.png`（改前）/ `shot-after-d480.png` / `shot-after-d440.png`（改后无蓝字）。

## 不倒退

失败态文案仍显示。085 重着色未碰。未 `git checkout`/`restore`。未碰用户 tmux。未扫 argv。
