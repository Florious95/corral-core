# t.repro · 打开会话白屏（契约 092 §1）· ledger r4

引入commit=未能钉死（main 未复现）
复现步骤=见下文「完整步骤」
截图=.worktrees/hl1.repro/.team/nodes/hl1-repro/shot-r4-01-pairing.png, .worktrees/hl1.repro/.team/nodes/hl1-repro/shot-r4-02-l1.png, .worktrees/hl1.repro/.team/nodes/hl1-repro/shot-r4-03-l2.png, .worktrees/hl1.repro/.team/nodes/hl1-repro/shot-r4-04-session.png

status=norepro

## 自检（r4 本回合）

```
PWD=/Volumes/nvme/Projects/远程Agent安卓/.worktrees/hl1.repro
BRANCH=
HEAD=a946101a0
```

detached HEAD，本格不建分支。main 同源。

## 结论

在 **main `a946101a0`**、worktree 现编 `app-debug.apk`、**当前源码编出的测试 daemon**（`tmp/agentmirrord`，含 `level2_subscribe`）上，按 092 §4 隔离配对后：

1. 一级能看到隔离工作区 `hl1reprocwd`（1 会话）。shot-r4-02-l1
2. 点进二级，出现隔离会话行 `bash` / 「空闲」。shot-r4-03-l2
3. 点开该行：**顶栏 + 终端画布（有 zsh 提示与 `claude-3.2$`）+ 底栏「输入指令」都在**。shot-r4-04-session

**不是整页白屏，对话界面出现。** 不能 bisect。⛔ 未硬编白屏。

未点任何真实舰队会话。真 Claude 会话是否白屏，本席无证据。

## 完整步骤

- 模拟器：已有 `emulator-5554`（`agentmirror_test_b` 1080x2400）。未自起/未杀。
- adb：`~/Library/Android/sdk/platform-tools/adb`
- 隔离 tmp（tmux socket 必须短路径，落相对路径 `.team/nodes/hl1-repro/tmp/`，socket 长 80）：
  1. `ln -s /bin/bash <tmp>/bin/claude`
  2. `unset TMUX`；`TMUX_TMPDIR=<tmp>/tmux tmux -f /dev/null new-session -d -s hl1repro -c <tmp>/hl1reprocwd`，PATH 前置后 `exec claude -i`
  3. 自检：`TMUX_TMPDIR=<tmp>/tmux tmux -f /dev/null list-sessions` → `hl1repro`；socket=`<tmp>/tmux/tmux-501/default`；pane `comm=claude`
  4. 当前源码 `agentmirrord` listen `0.0.0.0:22195`；`adb reverse tcp:22195 tcp:22195`
  5. `cd app &&` 已有 `:app:assembleDebug`；`pm clear` 后装包冷启
  6. 手填 `ws://10.0.2.2:22195/ws` + token；截图前 `input keyevent 111`
  7. 只点 `hl1reprocwd`，再只点该区 `bash`

## 环境坑（不是 092 白屏）

过旧 `server/bin/agentmirrord` / `e2e/bin/agentmirrord` 会把 `level2_subscribe` 判 `unsupported_type` / `unknown frame type`，二级只有页头没有会话行。换当前源码 daemon 后二级与会话页均正常。

## 14 条 land（未钉死）

`d1-userblock-theme` → `d2-meter` → `e6-upload-success` → `geom-resume` `2e9700ab3` → `recon-foreground` `7904f0768` → `e7-scroll-hint` → `e1-resync` `c06823ecd` → `icons-longpress` → `e2-composer` `5441c1d5e` → `sort-label` → `e12-close` `573cfa866` → `e13-new-pane` `01467a88c` → `e14-provider-cmd` → `gofix-scrubbedenv`

高嫌疑只读过 diff（会话进入相关）：e12-close / e13-new-pane / e1-resync / e2-composer / recon-foreground / geom-resume。

**证据边界：** 隔离路径会话页通，不能从这些 diff 指出「哪一处导致白屏」。以下为推测：若用户白屏只发生在真 Claude alt-screen，候选仍是 geom-resume / e1-resync / e2-composer。本轮未命中。

## 路径说明

派单「只准写 worktree `.team/nodes/hl1-repro/`」，handoff 必须交付仓根 `.team/nodes/hl1-repro/复现.md`。主写 worktree，并复制 复现.md 到仓根以满足 handoff。tmux 仍用短路径 `.team/nodes/hl1-repro/tmp/`（worktree 嵌套路径会超过 unix socket ~104 字节上限）。
