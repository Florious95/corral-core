# t.icons · E10/E11/E15 · 首列 Provider 图标 + 长按菜单

## 开工自检

```
$ pwd
/Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr2.listui

$ git branch --show-current
pr/icons-longpress
```

仓根在 `main`。本格只在 `pr/icons-longpress`（从已封版的 e7 ff 过来，保留 sheet 可滚）。未建其它 PR 分支，未做 E12/E13/E14/E9。

## 改了哪些文件

方案 §4 所需文件超出账本 write_paths（只给了 `workspace/`）。照方案做，并报出：

- `workspace/L2Models.kt` `FavoriteRecord.kt` `FavoriteBook.kt` `DesignListMapping.kt` `WorkspaceViewModel.kt`
- `conn/Frames.kt` Session.provider
- `ui/model/Models.kt` SessionItem.provider
- `ui/components/ProviderIcon.kt` 自绘六家+unknown
- `ui/components/SessionOverflowMenu.kt`
- `ui/screens/SessionListScreen.kt` `FavoritesScreen.kt`
- `ui/components/SessionSwitchSheet.kt` 首列图标 + 长按
- `ui/theme/DesignTokens.kt` box/pad/radius/well
- `server/internal/api/listing.go` toSession 填 Provider（identifyProvider）
- 测试：`ProviderIconMenuTest.kt` 及点星改为长按菜单的现有测

**未改** 关闭协议（E12）。菜单「关闭」只回调 `onClose`，不发帧。

## 修法

三处 `StarButton` 换成 `ProviderIcon`。名称列 `combinedClickable` 长按 `DropdownMenu`：会话/sheet 有收藏/取消收藏+关闭；收藏页只有取消收藏。空 provider 文案 `Agent`，不猜 claude。

## 先验红（改产品码之前）

```
$ ./gradlew :app:testDebugUnitTest --tests 'dev.agentmirror.app.workspace.ProviderIconMenuTest'

> Task :app:compileDebugUnitTestKotlin FAILED
e: ProviderIconMenuTest.kt:32:42 Unresolved reference 'ProviderIcon'.
e: ProviderIconMenuTest.kt:65:13 No parameter with name 'provider' found.
e: ProviderIconMenuTest.kt:90:13 No parameter with name 'provider' found.
e: ProviderIconMenuTest.kt:137:17 Unresolved reference 'ProviderIcon'.
BUILD FAILED
```

## 验绿

### ProviderIconMenuTest

`l2StarsGone_providerHasWhitelistDescription` / `closeScope_favoritesOmitClose` / `closeScope_sessionStarredHasClose` / `providerIconWellIsNotOpaqueWhiteBlock` passed。

收藏页先打开菜单再断言 `menu-close` DoesNotExist；会话页 starred 长按 `menu-close` Exists。`l2-provider-ref-g` contentDescription=`Grok`。`l2-star-*`/`fav-star-*` DoesNotExist。

### A-icons-wiki

初稿给 L2Entry/FavoriteRecord 各加一行，存量 T3 行号平移被棘轮当成新增。把 `provider` 与上一字段同行，行号回基线：

```
pkg=dev.agentmirror.app.workspace 基线=8 本次=8 新增=0 消掉=0
```

未 `--freeze`，未修存量 T3。

### A-icons-smell

```
face=app 基线=16 本次=16 新增=0 消掉=0
exit 0
```

初稿 `SessionSwitchSheet` 把 `onClose` 插在 `modifier` 前，lint `ModifierParameter` 新增 1 条；把 `modifier` 放回可选参数第一位后绿。未 `--freeze`。

### A-icons-suite

本格相关测绿：`ProviderIconMenuTest` / `L2RowLayoutTest` / `FavoriteToggleClickTest` / `TestFavorite` / `FavRowParityTest` BUILD SUCCESSFUL。

全量 `:app:testDebugUnitTest` 两次撞上「Could not write XML test results」（同 worktree 另有 gradle 在写 `test-results/`），exit 1，不是断言红。`A-icons-seal` 不在本席跑。

## 查不清

- Robolectric `captureToImage` 超时，位图中心 4×4 未抽成像素；改为断言 well 色不是不透明白。
- 方案 §4.4 浅/深各三张对比图未出（无本格模拟器装包）。
- 关闭菜单不发 `close_session`（E12）。
