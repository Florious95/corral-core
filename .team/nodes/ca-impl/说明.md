# t.path · 钉住性能关键路径

工作树：`.worktrees/wt-ca`。只写了 `docs/性能关键路径.md` 与本说明。未改产品代码，未 commit/push，未开模拟器。

## 八条（调用点均 `test -e` 过）

| 事件 | 核/壳 | 路径 |
|---|---|---|
| tap | 壳 | `app/app/src/main/java/dev/agentmirror/app/MainNavState.kt`（`openSession` → `onUserOpen`） |
| route_enter | 壳 | `app/app/src/main/java/dev/agentmirror/app/session/SessionRoute.kt`（`remember(ref)` `beginRoute`+`routeEnter`） |
| subscribe_sent | 核 | `app/core-conn/src/main/java/dev/agentmirror/app/conn/ConnectionManager.kt`（`subscribe` → `traceSubscribe`） |
| geom_seed | 核 | 同上（首次成功 subscribe 顺带 `geom_seed`） |
| first_frame_recv | 壳 | `app/app/src/main/java/dev/agentmirror/app/session/SessionViewModel.kt`（`onBinary` `emitFirstFrameIfFirst`） |
| snapshot_applied | 壳 | 同上（`replaySnapshot` 之后 `emitSnapshotIfFirst`） |
| first_draw | 壳 | `app/app/src/main/java/dev/agentmirror/app/termview/TermSurfaceView.kt`（`onDraw` `emitFirstDraw`） |
| layout_settled | 壳 | `app/app/src/main/java/dev/agentmirror/app/perf/PerfTrace.kt`（500ms 无重排）；重排触发源已在核 `ConnectionManager.resize`，与订阅同文件已进监视表 |

未列入（不在打开事件调用点上，写进去会误报）：`Connection.kt`（`ws_binary_recv` 仪表）、`ConnPerf.kt`（钩子）、`TerminalEmulator.kt`（apply 本体，发布后 app 仓改不到）、测试、主题、列表 UI。

任务书「六个在壳、两个沾核」：本树 ConnectionManager 已进 `:core-conn`，故两条标核、六条标壳。未静默改任务定义。

## 判据

cwd=`.worktrees/wt-ca`，`sh tools/perfbase/judge-perfpath.sh` 原文：

```
PASS 性能关键路径已钉住且守门判据可跑
```

退出码 0。抽取到的监视路径 6 个 `.kt` + 2 个判据 `.sh`，全部 `test -e` 通过。
