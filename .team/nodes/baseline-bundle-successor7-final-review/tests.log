successor7 post-commit lineage review; commit=79cd08f0f; read-only

cat-file:
- NUL-safe `git ls-tree -rz --name-only 79cd08f0f` successor7 sweep:
  50 paths, missing=0.
- DSL, compiled ledger, 12 compiled ScriptRef/command script paths,
  successor7 final wrappers, path-level taskbooks, and bootstrap fixture
  paths all resolved from 79cd08f0f.

ledger snapshot:
- ledger_id=ledger.baseline-bundle.successor7.v1 revision=1 tasks=9
- all nine states=planned; task attempts fields=0; task statuses fields=0;
  root statuses field absent
- current worktree metadata: wt-maple-core present; wt-s7-cedar and
  wt-s7-orbit absent
- successor7 lease and driver PID absent; old perf-regress lease/PID present
  as pre-migration state and not part of successor7 launch state

fresh mechanical check against the committed working-tree ledger:
- `ledger-run --preflight --json .team/ledgers/baseline-bundle-successor7-v1.json`
  exit=2
- `ledger-run --dry-run --json .team/ledgers/baseline-bundle-successor7-v1.json`
  exit=2
- both report schema rejection at
  `/tasks/t.baseline-bundle.apparatus`: `command` and `executor` are
  unexpected additional properties
- expected first frontier was not asserted because schema validation stopped
  before frontier evaluation; no ledger drive, task dispatch, WT creation,
  adb/qemu/emulator launch, or product modification occurred

The result is a package/runtime schema compatibility refutation, not a redo
or reversal of the previously passed semantic review.

verdict: refutes
