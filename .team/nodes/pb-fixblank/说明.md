# t.fixblank 说明（修白屏：订阅首帧按 ref 投递）

席位：施工席 `pb-impl` / 格 `pb-fixblank`。worktree：`.worktrees/wt-pb-fixblank`。未 commit / 未 push。

## 根因（读 说明-r18-第四轮根因定案.md）

18/18：`listener_ref=dev.agentmirror.app.workspace.WorkspaceViewModel`，
`SessionViewModel.onBinary` 从未被调用。`ConnectionManager` 单槽被列表页占着，
订阅快照交给它就地丢弃。订阅发生在 VM `init`，早于 `SessionRoute` 把 `uiConnector`
换成会话 VM。

## 先验红原始输出

工作目录：`/Volumes/nvme/Projects/远程Agent安卓/.worktrees/wt-pb-fixblank`

```
$ cd app && ./gradlew :app:testDebugUnitTest --offline --tests 'dev.agentmirror.app.conn.ConnListenerRoutingTest'
```

完整日志：`.team/nodes/pb-fixblank/tmp/prered-run.log`

退出码：**1**

```
> Task :app:testDebugUnitTest FAILED

ConnListenerRoutingTest > 帧只投给订阅该ref的接收者 FAILED
    java.lang.AssertionError at ConnListenerRoutingTest.kt:64

ConnListenerRoutingTest > 列表页占用槽位不影响会话页收帧 FAILED
    java.lang.AssertionError at ConnListenerRoutingTest.kt:79

2 tests completed, 2 failed
BUILD FAILED in 9s
```

## 改了什么

- `ConnectionManager.addBinaryListener(ref, listener)` / `removeBinaryListener`：按 ref 多接收者。
- `onBinary`：该 ref 有登记则只投给那些接收者；没有登记则仍投给 `setListener` 全局槽（存量单测）。
- `SessionViewModel.init` 在 **subscribe 之前** `addBinaryListener`；`dispose` 摘除。
- 仪表保留：`listener_ref=` / `ws_binary_recv` / `reason=ref_mismatch` 均在。派发点 `listener_ref` 打实际接收者（会话 VM 的 ref）。

## 转绿

```
$ bash tools/perfbase/judge-fixblank.sh
```

stdout：`PASS 路由红测转绿且仪表未被拆`  退出码：**0**

全量单测：`.team/nodes/pb-fixblank/tmp/green.log`

## 没改的

- 没拆 PerfTrace 仪表、没改 `tools/perfbase/*.sh`、没改红测方法名
- 没开模拟器、没碰 9900、没 commit/push
