# 061 App 二级菜单（t.app / r6，仓根重做）

上一轮写在 `.worktrees/wt.app/`，判据看不见。本轮在仓根当前 HEAD 重做，**没有**把旧树整棵搬回，**没有**复活 v1 的 `Level2LiveStreamScreen.kt` / `Level2ViewModel.kt` / `Level2*Test.kt`。

## 行为

- 二级 = 一个列表。每行：结构字段会话标识 + 右侧「进行中 / 空闲 / 未知」。
- `unknown` / 缺省 / 乱值显示「未知」，不回落空闲。
- 跳转只用 `ref` + `session_name` / `window_index` / `window_name`（展示名优先 window_name，再回落线上 `name`）。不从 title 抠。
- App 只订 `level2_subscribe`、只收 `level2_frame` / `level2_heartbeat`，不轮询。
- 状态挂在已接 `uiConnector` 的 `WorkspaceViewModel`。

## 新文件（非 v1 名）

- `L2Models.kt` / `L2SessionList.kt`
- `L2ListRendersStatusTest` / `L2UnknownStatusTest` / `L2NavigationIdentityTest` / `L2PushOnlyNoPollTest`
