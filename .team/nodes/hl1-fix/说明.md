# t.fix · 回炉③修复白屏 · ledger.hl1.v1 r15

status=blocked（不可判完整修复，未改产品代码、未造假）

根因=不可在本格 `app/` 落修。复现格（hl1-repro2）钉的是世界 C：顶栏/底栏在、`TermSurfaceView` 面积非 0、格子空（冷点开静态 alt-screen / 真 Claude / 大滚回 >16s 无首帧）。探针格 r14 明确未写测试类，且把可红落点指向 server `handleSubscribe`「先 Resize 再 capture」。本格 write_paths 只有 `.worktrees/hl1.fix/app/`，改 `ws_handler.go` 越权。
修法=未改 `app/`。⛔ 未做「渲染好了才展示」。⛔ 未自写探针冒充 pr/hl-probe。
探针绿输出=未跑产品探针（无本轮探针文件）。先验红见下。

HEAD=`a946101a01e10cfe95bc569ad34c2da05aef05a3`（`pr/hl-blank` 与 `pr/hl-probe` 同 sha，0 unique commit）

## 开工自检

```
PWD=/Volumes/nvme/Projects/远程Agent安卓/.worktrees/hl1.fix
BRANCH=pr/hl-blank
HEAD=a946101a01e10cfe95bc569ad34c2da05aef05a3
```

命令原文：

```
/Volumes/nvme/Projects/远程Agent安卓/.worktrees/hl1.fix
pr/hl-blank
```

分支已存在（r8 所建），本回合未再建、未 commit、未 push、未碰 main。

## 先验红

合入探针之前（产品代码零 diff）跑探针格点名的类：

```
cd /Volumes/nvme/Projects/远程Agent安卓/.worktrees/hl1.fix/app && ./gradlew :app:testDebugUnitTest --tests dev.agentmirror.app.session.Hl092StaticAltScreenBlankCanvasProbe

> Task :app:testDebugUnitTest FAILED
Execution failed for task ':app:testDebugUnitTest'.
> No tests found for given includes: [dev.agentmirror.app.session.Hl092StaticAltScreenBlankCanvasProbe](--tests filter)
BUILD FAILED in 958ms
```

```
git log HEAD..pr/hl-probe --oneline
(empty)
```

没有「根因不存在」的可执行探针。伪造一条会在现码上失败的 app 单测 = 造假先验红，不做。

## 输入（本回合读的是 hl1-repro2，不是 r4 norepro）

`.team/nodes/hl1-repro2/复现.md`：`status=done` / `复现=成功`。

- 现象：顶栏+底栏在，终端画布全黑无字；等 8s（大滚回采到 16s）仍无字。
- 触发：冷点开（或退后再进）一个几乎不吐 delta 的 pane；持续重绘会把画布救回来。
- `引入commit=未能钉死`（服务端 A/B `dc9aab11b` vs main 同形，两边 >16s 无首帧）。

`.team/nodes/hl1-probe/说明.md` r14：`status=blocked`；`探针=（未写测试类#方法）`；世界 A/B 排除、C 命中。

## 卡在哪

任务第 1 步要求 cherry-pick `pr/hl-probe` 探针。实收：该分支无 unique commit、无本轮测试文件。

任务第 2 步要求修根因且不许症状层补丁。探针格给出的可红落点是 **server** `handleSubscribe`：`Resize(cols,rows)` 之后才 `snapshotWithCursor` / `capture-pane`。静态 alt-screen 不响应 SIGWINCH → 快照空 → 无后续 delta → 客户端 `replaySnapshot` 忠实画出空格子（现有 `SessionViewModelTest.snapshotReplaysGridNotAppends` 在非空 snapshot 上已绿，钉的不是 092）。

本格只准写 `app/`。客户端若改成「空 snapshot 先不画 / 等有字再展示」即用户明令禁止的遮掩。客户端无法在不知道 alt-screen 的前提下取消 subscribe 自带的 cols/rows（005 要 reshape）。

任务第 3 步「修完探针必须绿」：无探针可绿。

## 试了什么

- 读 hl1-repro2 复现.md 与 hl1-probe 说明.md。
- `git log HEAD..pr/hl-probe` 为空；全仓无 `Hl092*` 测试类。
- 跑缺失探针类 → `No tests found`（上节原文）。
- 只读 `SessionViewModel.onBinary(SNAPSHOT)`：`emulator.replaySnapshot` 清屏重建，空数据 → 空格子，与世界 C 同构，不是「没组合」或「尺寸 0」。
- 只读 `server/internal/api/ws_handler.go` `handleSubscribe`：先 `br.Resize` 再 pipe 再 `snapshotWithCursor`。未改该文件。

## 需要裁定

1. `t.fix` write_paths 扩到 `server/`（按探针格建议钉 resize-then-capture），还是把修复改派 server 席？
2. 在探针文件仍不存在时，是否关 `t.fix`，等 `t.probe` 落可红探针再派？
3. 不要把「画布面积>0 / session chrome 在」当成修过 092——那是 A/B，用户看见的是 C。

⛔ 未改 `app/`。
⛔ 未改探针（探针本身不存在）。
⛔ 未 commit / 未 push。
