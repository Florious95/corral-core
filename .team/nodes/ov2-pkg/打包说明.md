# t.pkg 打包

未改任何实现代码。仓根 `app/` 出包。

## APK

- 路径：`app/app/build/outputs/apk/debug/app-debug.apk`
- 大小：39426276
- 时间：2026-08-19 10:12:47
- sha256：`ea94255cc0d446fcd91f371e2d3cd40fc270f57beeb5c193910954e3ea34507c`
- 第一次 `assembleDebug` 是 UP-TO-DATE（10:04 旧包，晚于 OverlayEmulator.kt）。`--rerun-tasks` 重打。A-k-fresh：无更晚 `.kt`。

## 收口判据（不是 dex 符号）

- 加强探针 `probe-ov2.sh`：隔离 tmux sock-a/`alpha-ov2`、sock-b/`beta-ov2` 自检；抓 8 帧 overlay_frame；`*OverlayEmulatorFixture*` exit 0。
- 尺寸不变 `size-invariance.sh`：隔离 `/tmp/ov-sizeinv/sock`，抓屏路径前后窗口全是 80x24。exit 0。

用户手测装上面这个 APK。
