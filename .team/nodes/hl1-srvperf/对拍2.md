status=done
night指标=两轮均值 fps=6823.9 帧间隔p50=0.159ms p95=0.209ms daemon_mean_pcpu=23.32%；r1 fps=6680.9 p50=0.159ms p95=0.214ms cpu=23.36%；r2 fps=6967.0 p50=0.158ms p95=0.203ms cpu=23.29%。大滚回首帧（静止 pane）均值 63.1ms（r1 63.759 / r2 62.476）KindSnapshot 194B hist=39982
night冷订阅首帧=未复现 >16s 无首帧（WS 层）。静止大滚回 hist=39982 alt=0：63.1ms 收到 KindSnapshot，daemon sendq snapshots_pushed=1 snapshots_from_subscribe=1；静态 alt-screen：62.2ms KindSnapshot 89B。等待上限 30s，两轮都 <0.07s。与 repro2 的「app 画布 14s 全黑」不是同一层：服务端已经把快照推出去了
三版结论=night(4120c0884) / base(dc9aab11b) / new(gofix/main) 隔离指标同量级，没有哪一版能单独解释用户体感。吞吐 fps night 6824 vs base 6244 vs new 6272（轮次方差 ±15% 量级）；帧间隔 p50 三版都是 0.159ms；冷订阅首帧 night 63ms / base 65.6ms / new 65.1ms。WS 层三版都会在 <0.1s 给出 KindSnapshot；>16s 无首帧在本装置上三版都不成立（那是 app 画布层，server sendq 已记 snapshots_from_subscribe=1）
引入窗口=4120c0884..dc9aab11b（用户：昨晚流畅 / dc9 仍卡）。server 实改 listing.go(+21/-8) 与 server.go 一处调用；本席首轮 A/B 无差是因为 dc9 与 main 都在此窗口之后
listing嫌疑=buildSnapshot 每个 catalog 条目调用 identify(panePID)（listing.go toSession 加 Provider，server.go rebuildCatalog 传入 s.identifyProvider）。filterModel 已用 IdentifySet 一次批量 ps；随后 Identify(单 pid) 的缓存键是单个 pid，与批量键不同 ⇒ 每个 listing 心跳额外 N 次 `ps -axo pid=,ppid=,comm=`。Session 结构体相等含 Provider，identify 抖动会变成 list_delta ChangedSessions（本装置 11 个稳定 grok pane 上未观测到抖动）
多会话对拍=隔离 5 session×2 window + 1 yes = 11 白名单 pane，list-interval 500ms，60s×2 交错。night CPU 均值 18.92% p95 23.3%；dc9 均值 23.85% p95 31.9%（+26% / p95 +37%）。list_delta_n 两边都是 0（无 Provider 抖动）。流 fps night 4879 vs dc9 4489（重叠，dc9-r1 偏低）。9900 pid 30114 未碰

# t.srvperf2 · night=4120c0884 对拍

measurement-only。未改产品代码。未碰 9900（pid 30114 comm=`./agentmirrord-night-4120c0884` 全程前后相同）。

## 量具身份

- night SHA=`4120c0884817aa312d97d52eea55f49dc0beeb38`（2026-08-20 23:48）
- `git archive 4120c0884 server` 自建 md5=`5e2353c49858383a2875c244cdc9538e`（go1.26.5 darwin/arm64）
- 生产文件只读核 md5=`2578645a940f0011de76b8709a8d3457`（`server/agentmirrord-night-4120c0884`，与 leader 确认值一致）。自建指纹不同，SHA 相同。
- 对照（上轮已测，不重跑 60s）：base md5=`3acf3f9ad56572ebe8c003d778f24152` sha=`dc9aab11b`；new md5=`ba1e91b7809897207aee585b8a54fcb2` sha_server=`2a1a562c8`
- 隔离端口 ≠9900；TMUX `-L hl1sp` 于 `/tmp/e2e-hl1srvperf`（chmod 700），跑完删除。

## 并排

| 版 | fps | p50_ms | p95_ms | mean_pcpu | 大滚回静止首帧 |
|---|---:|---:|---:|---:|---:|
| night 4120c0884 | 6823.9 | 0.159 | 0.209 | 23.32% | 63.1ms / 194B |
| base dc9aab11b | 6243.7 | 0.159 | 0.280 | 26.20% | 65.6ms / 194B |
| new main/gofix | 6272.0 | 0.160 | 0.269 | 26.13% | 65.1ms / 194B |

原始：`tmp/runs/night/summary.json`。

## 引入窗口 4120c0884..dc9aab11b（定向）

用户：4120c0884 流畅，dc9aab11b 仍卡。git log 该窗口 server 只有 `e34776e68`（icons-longpress 席位封版），diff 即 listing.go / server.go。

多会话原始：`tmp/runs/fleet/summary.json`。

| 版 | CPU mean | CPU p95 | list_delta_n | 流 fps | 流 p50 |
|---|---:|---:|---:|---:|---:|
| night-r1 | 20.30 | 22.9 | 0 | 5512 | 0.168 |
| night-r2 | 17.54 | 23.7 | 0 | 4246 | 0.171 |
| dc9-r1 | 21.40 | 31.3 | 0 | 3718 | 0.173 |
| dc9-r2 | 26.29 | 32.4 | 0 | 5259 | 0.170 |

## 冷订阅（静止 pane）

装置：history-limit 50000，seq 40000 后 `exec -a grok sleep`（不再吐 delta），再 WS auth→list→subscribe，等 KindSnapshot，超时 30s。

另测静态 alt-screen（`\033[?1049h` 打两行后 sleep）。

结果见 `night冷订阅首帧=`。未开模拟器（本席禁令），所以不评价 app 画布 14s 全黑；只评价服务端是否在 16s 内把首帧送出。
