# t.capp · corral-app 只引用发布产物

工作树：`.worktrees/wt-ca`。未 commit/push，未开模拟器，未动 `tools/perfbase/`。

## 改了什么

- `.team/staging/corral-app/settings.gradle.kts`：去掉 `includeBuild`。仓库顺序固定：
  1. `https://raw.githubusercontent.com/Florious95/corral-core/maven/`
  2. 本地 `.team/staging/maven-repo`（`rootDir.resolve("../maven-repo")`）
- `app/build.gradle.kts`：依赖钉死
  `dev.agentmirror.core:core-protocol:20260822.0` /
  `core-terminal:20260822.0` / `core-conn:20260822.0`；
  去掉未发布的 `dev.agentmirror:terminal` 门面。
- release 签名沿用 debug keystore（仅本地对比，不可分发）。
- 工程内无 `core-*/src`。

## 产物

`.team/staging/corral-app/app/build/outputs/apk/release/app-release.apk`

## 判据原文

cwd=`.worktrees/wt-ca`，`sh tools/perfbase/judge-capp.sh`：

```
PASS corral-app 只引用产物、无核源码、装出 release APK 且仪表在位
```

退出码 0。dex 中 `PerfTrace` / `addBinaryListener` / `debug.agentmirror.perftrace` 均在。
