# t.sprobe2 · 静态 alt-screen 首个 KindSnapshot 字节甄别

status=done
世界=app渲染层
快照hex=5354415449435f414c545f4d41524b45525f3039321b5b323b3148
快照含marker=yes
依据=封版 f116dd8d1 daemon 对「订阅前已冻结、capture 自证含 STATIC_ALT_MARKER_092」的隔离 pane，108ms 内发出 KindSnapshot；payload 27B = `STATIC_ALT_MARKER_092` + CUP `ESC[2;1H`。首个 binary 就是 snapshot（kind=1），不是空 CUP/清屏。Resize 之后 live pane 已空（108×96、glyph 无），说明 SIGWINCH 清屏仍发生，但首帧吃到的是 Resize 前的格子。服务端这条路径已把字形送出；t.verify 静态冷点开 >16s 全黑与「快照字节里没字」不是同一层，白屏在 app 渲染层。

## 被测物

- SHA=`f116dd8d16c7bdd9f3836bf2e22f66a53c133603`（`pr/srv-first-frame` 封版）
- `git archive f116dd8d1 server` → `.team/nodes/hl1-sprobe2/tmp/server/`，`ws_handler.go` 含 `Capture BEFORE Resize`
- 自建 `.team/nodes/hl1-sprobe2/tmp/agentmirrord`（未写产品树、未建分支、未 commit）
- listen=`127.0.0.1:22421`，`AGENTMIRROR_STATE_DIR=tmp/state`（pidfile 不在 9900 那棵）
- 生产 9900：全程 `agentmirr 30114`，本席未杀未绑

## 隔离装置（frozen-alt 同 t.verify）

- socket=`tmp/tmux/tmux-501/default` len=82；`list-sessions` 只有 `hl1sp2-static`
- 未向 `/private/tmp/tmux-501/default` 发键
- `ln -s /bin/bash tmp/bin/claude`；pane comm=`claude`（`ps -o comm=`，未取 argv）
- `source ./frozen-alt.sh`：`\033[?1049h` + marker + `trap WINCH` 只清屏不重绘
- **订阅前** `alternate_on=1`、capture 含 `STATIC_ALT_MARKER_092`、几何 80×24、cursor=0,1
  落盘：`tmp/out/pre-capture.txt` `tmp/out/pre-pane.txt`

## WS 路径（e2e/harness Client 改造：auth→list→subscribe→dump）

- 客户端：`tmp/dump/main.go`（抽到封版树 `cmd/sprobe2dump` 才能 import `internal/protocol`）
- subscribe 用手机几何 **108×96**（与 sprobe 探针同，强制 Resize/SIGWINCH）
- 超时 8s；实测 subscribe→KindSnapshot **108.4ms**

原始 payload（Data，不足 200B 则全长）：

```
00000000  53 54 41 54 49 43 5f 41  4c 54 5f 4d 41 52 4b 45  |STATIC_ALT_MARKE|
00000010  52 5f 30 39 32 1b 5b 32  3b 31 48                 |R_092.[2;1H|
```

- data_len=27 wire_len=125 first_bin_kind=1
- glyphs=`STATIC_ALT_MARKER_092`
- 预览=`STATIC_ALT_MARKER_092\e[2;1H`
- CUP `ESC[2;1H` 与订阅前 cursor row=1,col=0（1-based 2;1）吻合，不是空窗的 `ESC[1;1H`

落盘：`tmp/out/snapshot.data.bin` `tmp/out/snapshot.wire.bin` `tmp/out/result.json`

## 订阅后 live pane（对照：清屏发生在 Resize，不进首帧）

- post size=108×96 alt=1 cursor=0,0；capture 无 marker（空行）
- 落盘：`tmp/out/post-capture.txt` `tmp/out/post-pane.txt`

这排除了「冻结方式失败 / 订阅前已被 Resize 清空」：订阅前格子有 marker；首帧有同一 marker；Resize 后格子才空。sfix「先 snapshot 再 Resize 再 Subscribe」在本装置上按设计工作。pipe 接在 Resize 之后，WINCH 清屏不作为抹掉首帧的 delta 进入本测量窗口（本席只等第一帧 snapshot，未再等 delta）。

## 与前序材料对齐

| 材料 | 现象 | 本甄别 |
| --- | --- | --- |
| t.sprobe 修复前 | KindSnapshot 6B、glyphs 空、marker 不在其中（世界 B） | 修复后 payload 含 marker，世界 B 在 **WS 字节层** 已不成立 |
| t.srvperf 对拍2 | 静态 alt-screen 63ms 收到 KindSnapshot 89B | 同层：服务端 <0.2s 已推快照；89B vs 本席 27B 是冻结脚本/内容不同，不是「没发」 |
| t.verify 复验 | 装同一修复的 daemon，app 画布 >16s 全黑 | 快照字节已有字形 ⇒ 缺口在 **app 收到 alt-screen 快照之后没画出来** |

## 判定

- **有字形** ⇒ 世界=app渲染层
- 不是「server 修复不完整、快照仍是 CUP/清屏序列」
- 本席不评 app 哪一段没画（测量范围止于首个 KindSnapshot 字节）

## 纪律

- 未改产品代码、未建分支、未 git commit / push
- 临时文件只在 `.team/nodes/hl1-sprobe2/tmp/`
- 未开模拟器；未碰 9900
- 架构闭包无卡命中（BASE 原文）；本格只做字节甄别，不猜包
