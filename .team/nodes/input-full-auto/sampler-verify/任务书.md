# t.sampler.verify — 独立终审 sampler

固定审实现 worktree及两个上游报告。只写本目录 `VERDICT.md`。重跑 `sh tools/perfbase/run-input-ab.sh --self-test`；自行选破坏齿使顺序/身份/段数/阈值之一必红，恢复后绿且 porcelain 只保留实现产物。核 exit 2 不被染成1。不得改实现或旧判据。末行 `verdict: pass|fail|inconclusive`，产物齐后只 report_result。
