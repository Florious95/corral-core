# t.sampler.impl — 实现安全四段同批 sampler

在本格 worktree 只改 `tools/perfbase/run-input-ab.sh`、`tools/perfbase/parse-input-ab.py` 与本目录 `IMPL.md`。实现 `run-input-ab.sh --self-test`：机械验证 A/B/A/B 顺序、稳定 tag+参考 md5绑定、A/B不同md5、三夹具×四段、每组n>=10、事件单调、p50/p95 B/A<=1.10；量具/环境问题 exit2，真实回退 exit1，通过 exit0。实际采样必须先 envcheck，只用隔离夹具，禁止真实 tmux/会话/生产 daemon。不得改旧判据、产品码或历史基线。先记录旧入口缺口红，再转绿；测试禁缓存。产物齐后只 report_result。
