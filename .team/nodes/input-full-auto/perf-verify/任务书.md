# t.perf.verify — 独立零上下文复核性能门

固定输入：`.team/nodes/input-full-auto/perf-design/CONTRACT.md`、`.team/nodes/input-full-auto/perf-measure/perf-ab.json`、`MEASURE.md` 与原始日志。产出方自证不算。

只写 `.team/nodes/input-full-auto/perf-verify/VERDICT.md`：
1. 独立重算三夹具×四段的 n、p50、p95、B/A；
2. 核 A/B source revision 与 md5，不同包且 A 绑定稳定 tag；
3. 核 envcheck 操作数、A/B/A/B 顺序、原始 PerfTrace 与 JSON 样本可追溯；
4. 选一处破坏齿：在临时副本把一个 B 样本提高到越过 1.10，机械门必须红；还原后必须恢复，原文件不得改；
5. 任一证据缺失或不可判，不得 pass。末行固定 `verdict: pass|fail|inconclusive`。

不得修改产品码、判据、测量产物；不碰真实 tmux/真实会话/生产 daemon；required_artifacts 落盘后只 report_result。
