# t.perf.design — 冻结可执行性能基线契约

## 背景与必读
用户硬要求：性能体验最核心，任何输入透传改动不得低于 `baseline-20260822-release`。当前历史 JSON 与 tag/文档冲突：tag 同路径 JSON 是 INCONCLUSIVE/null，不能继续冒充可执行基线。必读：`CLAUDE.md` 基线章节、`.team/nodes/feat-remote-scroll-mouse-wheel/perf-attribution-20260823.md`、`tools/perfbase/` 现有脚本。

## 交付
只写 `.team/nodes/input-full-auto/perf-design/CONTRACT.md`，明确：
1. 行为基线身份=`baseline-20260822-release`，用户真机参考 APK md5=`0907d6881bb1e034ef33a49f89afaa44`；历史 null JSON仅作无效旧证据，不修改它。
2. 可执行门改用**同一时段 A/B/A/B**：A 从稳定 tag 源构建或使用精确 md5 本地件，B 为当前候选；记录两者 source revision+md5，禁止相同包。
3. 三夹具：`big_scrollback`、`real_claude_idle`、`redraw_tui`；每夹具每包至少 n=10。
4. 四段：`tap_to_route_enter`、`route_enter_to_first_frame`、`first_frame_to_first_draw`、`tap_to_first_draw`；每段 p50/p95 的 B/A 均不得超过 1.10。
5. 给出仓库既有、可执行且不触碰真实会话的采样命令和隔离边界；若现有入口无法产出上述 raw 样本，明确缺口，不猜命令。

## 硬约束与合法出口
不改产品码、判据、历史基线；不碰真实 tmux/会话/生产 daemon；不读凭据。无法唯一证明安全采样入口时，仍落盘完整冲突与建议，`contract: blocked`，如实 report_result，禁止造绿。可执行时末行写 `contract: ready`。required_artifacts 落盘后只调用 report_result，不另发消息。
