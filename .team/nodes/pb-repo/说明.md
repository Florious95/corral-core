# t.repo 说明（引用式构建暂存件）

席位：施工席 `pb-impl` / 格 `pb-repo`。worktree：`.worktrees/wt-pb-repo`。未 commit / 未 push。

## 做了什么

在 `.team/staging/corral-app/` 造独立 Gradle 工程：

- **壳**：从 t.core2（含仪表 + `addBinaryListener`）拷 `app/app` → `corral-app/app/`
- **核**：t.core2 的 `:core-protocol` `:core-terminal` `:core-conn` `:terminal` 快照到 `.team/staging/corral-core/`
- **引用**：`corral-app/settings.gradle.kts` `includeBuild("../corral-core")` + `dependencySubstitution`（`dev.agentmirror:core-*`）
- **清单**：`corral-app/迁移清单.md` 逐条列搬迁，并写明 Android 残留（本仓壳未删、核源码零 android import）

本 worktree HEAD=`ed79f179c` **没有三核**，不能 `includeBuild` 本树 `app/`。核走 t.core2 快照。已在清单写明，未谎称本树 app 已切分。

图标：未新增资源，沿用原 `res/`（093 §2）。

## 构建

```
$ cd .team/staging/corral-app && ./gradlew :app:assembleDebug --offline
```

`BUILD SUCCESSFUL in 18s`  日志：`.team/nodes/pb-repo/tmp/repo-build.log`

APK：`.team/staging/corral-app/app/build/outputs/apk/debug/app-debug.apk`

```
$ bash tools/perfbase/judge-repo.sh
```

stdout：`PASS 引用式构建成立（远端推送留给 leader）`  退出码：**0**

## 没做的

- ⛔ 未 git push、未动 mirror-pr、未碰远端 corral-app 仓
- 未从本仓删除 Android 壳（t.core 切分留下的壳仍在）
- 没开模拟器、没碰 9900
