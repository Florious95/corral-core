# t.provcmd · E14 · Provider 启动命令

分支：`pr/e14-provider-cmd`

## pwd 与 git branch --show-current

```
/Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr2.proto
pr/e14-provider-cmd
```

开工时该分支落后 main，已 `reset --hard main`（`208941659`）后再改，避免把 E14 交到旧树上。

## 改了哪些文件

write_paths 内：

- `app/app/src/main/java/dev/agentmirror/app/workspace/ProviderLaunch.kt` — `ProviderLaunch` / `buildArgv` / 出厂默认（Grok bypass 出厂 `always-approve` 旗）
- `app/app/src/main/java/dev/agentmirror/app/workspace/ProviderLaunchStore.kt` — prefs 名 `provider_launch`；缺项用默认补；Pi 的 bypassFlag 恒为空
- `app/app/src/main/java/dev/agentmirror/app/workspace/PackageDoc.kt`
- `app/app/src/main/java/dev/agentmirror/app/SettingsScreen.kt` — 接线 store + 第六张卡 UI（tag `settings-launch`）

方案要改 `ui/screens/SettingsScreen.kt`（运行时主路径），账本 write_paths 未列该文件。**显式扩写**：

- `app/app/src/main/java/dev/agentmirror/app/ui/screens/SettingsScreen.kt` — `extraCards` 插在现有五张卡之后
- `app/app/src/test/kotlin/dev/agentmirror/app/TabScrollTest.kt` — `assertIsDisplayed` 看 `settings-launch`；`lastFullyAboveTabs` 看 **settings-launch 卡内已布局最底子节点**（不再量「跟随系统」）

测试：`ProviderLaunchArgvTest.kt` `ProviderLaunchStoreTest.kt`

未改 `conn/`（本格不需要协议）。

## 先验红（改之前，原始输出）

方案 A-088-cmd：仓库无 `ProviderLaunchStore`，测试编译失败即红。

`./gradlew :app:testDebugUnitTest --tests dev.agentmirror.app.workspace.ProviderLaunchArgvTest`：

```
e: .../ProviderLaunchArgvTest.kt:13:22 Unresolved reference 'ProviderLaunch'.
e: .../ProviderLaunchArgvTest.kt:18:64 Unresolved reference 'buildArgv'.
e: .../ProviderLaunchArgvTest.kt:24:22 Unresolved reference 'ProviderLaunchDefaults'.
...
Execution failed for task ':app:compileDebugUnitTestKotlin'.
BUILD FAILED in 6s
```

A-provcmd-wiki / A-provcmd-smell **不是恒绿、不是恒真**。它们断言「相对冻结基线不新增违规」。本格当时没把 wiki 输出 tee 进 `tmp/`（tmp 里只有 `prior-red-argv.txt` / `argv-green.txt` / `A-provcmd-suite.txt`）；下面是当时终端原文。

**A-provcmd-wiki 先红两次，再修绿。**

第一次（新类型缺 T3-1 文档、T3-2 引用）：

```
红：本次引入了新的架构维基违规。⛔ 不许改判据、⛔ 不许 --freeze 洗掉，修掉新增的那几条。
pkg=dev.agentmirror.app.workspace 基线=8 本次=11 新增=2 消掉=0（键不含行号）
  ❌ 新增: T3-1 dev.agentmirror.app.workspace app/app/src/main/java/dev/agentmirror/app/workspace/ProviderLaunchStore.kt (0→2)
  ❌ 新增: T3-2 dev.agentmirror.app.workspace app/app/src/main/java/dev/agentmirror/app/workspace/ProviderLaunch.kt (0→1)
pkg=dev.agentmirror.app 基线=1 本次=1 新增=0 消掉=0（键不含行号）
pkg=internal/api 基线=3 本次=3 新增=0 消掉=0（键不含行号）
pkg=internal/protocol 基线=0 本次=0 新增=0 消掉=0（键不含行号）
EXIT:1
```

第二次（补文档后，`@inv` 里写了 `` `--always-approve` `` 被 T3-2 当未注册 CLI flag）：

```
红：本次引入了新的架构维基违规。
pkg=dev.agentmirror.app.workspace 基线=8 本次=9 新增=1 消掉=0（键不含行号）
  ❌ 新增: T3-2 dev.agentmirror.app.workspace app/app/src/main/java/dev/agentmirror/app/workspace/ProviderLaunch.kt (0→1)
EXIT:1
```

改 `@inv` 措辞后才到下面「验绿」那份 新增=0。棘轮抓住了本格引入的 T3，不是怎么跑都绿。

**A-provcmd-smell：** 棘轮本身两次都是 `基线=16 本次=16 新增=0`（EXIT:0）。第一次跑时套件里 `TabScrollTest.settingsTabScrollReachesLastControlWithoutPagerConsumingVertical` 红（smell 原文写「有真单测失败（不归本判据管）」）——那是加第六张卡后滚到底「外观」出屏，不是 smell 新增。没有「smell 先红」的日志，不能把 smell 写成先验红。

A-provcmd-doc：说明文件开工时不存在。A-provcmd-seal 由 leader 报完后跑；本席不 commit。

### TabScroll `lastFullyAboveTabs` 补牙（rev30/31）

`lastAfter` 不再用 `跟随系统`（滚到底后 bounds 0×0，`lastFullyAboveTabs` 恒真）。改为 `launchCardLastLaidOut()`：`settings-launch` 卡 children 里 `bottom>1` 的最底节点。

**先验红**（滚满后仍把 lastAfter 指回「跟随系统」，并断言 `lastBottom>1`）。`tmp/prior-red-tabscroll-bounds.txt` XML：

```
java.lang.AssertionError: 最后一项必须有真实布局（禁止 0×0 让 lastFullyAboveTabs 恒真） lastBottom=0.0 lastTop=0.0
TabScrollTest > settingsTabScrollReachesLastControlWithoutPagerConsumingVertical FAILED
BUILD FAILED
```

一次 swipe 后（卡刚进布局）最底非零子节点 bottom=455、tabs.top=460，贴住底栏；同屏「跟随系统」仍是 0×0。

**做不到（显式）：** 不改产品布局时，滚满后最后一项落在 scroll viewport 底（Pi/子节点 ≈338），tabs.top=460，`lastBottom > tabsTop+1` 不会成立——scroll 区本身停在底栏上方。无法靠缩小 h 造出「滚满仍被底栏切断」。先验红用的是「量错节点 → 0×0」这条牙。

wiki 两次先红已落盘：`tmp/wiki-ratchet-1.txt` `tmp/wiki-ratchet-2.txt`。

## 验绿（改之后，原始输出）

`ProviderLaunchArgvTest` + `ProviderLaunchStoreTest`：BUILD SUCCESSFUL。

TabScroll 补牙后 `./gradlew :app:testDebugUnitTest --tests dev.agentmirror.app.TabScrollTest`：**BUILD SUCCESSFUL**（`tmp/tabscroll-green.txt`）。滚满后 lastBottom>1 且 lastFullyAboveTabs；一次 swipe 后最底子节点贴住底栏。

断言：

- grok command=`grok-local` + bypass → `["grok-local", "--always-approve"]`；未勾 → `["grok-local"]`
- Pi 勾 Bypass 仍 `["pi"]`
- 六个 Provider 出厂表与契约 088 §4 一致
- prefs 改 grok 命令后新 store 实例读回 `grok-local`，bypass 旗仍在

A-provcmd-suite（`./gradlew -q :app:testDebugUnitTest`）：**EXIT:0**

A-provcmd-wiki：

```
pkg=dev.agentmirror.app.workspace 基线=8 本次=8 新增=0
pkg=dev.agentmirror.app 基线=1 本次=1 新增=0
pkg=internal/api 基线=3 本次=3 新增=0
pkg=internal/protocol 基线=0 本次=0 新增=0
EXIT:0
```

A-provcmd-smell：`face=app 基线=16 本次=16 新增=0 消掉=0` EXIT:0（棘轮无新增；套件 TabScroll 已在改 `TabScrollTest` 后绿）

## 行为

设置页第六张卡「Agent 启动命令」。六个 Provider 各两行：启动命令、Bypass 参数（Pi 禁用）。脚注：按空白分词，不经 shell。Grok 勾 Bypass 时 argv **必须出现** `--always-approve`，不依赖本机 config。

未 commit、未 push、未并线。模拟器手测查不清（本席未起模拟器）。
