# t.provcmd · E14 · Provider 启动命令

分支：`pr/e14-provider-cmd`

## pwd 与 git branch --show-current

```
/Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr2.proto
pr/e14-provider-cmd
```

开工时该分支落后 main，已 `reset --hard main`（`208941659`）后再改，避免把 E14 交到旧树上。

## 改了哪些文件

write_paths 内：

- `app/app/src/main/java/dev/agentmirror/app/workspace/ProviderLaunch.kt` — `ProviderLaunch` / `buildArgv` / 出厂默认（Grok bypass 出厂 `always-approve` 旗）
- `app/app/src/main/java/dev/agentmirror/app/workspace/ProviderLaunchStore.kt` — prefs 名 `provider_launch`；缺项用默认补；Pi 的 bypassFlag 恒为空
- `app/app/src/main/java/dev/agentmirror/app/workspace/PackageDoc.kt`
- `app/app/src/main/java/dev/agentmirror/app/SettingsScreen.kt` — 接线 store + 第六张卡 UI（tag `settings-launch`）

方案要改 `ui/screens/SettingsScreen.kt`（运行时主路径），账本 write_paths 未列该文件。**显式扩写**：

- `app/app/src/main/java/dev/agentmirror/app/ui/screens/SettingsScreen.kt` — `extraCards` 插在现有五张卡之后
- `app/app/src/test/kotlin/dev/agentmirror/app/TabScrollTest.kt` — 加卡后滚到底「外观」会出屏，最后一项改为 `settings-launch`（不是改 A-provcmd 判据）

测试：`ProviderLaunchArgvTest.kt` `ProviderLaunchStoreTest.kt`

未改 `conn/`（本格不需要协议）。

## 先验红（改之前，原始输出）

方案 A-088-cmd：仓库无 `ProviderLaunchStore`，测试编译失败即红。

`./gradlew :app:testDebugUnitTest --tests dev.agentmirror.app.workspace.ProviderLaunchArgvTest`：

```
e: .../ProviderLaunchArgvTest.kt:13:22 Unresolved reference 'ProviderLaunch'.
e: .../ProviderLaunchArgvTest.kt:18:64 Unresolved reference 'buildArgv'.
e: .../ProviderLaunchArgvTest.kt:24:22 Unresolved reference 'ProviderLaunchDefaults'.
...
Execution failed for task ':app:compileDebugUnitTestKotlin'.
BUILD FAILED in 6s
```

A-provcmd-wiki / A-provcmd-smell：改代码前相对 main 基线无新增（棘轮恒绿）。它们不断言启动命令存在。

A-provcmd-doc：说明文件开工时不存在。A-provcmd-seal 由 leader 报完后跑；本席不 commit。

## 验绿（改之后，原始输出）

`ProviderLaunchArgvTest` + `ProviderLaunchStoreTest` + 更新后的 `TabScrollTest`：BUILD SUCCESSFUL。

断言：

- grok command=`grok-local` + bypass → `["grok-local", "--always-approve"]`；未勾 → `["grok-local"]`
- Pi 勾 Bypass 仍 `["pi"]`
- 六个 Provider 出厂表与契约 088 §4 一致
- prefs 改 grok 命令后新 store 实例读回 `grok-local`，bypass 旗仍在

A-provcmd-suite（`./gradlew -q :app:testDebugUnitTest`）：**EXIT:0**

A-provcmd-wiki：

```
pkg=dev.agentmirror.app.workspace 基线=8 本次=8 新增=0
pkg=dev.agentmirror.app 基线=1 本次=1 新增=0
pkg=internal/api 基线=3 本次=3 新增=0
pkg=internal/protocol 基线=0 本次=0 新增=0
EXIT:0
```

A-provcmd-smell（改卡后、改 TabScrollTest 前曾见套件红 `TabScrollTest`，棘轮本身 16=16；修测试后套件全绿）：`face=app 基线=16 本次=16 新增=0`

## 行为

设置页第六张卡「Agent 启动命令」。六个 Provider 各两行：启动命令、Bypass 参数（Pi 禁用）。脚注：按空白分词，不经 shell。Grok 勾 Bypass 时 argv **必须出现** `--always-approve`，不依赖本机 config。

未 commit、未 push、未并线。模拟器手测查不清（本席未起模拟器）。
