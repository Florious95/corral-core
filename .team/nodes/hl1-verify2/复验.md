# t.verify2 · 模拟器终验 r60（两修复合体 daemon）

status=done
结果=stillblank
hist首帧=>16
截图=.team/nodes/hl1-verify2/shot-claude-cold16-firstframe.png, .team/nodes/hl1-verify2/shot-redraw-cold16-firstframe.png, .team/nodes/hl1-verify2/shot-hist-cold16-end.png, .team/nodes/hl1-verify2/shot-l2-claude.png, .team/nodes/hl1-verify2/shot-l2-redraw.png, .team/nodes/hl1-verify2/shot-l2-hist.png, .team/nodes/hl1-verify2/shot-01-pairing.png, .team/nodes/hl1-verify2/shot-03-l1.png

## 开工自检

```
PWD=/Volumes/nvme/Projects/远程Agent安卓/.worktrees/hl1.verify
BRANCH=（空：detached）
HEAD=cb044e6d9acb5567e732bed2cafbf4b36dce99b7
```

本格不建分支、未 commit、未 push、未碰仓根工作区。未自己解冲突。

## 被测物

- daemon **两修复合体**（tmp 内 clone，不碰仓根）：
  - `git clone --no-hardlinks <仓根> tmp/merged`
  - `git checkout pr/srv-first-frame` → b80bc2e14（含 f116dd8d1）
  - `git merge --no-edit origin/pr/listing-ps-storm`（clone 里本地名 `pr/listing-ps-storm` 不存在，用 origin/ 同 SHA `8272ec1ff`；**无冲突**，ort auto-merge `ws_handler.go`）
  - merge HEAD=`26ab60a25`
  - `go build -o ../../agentmirrord-merged` sha256=`39491eb1265aa5062ac1f5dba6f102805c462c9a77f0d0faf7599b898b886157`
- app 不变：pr/app-altsnap-render `f5f6c4ea6` afix assembleDebug

## 夹具（同 r58 三真实世界）

① cwd-claude 真 claude alt=1 空闲；② cwd-redraw WINCH 清屏+重绘 marker；③ cwd-hist=39983。
隔离 socket 82B；daemon 0.0.0.0:22371 + TMUX_TMPDIR。只点隔离 cwd。

## 计秒表

量具同前（画布 y=320..2000，color≥30 且 dark≥45%）。程序尺会把 copy-mode 色块当「有字」，下表眼看为准。

| 世界 | 冷点开程序尺 | 2s 内 | 眼看 | 2s 门 |
|---|---|---|---|---|
| ① 真 claude | **3.517s** FIRST_FRAME | t=1.897 仍亮页（L2） | shot-claude-cold16-firstframe.png **有** Claude Code TUI | **红**（>2s） |
| ② 会重绘 TUI | 15.804s 尺响（color=138） | t=2.036 亮页；其后长期 color=0 | shot-redraw-cold16-firstframe.png 仅 copy-mode 徽章，**无** REDRAW_ALT_MARKER | **红** |
| ③ 大滚回 | 16s 无 FIRST_FRAME | t=1.727 过渡色；其后 color=0 | shot-hist-cold16-end.png **画布全黑** | **红** hist首帧=>16 |

①② 冷点开都未进 2s 门（② 眼看甚至无正文）→ 按信封 **结果=stillblank**。未硬编。

## 步骤

1. pairing。shot-01-pairing.png / shot-03-l1.png（cwd-claude/hist/redraw 均在）
2. force-stop 冷启 → 只点各隔离工作区 → 对应行。shot-l2-*.png
3. BACK 再进、HOME 回前台（本轮再进/回前台多数仍空或仅 copy-mode）

## 未做

未点舰队。未起/杀模拟器。未 git commit / push。merge 无冲突故未报 blocked。
