# t.e8 · E8 · 侧滑关列表而不是退出会话（契约 088）

## 开工自检

```
$ pwd
/Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr2.listui

$ git branch --show-current
pr/e8-back-gesture
```

仓根在 `main`（开工时误在仓根 `git checkout -b pr/e8-back-gesture`，已立刻切回 `main` 并删掉那条错位置分支，再只在本 worktree 重建）。

- 分支名：`pr/e8-back-gesture`
- HEAD sha：`094c321da4ac57d944762a29237c15328b08bd83`
- 本席 ⛔ 不 commit / push / 不建别的分支 / 不切到别的分支。

## 改了哪些文件

- `app/app/src/main/java/dev/agentmirror/app/ui/components/SessionSwitchSheet.kt` — 方案 §2：`visible==true` 时挂 `BackHandler { onDismiss() }`；补外骨骼。Compose BackHandler 后进先出，sheet 先于根处理器收到系统返回 / 预测性返回 / 侧滑。
- `app/app/src/test/kotlin/dev/agentmirror/app/session/OverlayBackClosesSheetNotSessionTest.kt` — 方案判据 A-088-back。测试目录不在 write_paths，但 `A-e8-suite` 跑全量 gradle 单测，不写这里判据落不了地。显式报出。

**未改** `WorkspaceScreen.kt`、`MainNavState.kt`。方案原文：「不要改 `MainNavState.onSystemBack` 的优先级表（那是「不在 sheet 时」的栈）」。账本 write_paths 列了这两文件，与方案冲突 → 照方案做，并在此报出。

知识基底「闭包无卡命中」：实现按方案现读路径 `SessionSwitchSheet.kt`，未猜新包。

## 修法

`SessionSwitchSheet` 组合里：

```
BackHandler(enabled = visible) { onDismiss() }
```

`SessionScreen` 在 `overlayOpen` 时把 `onDismiss` 接到 `viewModel::closeOverlay`。一次返回只关 sheet，不调用 `MainNavState.onSystemBack`，不 finish Activity。`visible=false` 时 `enabled=false`，事件落到根处理器。

## 先验红（改产品码之前）

先写 `overlayOpen_systemBack_closesSheetKeepsSession`：根 `BackHandler` 接 `onSystemBack`（仿 `AgentMirrorApp`），打开 sheet 后 `onBackPressedDispatcher.onBackPressed()`，断言 sheet 关且 `activeSession` 仍在。当时 sheet 无 BackHandler。

```
$ cd /Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr2.listui/app
$ env -u TEAM_AGENT_API_KEY ./gradlew :app:testDebugUnitTest --tests 'dev.agentmirror.app.session.OverlayBackClosesSheetNotSessionTest'

OverlayBackClosesSheetNotSessionTest > overlayOpen_systemBack_closesSheetKeepsSession FAILED
    java.lang.AssertionError at OverlayBackClosesSheetNotSessionTest.kt:91

1 test completed, 1 failed
FAILURE: Build failed with an exception.

报告原文：
java.lang.AssertionError: 一次系统返回必须关 sheet
	at dev.agentmirror.app.session.OverlayBackClosesSheetNotSessionTest.overlayOpen_systemBack_closesSheetKeepsSession(OverlayBackClosesSheetNotSessionTest.kt:91)
```

失败点是 `h.vm.overlayOpen` 仍为 true。后面「会话仍在栈顶 / session-topbar」断言因短路没跑到，当时会话是否被弹出 **查不清**。修后两条都绿。

## 验绿

### A-088-back / OverlayBackClosesSheetNotSessionTest

```
$ env -u TEAM_AGENT_API_KEY ./gradlew :app:testDebugUnitTest --tests 'dev.agentmirror.app.session.OverlayBackClosesSheetNotSessionTest'
BUILD SUCCESSFUL in 6s
```

报告：`overlayOpen_systemBack_closesSheetKeepsSession` passed（0.148s）。
`overlayOpen==false`，`session-overlay` DoesNotExist，`activeSession != null`，`session-topbar` Exists。未 finish Activity。

### A-e8-suite

```
$ cd /Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr2.listui/app
$ env -u TEAM_AGENT_API_KEY ./gradlew -q :app:testDebugUnitTest
# 无输出，exit 0
# 日志 /Volumes/nvme/Projects/远程Agent安卓/.team/acclogs/A-e8-suite.log 0 字节（-q）
```

### A-e8-wiki

```
$ python3 tools/gate/wiki-ratchet.py --root /Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr2.listui --pkg dev.agentmirror.app.workspace
pkg=dev.agentmirror.app.workspace 基线=8 本次=8 新增=0 消掉=0
exit 0
```

本格未改 workspace 包。改前未单独跑 wiki → 改前是否同数 **查不清**（棘轮相对冻结基线，不是功能先验红）。未 `--freeze`。

### A-e8-smell

测试初稿在组合里读 `level2.value`，棘轮红过一次：

```
face=app 基线=16 本次=17 新增=1 消掉=0
  ❌ 新增: lint:StateFlowValueCalledInComposition:.../OverlayBackClosesSheetNotSessionTest.kt:76
```

把列表快照挪出 `setContent` 后再跑：

```
$ python3 tools/gate/smell-ratchet.py --face app --root /Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr2.listui
face=app 基线=16 本次=16 新增=0 消掉=0
exit 0
```

未修存量 16 条。未 `--freeze`。

### A-e8-doc

本文件。`A-e8-seal` 不在本席跑。

## 查不清

- 模拟器：`emulator-5554` 已在设备列表里，本格不抢装包、无修前/修后手势截图。机械判据不含模拟器。
- 先验失败短路，未记录当时 `activeSession` 原始值。
- 架构维基对本格 write_paths 无卡命中（知识基底原文）。
- 硬约束禁止 commit / amend。`main..pr/e8-back-gesture` 现有 1 个 commit `094c321da`，**标题误写** `[pr/e7-scroll-hint]`（r1 封版切错分支的遗留），**内容是 E8 三文件**（SessionSwitchSheet BackHandler + OverlayBackClosesSheetNotSessionTest + 本说明）。本席不改写该 commit。

## r2（2026-08-21）

账本 rev2：只做 E8，不许提前做方案 §1 后面的 PR，不许建/切别的分支。

本回合：

```
$ pwd
/Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr2.listui

$ git branch --show-current
pr/e8-back-gesture
```

- 未建 `pr/e7-scroll-hint` 或任何其他 PR 分支；`git rev-parse pr/e7-scroll-hint` 现为 `fatal: Needed a single revision`。
- 未改 SessionSwitchSheet 以外的产品码；未做 E7 滚动/渐隐。
- 未改 `MainNavState.kt` / `WorkspaceScreen.kt`（方案禁止改 onSystemBack 优先级表）。
- 产品码已在本分支：`BackHandler(enabled = visible) { onDismiss() }`。
