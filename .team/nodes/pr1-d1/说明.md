# t.d1 · userBlockBg 从当前主题现算（契约 089 §1）

## 自检

```
$ pwd
/Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr1.d1

$ git branch --show-current
pr/d1-userblock-theme
```

- 分支名：`pr/d1-userblock-theme`
- commit sha：`ee4102b3cc37817b5f7a5005f427362df88d07ba`
- 未 push（本仓无 remote，PR 由 leader 代开）

## 改了哪些文件

**产品（write_paths 内）**

- `app/app/src/main/java/dev/agentmirror/app/ui/theme/TermPalette.kt`
  - `assembleSlot()`：`userBlockBg` 不再取 `app.userBlockBackground`
  - 现算：优先 scheme `selection`（对 `defaultFg` 对比度 ≥ 3.0），否则 `defaultBg` 朝 `defaultFg` 抬一档（1/6 插值）
- `app/app/src/main/java/dev/agentmirror/app/ui/theme/PackageDoc.kt`（新建，包级 KDoc + `@consumes`）
- 同包外骨骼注释补齐（A-d1-wiki `--strict-t3` 先验即红，不补过不了机械门）：
  - `DesignTokens.kt` / `Theme.kt` / `TermThemeStore.kt` / `TerminalSpec.kt` / `TermSchemes.kt`（仅注释；`--sha` 改写成 `sha=` 以免 T3-2 当 CLI flag）

**判据（write_paths 未列 test/，但 A-d1-suite 是全量单测，A-d1-theme / A-d1-esc 必须进套件）**

- `app/app/src/test/kotlin/dev/agentmirror/app/ui/theme/UserBlockThemeTest.kt`（新建）
- `app/app/src/test/kotlin/dev/agentmirror/app/termview/TermRemapTest.kt`：浅纸 luma 例外名单改为空（APP 常量例外已失效；退回 APP 常量会再红）
- `app/app/src/test/resources/remap-gold-before.txt`：金样里的 `FFE6F5F2` / `FF10241F` 换成主题派生块色（否则等价性金样钉死旧逃逸值）

**未改**

- `colorFor` / `guardRgbBg` / `indexed` 分支结构（D2 不动）
- `asTerminalPalette()` 仍把 Compose 层 `userBlockBackground` 交给 APP 常量。`colorFor` 漏斗已封；Compose 外壳是否跟主题不在本格。

## 方案落实

1. 优先 `TermSchemeColors.selection`
2. `selection == null` 或与 `defaultFg` 的相对亮度对比度 `< 3.0`（与漏斗 `CONTRAST_MIN` 同值）→ `liftTowardFg(defaultBg, defaultFg)`，通道插值 `t = 1/6`

## 判据

### A-d1-theme 先验红（改代码前跑 `UserBlockThemeTest`，2 tests failed）

原始输出（`TEST-dev.agentmirror.app.ui.theme.UserBlockThemeTest.xml`）：

```
java.lang.AssertionError: vesper Indexed(254) 必须等于该主题 scheme 派生值 (selection=0xFF988049 lift=0xFF373737) expected:<-6782903> but was:<-15719393>
```

`-15719393` = `0xFF10241F`（APP 深色外壳常量）。Vesper 应得 `0xFF988049`（该主题 selection）。世界未变：三个主题都会钉在同一 APP 绿。

### A-d1-esc 先验红（同一跑次）

断言的是「逃逸路径已封死」，派生集合从 scheme **独立**算出（background/foreground/cursor/ansi16/selection/lift），**不读** `Scheme.userBlockBg`。穷举 256 索引 + 抽样真彩。先验命中 APP 常量：

```
java.lang.AssertionError: colorFor 逃出主题派生集合:
vesper dark=true Indexed(157) bg=true -> 0xFF10241F
vesper dark=true Indexed(158) bg=true -> 0xFF10241F
vesper dark=true Indexed(159) bg=true -> 0xFF10241F
vesper dark=true Indexed(191) bg=true -> 0xFF10241F
vesper dark=true Indexed(192) bg=true -> 0xFF10241F
vesper dark=true Indexed(193) bg=true -> 0xFF10241F
vesper dark=true Indexed(194) bg=true -> 0xFF10241F
vesper dark=true Indexed(195) bg=true -> 0xFF10241F
vesper dark=true Indexed(223) bg=true -> 0xFF10241F
vesper dark=true Indexed(224) bg=true -> 0xFF10241F
vesper dark=true Indexed(225) bg=true -> 0xFF10241F
vesper dark=true Indexed(226) bg=true -> 0xFF10241F
vesper dark=true Indexed(227) bg=true -> 0xFF10241F
vesper dark=true Indexed(228) bg=true -> 0xFF10241F
vesper dark=true Indexed(229) bg=true -> 0xFF10241F
vesper dark=true Indexed(230) bg=true -> 0xFF10241F
```

近白立方索引走 `guardRgbBg` 的 `luma ≥ 220` 分支，出口是写死的 `userBlockBg=0xFF10241F`，不属于 Vesper 派生集合。条件已造出（不是恒真）。

### A-d1-theme / A-d1-esc 验绿

```
$ ./gradlew -q :app:testDebugUnitTest --tests dev.agentmirror.app.ui.theme.UserBlockThemeTest
EXIT_A_D1_TESTS:0
suite tests= 2 failures= 0 time= 3.787
PASS aD1EscNoColorForEscapeFromSchemeDerivedSet 3.758
PASS aD1ThemeIndexed254DiffersAcrossThreeThemesAndIsSchemeDerived 0.029
```

vesper / dracula / catppuccin 深槽 `Indexed(254)` 互不相同且等于各自 selection 或抬档。

### A-d1-suite 验绿

```
$ { cd .../.worktrees/pr1.d1/app && env -u TEAM_AGENT_API_KEY ./gradlew -q :app:testDebugUnitTest ; }
SUITE_EXIT:0
xml totals tests=636 failures+errors=0
```

`-q` 成功时 acclog 为空（0 行）。这是 gradle quiet，不是没跑。

### A-d1-wiki 验绿

```
== 架构维基判据检查（--check）==
扫描 25 包，64 条依赖边
T1-1                  : PASS — 无环（10 个 Go 包）
T1-2                  : PASS — Go 10 包、Kotlin 15 包均有模块 doc

== T3 判据（单包 dev.agentmirror.app.ui.theme，严格（计入退出码）） ==
T3-1                  : PASS — 符号级 doc 覆盖：无违规
T3-2                  : PASS — 引用真实性：无违规
T3-3                  : PASS — 契约标签完备：无违规
T3-4                  : PASS — 跨层声明一致：无违规
结果：通过（exit 0）
```

A-d1-wiki **改之前即红**（T3-1 12 条缺 KDoc、T3-2 `--sha`、T3-3 缺 `@inv`、T3-4 缺 `@consumes diag/terminal`）。那些不是 D1 功能缺陷，是本包 strict-t3 机械门；只补注释，不改行为。

## 明确未修

修完 D1 **不会**让 Grok 变回黑底。绿色会变成主题派生的用户块色。整屏被染色是 D2，由 `t.meter` 补仪表。本格未动 `guardRgbBg` / `indexed` 的分支选择。

### A-d1-smell 验绿（账本 rev3 增量棘轮）

```
$ python3 tools/gate/smell-ratchet.py --face app --root /Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr1.d1
face=app 基线=16 本次=16 新增=0 消掉=0
exit 0
```

判据是「不许新增」不是「必须为 0」。本格未新增 lint；未修 main 上 16 条存量。

## 查不清

- Compose `asTerminalPalette().userBlockBackground` 是否也要跟主题：契约点名的是 `assembleSlot` / `colorFor`。本格没改 Compose 外壳那条。
- 「抬一档」没有给出插值比例的数字；实现用 1/6 通道插值，与单测同式。若应走 OKLab L 或别的步长，判据会继续绿（只要 selection 对比度达标就不用抬档）。三个取样主题的 selection 对比度均达标。
