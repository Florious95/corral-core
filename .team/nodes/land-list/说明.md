# t.list · 列表三屏落位 + 082 跨工作区收藏取数

账本 `ledger.land.v1` / `t.list` r6。施工在**仓根**（信封最高优先级：判据 cwd 是仓根；忽略派单后半段 worktree）。未 `git worktree add`，未 `git checkout` / `restore`，未 commit，未给 leader 发消息。

## 做了什么

① 换壳（业务逻辑仍在 VM / LaunchedEffect / 下拉刷 / 进二级订阅）：
- 一级列表 → `WorkspaceListScreen`
- 二级全屏 → `SessionListScreen`（悬浮窗「查看」仍走 `L2SessionList` → `SessionListRows`，避免把页头塞进 overlay）
- 收藏页 → `FavoriteList` 调用 `FavoritesScreen`

② 三态：`StatusChip` 已是 Busy 绿脉冲 / Idle 灰 / Unknown 红（t.base）。列表行真的画出「进行中 / 空闲 / 未知」。

③ 收藏行顺序：星（行首）→ 标题 → 目录副标题 → 右侧状态标。显示名走 076 §3a，不预截断。

④ 082：`WorkspaceViewModel.enterFavorites()` 按收藏项覆盖的**每个工作区**串行 `subscribeLevel2`（服务端每连接只绑一个 workspace）。进入收藏页这一次事件触发，不周期轮询（061）。`favorite_workspaces` 与 `fetched_workspaces` 两边都记进 DiagLog。live 对账改读各工作区缓存，不再只认最近进过的那一个。修好后失联快照不再挡住 076 §3a 显示名。

## 探针（ui-check.sh）

改前红（两份）：

1. **现场** `.team/issues/shots/09-收藏页不刷新误判不在线.png`：两行 `claude_code` 标「不在线」，只有最近工作区的 `team-leader-2` 空闲。
2. **单测先验** `landListFavoriteFetchCoversEveryWorkspaceWithoutEnteringLevel2`：三个工作区各收藏一行、**不** `enterLevel2`，`favoriteRows()` 在线数=0、`fetched_workspaces=0`、显示名回落 `claude_code`。

本席第一次跑 ui-check 也红（导航没点到收藏，仍停在一级「工作区」，没有状态文案）——尺子当时还在测「有没有进收藏页」。修好返回钮解析（设计把「‹」和「工作区」拆成两个 Text）后再跑。

改后绿：

```
$ bash .team/nodes/land-list/ui-check.sh
SCREEN: ['收藏', '3 SESSIONS · 1 ACTIVE', 'LAN', '★', 'leader', …, '空闲', '★', '远控 leader', …, '空闲', '★', 'team-leader-2', …, '进行中', '收藏', '会话', '设置']
A-fv-online offline=0 online_status=3 names=['远控 leader', 'team-leader-2', 'leader']
PASS t.list land-list
```

A-ls-ui：三个互异显示名；每行有状态文案；底栏 收藏/会话/设置。
A-fv-online：offline=0 且 online_status=3（不是只绿一行）。
A-fv-name-offline：无 `claude_code`。
截图：`.team/nodes/land-list/shot-favorites.png`。UI 树：`ui-tree.xml`。`trap` 关输入法。

## 单测

`./gradlew :app:testDebugUnitTest --tests '*LandList*'` 4/4：
- `landListTokensMatchDesignHandoff`（尺寸/灯色对照设计源）
- `landListRendersThreeStatusLabelsAndDoesNotPreTruncateChineseName`
- `landListFavoriteRowIsStarTitlePathStatus`
- `landListFavoriteFetchCoversEveryWorkspaceWithoutEnteringLevel2`（覆盖工作区数=3 且取过数=3）

全量 `./gradlew :app:testDebugUnitTest` rc=0。

## 未动

会话页外壳 / 设置页 / 底栏设计版（t.term / t.set）。`app/build.gradle.kts`。未碰用户 tmux。未扫 argv。
