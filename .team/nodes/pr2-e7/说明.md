# t.e7 · E7 · 列表超一屏可滚 + 「下面还有」提示（契约 088）

## 开工自检

```
$ pwd
/Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr2.listui

$ git branch --show-current
pr/e7-scroll-hint
```

仓根在 `main`。worktree 开工时停在 `pr/sort-label`（同 sha），已切到本格分支 `pr/e7-scroll-hint`。未新建其它 PR 分支，未做方案 §1 后续条目。

- 分支名：`pr/e7-scroll-hint`
- HEAD 开工 sha：`cdf9db4b80b9c1ad83dbc50d0dbf48e2e8ab6498`（其上已有 E8 封版提交）
- **未提交**：本格硬约束禁止 `git commit` / `push`。`A-e7-seal` 由 leader 在报完后提交。

## 改了哪些文件

- `app/app/src/main/java/dev/agentmirror/app/ui/components/SessionSwitchSheet.kt` — 方案 §3：外层 Column `heightIn(max=70%屏高)`；行容器改 `LazyColumn`（tag `session-overlay-list`）；`canScrollForward` 时底部 24dp 渐隐（tag `session-overlay-more-fade`）；`StaggeredRow` 只对首屏行。`screenHeightDp` 按方案使用，lint `ConfigurationScreenWidthHeight` 在函数上 `@SuppressLint`（不是改判据）。
- `app/app/src/test/kotlin/dev/agentmirror/app/session/SessionSwitchSheetScrollHintTest.kt` — 方案判据。测试目录不在 write_paths，但 `A-e7-suite` 跑全量单测。显式报出。

**未改** `L2SessionList.kt`、`WorkspaceScreen.kt`。账本 write_paths 列了这两文件，方案 §3 写的是 `SessionSwitchSheet.kt`（「查看」浮层）。照方案做，并在此报出。

知识基底「闭包无卡命中」：实现按方案现读路径，未猜新包。

## 修法

20 条时列表高度封在 70% 屏高内，`LazyColumn` 可滚。未滚到底 `session-overlay-more-fade` 存在；滚到最后一项后节点不存在。空列表不断言 fade（恒真禁止）。

## 先验红（改产品码之前）

先写 `twentyItems_listScrolls_fadeUntilBottom`：20 条 `SessionItem`，断言 `session-overlay-list` 有滚动、未尽时 fade 存在、滚到底 fade 消失。当时行容器是 `Column`+`forEachIndexed`，无 list tag、无 fade。

```
$ cd /Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr2.listui/app
$ env -u TEAM_AGENT_API_KEY ./gradlew :app:testDebugUnitTest --tests 'dev.agentmirror.app.session.SessionSwitchSheetScrollHintTest'

SessionSwitchSheetScrollHintTest > twentyItems_listScrolls_fadeUntilBottom FAILED
    java.lang.AssertionError at SessionSwitchSheetScrollHintTest.kt:63

1 test completed, 1 failed

报告原文：
java.lang.AssertionError: Failed to assert the following: (ScrollBy is defined)
Reason: Expected exactly '1' node but could not find any node that satisfies: (TestTag = 'session-overlay-list')
```

## 验绿

### SessionSwitchSheetScrollHintTest

```
$ env -u TEAM_AGENT_API_KEY ./gradlew :app:testDebugUnitTest --tests 'dev.agentmirror.app.session.SessionSwitchSheetScrollHintTest'
BUILD SUCCESSFUL in 6s
```

20 条：`hasScrollAction()`；未滚到底 fade Exists；`performScrollToIndex(19)` 后 fade DoesNotExist。

回归：`OverlayDismissOnOutsideTapTest` / `OverlayMenuTest` / `OverlayBackClosesSheetNotSessionTest` BUILD SUCCESSFUL。

### A-e7-suite

```
$ cd /Volumes/nvme/Projects/远程Agent安卓/.worktrees/pr2.listui/app
$ env -u TEAM_AGENT_API_KEY ./gradlew -q :app:testDebugUnitTest
# 无输出，exit 0
```

### A-e7-wiki

```
$ python3 tools/gate/wiki-ratchet.py --root .../.worktrees/pr2.listui --pkg dev.agentmirror.app.workspace
pkg=dev.agentmirror.app.workspace 基线=8 本次=8 新增=0 消掉=0
exit 0
```

未改 workspace 包。未 `--freeze`。改前 wiki 是否同数 **查不清**（未在改产品码前跑）。

### A-e7-smell

产品码初稿用 `LocalConfiguration.screenHeightDp`（方案原文），棘轮红过一次：

```
face=app 基线=16 本次=17 新增=1 消掉=0
  ❌ 新增: lint:ConfigurationScreenWidthHeight:.../SessionSwitchSheet.kt:88
```

按方案保留该 API，函数加 `@SuppressLint("ConfigurationScreenWidthHeight")` 后再跑：

```
face=app 基线=16 本次=16 新增=0 消掉=0
exit 0
```

未修存量 16 条。未 `--freeze`。

### A-e7-doc

本文件。`A-e7-seal` 不在本席跑。

## 查不清

- 模拟器未装本格包、无超一屏手势截图。机械判据不含模拟器。
- 架构维基对本格 write_paths 无卡命中（知识基底原文）。
- 首屏行数用 `70%屏高 - chrome` / 行高估算，chrome 里标题行用了 48dp 估值，不是实测 header 高度。
