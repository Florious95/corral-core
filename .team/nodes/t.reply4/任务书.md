# 任务书 · t.reply4 · 给桌面端 4 条架构问题写精确答复（读两边源码）

## 一、背景（读这段就够）

下游 `tmux桌面端::corral-desktop`（AgentMirror macOS 桌面客户端，**对我方只读**）镜像宽主机
（tmux pane 235 列）的 Agent CLI 时**部分会话画面错乱**（框线断裂、整屏下移），
同一会话时好时坏，切走再点回来有时又好。

**他们已经翻过一次车**：基于一个**从未验证的前提**（「`capture-pane -e -p` 的一行 = 网格一行」）
在客户端把每个捕获行裁到 `term.cols`，结果**把用户所有会话都搞错乱了**，整条回退。

他们撤回了原先的 8 条下沉问题，改问 4 条抽象层问题（原文在第三节），并撤回了
「50ms 目标在现有协议下不可达」这个结论。

**关键前提，⛔ 不许搞混**：他们的错乱是**持续存在的独立问题**。
我方用户 2026-08-23 撞过一次白屏，根因是**环境**（死 tmux socket 让 daemon 空转烧 CPU），
**那是另一回事**。⛔ 不许在答复里把两件事混为一谈，也⛔ 不许拿我方那次事故去解释他们的错乱——
会把他们引到一个跟他们无关的方向。

## 二、已经查清的一条（⛔ 不必重查，但要在答复里用上）

`.team/nodes/t.q8/答复.md` 的 Q5 已经**源码确定**我方一个缺陷：

- `server/internal/bridge/stream.go:14-16` 包注释：`One active pipe per pane (tmux's own limit):
  a new Subscribe replaces any previous pipe`
- `stream.go:132-144`：新订阅者**先无参 `pipe-pane` 拆掉别人的**再挂自己的
- `ws_conn.go:534-555`：被顶掉的那个**零通知**（只 `teardownSubscription`，不 `sendError`）
- 且第一个 relay 退出时对**整个 pane** 再拆一次，**可能误伤第二个订阅者**

**这条能解释「某一端画面停住 / 白屏后不再更新」，⛔ 但不能解释「框线断裂 / 整屏下移」这种错乱。**
答复里要把这个界划清楚。我方已立格在修（`ledger.fanout.v1`）。

## 三、他们真正问的 4 条（原文，⛔ 不许改写）

**①** 打开会话时，你们**等不等**首帧快照？
「我是『点击 → subscribe → 等 snapshot → 渲染』，所以一个往返直接吃满预算。
你们是先拿本地缓存/上一次的画面立刻上屏、再用新帧收敛？还是别的？」

**②** reshape 期间你们**怎么不撕**？
「我方现象：订阅时把 pane 从 235 拉到我的宽度，这期间的帧会错位（框线断裂、整屏下移）。
你们同样会 reshape，却『展示出来就是排布好的』——是**避开**了在 reshape 未稳时渲染，
还是**渲染方式**本身不受它影响？」

**③** 客户端拿什么宽度渲染？
「我是按容器像素算出 cols 再申报给主机，于是我和主机在互相追。
你们是**跟随主机几何**（客户端自适应缩放/滚动），还是**主机跟随客户端**？」

**④** 你们判断「排布正确」的标准是什么？
「我为此造了一套判定器 + 埋点日志，绕了很久也没做到 100% 区分。
你们如果有更简单的判据（哪怕是『不需要判据，因为结构上不会错』），那才是我该学的。」

他们的原话：**「我不要你们的工时，只要一个方向。哪怕回一句『去读 `<某个文件>`』我就自己去读。」**

## 四、你要读的两边

### 我方（写答案的依据）

- `app/app/src/main/java/dev/agentmirror/app/perf/PerfTrace.kt` —— **打开链路的八事件契约**。
  ⚠️ 特别注意 `geom_seed`（`PerfTrace.kt:95`）**排在 `subscribe_sent` 之后、`first_frame_recv` 之前**，
  带 `rows=` `cols=` 操作数。**这可能正是①②③的答案**——我方在首帧到达之前就把几何定下来了。
  **但这是 leader 从事件名与注释读出的方向，⛔ 不是结论。你要去找真正的调用点与实现，用代码说话。**
- `app/core-conn/src/main/java/dev/agentmirror/app/conn/ConnectionManager.kt` —— 订阅与分发
- `app/core-terminal/` —— 终端仿真、`replaySnapshot` / `feed` / `snapshot`
- `app/app/src/main/java/dev/agentmirror/app/session/SessionViewModel.kt`
- `app/app/src/main/java/dev/agentmirror/app/termview/TermSurfaceView.kt` —— 绘制与视口
- `server/internal/api/ws_handler.go`、`server/internal/bridge/` —— 服务端 resize/capture 时序

### 他们的（⛔ 只读，不许改一个字）

- `/Volumes/nvme/Projects/tmux桌面端/src/term/TerminalView.js` —— 渲染/订阅主逻辑
- `/Volumes/nvme/Projects/tmux桌面端/src/vendor/agentmirror/client.js` —— WS 客户端（协议 v1）
- `/Volumes/nvme/Projects/tmux桌面端/src/components/terminal/TerminalPane.jsx`
- `/Volumes/nvme/Projects/tmux桌面端/docs/CLIENT-CONTRACT.md`、`docs/UI-SPEC.md`
- `/Volumes/nvme/Projects/tmux桌面端/.team/nodes/garble/`（昨夜调查产物、两轮 550 行埋点 JSONL）

⚠️ **读之前先确认你读的是哪个状态**：他们的 `main` = `ca1f54c`，**里面仍带着那个把所有会话
搞错乱的裁行改动**；工作树里 `src/` 已退回 `6aa5921`。
**答复里引用他们代码时必须标明「工作树」还是「main」**——否则会指导到一份别人 clone 不到的代码上。
（顺带：如果你发现他们 `main` 上那个改动确实还在，在答复里提醒他们先把回退 PR 提了。）

## 五、交付物

**`.team/nodes/t.reply4/答复.md`** —— 直接可投递的答复稿。结构：

```
## 0. 先纠正一件事
（他们撤回「50ms 不可达」的理由是「你们做到了」。请写清：我方确实有 <50ms 的路径，
 但**不是因为协议更快**，而是因为 <机制>。⛔ 不许让他们以为我方客户端天生不撕。）

## ① 等不等首帧
**结论** / **置信度**（源码确定|需实测|查不出） / **证据**（我方 文件:行号 + 代码原文）
/ **你们该怎么改**（指到他们的 文件:行号）

## ② reshape 不撕
（同上四段）

## ③ 用什么宽度渲染
（同上四段）

## ④ 「排布正确」的判据
（同上四段；若我方答案是「结构上不会错，所以不需要判据」，要说清结构是什么）

## ⑤ 顺带：我方一个已坐实的缺陷（Q5）
（一条 pipe 单槽 + 零通知 + 拆除误伤；能解释什么、⛔ 不能解释什么；我方已在修）

## ⑥ 我方答不出的
（老实列。⛔ 这一节不许空着——真的每条都源码确定才可以写「无」）
```

外加 **`.team/nodes/t.reply4/说明.md`**：你读了哪些文件、哪几条卡住、为什么。

## 六、硬约束

- ⛔ **不许改我方任何产品码**；⛔ **不许改他们仓里任何文件**（只读）。
- ⛔ 不 commit / 不 push；⛔ 不改任何 `judge-*.sh`。
- ⛔ **不许 `team-agent send` 给对方或 leader**——对外投递是 leader 的动作，你只写稿。
- ⛔ 临时文件只写 `.team/nodes/t.reply4/tmp/`；⛔ 不读凭据；⛔ 无过滤 `ps aux`。
- ⛔ 不碰 9900 生产 daemon、⛔ 不碰用户真实 tmux、⛔ 不开模拟器。
- **每条结论标置信度**；`源码确定` 必须带 `文件:行号` 且行号真实存在。

## 七、合法出口

- 我方某条的机制**源码里定不下来** ⇒ 标 `需实测`，写清要测什么才能定。
- 连该看哪都定不下来 ⇒ 标 `查不出`。
- 你读完认为 **leader 关于 `geom_seed` 的方向判断不成立** ⇒ **照实写**，
  `status=direction_disputed`。⛔ 不许因为任务书这么提示就顺着编。
- **判不出是合法终态。**

## 八、收工

产物落盘后 `report_result`，`status` 二选一：`done` / `direction_disputed`。
⛔ 不许发消息。我从产物里读。
