# 说明 · t.reply4 · 我读了什么、哪几条卡住、为什么

## 一、结论摘要

- 四条主问题：**①②③④ 全部 `源码确定`**（结论层面）。
  ⚠️ 但**每一条里都嵌了一个我标注为 `需实测` 的子断言**，见 ⑥，⛔ 不要把「四条都源码确定」
  读成「四条都没有不确定性」。判据本身也写明不奖励「都有答案」。
- 关于 leader 在任务书里提的 `geom_seed` 方向：**成立，但要修正一半**，
  故 `status=done` 而非 `direction_disputed`。
  - 成立的那半：**我方确实在首帧到达之前把几何定死了**——但机制**不在 `PerfTrace` 里**。
    `PerfTrace.geom_seed` 只是**仪表**（`PerfTrace.kt:410` / `:662`），
    真正的机制在 `SessionRoute.kt:159-167` 的**持久化几何缓存** +
    `TermViewPresenter.kt:388` 的 `seedCellMetrics` 一次性写入 + 那两处「未 seed 就抛」的守卫
    （`TermViewPresenter.kt:238` / `:313`）。
  - **必须修正的那半**：`geom_seed` 排在 `first_frame_recv` 之前，**不等于我方不等首帧**。
    我方一样是「subscribe → 等 snapshot → 渲染」（`SessionViewModel.kt:196` / `:293`）。
    如果照「geom_seed 在前 ⇒ 我方提前上屏」写答复，就是又送给下游一个**未验证的前提**——
    正是他们上次翻车的同一类错误。答复 §0 专门纠正了这一点。

## 二、最有价值的一条发现（在答复 ④）

`/Volumes/nvme/Projects/tmux桌面端/src/term/TerminalView.js:63` = `convertEol: false`，
而 `:218` 的 `writeSnapshot` 把 **`capture-pane -e -p` 原始字节**直喂 `term.write()`。
capture-pane 行间是**裸 LF 无 CR**（我方 `TerminalEmulator.kt:84-91` KDoc 明确记载，
且我方为此专门做了 `replayImplicitCr`，`TerminalEmulator.kt:152` / `:237`），
**xterm.js 在 `convertEol:false` 下 `\n` 只下移不回车** ⇒ 逐行右移 ⇒ 溢出触发 autowrap ⇒ 整屏下移。

**形状与他们描述的「框线断裂、整屏下移」完全吻合，且我方在真机上撞过同一类**
（`TerminalEmulator.kt:90` 注释点名了真机截图 `term-glyph-after.png` 的逐行右移）。

⚠️ **但我把它标成 `需实测` 而不是 `源码确定`**，理由写在答复 ⑥ 第 2 条：
我没跑过他们的 UI，也没有逐行核过 xterm.js 里 `\n` 的实现。
给了他们一个一分钟的最小复现（`"AAA\nBBB"` → 看第二行起点在第 0 列还是第 3 列）。
**⛔ 我拒绝把这条写成确定结论**——它「说得通」的程度恰好就是让人翻车的那个程度。

## 三、必须先说的一件事：他们的回退**没有发生**

任务书写「工作树里 `src/` 已退回 `6aa5921`」。**我实测不成立**（2026-08-23 读，只读操作）：

```
$ cd /Volumes/nvme/Projects/tmux桌面端
$ git status --short -- src/          # 空 —— 工作树干净
$ git log --oneline -1               # ca1f54c Merge pull request #78 ...
$ git merge-base --is-ancestor db7fc60 main && echo YES   # YES
$ git log --oneline -2 revert/garble-all
  ca1f54c Merge pull request #78 from Florious95/feat/garble-fix2-r1
  db7fc60 fix: clip capture lines to term.cols by display width
```

⇒ 裁行改动 `db7fc60` **仍在 main 上**，`revert/garble-all` 分支**里没有 revert 提交**，
工作树 `src/term/TerminalView.js:22` 仍 `import { clipCaptureBytes }`，`:218`/`:229` 仍在裁。
**任何人现在 clone 他们的 main，拿到的都是带裁行的版本。**
所以答复顶部把这条放在最前面——否则 ①–④ 的建议会指到一份不存在的代码上，
而这正是本席位铁律里点名要避免的那件事。

（我读的是**工作树**，且工作树 == main == `ca1f54c`；答复顶部已写明。⛔ 我一个字没改他们的仓。）

## 四、我读过的文件（全部只读）

**我方**（worktree `wt-reply4`，revision `c810fe70c`）：

- `app/app/src/main/java/dev/agentmirror/app/perf/PerfTrace.kt`（八事件契约、`geomSeed`、`onSubscribeResult`）
- `app/core-conn/src/main/java/dev/agentmirror/app/conn/ConnectionManager.kt`（`subscribe` / `resize` / `traceSubscribe`）
- `app/app/src/main/java/dev/agentmirror/app/session/SessionViewModel.kt`（`init` 订阅、`onBinary` 三 kind 分派）
- `app/app/src/main/java/dev/agentmirror/app/session/SessionRoute.kt`（几何缓存 → 构造 VM）
- `app/app/src/main/java/dev/agentmirror/app/termview/TermViewPresenter.kt`（`seedCellMetrics` 与两处守卫）
- `app/core-terminal/src/main/kotlin/dev/agentmirror/terminal/TerminalEmulator.kt`（`replaySnapshot` / `replayImplicitCr` / `control`）
- `app/core-terminal/src/main/kotlin/dev/agentmirror/terminal/TerminalGrid.kt`（`pendingWrap` 延迟换行）
- `server/internal/api/ws_handler.go`（`handleSubscribe` / `handleResize`）
- `server/internal/api/ws_conn.go`（`relay` 的 teardown defer）
- `server/internal/bridge/bridge.go`（`Snapshot` / `Resize`）、`server/internal/bridge/stream.go`（单管道）
- `.team/nodes/t.reply4/任务书.md`、`tools/perfbase/judge-reply4.sh`、`CLAUDE.md`

**他们的**（`/Volumes/nvme/Projects/tmux桌面端`，工作树 == main == `ca1f54c`）：

- `src/term/TerminalView.js`（构造参数、`fit` / `_commitGrid` / `writeSnapshot` / `writeDelta`）
- `src/components/terminal/TerminalPane.jsx`（`handleBinary`、`start()` 订阅、`ResizeObserver`、`detectGarble` 调用点）
- `git log` / `git status`（判回退状态）

## 五、⛔ 我**没**读、因此不敢说的

- **`src/vendor/agentmirror/client.js`**：我只从 `TerminalPane.jsx` 的调用面看到
  `subscribe/unsubscribe/resize/scrollWheel` 的签名，**没读实现**。
  答复里凡涉及他们 WS 层时序的地方，我只讲了「调用点在哪一行」，⛔ 没有对 client.js 的行为下结论。
- **`src/term/garbleDetect.js` 的实现**：只见调用点与指标名。
  答复 ④ 里「这个判定器不该存在」是**架构判断**，⛔ 不是「我核过它哪条阈值错了」——
  答复 ⑥ 第 4 条已把这条标成 `查不出`。
- **`docs/CLIENT-CONTRACT.md` / `docs/UI-SPEC.md` / `.team/nodes/garble/` 的 550 行 JSONL**：
  没读。理由：答复要的是「我方是怎么做的」，读他们的调查产物只会让我倾向于
  **顺着他们已有的假设**给一个说得通的解释，而那正是本次要防的事。
  ⇒ 如果 leader 认为该用他们的 JSONL 交叉验证第 2 条推论，请另立一格，⛔ 我不在本格里补。
- **`docs/protocol.md` §4.2 / §6.2 我方协议文档**：没读原文，只读了服务端代码里对它的引用注释。

## 六、卡住 / 判不出的（与答复 ⑥ 一一对应）

1. `tmux resize-window` 是否同步 reflow —— 只有我方注释的断言
   （`server/internal/api/ws_handler.go:506`），**我没实测**。
   这是答复 ② 后半的地基：若不成立，②「reshape 期间不产生帧」要降级成「窗口比你们小」。
   要定它必须在服务端 `Resize` 与 `capture-pane` 之间插可变延迟对比捕获内容——
   属于**改我方产品码 + 起 tmux**，两条都是本格禁项，⇒ 只能标 `需实测`。
2. `convertEol:false` 是否他们的真根因 —— 见上文第二节。
3. 「整屏下移」的完整机理 —— 我只能推到「右移溢出 → autowrap 多吃一行 → 下顶」，
   **这是推理链，不是证据链**，答复里已经这么标了。
4. `detectGarble` 在哪些样本上判错 —— `查不出`（没读实现、没读样本）。
5. **多客户端同时看同一 pane 时几何谁赢** —— `查不出`。
   `server/internal/api/ws_handler.go:145-150` 有 pane 级原始几何簿记（首个订阅者记基线、
   最后一个离开时恢复），但**并发订阅者宽度不同时的仲裁**我在源码里没找到，
   看上去就是后到的 `Resize` 覆盖前面的。这与 ⑤ 的单管道叠在一起是我方一块糊涂账。
   ⛔ 我没给它编解释，直接列进答复 ⑥。

## 七、纪律自证

- ⛔ 未修改我方任何产品码（只写了本格两个 `.md`）。
- ⛔ 未修改 / commit / checkout / 建分支于 `/Volumes/nvme/Projects/tmux桌面端`——
  只跑了 `git log` / `git status` / `git merge-base` / `sed -n` / `grep` 等只读命令。
- ⛔ 未 commit / push；⛔ 未改任何 `judge-*.sh`；⛔ 未 `team-agent send`。
- ⛔ 未碰 9900 daemon、未碰用户真实 tmux、未开模拟器、未读凭据、未跑无过滤 `ps aux`。
- ⛔ 未写 `tmp/` 外的临时文件（本格未产生临时文件）。
