# nodeprobe 白名单（t.np / r4）

仓根。只读 **一份** `tools/nodeprobe/fixtures/providers.tsv`（未复制）。

## 行为

- `list-panes -a` 取 `pane_pid`；`ps -axo pid=,ppid=,comm=` 走后代树；`filepath` basename 与 TSV 求交。
- 非白名单 pane 不输出为节点。JSON 节点带 `provider`。
- basename 匹配：`/opt/homebrew/.../bin/codex` 命中 `codex`。
- 取数只用 comm，无 `-f` / `args=` / `command=`。

## parity

`provider_corpus_parity_with_go`：与 `server/internal/provider/table_test.go` 同一张 comm→id 表 + 全路径 + 噪声 comm。

## 量具身份

`ls -la $(which nodeprobe)`：

```
-rwxr-xr-x  1 alauda  staff  537920  8月 18 23:18 /Users/alauda/.local/bin/nodeprobe
```

本次 `cargo build --release` 产物（CARGO_TARGET_DIR）：

```
-rwxr-xr-x  1 alauda  staff  559264  8月 19 07:06 /Volumes/nvme/cargo-target/release/nodeprobe
```

PATH 里的二进制 inode 与本次产物不同（旧的 8月18 23:18）。`cargo test -q` 与本次 release 构建均 exit 0。
