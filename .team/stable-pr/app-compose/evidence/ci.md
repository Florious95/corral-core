# GitHub CI

- PR: https://github.com/Florious95/corral-core/pull/68
- state observed after initial mirror: `OPEN`, non-draft
- base: `main`
- `statusCheckRollup`: empty array
- CI conclusion: **unknown** (no reported checks), neither pass nor fail

The PR must remain open and unmerged.
