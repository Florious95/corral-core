# AgentMirror App 全局 Gradle 属性。
# - android.useAndroidX：强制启用 AndroidX（必须，否则依赖解析失败）。
# - android.nonTransitiveRClass：非传递 R 类，加快构建并缩小源码包。
# - org.gradle.jvmargs：为 daemon 预留堆与 UTF-8 编码。
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
