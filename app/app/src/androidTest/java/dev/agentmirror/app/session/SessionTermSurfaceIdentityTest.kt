package dev.agentmirror.app.session

/** Test-only identity recorder labels for the actual TermSurfaceView hierarchy walk. */
class SessionTermSurfaceIdentityTest { var factoryCount = 0; val bindings = mutableListOf<String>() }
