# 归档说明：level2 v1（回炉，失败的 diff 不是垃圾）

用户真机上二级菜单**全空**。按回炉流程：先完整归档上一版实现，再回到 `f1c5a27c8`「已拔干净」状态。本目录是下一格反推根因的原材料。

回退后二级菜单为空是**预期**，不是漏实现。

## 身份

| 项 | 值 |
| --- | --- |
| 干净基线（已拔干净） | `f1c5a27c8137653da1e8c20f6883b1904144f0af` 账本证据：level2-uproot 两任务 8 条机械判据全过 |
| 服务端实现 | `82865233cf2629d2099640a48e947ac3d554c0d3` 服务端：二级菜单实时流，pane 标题原样透传 |
| App 实现 | `53ac62866eb75e641484f5644d539f932f328644` App：二级菜单实时流，标题原样画、点一行进会话 |
| 归档时 worktree HEAD | `1d3f60602d7cc45b28a4343b5c19583600c4d33c`（其后提交不碰 `server/` 与 `app/app/src/`） |
| 归档时刻 | 见 `MANIFEST.txt` |

`f1c5a27c8..HEAD` 里碰 `server/` + `app/app/src/` 的提交只有上面两个。契约 061 / 账本提交只动文档，不含实现。

## 目录怎么读

```
归档说明.md          本文件
MANIFEST.txt          提交 SHA + 增删文件列表
TREE.txt              本目录文件清单
commits/
  82865233c.{meta,stat,patch}   服务端提交原样（git show --binary）
  53ac62866.{meta,stat,patch}   App 提交原样（含误提交的 app/.kotlin/errors/*.log）
combined/
  f1c5a27c8..53ac62866.server+app.src.patch   干净基线 → 坏实现的完整合并 diff
  name-status.txt / added.txt / modified.txt
before/               f1c5a27c8 上被改文件的全文（仅 modified）
after/                53ac62866 上全部相关文件全文（added + modified）
```

不要只看 `after/` 有没有符号。上一版死在「dex 里有 level2 符号、装上去仍空」。反推时用 `combined/*.patch` 对照订阅/帧字段/解帧，不要用文件存在当下结论。

## 实现落点（方便下一格对读）

服务端 `82865233c`：

- 新增 `server/internal/api/level2.go`、`level2_test.go`
- 改 `server.go`、`ws_conn.go`
- 协议：`frames.go` / `frametype.go` / `json.go` / `validate.go` + testdata

App `53ac62866`：

- 新增 `Level2LiveStreamScreen.kt`、`Level2ViewModel.kt` 及三个测试
- 改 `AgentMirrorApp.kt`、`ConnectionManager.kt`、`Frame.kt`、`Frames.kt`、`WorkspaceScreen.kt`

## 回退范围（t.rollback 已做 / 未做）

- **已做**：在 git worktree `wt.rollback` 内把 `server/` 与 `app/app/src/` 恢复到 `f1c5a27c8`，新增文件从该 worktree 工作树摘掉（归档完成之后才动，没有先 rm）。
- **未做**：没有在仓根跑 `git checkout --` / `git restore`，没有 commit / push，没有起模拟器，没有碰用户 tmux / 生产 daemon。
- **双写**：本归档同时存在于
  - 允许写路径：`.worktrees/wt.rollback/scratch/archive-level2-v1/`
  - 账本 `required_artifacts` / `A-rb-archived` / 下一格 `t.design` 读的绝对路径：`/Volumes/nvme/Projects/远程Agent安卓/scratch/archive-level2-v1/`
  两处内容一致。只写 worktree 时，下一格 `wt.design` 看不见（独立工作树、未提交）。

## 复现坏树（只读，勿在仓根覆盖）

```
git show 82865233c
git show 53ac62866
git diff f1c5a27c8 53ac62866 -- server/ app/app/src/
```

## 账本机械判据与本格实际（显式：没有为过判据改任务定义）

- `A-rb-archived` 查的是仓根 `/Volumes/nvme/Projects/远程Agent安卓/scratch/archive-level2-v1`：已双写，目录非空。
- `A-rb-server-clean` / `A-rb-app-clean` / `A-rb-go` / `A-rb-app` 的 argv cwd 写的是**仓根**。本格按硬约束**没有**在仓根 `git checkout --`，所以仓根 `server/`、`app/app/src/` 仍是坏实现。编译与单测改在 `wt.rollback` 上跑，均绿。
- 即便把 `A-rb-server-clean` 改映射到 worktree，`! grep -rqi level2 server/internal/` 在 **f1c5a27c8 自身也会红**：`connected_idle_economy_test.go:10` 有注释 `scratch/archive-level2/`。这是拔除归档路径里的字面量，不是实现。没有改这条注释去迎合 grep。
