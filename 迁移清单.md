# corral-app 当前迁移/构建记录

本仓已接收 corral-core `52fc4bd72e5d60fcf24cfa1ae43811ad23e4cfb7` 的完整 `app/app` 源码、资源及测试；生产源码以 `app/src/main/` 为准。`ServerSessionNameTest.kt` 保留自 App PR12，用于验证服务端会话名消费。

## 核依赖

`settings.gradle.kts` 固定到 corral-core Maven 发布提交 `7bdd02c1e1914a99df77ab8f1d8c2aab1652fbd5`，`app/build.gradle.kts` 固定：

- `dev.agentmirror.core:core-protocol:20260913.52fc4bd`
- `dev.agentmirror.core:core-terminal:20260913.52fc4bd`
- `dev.agentmirror.core:core-conn:20260913.52fc4bd`

这些发布物均由接受的 core commit 构建；构建不依赖本机 `.team` 目录或私有缓存，也不复制核心算法。`:terminal` 门面只存在于 corral-core 的源码工程，不在独立 App 中重复引入。

## 构建入口

从仓库 `main` 开始，使用 JDK 17 和 Android SDK：

```bash
./gradlew :app:assembleDebug
```

应用 label 与 launcher 资源必须保持 `corral`、`@mipmap/ic_launcher`、`@mipmap/ic_launcher_round` 及 `res/drawable-nodpi/corral_launcher_foreground.png` 的接受内容。
