[package]
name = "nodeprobe"
version = "0.1.0"
edition = "2021"
description = "Read-only tmux node liveness probe (requirement 063)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
