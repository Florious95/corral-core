# comm-basename	provider-id	display-name	[match]
# match empty = basename only; path-segment = also hit when raw comm contains /<comm-basename>/ as a directory.
# argv-contains:<text> gates a generic comm match on the process command line containing <text>.
# (Needed when the executable basename is generic, such as agent or node; never whitelist it without the gate.)
# Shared by Go (server) and Rust (nodeprobe). Do not fork this table.
claude	claude_code	Claude Code
codex	codex	Codex
copilot	copilot	Copilot
grok	grok	Grok
cursor-agent	cursor	Cursor	path-segment
agent	cursor	Cursor	argv-contains:cursor-agent
pi	pi	Pi
